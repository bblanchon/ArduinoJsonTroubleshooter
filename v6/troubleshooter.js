(function(){"use strict";function an(e,s){const n=Object.create(null),t=e.split(",");for(let o=0;o<t.length;o++)n[t[o]]=!0;return s?o=>!!n[o.toLowerCase()]:o=>!!n[o]}function rn(e){if(D(e)){const s={};for(let n=0;n<e.length;n++){const t=e[n],o=Z(t)?Xo(t):rn(t);if(o)for(const a in o)s[a]=o[a]}return s}else{if(Z(e))return e;if(L(e))return e}}const Vo=/;(?![^(]*\))/g,Ko=/:([^]+)/,Go=/\/\*.*?\*\//gs;function Xo(e){const s={};return e.replace(Go,"").split(Vo).forEach(n=>{if(n){const t=n.split(Ko);t.length>1&&(s[t[0].trim()]=t[1].trim())}}),s}function Ds(e){let s="";if(Z(e))s=e;else if(D(e))for(let n=0;n<e.length;n++){const t=Ds(e[n]);t&&(s+=t+" ")}else if(L(e))for(const n in e)e[n]&&(s+=n+" ");return s.trim()}const Zo=an("itemscope,allowfullscreen,formnovalidate,ismap,nomodule,novalidate,readonly");function tt(e){return!!e||e===""}const cn=e=>Z(e)?e:e==null?"":D(e)||L(e)&&(e.toString===rt||!I(e.toString))?JSON.stringify(e,ot,2):String(e),ot=(e,s)=>s&&s.__v_isRef?ot(e,s.value):as(s)?{[`Map(${s.size})`]:[...s.entries()].reduce((n,[t,o])=>(n[`${t} =>`]=o,n),{})}:at(s)?{[`Set(${s.size})`]:[...s.values()]}:L(s)&&!D(s)&&!ct(s)?String(s):s,B={},os=[],ve=()=>{},Qo=()=>!1,ea=/^on[^a-z]/,As=e=>ea.test(e),ln=e=>e.startsWith("onUpdate:"),Q=Object.assign,dn=(e,s)=>{const n=e.indexOf(s);n>-1&&e.splice(n,1)},sa=Object.prototype.hasOwnProperty,P=(e,s)=>sa.call(e,s),D=Array.isArray,as=e=>Ns(e)==="[object Map]",at=e=>Ns(e)==="[object Set]",I=e=>typeof e=="function",Z=e=>typeof e=="string",un=e=>typeof e=="symbol",L=e=>e!==null&&typeof e=="object",it=e=>L(e)&&I(e.then)&&I(e.catch),rt=Object.prototype.toString,Ns=e=>rt.call(e),na=e=>Ns(e).slice(8,-1),ct=e=>Ns(e)==="[object Object]",pn=e=>Z(e)&&e!=="NaN"&&e[0]!=="-"&&""+parseInt(e,10)===e,Is=an(",key,ref,ref_for,ref_key,onVnodeBeforeMount,onVnodeMounted,onVnodeBeforeUpdate,onVnodeUpdated,onVnodeBeforeUnmount,onVnodeUnmounted"),Cs=e=>{const s=Object.create(null);return n=>s[n]||(s[n]=e(n))},ta=/-(\w)/g,_e=Cs(e=>e.replace(ta,(s,n)=>n?n.toUpperCase():"")),oa=/\B([A-Z])/g,is=Cs(e=>e.replace(oa,"-$1").toLowerCase()),Os=Cs(e=>e.charAt(0).toUpperCase()+e.slice(1)),hn=Cs(e=>e?`on${Os(e)}`:""),qs=(e,s)=>!Object.is(e,s),mn=(e,s)=>{for(let n=0;n<e.length;n++)e[n](s)},Ps=(e,s,n)=>{Object.defineProperty(e,s,{configurable:!0,enumerable:!1,value:n})},fn=e=>{const s=parseFloat(e);return isNaN(s)?e:s};let lt;const aa=()=>lt||(lt=typeof globalThis<"u"?globalThis:typeof self<"u"?self:typeof window<"u"?window:typeof global<"u"?global:{});let Se;class ia{constructor(s=!1){this.detached=s,this.active=!0,this.effects=[],this.cleanups=[],this.parent=Se,!s&&Se&&(this.index=(Se.scopes||(Se.scopes=[])).push(this)-1)}run(s){if(this.active){const n=Se;try{return Se=this,s()}finally{Se=n}}}on(){Se=this}off(){Se=this.parent}stop(s){if(this.active){let n,t;for(n=0,t=this.effects.length;n<t;n++)this.effects[n].stop();for(n=0,t=this.cleanups.length;n<t;n++)this.cleanups[n]();if(this.scopes)for(n=0,t=this.scopes.length;n<t;n++)this.scopes[n].stop(!0);if(!this.detached&&this.parent&&!s){const o=this.parent.scopes.pop();o&&o!==this&&(this.parent.scopes[this.index]=o,o.index=this.index)}this.parent=void 0,this.active=!1}}}function ra(e,s=Se){s&&s.active&&s.effects.push(e)}const gn=e=>{const s=new Set(e);return s.w=0,s.n=0,s},dt=e=>(e.w&Re)>0,ut=e=>(e.n&Re)>0,ca=({deps:e})=>{if(e.length)for(let s=0;s<e.length;s++)e[s].w|=Re},la=e=>{const{deps:s}=e;if(s.length){let n=0;for(let t=0;t<s.length;t++){const o=s[t];dt(o)&&!ut(o)?o.delete(e):s[n++]=o,o.w&=~Re,o.n&=~Re}s.length=n}},yn=new WeakMap;let fs=0,Re=1;const bn=30;let je;const $e=Symbol(""),vn=Symbol("");class jn{constructor(s,n=null,t){this.fn=s,this.scheduler=n,this.active=!0,this.deps=[],this.parent=void 0,ra(this,t)}run(){if(!this.active)return this.fn();let s=je,n=Me;for(;s;){if(s===this)return;s=s.parent}try{return this.parent=je,je=this,Me=!0,Re=1<<++fs,fs<=bn?ca(this):pt(this),this.fn()}finally{fs<=bn&&la(this),Re=1<<--fs,je=this.parent,Me=n,this.parent=void 0,this.deferStop&&this.stop()}}stop(){je===this?this.deferStop=!0:this.active&&(pt(this),this.onStop&&this.onStop(),this.active=!1)}}function pt(e){const{deps:s}=e;if(s.length){for(let n=0;n<s.length;n++)s[n].delete(e);s.length=0}}let Me=!0;const ht=[];function rs(){ht.push(Me),Me=!1}function cs(){const e=ht.pop();Me=e===void 0?!0:e}function ue(e,s,n){if(Me&&je){let t=yn.get(e);t||yn.set(e,t=new Map);let o=t.get(n);o||t.set(n,o=gn()),mt(o)}}function mt(e,s){let n=!1;fs<=bn?ut(e)||(e.n|=Re,n=!dt(e)):n=!e.has(je),n&&(e.add(je),je.deps.push(e))}function Ce(e,s,n,t,o,a){const i=yn.get(e);if(!i)return;let r=[];if(s==="clear")r=[...i.values()];else if(n==="length"&&D(e)){const l=fn(t);i.forEach((u,p)=>{(p==="length"||p>=l)&&r.push(u)})}else switch(n!==void 0&&r.push(i.get(n)),s){case"add":D(e)?pn(n)&&r.push(i.get("length")):(r.push(i.get($e)),as(e)&&r.push(i.get(vn)));break;case"delete":D(e)||(r.push(i.get($e)),as(e)&&r.push(i.get(vn)));break;case"set":as(e)&&r.push(i.get($e));break}if(r.length===1)r[0]&&wn(r[0]);else{const l=[];for(const u of r)u&&l.push(...u);wn(gn(l))}}function wn(e,s){const n=D(e)?e:[...e];for(const t of n)t.computed&&ft(t);for(const t of n)t.computed||ft(t)}function ft(e,s){(e!==je||e.allowRecurse)&&(e.scheduler?e.scheduler():e.run())}const da=an("__proto__,__v_isRef,__isVue"),gt=new Set(Object.getOwnPropertyNames(Symbol).filter(e=>e!=="arguments"&&e!=="caller").map(e=>Symbol[e]).filter(un)),ua=xn(),pa=xn(!1,!0),ha=xn(!0),yt=ma();function ma(){const e={};return["includes","indexOf","lastIndexOf"].forEach(s=>{e[s]=function(...n){const t=E(this);for(let a=0,i=this.length;a<i;a++)ue(t,"get",a+"");const o=t[s](...n);return o===-1||o===!1?t[s](...n.map(E)):o}}),["push","pop","shift","unshift","splice"].forEach(s=>{e[s]=function(...n){rs();const t=E(this)[s].apply(this,n);return cs(),t}}),e}function xn(e=!1,s=!1){return function(t,o,a){if(o==="__v_isReactive")return!e;if(o==="__v_isReadonly")return e;if(o==="__v_isShallow")return s;if(o==="__v_raw"&&a===(e?s?Na:St:s?_t:Jt).get(t))return t;const i=D(t);if(!e&&i&&P(yt,o))return Reflect.get(yt,o,a);const r=Reflect.get(t,o,a);return(un(o)?gt.has(o):da(o))||(e||ue(t,"get",o),s)?r:ae(r)?i&&pn(o)?r:r.value:L(r)?e?Tt(r):_n(r):r}}const fa=bt(),ya=bt(!0);function bt(e=!1){return function(n,t,o,a){let i=n[t];if(gs(i)&&ae(i)&&!ae(o))return!1;if(!e&&(!Tn(o)&&!gs(o)&&(i=E(i),o=E(o)),!D(n)&&ae(i)&&!ae(o)))return i.value=o,!0;const r=D(n)&&pn(t)?Number(t)<n.length:P(n,t),l=Reflect.set(n,t,o,a);return n===E(a)&&(r?qs(o,i)&&Ce(n,"set",t,o):Ce(n,"add",t,o)),l}}function ba(e,s){const n=P(e,s);e[s];const t=Reflect.deleteProperty(e,s);return t&&n&&Ce(e,"delete",s,void 0),t}function va(e,s){const n=Reflect.has(e,s);return(!un(s)||!gt.has(s))&&ue(e,"has",s),n}function ja(e){return ue(e,"iterate",D(e)?"length":$e),Reflect.ownKeys(e)}const vt={get:ua,set:fa,deleteProperty:ba,has:va,ownKeys:ja},wa={get:ha,set(e,s){return!0},deleteProperty(e,s){return!0}},xa=Q({},vt,{get:pa,set:ya}),zn=e=>e,Es=e=>Reflect.getPrototypeOf(e);function Rs(e,s,n=!1,t=!1){e=e.__v_raw;const o=E(e),a=E(s);n||(s!==a&&ue(o,"get",s),ue(o,"get",a));const{has:i}=Es(o),r=t?zn:n?Dn:kn;if(i.call(o,s))return r(e.get(s));if(i.call(o,a))return r(e.get(a));e!==o&&e.get(s)}function Ms(e,s=!1){const n=this.__v_raw,t=E(n),o=E(e);return s||(e!==o&&ue(t,"has",e),ue(t,"has",o)),e===o?n.has(e):n.has(e)||n.has(o)}function Fs(e,s=!1){return e=e.__v_raw,!s&&ue(E(e),"iterate",$e),Reflect.get(e,"size",e)}function jt(e){e=E(e);const s=E(this);return Es(s).has.call(s,e)||(s.add(e),Ce(s,"add",e,e)),this}function wt(e,s){s=E(s);const n=E(this),{has:t,get:o}=Es(n);let a=t.call(n,e);a||(e=E(e),a=t.call(n,e));const i=o.call(n,e);return n.set(e,s),a?qs(s,i)&&Ce(n,"set",e,s):Ce(n,"add",e,s),this}function xt(e){const s=E(this),{has:n,get:t}=Es(s);let o=n.call(s,e);o||(e=E(e),o=n.call(s,e)),t&&t.call(s,e);const a=s.delete(e);return o&&Ce(s,"delete",e,void 0),a}function zt(){const e=E(this),s=e.size!==0,n=e.clear();return s&&Ce(e,"clear",void 0,void 0),n}function Hs(e,s){return function(t,o){const a=this,i=a.__v_raw,r=E(i),l=s?zn:e?Dn:kn;return!e&&ue(r,"iterate",$e),i.forEach((u,p)=>t.call(o,l(u),l(p),a))}}function Ys(e,s,n){return function(...t){const o=this.__v_raw,a=E(o),i=as(a),r=e==="entries"||e===Symbol.iterator&&i,l=e==="keys"&&i,u=o[e](...t),p=n?zn:s?Dn:kn;return!s&&ue(a,"iterate",l?vn:$e),{next(){const{value:y,done:b}=u.next();return b?{value:y,done:b}:{value:r?[p(y[0]),p(y[1])]:p(y),done:b}},[Symbol.iterator](){return this}}}}function Fe(e){return function(...s){return e==="delete"?!1:this}}function za(){const e={get(a){return Rs(this,a)},get size(){return Fs(this)},has:Ms,add:jt,set:wt,delete:xt,clear:zt,forEach:Hs(!1,!1)},s={get(a){return Rs(this,a,!1,!0)},get size(){return Fs(this)},has:Ms,add:jt,set:wt,delete:xt,clear:zt,forEach:Hs(!1,!0)},n={get(a){return Rs(this,a,!0)},get size(){return Fs(this,!0)},has(a){return Ms.call(this,a,!0)},add:Fe("add"),set:Fe("set"),delete:Fe("delete"),clear:Fe("clear"),forEach:Hs(!0,!1)},t={get(a){return Rs(this,a,!0,!0)},get size(){return Fs(this,!0)},has(a){return Ms.call(this,a,!0)},add:Fe("add"),set:Fe("set"),delete:Fe("delete"),clear:Fe("clear"),forEach:Hs(!0,!0)};return["keys","values","entries",Symbol.iterator].forEach(a=>{e[a]=Ys(a,!1,!1),n[a]=Ys(a,!0,!1),s[a]=Ys(a,!1,!0),t[a]=Ys(a,!0,!0)}),[e,n,s,t]}const[Ja,_a,Sa,Ta]=za();function Jn(e,s){const n=s?e?Ta:Sa:e?_a:Ja;return(t,o,a)=>o==="__v_isReactive"?!e:o==="__v_isReadonly"?e:o==="__v_raw"?t:Reflect.get(P(n,o)&&o in t?n:t,o,a)}const ka={get:Jn(!1,!1)},Da={get:Jn(!1,!0)},Aa={get:Jn(!0,!1)},Jt=new WeakMap,_t=new WeakMap,St=new WeakMap,Na=new WeakMap;function Ia(e){switch(e){case"Object":case"Array":return 1;case"Map":case"Set":case"WeakMap":case"WeakSet":return 2;default:return 0}}function Ca(e){return e.__v_skip||!Object.isExtensible(e)?0:Ia(na(e))}function _n(e){return gs(e)?e:Sn(e,!1,vt,ka,Jt)}function Oa(e){return Sn(e,!1,xa,Da,_t)}function Tt(e){return Sn(e,!0,wa,Aa,St)}function Sn(e,s,n,t,o){if(!L(e)||e.__v_raw&&!(s&&e.__v_isReactive))return e;const a=o.get(e);if(a)return a;const i=Ca(e);if(i===0)return e;const r=new Proxy(e,i===2?t:n);return o.set(e,r),r}function ls(e){return gs(e)?ls(e.__v_raw):!!(e&&e.__v_isReactive)}function gs(e){return!!(e&&e.__v_isReadonly)}function Tn(e){return!!(e&&e.__v_isShallow)}function kt(e){return ls(e)||gs(e)}function E(e){const s=e&&e.__v_raw;return s?E(s):e}function Dt(e){return Ps(e,"__v_skip",!0),e}const kn=e=>L(e)?_n(e):e,Dn=e=>L(e)?Tt(e):e;function qa(e){Me&&je&&(e=E(e),mt(e.dep||(e.dep=gn())))}function Pa(e,s){e=E(e),e.dep&&wn(e.dep)}function ae(e){return!!(e&&e.__v_isRef===!0)}function Ea(e){return ae(e)?e.value:e}const Ra={get:(e,s,n)=>Ea(Reflect.get(e,s,n)),set:(e,s,n,t)=>{const o=e[s];return ae(o)&&!ae(n)?(o.value=n,!0):Reflect.set(e,s,n,t)}};function At(e){return ls(e)?e:new Proxy(e,Ra)}var Nt;class Ma{constructor(s,n,t,o){this._setter=n,this.dep=void 0,this.__v_isRef=!0,this[Nt]=!1,this._dirty=!0,this.effect=new jn(s,()=>{this._dirty||(this._dirty=!0,Pa(this))}),this.effect.computed=this,this.effect.active=this._cacheable=!o,this.__v_isReadonly=t}get value(){const s=E(this);return qa(s),(s._dirty||!s._cacheable)&&(s._dirty=!1,s._value=s.effect.run()),s._value}set value(s){this._setter(s)}}Nt="__v_isReadonly";function Fa(e,s,n=!1){let t,o;const a=I(e);return a?(t=e,o=ve):(t=e.get,o=e.set),new Ma(t,o,a||!o,n)}function dc(e,...s){}function He(e,s,n,t){let o;try{o=t?e(...t):e()}catch(a){Us(a,s,n)}return o}function fe(e,s,n,t){if(I(e)){const a=He(e,s,n,t);return a&&it(a)&&a.catch(i=>{Us(i,s,n)}),a}const o=[];for(let a=0;a<e.length;a++)o.push(fe(e[a],s,n,t));return o}function Us(e,s,n,t=!0){const o=s?s.vnode:null;if(s){let a=s.parent;const i=s.proxy,r=n;for(;a;){const u=a.ec;if(u){for(let p=0;p<u.length;p++)if(u[p](e,i,r)===!1)return}a=a.parent}const l=s.appContext.config.errorHandler;if(l){He(l,null,10,[e,i,r]);return}}Ha(e,n,o,t)}function Ha(e,s,n,t=!0){console.error(e)}let ys=!1,An=!1;const te=[];let Te=0;const ds=[];let Oe=null,Ve=0;const It=Promise.resolve();let Nn=null;function Ya(e){const s=Nn||It;return e?s.then(this?e.bind(this):e):s}function Ua(e){let s=Te+1,n=te.length;for(;s<n;){const t=s+n>>>1;bs(te[t])<e?s=t+1:n=t}return s}function In(e){(!te.length||!te.includes(e,ys&&e.allowRecurse?Te+1:Te))&&(e.id==null?te.push(e):te.splice(Ua(e.id),0,e),Ct())}function Ct(){!ys&&!An&&(An=!0,Nn=It.then(Pt))}function Ba(e){const s=te.indexOf(e);s>Te&&te.splice(s,1)}function La(e){D(e)?ds.push(...e):(!Oe||!Oe.includes(e,e.allowRecurse?Ve+1:Ve))&&ds.push(e),Ct()}function Ot(e,s=ys?Te+1:0){for(;s<te.length;s++){const n=te[s];n&&n.pre&&(te.splice(s,1),s--,n())}}function qt(e){if(ds.length){const s=[...new Set(ds)];if(ds.length=0,Oe){Oe.push(...s);return}for(Oe=s,Oe.sort((n,t)=>bs(n)-bs(t)),Ve=0;Ve<Oe.length;Ve++)Oe[Ve]();Oe=null,Ve=0}}const bs=e=>e.id==null?1/0:e.id,Wa=(e,s)=>{const n=bs(e)-bs(s);if(n===0){if(e.pre&&!s.pre)return-1;if(s.pre&&!e.pre)return 1}return n};function Pt(e){An=!1,ys=!0,te.sort(Wa);const s=ve;try{for(Te=0;Te<te.length;Te++){const n=te[Te];n&&n.active!==!1&&He(n,null,14)}}finally{Te=0,te.length=0,qt(),ys=!1,Nn=null,(te.length||ds.length)&&Pt()}}function $a(e,s,...n){if(e.isUnmounted)return;const t=e.vnode.props||B;let o=n;const a=s.startsWith("update:"),i=a&&s.slice(7);if(i&&i in t){const p=`${i==="modelValue"?"model":i}Modifiers`,{number:y,trim:b}=t[p]||B;b&&(o=n.map(T=>Z(T)?T.trim():T)),y&&(o=n.map(fn))}let r,l=t[r=hn(s)]||t[r=hn(_e(s))];!l&&a&&(l=t[r=hn(is(s))]),l&&fe(l,e,6,o);const u=t[r+"Once"];if(u){if(!e.emitted)e.emitted={};else if(e.emitted[r])return;e.emitted[r]=!0,fe(u,e,6,o)}}function Et(e,s,n=!1){const t=s.emitsCache,o=t.get(e);if(o!==void 0)return o;const a=e.emits;let i={},r=!1;if(!I(e)){const l=u=>{const p=Et(u,s,!0);p&&(r=!0,Q(i,p))};!n&&s.mixins.length&&s.mixins.forEach(l),e.extends&&l(e.extends),e.mixins&&e.mixins.forEach(l)}return!a&&!r?(L(e)&&t.set(e,null),null):(D(a)?a.forEach(l=>i[l]=null):Q(i,a),L(e)&&t.set(e,i),i)}function Bs(e,s){return!e||!As(s)?!1:(s=s.slice(2).replace(/Once$/,""),P(e,s[0].toLowerCase()+s.slice(1))||P(e,is(s))||P(e,s))}let ge=null,Rt=null;function Ls(e){const s=ge;return ge=e,Rt=e&&e.type.__scopeId||null,s}function Mt(e,s=ge,n){if(!s||e._n)return e;const t=(...o)=>{t._d&&fo(-1);const a=Ls(s);let i;try{i=e(...o)}finally{Ls(a),t._d&&fo(1)}return i};return t._n=!0,t._c=!0,t._d=!0,t}function uc(){}function Cn(e){const{type:s,vnode:n,proxy:t,withProxy:o,props:a,propsOptions:[i],slots:r,attrs:l,emit:u,render:p,renderCache:y,data:b,setupState:T,ctx:O,inheritAttrs:k}=e;let V,R;const re=Ls(e);try{if(n.shapeFlag&4){const Y=o||t;V=ke(p.call(Y,Y,y,a,T,b,O)),R=l}else{const Y=s;V=ke(Y.length>1?Y(a,{attrs:l,slots:r,emit:u}):Y(a,null)),R=s.props?l:Va(l)}}catch(Y){xs.length=0,Us(Y,e,1),V=de(we)}let N=V;if(R&&k!==!1){const Y=Object.keys(R),{shapeFlag:se}=N;Y.length&&se&7&&(i&&Y.some(ln)&&(R=Ka(R,i)),N=Ye(N,R))}return n.dirs&&(N=Ye(N),N.dirs=N.dirs?N.dirs.concat(n.dirs):n.dirs),n.transition&&(N.transition=n.transition),V=N,Ls(re),V}const Va=e=>{let s;for(const n in e)(n==="class"||n==="style"||As(n))&&((s||(s={}))[n]=e[n]);return s},Ka=(e,s)=>{const n={};for(const t in e)(!ln(t)||!(t.slice(9)in s))&&(n[t]=e[t]);return n};function Ga(e,s,n){const{props:t,children:o,component:a}=e,{props:i,children:r,patchFlag:l}=s,u=a.emitsOptions;if(s.dirs||s.transition)return!0;if(n&&l>=0){if(l&1024)return!0;if(l&16)return t?Ft(t,i,u):!!i;if(l&8){const p=s.dynamicProps;for(let y=0;y<p.length;y++){const b=p[y];if(i[b]!==t[b]&&!Bs(u,b))return!0}}}else return(o||r)&&(!r||!r.$stable)?!0:t===i?!1:t?i?Ft(t,i,u):!0:!!i;return!1}function Ft(e,s,n){const t=Object.keys(s);if(t.length!==Object.keys(e).length)return!0;for(let o=0;o<t.length;o++){const a=t[o];if(s[a]!==e[a]&&!Bs(n,a))return!0}return!1}function Xa({vnode:e,parent:s},n){for(;s&&s.subTree===e;)(e=s.vnode).el=n,s=s.parent}const Za=e=>e.__isSuspense;function Qa(e,s){s&&s.pendingBranch?D(e)?s.effects.push(...e):s.effects.push(e):La(e)}function ei(e,s){if(ee){let n=ee.provides;const t=ee.parent&&ee.parent.provides;t===n&&(n=ee.provides=Object.create(t)),n[e]=s}}function Ws(e,s,n=!1){const t=ee||ge;if(t){const o=t.parent==null?t.vnode.appContext&&t.vnode.appContext.provides:t.parent.provides;if(o&&e in o)return o[e];if(arguments.length>1)return n&&I(s)?s.call(t.proxy):s}}const $s={};function On(e,s,n){return Ht(e,s,n)}function Ht(e,s,{immediate:n,deep:t,flush:o,onTrack:a,onTrigger:i}=B){const r=ee;let l,u=!1,p=!1;if(ae(e)?(l=()=>e.value,u=Tn(e)):ls(e)?(l=()=>e,t=!0):D(e)?(p=!0,u=e.some(N=>ls(N)||Tn(N)),l=()=>e.map(N=>{if(ae(N))return N.value;if(ls(N))return us(N);if(I(N))return He(N,r,2)})):I(e)?s?l=()=>He(e,r,2):l=()=>{if(!(r&&r.isUnmounted))return y&&y(),fe(e,r,3,[b])}:l=ve,s&&t){const N=l;l=()=>us(N())}let y,b=N=>{y=R.onStop=()=>{He(N,r,4)}},T;if(Js)if(b=ve,s?n&&fe(s,r,3,[l(),p?[]:void 0,b]):l(),o==="sync"){const N=Vi();T=N.__watcherHandles||(N.__watcherHandles=[])}else return ve;let O=p?new Array(e.length).fill($s):$s;const k=()=>{if(R.active)if(s){const N=R.run();(t||u||(p?N.some((Y,se)=>qs(Y,O[se])):qs(N,O)))&&(y&&y(),fe(s,r,3,[N,O===$s?void 0:p&&O[0]===$s?[]:O,b]),O=N)}else R.run()};k.allowRecurse=!!s;let V;o==="sync"?V=k:o==="post"?V=()=>ce(k,r&&r.suspense):(k.pre=!0,r&&(k.id=r.uid),V=()=>In(k));const R=new jn(l,V);s?n?k():O=R.run():o==="post"?ce(R.run.bind(R),r&&r.suspense):R.run();const re=()=>{R.stop(),r&&r.scope&&dn(r.scope.effects,R)};return T&&T.push(re),re}function si(e,s,n){const t=this.proxy,o=Z(e)?e.includes(".")?Yt(t,e):()=>t[e]:e.bind(t,t);let a;I(s)?a=s:(a=s.handler,n=s);const i=ee;ps(this);const r=Ht(o,a.bind(t),n);return i?ps(i):Qe(),r}function Yt(e,s){const n=s.split(".");return()=>{let t=e;for(let o=0;o<n.length&&t;o++)t=t[n[o]];return t}}function us(e,s){if(!L(e)||e.__v_skip||(s=s||new Set,s.has(e)))return e;if(s.add(e),ae(e))us(e.value,s);else if(D(e))for(let n=0;n<e.length;n++)us(e[n],s);else if(at(e)||as(e))e.forEach(n=>{us(n,s)});else if(ct(e))for(const n in e)us(e[n],s);return e}function Ut(){const e={isMounted:!1,isLeaving:!1,isUnmounting:!1,leavingVNodes:new Map};return $t(()=>{e.isMounted=!0}),Kt(()=>{e.isUnmounting=!0}),e}const ye=[Function,Array],ni={name:"BaseTransition",props:{mode:String,appear:Boolean,persisted:Boolean,onBeforeEnter:ye,onEnter:ye,onAfterEnter:ye,onEnterCancelled:ye,onBeforeLeave:ye,onLeave:ye,onAfterLeave:ye,onLeaveCancelled:ye,onBeforeAppear:ye,onAppear:ye,onAfterAppear:ye,onAppearCancelled:ye},setup(e,{slots:s}){const n=vo(),t=Ut();let o;return()=>{const a=s.default&&Pn(s.default(),!0);if(!a||!a.length)return;let i=a[0];if(a.length>1){for(const k of a)if(k.type!==we){i=k;break}}const r=E(e),{mode:l}=r;if(t.isLeaving)return qn(i);const u=Lt(i);if(!u)return qn(i);const p=vs(u,r,t,n);js(u,p);const y=n.subTree,b=y&&Lt(y);let T=!1;const{getTransitionKey:O}=u.type;if(O){const k=O();o===void 0?o=k:k!==o&&(o=k,T=!0)}if(b&&b.type!==we&&(!Ze(u,b)||T)){const k=vs(b,r,t,n);if(js(b,k),l==="out-in")return t.isLeaving=!0,k.afterLeave=()=>{t.isLeaving=!1,n.update.active!==!1&&n.update()},qn(i);l==="in-out"&&u.type!==we&&(k.delayLeave=(V,R,re)=>{const N=Bt(t,b);N[String(b.key)]=b,V._leaveCb=()=>{R(),V._leaveCb=void 0,delete p.delayedLeave},p.delayedLeave=re})}return i}}};function Bt(e,s){const{leavingVNodes:n}=e;let t=n.get(s.type);return t||(t=Object.create(null),n.set(s.type,t)),t}function vs(e,s,n,t){const{appear:o,mode:a,persisted:i=!1,onBeforeEnter:r,onEnter:l,onAfterEnter:u,onEnterCancelled:p,onBeforeLeave:y,onLeave:b,onAfterLeave:T,onLeaveCancelled:O,onBeforeAppear:k,onAppear:V,onAfterAppear:R,onAppearCancelled:re}=s,N=String(e.key),Y=Bt(n,e),se=(C,K)=>{C&&fe(C,t,9,K)},We=(C,K)=>{const U=K[1];se(C,K),D(C)?C.every(oe=>oe.length<=1)&&U():C.length<=1&&U()},Ae={mode:a,persisted:i,beforeEnter(C){let K=r;if(!n.isMounted)if(o)K=k||r;else return;C._leaveCb&&C._leaveCb(!0);const U=Y[N];U&&Ze(e,U)&&U.el._leaveCb&&U.el._leaveCb(),se(K,[C])},enter(C){let K=l,U=u,oe=p;if(!n.isMounted)if(o)K=V||l,U=R||u,oe=re||p;else return;let J=!1;const W=C._enterCb=he=>{J||(J=!0,he?se(oe,[C]):se(U,[C]),Ae.delayedLeave&&Ae.delayedLeave(),C._enterCb=void 0)};K?We(K,[C,W]):W()},leave(C,K){const U=String(e.key);if(C._enterCb&&C._enterCb(!0),n.isUnmounting)return K();se(y,[C]);let oe=!1;const J=C._leaveCb=W=>{oe||(oe=!0,K(),W?se(O,[C]):se(T,[C]),C._leaveCb=void 0,Y[U]===e&&delete Y[U])};Y[U]=e,b?We(b,[C,J]):J()},clone(C){return vs(C,s,n,t)}};return Ae}function qn(e){if(Ks(e))return e=Ye(e),e.children=null,e}function Lt(e){return Ks(e)?e.children?e.children[0]:void 0:e}function js(e,s){e.shapeFlag&6&&e.component?js(e.component.subTree,s):e.shapeFlag&128?(e.ssContent.transition=s.clone(e.ssContent),e.ssFallback.transition=s.clone(e.ssFallback)):e.transition=s}function Pn(e,s=!1,n){let t=[],o=0;for(let a=0;a<e.length;a++){let i=e[a];const r=n==null?i.key:String(n)+String(i.key!=null?i.key:a);i.type===pe?(i.patchFlag&128&&o++,t=t.concat(Pn(i.children,s,r))):(s||i.type!==we)&&t.push(r!=null?Ye(i,{key:r}):i)}if(o>1)for(let a=0;a<t.length;a++)t[a].patchFlag=-2;return t}function En(e){return I(e)?{setup:e,name:e.name}:e}const Vs=e=>!!e.type.__asyncLoader,Ks=e=>e.type.__isKeepAlive;function ti(e,s){Wt(e,"a",s)}function oi(e,s){Wt(e,"da",s)}function Wt(e,s,n=ee){const t=e.__wdc||(e.__wdc=()=>{let o=n;for(;o;){if(o.isDeactivated)return;o=o.parent}return e()});if(Gs(s,t,n),n){let o=n.parent;for(;o&&o.parent;)Ks(o.parent.vnode)&&ai(t,s,n,o),o=o.parent}}function ai(e,s,n,t){const o=Gs(s,e,t,!0);Gt(()=>{dn(t[s],o)},n)}function Gs(e,s,n=ee,t=!1){if(n){const o=n[e]||(n[e]=[]),a=s.__weh||(s.__weh=(...i)=>{if(n.isUnmounted)return;rs(),ps(n);const r=fe(s,n,e,i);return Qe(),cs(),r});return t?o.unshift(a):o.push(a),a}}const qe=e=>(s,n=ee)=>(!Js||e==="sp")&&Gs(e,(...t)=>s(...t),n),ii=qe("bm"),$t=qe("m"),ri=qe("bu"),Vt=qe("u"),Kt=qe("bum"),Gt=qe("um"),ci=qe("sp"),li=qe("rtg"),di=qe("rtc");function ui(e,s=ee){Gs("ec",e,s)}function Ke(e,s,n,t){const o=e.dirs,a=s&&s.dirs;for(let i=0;i<o.length;i++){const r=o[i];a&&(r.oldValue=a[i].value);let l=r.dir[t];l&&(rs(),fe(l,n,8,[e.el,r,e,s]),cs())}}const Xt="components";function Xs(e,s){return hi(Xt,e,!0,s)||e}const pi=Symbol();function hi(e,s,n=!0,t=!1){const o=ge||ee;if(o){const a=o.type;if(e===Xt){const r=Bi(a,!1);if(r&&(r===s||r===_e(s)||r===Os(_e(s))))return a}const i=Zt(o[e]||a[e],s)||Zt(o.appContext[e],s);return!i&&t?a:i}}function Zt(e,s){return e&&(e[s]||e[_e(s)]||e[Os(_e(s))])}function Qt(e,s,n,t){let o;const a=n&&n[t];if(D(e)||Z(e)){o=new Array(e.length);for(let i=0,r=e.length;i<r;i++)o[i]=s(e[i],i,void 0,a&&a[i])}else if(typeof e=="number"){o=new Array(e);for(let i=0;i<e;i++)o[i]=s(i+1,i,void 0,a&&a[i])}else if(L(e))if(e[Symbol.iterator])o=Array.from(e,(i,r)=>s(i,r,void 0,a&&a[r]));else{const i=Object.keys(e);o=new Array(i.length);for(let r=0,l=i.length;r<l;r++){const u=i[r];o[r]=s(e[u],u,r,a&&a[r])}}else o=[];return n&&(n[t]=o),o}const Rn=e=>e?jo(e)?Kn(e)||e.proxy:Rn(e.parent):null,ws=Q(Object.create(null),{$:e=>e,$el:e=>e.vnode.el,$data:e=>e.data,$props:e=>e.props,$attrs:e=>e.attrs,$slots:e=>e.slots,$refs:e=>e.refs,$parent:e=>Rn(e.parent),$root:e=>Rn(e.root),$emit:e=>e.emit,$options:e=>Hn(e),$forceUpdate:e=>e.f||(e.f=()=>In(e.update)),$nextTick:e=>e.n||(e.n=Ya.bind(e.proxy)),$watch:e=>si.bind(e)}),Mn=(e,s)=>e!==B&&!e.__isScriptSetup&&P(e,s),mi={get({_:e},s){const{ctx:n,setupState:t,data:o,props:a,accessCache:i,type:r,appContext:l}=e;let u;if(s[0]!=="$"){const T=i[s];if(T!==void 0)switch(T){case 1:return t[s];case 2:return o[s];case 4:return n[s];case 3:return a[s]}else{if(Mn(t,s))return i[s]=1,t[s];if(o!==B&&P(o,s))return i[s]=2,o[s];if((u=e.propsOptions[0])&&P(u,s))return i[s]=3,a[s];if(n!==B&&P(n,s))return i[s]=4,n[s];Fn&&(i[s]=0)}}const p=ws[s];let y,b;if(p)return s==="$attrs"&&ue(e,"get",s),p(e);if((y=r.__cssModules)&&(y=y[s]))return y;if(n!==B&&P(n,s))return i[s]=4,n[s];if(b=l.config.globalProperties,P(b,s))return b[s]},set({_:e},s,n){const{data:t,setupState:o,ctx:a}=e;return Mn(o,s)?(o[s]=n,!0):t!==B&&P(t,s)?(t[s]=n,!0):P(e.props,s)||s[0]==="$"&&s.slice(1)in e?!1:(a[s]=n,!0)},has({_:{data:e,setupState:s,accessCache:n,ctx:t,appContext:o,propsOptions:a}},i){let r;return!!n[i]||e!==B&&P(e,i)||Mn(s,i)||(r=a[0])&&P(r,i)||P(t,i)||P(ws,i)||P(o.config.globalProperties,i)},defineProperty(e,s,n){return n.get!=null?e._.accessCache[s]=0:P(n,"value")&&this.set(e,s,n.value,null),Reflect.defineProperty(e,s,n)}};let Fn=!0;function fi(e){const s=Hn(e),n=e.proxy,t=e.ctx;Fn=!1,s.beforeCreate&&eo(s.beforeCreate,e,"bc");const{data:o,computed:a,methods:i,watch:r,provide:l,inject:u,created:p,beforeMount:y,mounted:b,beforeUpdate:T,updated:O,activated:k,deactivated:V,beforeDestroy:R,beforeUnmount:re,destroyed:N,unmounted:Y,render:se,renderTracked:We,renderTriggered:Ae,errorCaptured:C,serverPrefetch:K,expose:U,inheritAttrs:oe,components:J,directives:W,filters:he}=s;if(u&&gi(u,t,null,e.appContext.config.unwrapInjectedRef),i)for(const G in i){const F=i[G];I(F)&&(t[G]=F.bind(n))}if(o){const G=o.call(n,n);L(G)&&(e.data=_n(G))}if(Fn=!0,a)for(const G in a){const F=a[G],ns=I(F)?F.bind(n,n):I(F.get)?F.get.bind(n,n):ve,tn=!I(F)&&I(F.set)?F.set.bind(n):ve,ts=Wi({get:ns,set:tn});Object.defineProperty(t,G,{enumerable:!0,configurable:!0,get:()=>ts.value,set:Ne=>ts.value=Ne})}if(r)for(const G in r)so(r[G],t,n,G);if(l){const G=I(l)?l.call(n):l;Reflect.ownKeys(G).forEach(F=>{ei(F,G[F])})}p&&eo(p,e,"c");function ne(G,F){D(F)?F.forEach(ns=>G(ns.bind(n))):F&&G(F.bind(n))}if(ne(ii,y),ne($t,b),ne(ri,T),ne(Vt,O),ne(ti,k),ne(oi,V),ne(ui,C),ne(di,We),ne(li,Ae),ne(Kt,re),ne(Gt,Y),ne(ci,K),D(U))if(U.length){const G=e.exposed||(e.exposed={});U.forEach(F=>{Object.defineProperty(G,F,{get:()=>n[F],set:ns=>n[F]=ns})})}else e.exposed||(e.exposed={});se&&e.render===ve&&(e.render=se),oe!=null&&(e.inheritAttrs=oe),J&&(e.components=J),W&&(e.directives=W)}function gi(e,s,n=ve,t=!1){D(e)&&(e=Yn(e));for(const o in e){const a=e[o];let i;L(a)?"default"in a?i=Ws(a.from||o,a.default,!0):i=Ws(a.from||o):i=Ws(a),ae(i)&&t?Object.defineProperty(s,o,{enumerable:!0,configurable:!0,get:()=>i.value,set:r=>i.value=r}):s[o]=i}}function eo(e,s,n){fe(D(e)?e.map(t=>t.bind(s.proxy)):e.bind(s.proxy),s,n)}function so(e,s,n,t){const o=t.includes(".")?Yt(n,t):()=>n[t];if(Z(e)){const a=s[e];I(a)&&On(o,a)}else if(I(e))On(o,e.bind(n));else if(L(e))if(D(e))e.forEach(a=>so(a,s,n,t));else{const a=I(e.handler)?e.handler.bind(n):s[e.handler];I(a)&&On(o,a,e)}}function Hn(e){const s=e.type,{mixins:n,extends:t}=s,{mixins:o,optionsCache:a,config:{optionMergeStrategies:i}}=e.appContext,r=a.get(s);let l;return r?l=r:!o.length&&!n&&!t?l=s:(l={},o.length&&o.forEach(u=>Zs(l,u,i,!0)),Zs(l,s,i)),L(s)&&a.set(s,l),l}function Zs(e,s,n,t=!1){const{mixins:o,extends:a}=s;a&&Zs(e,a,n,!0),o&&o.forEach(i=>Zs(e,i,n,!0));for(const i in s)if(!(t&&i==="expose")){const r=yi[i]||n&&n[i];e[i]=r?r(e[i],s[i]):s[i]}return e}const yi={data:no,props:Ge,emits:Ge,methods:Ge,computed:Ge,beforeCreate:ie,created:ie,beforeMount:ie,mounted:ie,beforeUpdate:ie,updated:ie,beforeDestroy:ie,beforeUnmount:ie,destroyed:ie,unmounted:ie,activated:ie,deactivated:ie,errorCaptured:ie,serverPrefetch:ie,components:Ge,directives:Ge,watch:vi,provide:no,inject:bi};function no(e,s){return s?e?function(){return Q(I(e)?e.call(this,this):e,I(s)?s.call(this,this):s)}:s:e}function bi(e,s){return Ge(Yn(e),Yn(s))}function Yn(e){if(D(e)){const s={};for(let n=0;n<e.length;n++)s[e[n]]=e[n];return s}return e}function ie(e,s){return e?[...new Set([].concat(e,s))]:s}function Ge(e,s){return e?Q(Q(Object.create(null),e),s):s}function vi(e,s){if(!e)return s;if(!s)return e;const n=Q(Object.create(null),e);for(const t in s)n[t]=ie(e[t],s[t]);return n}function ji(e,s,n,t=!1){const o={},a={};Ps(a,sn,1),e.propsDefaults=Object.create(null),to(e,s,o,a);for(const i in e.propsOptions[0])i in o||(o[i]=void 0);n?e.props=t?o:Oa(o):e.type.props?e.props=o:e.props=a,e.attrs=a}function wi(e,s,n,t){const{props:o,attrs:a,vnode:{patchFlag:i}}=e,r=E(o),[l]=e.propsOptions;let u=!1;if((t||i>0)&&!(i&16)){if(i&8){const p=e.vnode.dynamicProps;for(let y=0;y<p.length;y++){let b=p[y];if(Bs(e.emitsOptions,b))continue;const T=s[b];if(l)if(P(a,b))T!==a[b]&&(a[b]=T,u=!0);else{const O=_e(b);o[O]=Un(l,r,O,T,e,!1)}else T!==a[b]&&(a[b]=T,u=!0)}}}else{to(e,s,o,a)&&(u=!0);let p;for(const y in r)(!s||!P(s,y)&&((p=is(y))===y||!P(s,p)))&&(l?n&&(n[y]!==void 0||n[p]!==void 0)&&(o[y]=Un(l,r,y,void 0,e,!0)):delete o[y]);if(a!==r)for(const y in a)(!s||!P(s,y))&&(delete a[y],u=!0)}u&&Ce(e,"set","$attrs")}function to(e,s,n,t){const[o,a]=e.propsOptions;let i=!1,r;if(s)for(let l in s){if(Is(l))continue;const u=s[l];let p;o&&P(o,p=_e(l))?!a||!a.includes(p)?n[p]=u:(r||(r={}))[p]=u:Bs(e.emitsOptions,l)||(!(l in t)||u!==t[l])&&(t[l]=u,i=!0)}if(a){const l=E(n),u=r||B;for(let p=0;p<a.length;p++){const y=a[p];n[y]=Un(o,l,y,u[y],e,!P(u,y))}}return i}function Un(e,s,n,t,o,a){const i=e[n];if(i!=null){const r=P(i,"default");if(r&&t===void 0){const l=i.default;if(i.type!==Function&&I(l)){const{propsDefaults:u}=o;n in u?t=u[n]:(ps(o),t=u[n]=l.call(null,s),Qe())}else t=l}i[0]&&(a&&!r?t=!1:i[1]&&(t===""||t===is(n))&&(t=!0))}return t}function oo(e,s,n=!1){const t=s.propsCache,o=t.get(e);if(o)return o;const a=e.props,i={},r=[];let l=!1;if(!I(e)){const p=y=>{l=!0;const[b,T]=oo(y,s,!0);Q(i,b),T&&r.push(...T)};!n&&s.mixins.length&&s.mixins.forEach(p),e.extends&&p(e.extends),e.mixins&&e.mixins.forEach(p)}if(!a&&!l)return L(e)&&t.set(e,os),os;if(D(a))for(let p=0;p<a.length;p++){const y=_e(a[p]);ao(y)&&(i[y]=B)}else if(a)for(const p in a){const y=_e(p);if(ao(y)){const b=a[p],T=i[y]=D(b)||I(b)?{type:b}:Object.assign({},b);if(T){const O=co(Boolean,T.type),k=co(String,T.type);T[0]=O>-1,T[1]=k<0||O<k,(O>-1||P(T,"default"))&&r.push(y)}}}const u=[i,r];return L(e)&&t.set(e,u),u}function ao(e){return e[0]!=="$"}function io(e){const s=e&&e.toString().match(/^\s*function (\w+)/);return s?s[1]:e===null?"null":""}function ro(e,s){return io(e)===io(s)}function co(e,s){return D(s)?s.findIndex(n=>ro(n,e)):I(s)&&ro(s,e)?0:-1}const lo=e=>e[0]==="_"||e==="$stable",Bn=e=>D(e)?e.map(ke):[ke(e)],xi=(e,s,n)=>{if(s._n)return s;const t=Mt((...o)=>Bn(s(...o)),n);return t._c=!1,t},uo=(e,s,n)=>{const t=e._ctx;for(const o in e){if(lo(o))continue;const a=e[o];if(I(a))s[o]=xi(o,a,t);else if(a!=null){const i=Bn(a);s[o]=()=>i}}},po=(e,s)=>{const n=Bn(s);e.slots.default=()=>n},zi=(e,s)=>{if(e.vnode.shapeFlag&32){const n=s._;n?(e.slots=E(s),Ps(s,"_",n)):uo(s,e.slots={})}else e.slots={},s&&po(e,s);Ps(e.slots,sn,1)},Ji=(e,s,n)=>{const{vnode:t,slots:o}=e;let a=!0,i=B;if(t.shapeFlag&32){const r=s._;r?n&&r===1?a=!1:(Q(o,s),!n&&r===1&&delete o._):(a=!s.$stable,uo(s,o)),i=s}else s&&(po(e,s),i={default:1});if(a)for(const r in o)!lo(r)&&!(r in i)&&delete o[r]};function ho(){return{app:null,config:{isNativeTag:Qo,performance:!1,globalProperties:{},optionMergeStrategies:{},errorHandler:void 0,warnHandler:void 0,compilerOptions:{}},mixins:[],components:{},directives:{},provides:Object.create(null),optionsCache:new WeakMap,propsCache:new WeakMap,emitsCache:new WeakMap}}let _i=0;function Si(e,s){return function(t,o=null){I(t)||(t=Object.assign({},t)),o!=null&&!L(o)&&(o=null);const a=ho(),i=new Set;let r=!1;const l=a.app={_uid:_i++,_component:t,_props:o,_container:null,_context:a,_instance:null,version:Ki,get config(){return a.config},set config(u){},use(u,...p){return i.has(u)||(u&&I(u.install)?(i.add(u),u.install(l,...p)):I(u)&&(i.add(u),u(l,...p))),l},mixin(u){return a.mixins.includes(u)||a.mixins.push(u),l},component(u,p){return p?(a.components[u]=p,l):a.components[u]},directive(u,p){return p?(a.directives[u]=p,l):a.directives[u]},mount(u,p,y){if(!r){const b=de(t,o);return b.appContext=a,p&&s?s(b,u):e(b,u,y),r=!0,l._container=u,u.__vue_app__=l,Kn(b.component)||b.component.proxy}},unmount(){r&&(e(null,l._container),delete l._container.__vue_app__)},provide(u,p){return a.provides[u]=p,l}};return l}}function Ln(e,s,n,t,o=!1){if(D(e)){e.forEach((b,T)=>Ln(b,s&&(D(s)?s[T]:s),n,t,o));return}if(Vs(t)&&!o)return;const a=t.shapeFlag&4?Kn(t.component)||t.component.proxy:t.el,i=o?null:a,{i:r,r:l}=e,u=s&&s.r,p=r.refs===B?r.refs={}:r.refs,y=r.setupState;if(u!=null&&u!==l&&(Z(u)?(p[u]=null,P(y,u)&&(y[u]=null)):ae(u)&&(u.value=null)),I(l))He(l,r,12,[i,p]);else{const b=Z(l),T=ae(l);if(b||T){const O=()=>{if(e.f){const k=b?P(y,l)?y[l]:p[l]:l.value;o?D(k)&&dn(k,a):D(k)?k.includes(a)||k.push(a):b?(p[l]=[a],P(y,l)&&(y[l]=p[l])):(l.value=[a],e.k&&(p[e.k]=l.value))}else b?(p[l]=i,P(y,l)&&(y[l]=i)):T&&(l.value=i,e.k&&(p[e.k]=i))};i?(O.id=-1,ce(O,n)):O()}}}const ce=Qa;function Ti(e){return ki(e)}function ki(e,s){const n=aa();n.__VUE__=!0;const{insert:t,remove:o,patchProp:a,createElement:i,createText:r,createComment:l,setText:u,setElementText:p,parentNode:y,nextSibling:b,setScopeId:T=ve,insertStaticContent:O}=e,k=(c,d,h,f=null,m=null,j=null,x=!1,v=null,w=!!d.dynamicChildren)=>{if(c===d)return;c&&!Ze(c,d)&&(f=on(c),Ne(c,m,j,!0),c=null),d.patchFlag===-2&&(w=!1,d.dynamicChildren=null);const{type:g,ref:_,shapeFlag:z}=d;switch(g){case Qs:V(c,d,h,f);break;case we:R(c,d,h,f);break;case en:c==null&&re(d,h,f,x);break;case pe:J(c,d,h,f,m,j,x,v,w);break;default:z&1?se(c,d,h,f,m,j,x,v,w):z&6?W(c,d,h,f,m,j,x,v,w):(z&64||z&128)&&g.process(c,d,h,f,m,j,x,v,w,hs)}_!=null&&m&&Ln(_,c&&c.ref,j,d||c,!d)},V=(c,d,h,f)=>{if(c==null)t(d.el=r(d.children),h,f);else{const m=d.el=c.el;d.children!==c.children&&u(m,d.children)}},R=(c,d,h,f)=>{c==null?t(d.el=l(d.children||""),h,f):d.el=c.el},re=(c,d,h,f)=>{[c.el,c.anchor]=O(c.children,d,h,f,c.el,c.anchor)},N=({el:c,anchor:d},h,f)=>{let m;for(;c&&c!==d;)m=b(c),t(c,h,f),c=m;t(d,h,f)},Y=({el:c,anchor:d})=>{let h;for(;c&&c!==d;)h=b(c),o(c),c=h;o(d)},se=(c,d,h,f,m,j,x,v,w)=>{x=x||d.type==="svg",c==null?We(d,h,f,m,j,x,v,w):K(c,d,m,j,x,v,w)},We=(c,d,h,f,m,j,x,v)=>{let w,g;const{type:_,props:z,shapeFlag:S,transition:A,dirs:q}=c;if(w=c.el=i(c.type,j,z&&z.is,z),S&8?p(w,c.children):S&16&&C(c.children,w,null,f,m,j&&_!=="foreignObject",x,v),q&&Ke(c,null,f,"created"),z){for(const M in z)M!=="value"&&!Is(M)&&a(w,M,null,z[M],j,c.children,f,m,Ee);"value"in z&&a(w,"value",null,z.value),(g=z.onVnodeBeforeMount)&&De(g,f,c)}Ae(w,c,c.scopeId,x,f),q&&Ke(c,null,f,"beforeMount");const H=(!m||m&&!m.pendingBranch)&&A&&!A.persisted;H&&A.beforeEnter(w),t(w,d,h),((g=z&&z.onVnodeMounted)||H||q)&&ce(()=>{g&&De(g,f,c),H&&A.enter(w),q&&Ke(c,null,f,"mounted")},m)},Ae=(c,d,h,f,m)=>{if(h&&T(c,h),f)for(let j=0;j<f.length;j++)T(c,f[j]);if(m){let j=m.subTree;if(d===j){const x=m.vnode;Ae(c,x,x.scopeId,x.slotScopeIds,m.parent)}}},C=(c,d,h,f,m,j,x,v,w=0)=>{for(let g=w;g<c.length;g++){const _=c[g]=v?Ue(c[g]):ke(c[g]);k(null,_,d,h,f,m,j,x,v)}},K=(c,d,h,f,m,j,x)=>{const v=d.el=c.el;let{patchFlag:w,dynamicChildren:g,dirs:_}=d;w|=c.patchFlag&16;const z=c.props||B,S=d.props||B;let A;h&&Xe(h,!1),(A=S.onVnodeBeforeUpdate)&&De(A,h,d,c),_&&Ke(d,c,h,"beforeUpdate"),h&&Xe(h,!0);const q=m&&d.type!=="foreignObject";if(g?U(c.dynamicChildren,g,v,h,f,q,j):x||F(c,d,v,null,h,f,q,j,!1),w>0){if(w&16)oe(v,d,z,S,h,f,m);else if(w&2&&z.class!==S.class&&a(v,"class",null,S.class,m),w&4&&a(v,"style",z.style,S.style,m),w&8){const H=d.dynamicProps;for(let M=0;M<H.length;M++){const X=H[M],Je=z[X],ms=S[X];(ms!==Je||X==="value")&&a(v,X,Je,ms,m,c.children,h,f,Ee)}}w&1&&c.children!==d.children&&p(v,d.children)}else!x&&g==null&&oe(v,d,z,S,h,f,m);((A=S.onVnodeUpdated)||_)&&ce(()=>{A&&De(A,h,d,c),_&&Ke(d,c,h,"updated")},f)},U=(c,d,h,f,m,j,x)=>{for(let v=0;v<d.length;v++){const w=c[v],g=d[v],_=w.el&&(w.type===pe||!Ze(w,g)||w.shapeFlag&70)?y(w.el):h;k(w,g,_,null,f,m,j,x,!0)}},oe=(c,d,h,f,m,j,x)=>{if(h!==f){if(h!==B)for(const v in h)!Is(v)&&!(v in f)&&a(c,v,h[v],null,x,d.children,m,j,Ee);for(const v in f){if(Is(v))continue;const w=f[v],g=h[v];w!==g&&v!=="value"&&a(c,v,g,w,x,d.children,m,j,Ee)}"value"in f&&a(c,"value",h.value,f.value)}},J=(c,d,h,f,m,j,x,v,w)=>{const g=d.el=c?c.el:r(""),_=d.anchor=c?c.anchor:r("");let{patchFlag:z,dynamicChildren:S,slotScopeIds:A}=d;A&&(v=v?v.concat(A):A),c==null?(t(g,h,f),t(_,h,f),C(d.children,h,_,m,j,x,v,w)):z>0&&z&64&&S&&c.dynamicChildren?(U(c.dynamicChildren,S,h,m,j,x,v),(d.key!=null||m&&d===m.subTree)&&mo(c,d,!0)):F(c,d,h,_,m,j,x,v,w)},W=(c,d,h,f,m,j,x,v,w)=>{d.slotScopeIds=v,c==null?d.shapeFlag&512?m.ctx.activate(d,h,f,x,w):he(d,h,f,m,j,x,w):Ts(c,d,w)},he=(c,d,h,f,m,j,x)=>{const v=c.component=Mi(c,f,m);if(Ks(c)&&(v.ctx.renderer=hs),Fi(v),v.asyncDep){if(m&&m.registerDep(v,ne),!c.el){const w=v.subTree=de(we);R(null,w,d,h)}return}ne(v,c,d,h,m,j,x)},Ts=(c,d,h)=>{const f=d.component=c.component;if(Ga(c,d,h))if(f.asyncDep&&!f.asyncResolved){G(f,d,h);return}else f.next=d,Ba(f.update),f.update();else d.el=c.el,f.vnode=d},ne=(c,d,h,f,m,j,x)=>{const v=()=>{if(c.isMounted){let{next:_,bu:z,u:S,parent:A,vnode:q}=c,H=_,M;Xe(c,!1),_?(_.el=q.el,G(c,_,x)):_=q,z&&mn(z),(M=_.props&&_.props.onVnodeBeforeUpdate)&&De(M,A,_,q),Xe(c,!0);const X=Cn(c),Je=c.subTree;c.subTree=X,k(Je,X,y(Je.el),on(Je),c,m,j),_.el=X.el,H===null&&Xa(c,X.el),S&&ce(S,m),(M=_.props&&_.props.onVnodeUpdated)&&ce(()=>De(M,A,_,q),m)}else{let _;const{el:z,props:S}=d,{bm:A,m:q,parent:H}=c,M=Vs(d);if(Xe(c,!1),A&&mn(A),!M&&(_=S&&S.onVnodeBeforeMount)&&De(_,H,d),Xe(c,!0),z&&nt){const X=()=>{c.subTree=Cn(c),nt(z,c.subTree,c,m,null)};M?d.type.__asyncLoader().then(()=>!c.isUnmounted&&X()):X()}else{const X=c.subTree=Cn(c);k(null,X,h,f,c,m,j),d.el=X.el}if(q&&ce(q,m),!M&&(_=S&&S.onVnodeMounted)){const X=d;ce(()=>De(_,H,X),m)}(d.shapeFlag&256||H&&Vs(H.vnode)&&H.vnode.shapeFlag&256)&&c.a&&ce(c.a,m),c.isMounted=!0,d=h=f=null}},w=c.effect=new jn(v,()=>In(g),c.scope),g=c.update=()=>w.run();g.id=c.uid,Xe(c,!0),g()},G=(c,d,h)=>{d.component=c;const f=c.vnode.props;c.vnode=d,c.next=null,wi(c,d.props,f,h),Ji(c,d.children,h),rs(),Ot(),cs()},F=(c,d,h,f,m,j,x,v,w=!1)=>{const g=c&&c.children,_=c?c.shapeFlag:0,z=d.children,{patchFlag:S,shapeFlag:A}=d;if(S>0){if(S&128){tn(g,z,h,f,m,j,x,v,w);return}else if(S&256){ns(g,z,h,f,m,j,x,v,w);return}}A&8?(_&16&&Ee(g,m,j),z!==g&&p(h,z)):_&16?A&16?tn(g,z,h,f,m,j,x,v,w):Ee(g,m,j,!0):(_&8&&p(h,""),A&16&&C(z,h,f,m,j,x,v,w))},ns=(c,d,h,f,m,j,x,v,w)=>{c=c||os,d=d||os;const g=c.length,_=d.length,z=Math.min(g,_);let S;for(S=0;S<z;S++){const A=d[S]=w?Ue(d[S]):ke(d[S]);k(c[S],A,h,null,m,j,x,v,w)}g>_?Ee(c,m,j,!0,!1,z):C(d,h,f,m,j,x,v,w,z)},tn=(c,d,h,f,m,j,x,v,w)=>{let g=0;const _=d.length;let z=c.length-1,S=_-1;for(;g<=z&&g<=S;){const A=c[g],q=d[g]=w?Ue(d[g]):ke(d[g]);if(Ze(A,q))k(A,q,h,null,m,j,x,v,w);else break;g++}for(;g<=z&&g<=S;){const A=c[z],q=d[S]=w?Ue(d[S]):ke(d[S]);if(Ze(A,q))k(A,q,h,null,m,j,x,v,w);else break;z--,S--}if(g>z){if(g<=S){const A=S+1,q=A<_?d[A].el:f;for(;g<=S;)k(null,d[g]=w?Ue(d[g]):ke(d[g]),h,q,m,j,x,v,w),g++}}else if(g>S)for(;g<=z;)Ne(c[g],m,j,!0),g++;else{const A=g,q=g,H=new Map;for(g=q;g<=S;g++){const me=d[g]=w?Ue(d[g]):ke(d[g]);me.key!=null&&H.set(me.key,g)}let M,X=0;const Je=S-q+1;let ms=!1,Lo=0;const ks=new Array(Je);for(g=0;g<Je;g++)ks[g]=0;for(g=A;g<=z;g++){const me=c[g];if(X>=Je){Ne(me,m,j,!0);continue}let Ie;if(me.key!=null)Ie=H.get(me.key);else for(M=q;M<=S;M++)if(ks[M-q]===0&&Ze(me,d[M])){Ie=M;break}Ie===void 0?Ne(me,m,j,!0):(ks[Ie-q]=g+1,Ie>=Lo?Lo=Ie:ms=!0,k(me,d[Ie],h,null,m,j,x,v,w),X++)}const Wo=ms?Di(ks):os;for(M=Wo.length-1,g=Je-1;g>=0;g--){const me=q+g,Ie=d[me],$o=me+1<_?d[me+1].el:f;ks[g]===0?k(null,Ie,h,$o,m,j,x,v,w):ms&&(M<0||g!==Wo[M]?ts(Ie,h,$o,2):M--)}}},ts=(c,d,h,f,m=null)=>{const{el:j,type:x,transition:v,children:w,shapeFlag:g}=c;if(g&6){ts(c.component.subTree,d,h,f);return}if(g&128){c.suspense.move(d,h,f);return}if(g&64){x.move(c,d,h,hs);return}if(x===pe){t(j,d,h);for(let z=0;z<w.length;z++)ts(w[z],d,h,f);t(c.anchor,d,h);return}if(x===en){N(c,d,h);return}if(f!==2&&g&1&&v)if(f===0)v.beforeEnter(j),t(j,d,h),ce(()=>v.enter(j),m);else{const{leave:z,delayLeave:S,afterLeave:A}=v,q=()=>t(j,d,h),H=()=>{z(j,()=>{q(),A&&A()})};S?S(j,q,H):H()}else t(j,d,h)},Ne=(c,d,h,f=!1,m=!1)=>{const{type:j,props:x,ref:v,children:w,dynamicChildren:g,shapeFlag:_,patchFlag:z,dirs:S}=c;if(v!=null&&Ln(v,null,h,c,!0),_&256){d.ctx.deactivate(c);return}const A=_&1&&S,q=!Vs(c);let H;if(q&&(H=x&&x.onVnodeBeforeUnmount)&&De(H,d,c),_&6)cc(c.component,h,f);else{if(_&128){c.suspense.unmount(h,f);return}A&&Ke(c,null,d,"beforeUnmount"),_&64?c.type.remove(c,d,h,m,hs,f):g&&(j!==pe||z>0&&z&64)?Ee(g,d,h,!1,!0):(j===pe&&z&384||!m&&_&16)&&Ee(w,d,h),f&&Uo(c)}(q&&(H=x&&x.onVnodeUnmounted)||A)&&ce(()=>{H&&De(H,d,c),A&&Ke(c,null,d,"unmounted")},h)},Uo=c=>{const{type:d,el:h,anchor:f,transition:m}=c;if(d===pe){rc(h,f);return}if(d===en){Y(c);return}const j=()=>{o(h),m&&!m.persisted&&m.afterLeave&&m.afterLeave()};if(c.shapeFlag&1&&m&&!m.persisted){const{leave:x,delayLeave:v}=m,w=()=>x(h,j);v?v(c.el,j,w):w()}else j()},rc=(c,d)=>{let h;for(;c!==d;)h=b(c),o(c),c=h;o(d)},cc=(c,d,h)=>{const{bum:f,scope:m,update:j,subTree:x,um:v}=c;f&&mn(f),m.stop(),j&&(j.active=!1,Ne(x,c,d,h)),v&&ce(v,d),ce(()=>{c.isUnmounted=!0},d),d&&d.pendingBranch&&!d.isUnmounted&&c.asyncDep&&!c.asyncResolved&&c.suspenseId===d.pendingId&&(d.deps--,d.deps===0&&d.resolve())},Ee=(c,d,h,f=!1,m=!1,j=0)=>{for(let x=j;x<c.length;x++)Ne(c[x],d,h,f,m)},on=c=>c.shapeFlag&6?on(c.component.subTree):c.shapeFlag&128?c.suspense.next():b(c.anchor||c.el),Bo=(c,d,h)=>{c==null?d._vnode&&Ne(d._vnode,null,null,!0):k(d._vnode||null,c,d,null,null,null,h),Ot(),qt(),d._vnode=c},hs={p:k,um:Ne,m:ts,r:Uo,mt:he,mc:C,pc:F,pbc:U,n:on,o:e};let st,nt;return s&&([st,nt]=s(hs)),{render:Bo,hydrate:st,createApp:Si(Bo,st)}}function Xe({effect:e,update:s},n){e.allowRecurse=s.allowRecurse=n}function mo(e,s,n=!1){const t=e.children,o=s.children;if(D(t)&&D(o))for(let a=0;a<t.length;a++){const i=t[a];let r=o[a];r.shapeFlag&1&&!r.dynamicChildren&&((r.patchFlag<=0||r.patchFlag===32)&&(r=o[a]=Ue(o[a]),r.el=i.el),n||mo(i,r)),r.type===Qs&&(r.el=i.el)}}function Di(e){const s=e.slice(),n=[0];let t,o,a,i,r;const l=e.length;for(t=0;t<l;t++){const u=e[t];if(u!==0){if(o=n[n.length-1],e[o]<u){s[t]=o,n.push(t);continue}for(a=0,i=n.length-1;a<i;)r=a+i>>1,e[n[r]]<u?a=r+1:i=r;u<e[n[a]]&&(a>0&&(s[t]=n[a-1]),n[a]=t)}}for(a=n.length,i=n[a-1];a-- >0;)n[a]=i,i=s[i];return n}const Ai=e=>e.__isTeleport,pe=Symbol(void 0),Qs=Symbol(void 0),we=Symbol(void 0),en=Symbol(void 0),xs=[];let xe=null;function le(e=!1){xs.push(xe=e?null:[])}function Ni(){xs.pop(),xe=xs[xs.length-1]||null}let zs=1;function fo(e){zs+=e}function go(e){return e.dynamicChildren=zs>0?xe||os:null,Ni(),zs>0&&xe&&xe.push(e),e}function ze(e,s,n,t,o,a){return go(be(e,s,n,t,o,a,!0))}function Wn(e,s,n,t,o){return go(de(e,s,n,t,o,!0))}function Ii(e){return e?e.__v_isVNode===!0:!1}function Ze(e,s){return e.type===s.type&&e.key===s.key}const sn="__vInternal",yo=({key:e})=>e??null,nn=({ref:e,ref_key:s,ref_for:n})=>e!=null?Z(e)||ae(e)||I(e)?{i:ge,r:e,k:s,f:!!n}:e:null;function be(e,s=null,n=null,t=0,o=null,a=e===pe?0:1,i=!1,r=!1){const l={__v_isVNode:!0,__v_skip:!0,type:e,props:s,key:s&&yo(s),ref:s&&nn(s),scopeId:Rt,slotScopeIds:null,children:n,component:null,suspense:null,ssContent:null,ssFallback:null,dirs:null,transition:null,el:null,anchor:null,target:null,targetAnchor:null,staticCount:0,shapeFlag:a,patchFlag:t,dynamicProps:o,dynamicChildren:null,appContext:null,ctx:ge};return r?(Vn(l,n),a&128&&e.normalize(l)):n&&(l.shapeFlag|=Z(n)?8:16),zs>0&&!i&&xe&&(l.patchFlag>0||a&6)&&l.patchFlag!==32&&xe.push(l),l}const de=Ci;function Ci(e,s=null,n=null,t=0,o=null,a=!1){if((!e||e===pi)&&(e=we),Ii(e)){const r=Ye(e,s,!0);return n&&Vn(r,n),zs>0&&!a&&xe&&(r.shapeFlag&6?xe[xe.indexOf(e)]=r:xe.push(r)),r.patchFlag|=-2,r}if(Li(e)&&(e=e.__vccOpts),s){s=Oi(s);let{class:r,style:l}=s;r&&!Z(r)&&(s.class=Ds(r)),L(l)&&(kt(l)&&!D(l)&&(l=Q({},l)),s.style=rn(l))}const i=Z(e)?1:Za(e)?128:Ai(e)?64:L(e)?4:I(e)?2:0;return be(e,s,n,t,o,i,a,!0)}function Oi(e){return e?kt(e)||sn in e?Q({},e):e:null}function Ye(e,s,n=!1){const{props:t,ref:o,patchFlag:a,children:i}=e,r=s?Pi(t||{},s):t;return{__v_isVNode:!0,__v_skip:!0,type:e.type,props:r,key:r&&yo(r),ref:s&&s.ref?n&&o?D(o)?o.concat(nn(s)):[o,nn(s)]:nn(s):o,scopeId:e.scopeId,slotScopeIds:e.slotScopeIds,children:i,target:e.target,targetAnchor:e.targetAnchor,staticCount:e.staticCount,shapeFlag:e.shapeFlag,patchFlag:s&&e.type!==pe?a===-1?16:a|16:a,dynamicProps:e.dynamicProps,dynamicChildren:e.dynamicChildren,appContext:e.appContext,dirs:e.dirs,transition:e.transition,component:e.component,suspense:e.suspense,ssContent:e.ssContent&&Ye(e.ssContent),ssFallback:e.ssFallback&&Ye(e.ssFallback),el:e.el,anchor:e.anchor,ctx:e.ctx}}function bo(e=" ",s=0){return de(Qs,null,e,s)}function qi(e,s){const n=de(en,null,e);return n.staticCount=s,n}function $n(e="",s=!1){return s?(le(),Wn(we,null,e)):de(we,null,e)}function ke(e){return e==null||typeof e=="boolean"?de(we):D(e)?de(pe,null,e.slice()):typeof e=="object"?Ue(e):de(Qs,null,String(e))}function Ue(e){return e.el===null&&e.patchFlag!==-1||e.memo?e:Ye(e)}function Vn(e,s){let n=0;const{shapeFlag:t}=e;if(s==null)s=null;else if(D(s))n=16;else if(typeof s=="object")if(t&65){const o=s.default;o&&(o._c&&(o._d=!1),Vn(e,o()),o._c&&(o._d=!0));return}else{n=32;const o=s._;!o&&!(sn in s)?s._ctx=ge:o===3&&ge&&(ge.slots._===1?s._=1:(s._=2,e.patchFlag|=1024))}else I(s)?(s={default:s,_ctx:ge},n=32):(s=String(s),t&64?(n=16,s=[bo(s)]):n=8);e.children=s,e.shapeFlag|=n}function Pi(...e){const s={};for(let n=0;n<e.length;n++){const t=e[n];for(const o in t)if(o==="class")s.class!==t.class&&(s.class=Ds([s.class,t.class]));else if(o==="style")s.style=rn([s.style,t.style]);else if(As(o)){const a=s[o],i=t[o];i&&a!==i&&!(D(a)&&a.includes(i))&&(s[o]=a?[].concat(a,i):i)}else o!==""&&(s[o]=t[o])}return s}function De(e,s,n,t=null){fe(e,s,7,[n,t])}const Ei=ho();let Ri=0;function Mi(e,s,n){const t=e.type,o=(s?s.appContext:e.appContext)||Ei,a={uid:Ri++,vnode:e,type:t,parent:s,appContext:o,root:null,next:null,subTree:null,effect:null,update:null,scope:new ia(!0),render:null,proxy:null,exposed:null,exposeProxy:null,withProxy:null,provides:s?s.provides:Object.create(o.provides),accessCache:null,renderCache:[],components:null,directives:null,propsOptions:oo(t,o),emitsOptions:Et(t,o),emit:null,emitted:null,propsDefaults:B,inheritAttrs:t.inheritAttrs,ctx:B,data:B,props:B,attrs:B,slots:B,refs:B,setupState:B,setupContext:null,suspense:n,suspenseId:n?n.pendingId:0,asyncDep:null,asyncResolved:!1,isMounted:!1,isUnmounted:!1,isDeactivated:!1,bc:null,c:null,bm:null,m:null,bu:null,u:null,um:null,bum:null,da:null,a:null,rtg:null,rtc:null,ec:null,sp:null};return a.ctx={_:a},a.root=s?s.root:a,a.emit=$a.bind(null,a),e.ce&&e.ce(a),a}let ee=null;const vo=()=>ee||ge,ps=e=>{ee=e,e.scope.on()},Qe=()=>{ee&&ee.scope.off(),ee=null};function jo(e){return e.vnode.shapeFlag&4}let Js=!1;function Fi(e,s=!1){Js=s;const{props:n,children:t}=e.vnode,o=jo(e);ji(e,n,o,s),zi(e,t);const a=o?Hi(e,s):void 0;return Js=!1,a}function Hi(e,s){const n=e.type;e.accessCache=Object.create(null),e.proxy=Dt(new Proxy(e.ctx,mi));const{setup:t}=n;if(t){const o=e.setupContext=t.length>1?Ui(e):null;ps(e),rs();const a=He(t,e,0,[e.props,o]);if(cs(),Qe(),it(a)){if(a.then(Qe,Qe),s)return a.then(i=>{wo(e,i,s)}).catch(i=>{Us(i,e,0)});e.asyncDep=a}else wo(e,a,s)}else zo(e,s)}function wo(e,s,n){I(s)?e.type.__ssrInlineRender?e.ssrRender=s:e.render=s:L(s)&&(e.setupState=At(s)),zo(e,n)}let xo;function zo(e,s,n){const t=e.type;if(!e.render){if(!s&&xo&&!t.render){const o=t.template||Hn(e).template;if(o){const{isCustomElement:a,compilerOptions:i}=e.appContext.config,{delimiters:r,compilerOptions:l}=t,u=Q(Q({isCustomElement:a,delimiters:r},i),l);t.render=xo(o,u)}}e.render=t.render||ve}ps(e),rs(),fi(e),cs(),Qe()}function Yi(e){return new Proxy(e.attrs,{get(s,n){return ue(e,"get","$attrs"),s[n]}})}function Ui(e){const s=t=>{e.exposed=t||{}};let n;return{get attrs(){return n||(n=Yi(e))},slots:e.slots,emit:e.emit,expose:s}}function Kn(e){if(e.exposed)return e.exposeProxy||(e.exposeProxy=new Proxy(At(Dt(e.exposed)),{get(s,n){if(n in s)return s[n];if(n in ws)return ws[n](e)},has(s,n){return n in s||n in ws}}))}function Bi(e,s=!0){return I(e)?e.displayName||e.name:e.name||s&&e.__name}function Li(e){return I(e)&&"__vccOpts"in e}const Wi=(e,s)=>Fa(e,s,Js),$i=Symbol(""),Vi=()=>Ws($i),Ki="3.2.45",Gi="http://www.w3.org/2000/svg",es=typeof document<"u"?document:null,Jo=es&&es.createElement("template"),Xi={insert:(e,s,n)=>{s.insertBefore(e,n||null)},remove:e=>{const s=e.parentNode;s&&s.removeChild(e)},createElement:(e,s,n,t)=>{const o=s?es.createElementNS(Gi,e):es.createElement(e,n?{is:n}:void 0);return e==="select"&&t&&t.multiple!=null&&o.setAttribute("multiple",t.multiple),o},createText:e=>es.createTextNode(e),createComment:e=>es.createComment(e),setText:(e,s)=>{e.nodeValue=s},setElementText:(e,s)=>{e.textContent=s},parentNode:e=>e.parentNode,nextSibling:e=>e.nextSibling,querySelector:e=>es.querySelector(e),setScopeId(e,s){e.setAttribute(s,"")},insertStaticContent(e,s,n,t,o,a){const i=n?n.previousSibling:s.lastChild;if(o&&(o===a||o.nextSibling))for(;s.insertBefore(o.cloneNode(!0),n),!(o===a||!(o=o.nextSibling)););else{Jo.innerHTML=t?`<svg>${e}</svg>`:e;const r=Jo.content;if(t){const l=r.firstChild;for(;l.firstChild;)r.appendChild(l.firstChild);r.removeChild(l)}s.insertBefore(r,n)}return[i?i.nextSibling:s.firstChild,n?n.previousSibling:s.lastChild]}};function Zi(e,s,n){const t=e._vtc;t&&(s=(s?[s,...t]:[...t]).join(" ")),s==null?e.removeAttribute("class"):n?e.setAttribute("class",s):e.className=s}function Qi(e,s,n){const t=e.style,o=Z(n);if(n&&!o){for(const a in n)Gn(t,a,n[a]);if(s&&!Z(s))for(const a in s)n[a]==null&&Gn(t,a,"")}else{const a=t.display;o?s!==n&&(t.cssText=n):s&&e.removeAttribute("style"),"_vod"in e&&(t.display=a)}}const _o=/\s*!important$/;function Gn(e,s,n){if(D(n))n.forEach(t=>Gn(e,s,t));else if(n==null&&(n=""),s.startsWith("--"))e.setProperty(s,n);else{const t=er(e,s);_o.test(n)?e.setProperty(is(t),n.replace(_o,""),"important"):e[t]=n}}const So=["Webkit","Moz","ms"],Xn={};function er(e,s){const n=Xn[s];if(n)return n;let t=_e(s);if(t!=="filter"&&t in e)return Xn[s]=t;t=Os(t);for(let o=0;o<So.length;o++){const a=So[o]+t;if(a in e)return Xn[s]=a}return s}const To="http://www.w3.org/1999/xlink";function sr(e,s,n,t,o){if(t&&s.startsWith("xlink:"))n==null?e.removeAttributeNS(To,s.slice(6,s.length)):e.setAttributeNS(To,s,n);else{const a=Zo(s);n==null||a&&!tt(n)?e.removeAttribute(s):e.setAttribute(s,a?"":n)}}function nr(e,s,n,t,o,a,i){if(s==="innerHTML"||s==="textContent"){t&&i(t,o,a),e[s]=n??"";return}if(s==="value"&&e.tagName!=="PROGRESS"&&!e.tagName.includes("-")){e._value=n;const l=n??"";(e.value!==l||e.tagName==="OPTION")&&(e.value=l),n==null&&e.removeAttribute(s);return}let r=!1;if(n===""||n==null){const l=typeof e[s];l==="boolean"?n=tt(n):n==null&&l==="string"?(n="",r=!0):l==="number"&&(n=0,r=!0)}try{e[s]=n}catch{}r&&e.removeAttribute(s)}function tr(e,s,n,t){e.addEventListener(s,n,t)}function or(e,s,n,t){e.removeEventListener(s,n,t)}function ar(e,s,n,t,o=null){const a=e._vei||(e._vei={}),i=a[s];if(t&&i)i.value=t;else{const[r,l]=ir(s);if(t){const u=a[s]=lr(t,o);tr(e,r,u,l)}else i&&(or(e,r,i,l),a[s]=void 0)}}const ko=/(?:Once|Passive|Capture)$/;function ir(e){let s;if(ko.test(e)){s={};let t;for(;t=e.match(ko);)e=e.slice(0,e.length-t[0].length),s[t[0].toLowerCase()]=!0}return[e[2]===":"?e.slice(3):is(e.slice(2)),s]}let Zn=0;const rr=Promise.resolve(),cr=()=>Zn||(rr.then(()=>Zn=0),Zn=Date.now());function lr(e,s){const n=t=>{if(!t._vts)t._vts=Date.now();else if(t._vts<=n.attached)return;fe(dr(t,n.value),s,5,[t])};return n.value=e,n.attached=cr(),n}function dr(e,s){if(D(s)){const n=e.stopImmediatePropagation;return e.stopImmediatePropagation=()=>{n.call(e),e._stopped=!0},s.map(t=>o=>!o._stopped&&t&&t(o))}else return s}const Do=/^on[a-z]/,ur=(e,s,n,t,o=!1,a,i,r,l)=>{s==="class"?Zi(e,t,o):s==="style"?Qi(e,n,t):As(s)?ln(s)||ar(e,s,n,t,i):(s[0]==="."?(s=s.slice(1),!0):s[0]==="^"?(s=s.slice(1),!1):pr(e,s,t,o))?nr(e,s,t,a,i,r,l):(s==="true-value"?e._trueValue=t:s==="false-value"&&(e._falseValue=t),sr(e,s,t,o))};function pr(e,s,n,t){return t?!!(s==="innerHTML"||s==="textContent"||s in e&&Do.test(s)&&I(n)):s==="spellcheck"||s==="draggable"||s==="translate"||s==="form"||s==="list"&&e.tagName==="INPUT"||s==="type"&&e.tagName==="TEXTAREA"||Do.test(s)&&Z(n)?!1:s in e}const Be="transition",_s="animation",Ao={name:String,type:String,css:{type:Boolean,default:!0},duration:[String,Number,Object],enterFromClass:String,enterActiveClass:String,enterToClass:String,appearFromClass:String,appearActiveClass:String,appearToClass:String,leaveFromClass:String,leaveActiveClass:String,leaveToClass:String},hr=Q({},ni.props,Ao),ss=(e,s=[])=>{D(e)?e.forEach(n=>n(...s)):e&&e(...s)},No=e=>e?D(e)?e.some(s=>s.length>1):e.length>1:!1;function mr(e){const s={};for(const J in e)J in Ao||(s[J]=e[J]);if(e.css===!1)return s;const{name:n="v",type:t,duration:o,enterFromClass:a=`${n}-enter-from`,enterActiveClass:i=`${n}-enter-active`,enterToClass:r=`${n}-enter-to`,appearFromClass:l=a,appearActiveClass:u=i,appearToClass:p=r,leaveFromClass:y=`${n}-leave-from`,leaveActiveClass:b=`${n}-leave-active`,leaveToClass:T=`${n}-leave-to`}=e,O=fr(o),k=O&&O[0],V=O&&O[1],{onBeforeEnter:R,onEnter:re,onEnterCancelled:N,onLeave:Y,onLeaveCancelled:se,onBeforeAppear:We=R,onAppear:Ae=re,onAppearCancelled:C=N}=s,K=(J,W,he)=>{Le(J,W?p:r),Le(J,W?u:i),he&&he()},U=(J,W)=>{J._isLeaving=!1,Le(J,y),Le(J,T),Le(J,b),W&&W()},oe=J=>(W,he)=>{const Ts=J?Ae:re,ne=()=>K(W,J,he);ss(Ts,[W,ne]),Io(()=>{Le(W,J?l:a),Pe(W,J?p:r),No(Ts)||Co(W,t,k,ne)})};return Q(s,{onBeforeEnter(J){ss(R,[J]),Pe(J,a),Pe(J,i)},onBeforeAppear(J){ss(We,[J]),Pe(J,l),Pe(J,u)},onEnter:oe(!1),onAppear:oe(!0),onLeave(J,W){J._isLeaving=!0;const he=()=>U(J,W);Pe(J,y),Eo(),Pe(J,b),Io(()=>{J._isLeaving&&(Le(J,y),Pe(J,T),No(Y)||Co(J,t,V,he))}),ss(Y,[J,he])},onEnterCancelled(J){K(J,!1),ss(N,[J])},onAppearCancelled(J){K(J,!0),ss(C,[J])},onLeaveCancelled(J){U(J),ss(se,[J])}})}function fr(e){if(e==null)return null;if(L(e))return[Qn(e.enter),Qn(e.leave)];{const s=Qn(e);return[s,s]}}function Qn(e){return fn(e)}function Pe(e,s){s.split(/\s+/).forEach(n=>n&&e.classList.add(n)),(e._vtc||(e._vtc=new Set)).add(s)}function Le(e,s){s.split(/\s+/).forEach(t=>t&&e.classList.remove(t));const{_vtc:n}=e;n&&(n.delete(s),n.size||(e._vtc=void 0))}function Io(e){requestAnimationFrame(()=>{requestAnimationFrame(e)})}let gr=0;function Co(e,s,n,t){const o=e._endId=++gr,a=()=>{o===e._endId&&t()};if(n)return setTimeout(a,n);const{type:i,timeout:r,propCount:l}=Oo(e,s);if(!i)return t();const u=i+"end";let p=0;const y=()=>{e.removeEventListener(u,b),a()},b=T=>{T.target===e&&++p>=l&&y()};setTimeout(()=>{p<l&&y()},r+1),e.addEventListener(u,b)}function Oo(e,s){const n=window.getComputedStyle(e),t=O=>(n[O]||"").split(", "),o=t(`${Be}Delay`),a=t(`${Be}Duration`),i=qo(o,a),r=t(`${_s}Delay`),l=t(`${_s}Duration`),u=qo(r,l);let p=null,y=0,b=0;s===Be?i>0&&(p=Be,y=i,b=a.length):s===_s?u>0&&(p=_s,y=u,b=l.length):(y=Math.max(i,u),p=y>0?i>u?Be:_s:null,b=p?p===Be?a.length:l.length:0);const T=p===Be&&/\b(transform|all)(,|$)/.test(t(`${Be}Property`).toString());return{type:p,timeout:y,propCount:b,hasTransform:T}}function qo(e,s){for(;e.length<s.length;)e=e.concat(e);return Math.max(...s.map((n,t)=>Po(n)+Po(e[t])))}function Po(e){return Number(e.slice(0,-1).replace(",","."))*1e3}function Eo(){return document.body.offsetHeight}const Ro=new WeakMap,Mo=new WeakMap,yr={name:"TransitionGroup",props:Q({},hr,{tag:String,moveClass:String}),setup(e,{slots:s}){const n=vo(),t=Ut();let o,a;return Vt(()=>{if(!o.length)return;const i=e.moveClass||`${e.name||"v"}-move`;if(!wr(o[0].el,n.vnode.el,i))return;o.forEach(br),o.forEach(vr);const r=o.filter(jr);Eo(),r.forEach(l=>{const u=l.el,p=u.style;Pe(u,i),p.transform=p.webkitTransform=p.transitionDuration="";const y=u._moveCb=b=>{b&&b.target!==u||(!b||/transform$/.test(b.propertyName))&&(u.removeEventListener("transitionend",y),u._moveCb=null,Le(u,i))};u.addEventListener("transitionend",y)})}),()=>{const i=E(e),r=mr(i);let l=i.tag||pe;o=a,a=s.default?Pn(s.default()):[];for(let u=0;u<a.length;u++){const p=a[u];p.key!=null&&js(p,vs(p,r,t,n))}if(o)for(let u=0;u<o.length;u++){const p=o[u];js(p,vs(p,r,t,n)),Ro.set(p,p.el.getBoundingClientRect())}return de(l,null,a)}}};function br(e){const s=e.el;s._moveCb&&s._moveCb(),s._enterCb&&s._enterCb()}function vr(e){Mo.set(e,e.el.getBoundingClientRect())}function jr(e){const s=Ro.get(e),n=Mo.get(e),t=s.left-n.left,o=s.top-n.top;if(t||o){const a=e.el.style;return a.transform=a.webkitTransform=`translate(${t}px,${o}px)`,a.transitionDuration="0s",e}}function wr(e,s,n){const t=e.cloneNode();e._vtc&&e._vtc.forEach(i=>{i.split(/\s+/).forEach(r=>r&&t.classList.remove(r))}),n.split(/\s+/).forEach(i=>i&&t.classList.add(i)),t.style.display="none";const o=s.nodeType===1?s:s.parentNode;o.appendChild(t);const{hasTransform:a}=Oo(t);return o.removeChild(t),a}const xr=["ctrl","shift","alt","meta"],zr={stop:e=>e.stopPropagation(),prevent:e=>e.preventDefault(),self:e=>e.target!==e.currentTarget,ctrl:e=>!e.ctrlKey,shift:e=>!e.shiftKey,alt:e=>!e.altKey,meta:e=>!e.metaKey,left:e=>"button"in e&&e.button!==0,middle:e=>"button"in e&&e.button!==1,right:e=>"button"in e&&e.button!==2,exact:(e,s)=>xr.some(n=>e[`${n}Key`]&&!s.includes(n))},Jr=(e,s)=>(n,...t)=>{for(let o=0;o<s.length;o++){const a=zr[s[o]];if(a&&a(n,s))return}return e(n,...t)},_r=Q({patchProp:ur},Xi);let Fo;function Sr(){return Fo||(Fo=Ti(_r))}const Tr=(...e)=>{const s=Sr().createApp(...e),{mount:n}=s;return s.mount=t=>{const o=kr(t);if(!o)return;const a=s._component;!I(a)&&!a.render&&!a.template&&(a.template=o.innerHTML),o.innerHTML="";const i=n(o,!1,o instanceof SVGElement);return o instanceof Element&&(o.removeAttribute("v-cloak"),o.setAttribute("data-v-app","")),i},s};function kr(e){return Z(e)?document.querySelector(e):e}const et={"compiletime/ambiguous-string-assign":{choices:[{id:"success",label:"Yes",summary:"Casting the <code>JsonVariant</code> fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Casting the <code>JsonVariant</code> doesn't fix the issue",next:"deadend"}],content:`<p>This error occurs when you try to assign a <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a> to a <a href="https://www.arduino.cc/reference/en/language/variables/data-types/stringobject/"><code>String</code></a>.</p>
<p>Indeed, due to the way these two classes are defined, you can <em>construct</em> a <a href="https://www.arduino.cc/reference/en/language/variables/data-types/stringobject/"><code>String</code></a> from a <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a>, but you cannot <em>assign</em> a <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a> to an existing string.</p>
<pre><code class="hljs">String name <span class="hljs-operator">=</span> doc[<span class="hljs-string">&quot;name&quot;</span>]<span class="hljs-comment">;  // contruction -&gt; works</span>
<span class="hljs-attribute">name</span> <span class="hljs-operator">=</span> doc[<span class="hljs-string">&quot;name&quot;</span>]<span class="hljs-comment">;         // assignment  -&gt; fails</span>
</code></pre>
<p>You can easily workaround this problem by explicitly casting the <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a>, like so:</p>
<pre><code class="hljs"><span class="hljs-attribute">name</span> <span class="hljs-operator">=</span> doc[<span class="hljs-string">&quot;name&quot;</span>].as&lt;String&gt;()<span class="hljs-comment">;</span>
</code></pre>
<p>Please see <a href="/v6/error/ambiguous-overload-for-operator-equal/">error: ambiguous overload for 'operator=' (operand types are 'String' and ...)</a> for details.</p>
<p>Did this solve your issue?</p>
`},"compiletime/assignment-of-read-only-location":{choices:[{id:"success",label:"Yes",summary:"Using an integer or switching to an object fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Neither using an integer nor switching to an object fixes the issue",next:"deadend"}],content:`<p>This error occurs when you pass a string to <a href="/v6/api/jsonarray/subscript/"><code>JsonArray::operator[]</code></a>; i.e., when you use an array like an object.</p>
<p>For example, if you try to compile the following code:</p>
<pre><code class="hljs language-c++">StaticJsonDocument&lt;<span class="hljs-number">128</span>&gt; doc;
JsonArray array = doc.<span class="hljs-built_in">createNestedArray</span>(<span class="hljs-string">&quot;array&quot;</span>);
array[<span class="hljs-string">&quot;key&quot;</span>] = <span class="hljs-string">&quot;value&quot;</span>;  <span class="hljs-comment">// ERROR</span>
</code></pre>
<p>...you'll get the following error message:</p>
<pre><code class="hljs language-text">error: assignment of read-only location &#x27;*(((const char*)&quot;key&quot;) + ((sizetype)data.ArduinoJson6194_F1::ArrayRef::&lt;anonymous&gt;.ArduinoJson6194_F1::ArrayRefBase&lt;ArduinoJson6194_F1::CollectionData&gt;::operator bool()))&#x27;
   38 |   data[&quot;key&quot;] = &quot;value&quot;;
      |   ~~~~~~~~~~~~^~~~~~~~~
</code></pre>
<p>The solution is to use the array correctly or to switch to an object.</p>
<p>So do either this:</p>
<pre><code class="hljs language-c++">StaticJsonDocument&lt;<span class="hljs-number">128</span>&gt; doc;
JsonArray array = doc.<span class="hljs-built_in">createNestedArray</span>(<span class="hljs-string">&quot;array&quot;</span>);
array[<span class="hljs-number">0</span>] = <span class="hljs-string">&quot;value&quot;</span>;
</code></pre>
<p>...or that:</p>
<pre><code class="hljs language-c++">StaticJsonDocument&lt;<span class="hljs-number">128</span>&gt; doc;
JsonArray obj = doc.<span class="hljs-built_in">createNestedObject</span>(<span class="hljs-string">&quot;obj&quot;</span>);
obj[<span class="hljs-string">&quot;key&quot;</span>] = <span class="hljs-string">&quot;value&quot;</span>;
</code></pre>
<p>Did this solve your issue?</p>
`},"compiletime/basicjsondocument-default-constructor":{choices:[{id:"success",label:"Yes",summary:"Passing the capacity to the constructor fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Passing the capacity to the constructor doesn't fix the issue",next:"deadend"}],content:`<p>This is a linting error you may have in Visual Studio Code if your program contains something like this:</p>
<pre><code class="hljs language-c++">DynamicJsonDocument doc;
</code></pre>
<p>If you try to compile this code, you will get the following error:</p>
<pre><code class="hljs language-text">no matching function for call to &#x27;ArduinoJson6194_F1::BasicJsonDocument&lt;ArduinoJson6194_F1::DefaultAllocator&gt;::BasicJsonDocument()&#x27;
</code></pre>
<p>These errors occur when you forget to pass the capacity to the constructor of <a href="/v6/api/dynamicjsondocument/"><code>DynamicJsonDocument</code></a>.</p>
<p>To fix them, you must specify the capacity of the memory pool, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-function">DynamicJsonDocument <span class="hljs-title">doc</span><span class="hljs-params">(<span class="hljs-number">2048</span>)</span></span>;
</code></pre>
<p>As usual, you can use the <a href="/v6/assistant/">ArduinoJson Assistant</a> to compute the right capacity for your project.</p>
<p>For more information, please read <a href="/v6/error/no-matching-function-for-call-to-basicjsondocument-basicjsondocument/">no matching function for call to 'BasicJsonDocument::BasicJsonDocument()'</a>.</p>
<p>Did this solve your issue?</p>
`},"compiletime/basicjsondocument-missing-argument":{choices:[{id:"success",label:"Yes",summary:"Passing the capacity to the constructor fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Passing the capacity to the constructor doesn't fix the issue",next:"deadend"}],content:`<p>This error occurs when you forget to pass the capacity to the constructor of <a href="/v6/api/dynamicjsondocument/"><code>DynamicJsonDocument</code></a>, like so:</p>
<pre><code class="hljs language-c++">DynamicJsonDocument doc;
</code></pre>
<p>Instead, you need to specify the capacity of the memory pool, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-function">DynamicJsonDocument <span class="hljs-title">doc</span><span class="hljs-params">(<span class="hljs-number">2048</span>)</span></span>;
</code></pre>
<p>As usual, you can use the <a href="/v6/assistant/">ArduinoJson Assistant</a> to compute the right capacity for your project.</p>
<p>For more information, please read <a href="/v6/error/no-matching-function-for-call-to-basicjsondocument-basicjsondocument/">no matching function for call to 'BasicJsonDocument::BasicJsonDocument()'</a>.</p>
<p>Did this solve your issue?</p>
`},"compiletime/cast-void-to-flashstringhelper":{choices:[{id:"success",label:"Yes",summary:"Setting <code>ARDUINOJSON_ENABLE_PROGMEM</code> to <code>0</code> fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Setting <code>ARDUINOJSON_ENABLE_PROGMEM</code> to <code>0</code> doesn't fix the issue",next:"deadend"}],content:`<p>The error should look like this:</p>
<pre><code class="hljs language-text">.../src/ArduinoJson/Polyfills/pgmspace_generic.hpp:14:10: error: reinterpret_cast from &#x27;const void&#x27; to &#x27;const __FlashStringHelper *&#x27; is not allowed
  return reinterpret_cast&lt;T&gt;(pgm_read_ptr(p));
         ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
</code></pre>
<p>This error indicate that the return type of <code>pgm_read_ptr()</code> is incorrect: it should be <code>const void*</code>, but the error message shows it's <code>const void</code>.</p>
<p>This is a common bug in cores (or &quot;framework&quot;) that emulate the original <code>pgmspace.h</code> header.
It already poped up in:</p>
<ul>
<li>ArduinoFake (issue <a href="https://github.com/bblanchon/ArduinoJson/issues/1676">#1676</a>)</li>
<li>ESP8266 (issue <a href="https://github.com/bblanchon/ArduinoJson/issues/1442">#1442</a>)</li>
<li>Industruino (issue <a href="https://github.com/bblanchon/ArduinoJson/issues/1519">#1519</a>)</li>
<li>Particle Argon (issue <a href="https://github.com/bblanchon/ArduinoJson/issues/1591">#1591</a>)</li>
<li>Teknic ClearCore (issue <a href="https://github.com/bblanchon/ArduinoJson/issues/1381">#1381</a>)</li>
<li>Others (issue <a href="https://github.com/bblanchon/ArduinoJson/issues/1536">#1536</a>)</li>
</ul>
<p>The root of this bug was fixed in October 2020 in <a href="https://github.com/arduino/ArduinoCore-API/pull/118">arduino/ArduinoCore-API#118</a>, but many clones are still using the old code.</p>
<p>To fix this issue, update your Arduino Core (or &quot;framework&quot;) to the newest version.</p>
<p>If the same error still occurs, please contact the relevant support team.
In the meantime, you can work around this issue by disabling PROGMEM support in ArduinoJson by setting <a href="/v6/api/config/enable_progmem/"><code>ARDUINOJSON_ENABLE_PROGMEM</code></a> to <code>0</code>, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-meta">#<span class="hljs-keyword">define</span> ARDUINOJSON_ENABLE_PROGMEM 0</span>
<span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;ArduinoJson.h&gt;</span></span>
</code></pre>
<p>Did this solve your issue?</p>
`},"compiletime/char-pointer-conversion":{choices:[{id:"success",label:"Yes",summary:"Changing the pointer type fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Changing the pointer type doesn't fix the issue",next:"deadend"}],content:`<p>This error occurs when you try to store a pointer of type <code>const char*</code> into a variable of type <code>char*</code>, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-type">char</span>* eventName = doc[<span class="hljs-string">&quot;event&quot;</span>];
</code></pre>
<p>Indeed, <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a> returns a <code>const char*</code>, not a <code>char*</code>. You must change the type of the pointer, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-type">const</span> <span class="hljs-type">char</span>* eventName = doc[<span class="hljs-string">&quot;event&quot;</span>];
</code></pre>
<p>There are other similar situations where this error can occur. For more information, please read <a href="/v6/error/invalid-conversion-from-const-char-to-char/">invalid conversion from 'const char*' to 'char*' [-fpermissive]</a>.</p>
<p>Did this solve your issue?</p>
`},"compiletime/class-from-arduinojson5-keep":{choices:[{id:"success",label:"Yes",summary:"Downloading ArduinoJson 5 fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Downloading ArduinoJson 5 doesn't fix the issue",next:"deadend"}],content:`<p>I recommend that you download <a href="https://github.com/bblanchon/ArduinoJson/releases/download/v5.13.5/ArduinoJson-v5.13.5.h"><code>ArduinoJson-v5.13.5.h</code></a> and save it among the project files, this way you're sure that the project is distributed with the right version of the library.</p>
<p>You can choose to keep the name <code>ArduinoJson-v5.13.5.h</code> and update the <code>#include</code> directive, or you can rename the file to <code>ArduinoJson.h</code>; either way is fine, as long as the header is in the same folder as the file that includes it.</p>
<p>Please see <a href="/v5/doc/installation/">installation instructions</a> for details</p>
<p>Did this solve your issue?</p>
`},"compiletime/class-from-arduinojson5-upgrade":{choices:[{id:"success",label:"Yes",summary:"Upgrading fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Upgrading doesn't fix the issue",next:"deadend"}],content:`<p>Upgrading from v5 to v6 isn't trivial but isn't complicated either.<br>
Please follow the <a href="/v6/doc/upgrade/">upgrade instructions</a>.</p>
<p>Did this solve your issue?</p>
`},"compiletime/class-from-arduinojson5":{choices:[{id:"upgrade",label:"I'd like to upgrade to ArduinoJson 6",summary:"User prefers upgrading to ArduinoJson 6",next:"compiletime/class-from-arduinojson5-upgrade"},{id:"keep-v5",label:"I prefer not touching the program",summary:"User prefers keeping ArduinoJson 5",next:"compiletime/class-from-arduinojson5-keep"}],content:`<p>This error occurs when you try to use ArduinoJson 6 with a program written for ArduinoJson 5.</p>
<p>Can you upgrade the program or do you prefer keeping it as it is?</p>
`},"compiletime/converttojson":{choices:[{id:"success",label:"Yes",summary:"Converting the value fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Converting the value doesn't fix the issue",next:"deadend"}],content:`<p>This error occurs when you try to insert an unsupported value type in a <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>.</p>
<p>To fix this error, you must either:</p>
<ul>
<li>change the type of value to a supported one (<code>const char*</code>, <code>int</code>, <code>float</code>, etc.)</li>
<li>write a <a href="/news/2021/05/04/version-6-18-0/">custom converter</a> for this type</li>
</ul>
<p>For more information, see <a href="/v6/error/no-matching-function-for-call-to-converttojson/">error: no matching function for call to 'convertToJson(...)'</a>.</p>
<p>Did this solve your issue?</p>
`},"compiletime/doesnt-name-a-type":{choices:[{id:"success",label:"Yes",summary:"Moving statement to <code>setup()</code> fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Moving statement to <code>setup()</code> doesn't fix the issue",next:"deadend"}],content:`<p>This error usually occurs when you write statements at the global scope, which isn't possible in C++.</p>
<p>Unlike many popular languages like Python or JavaScript, the C++ language doesn't allow statements at the global scope.
Instead, you must move all statements into a function.
The entry point for a regular C++ program is the <code>main()</code> function, but in an Arduino program, it's the <code>setup()</code> function.</p>
<p>For example, if your program is like this:</p>
<pre><code class="hljs language-c++">StaticJsonDocument&lt;<span class="hljs-number">200</span>&gt; doc;
doc[<span class="hljs-string">&quot;hello&quot;</span>] = <span class="hljs-string">&quot;world&quot;</span>;
<span class="hljs-built_in">serializeJson</span>(doc, Serial);
</code></pre>
<p>...you must rewrite it like this:</p>
<pre><code class="hljs language-c++"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">setup</span><span class="hljs-params">()</span> </span>{
  StaticJsonDocument&lt;<span class="hljs-number">200</span>&gt; doc;
  doc[<span class="hljs-string">&quot;hello&quot;</span>] = <span class="hljs-string">&quot;world&quot;</span>;
  <span class="hljs-built_in">serializeJson</span>(doc, Serial);
}
</code></pre>
<p>Did this solve your issue?</p>
`},"compiletime/invalid-conversion-array":{choices:[{id:"success",label:"Yes",summary:"Replacing <code>JsonArray</code> with <code>JsonArrayConst</code> fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Replacing <code>JsonArray</code> with <code>JsonArrayConst</code> doesn't fix the issue",next:"deadend"}],content:`<p>Somewhere in your program, there is a conversion from <a href="/v6/api/jsonvariantconst/"><code>JsonVariantConst</code></a> to <a href="/v6/api/jsonarray/"><code>JsonArray</code></a>. This conversion is invalid because it would convert a <em>read-only</em> reference to a <em>read-write</em> reference.</p>
<p>To fix this issue, you must use <a href="/v6/api/jsonarrayconst/"><code>JsonArrayConst</code></a> in place of <a href="/v6/api/jsonarray/"><code>JsonArray</code></a>.</p>
<p>For example, if your program contains the expression <code>variant.as&lt;JsonArray&gt;()</code>, you must replace it with <code>variant.as&lt;JsonArrayConst&gt;()</code>. Alternatively, if your program contains a statement like <code>JsonArray array = variant</code>, you must replace it with <code>JsonArrayConst array = variant</code>.</p>
<p>Did this solve your issue?</p>
`},"compiletime/invalid-conversion-object":{choices:[{id:"success",label:"Yes",summary:"Replacing <code>JsonObject</code> with <code>JsonObjectConst</code> fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Replacing <code>JsonObject</code> with <code>JsonObjectConst</code> doesn't fix the issue",next:"deadend"}],content:`<p>Somewhere in your program, there is a conversion from <a href="/v6/api/jsonvariantconst/"><code>JsonVariantConst</code></a> to <a href="/v6/api/jsonobject/"><code>JsonObject</code></a>. This conversion is invalid because it would convert a <em>read-only</em> reference to a <em>read-write</em> reference.</p>
<p>To fix this issue, you must use <a href="/v6/api/jsonobjectconst/"><code>JsonObjectConst</code></a> in place of <a href="/v6/api/jsonobject/"><code>JsonObject</code></a>.</p>
<p>For example, if your program contains the expression <code>variant.as&lt;JsonObject&gt;()</code>, you must replace it with <code>variant.as&lt;JsonObjectConst&gt;()</code>. Alternatively, if your program contains a statement like <code>JsonObject obj = variant</code>, you must replace it with <code>JsonObjectConst obj = variant</code>.</p>
<p>Did this solve your issue?</p>
`},"compiletime/invalid-conversion-variant":{choices:[{id:"success",label:"Yes",summary:"Replacing <code>JsonVariant</code> with <code>JsonVariantConst</code> fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Replacing <code>JsonVariant</code> with <code>JsonVariantConst</code> doesn't fix the issue",next:"deadend"}],content:`<p>Somewhere in your program, there is a conversion from <a href="/v6/api/jsonvariantconst/"><code>JsonVariantConst</code></a> to <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a>. This conversion is invalid because it would convert a <em>read-only</em> reference to a <em>read-write</em> reference.</p>
<p>To fix this issue, you must use <a href="/v6/api/jsonvariantconst/"><code>JsonVariantConst</code></a> in place of <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a>.</p>
<p>For example, if your program contains the expression <code>variant.as&lt;JsonVariant&gt;()</code>, you must replace it with <code>variant.as&lt;JsonVariantConst&gt;()</code>. Alternatively, if your program contains a statement like <code>JsonVariant v = variant</code>, you must replace it with <code>JsonVariantConst v = variant</code>.</p>
<p>Did this solve your issue?</p>
`},"compiletime/invalid-conversion":{choices:[{id:"array",label:"<code>class InvalidConversion&lt;VariantConstRef, ArrayRef&gt;</code>",summary:"Specialization is <code>InvalidConversion&lt;VariantConstRef, ArrayRef&gt;</code>",next:"compiletime/invalid-conversion-array"},{id:"object",label:"<code>class InvalidConversion&lt;VariantConstRef, ObjectRef&gt;</code>",summary:"Specialization is <code>InvalidConversion&lt;VariantConstRef, ObjectRef&gt;</code>",next:"compiletime/invalid-conversion-object"},{id:"variant",label:"<code>class InvalidConversion&lt;VariantConstRef, VariantRef&gt;</code>",summary:"Specialization is <code>InvalidConversion&lt;VariantConstRef, VariantRef&gt;</code>",next:"compiletime/invalid-conversion-variant"}],content:`<p>Assuming we removed the namespace <code>ArduinoJson6XXX</code> from the error message, what type is incomplete?</p>
`},"compiletime/iterate-ambiguous":{choices:[{id:"success",label:"Yes",summary:"Calling <code>as&lt;JsonObject&gt;()</code> or <code>as&lt;JsonArray&gt;()</code> solves the issue",next:"done"},{id:"failure",label:"No",summary:"Calling <code>as&lt;JsonObject&gt;()</code> or <code>as&lt;JsonArray&gt;()</code> doesn't solve the issue",next:"deadend"}],content:`<p>You get this error when you try to iterate over a <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> or a <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a>.</p>
<p>For example, it happens if you do this:</p>
<pre><code class="hljs language-c++"><span class="hljs-keyword">for</span> (JsonPair p : doc[<span class="hljs-string">&quot;config&quot;</span>]) {
  <span class="hljs-comment">// ...</span>
}
</code></pre>
<p>Indeed, you cannot iterate a <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a> like so because there is an ambiguity: the <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a> could point to either an array or an object, and this information is only available at runtime.</p>
<p>For this reason, we need to tell the compiler which type of structure we expect by casting the <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a> to a <a href="/v6/api/jsonarray/"><code>JsonArray</code></a> or <a href="/v6/api/jsonobject/"><code>JsonObject</code></a>.
For example, we could solve the above snippet like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-keyword">for</span> (JsonPair p : doc[<span class="hljs-string">&quot;config&quot;</span>].<span class="hljs-built_in">as</span>&lt;JsonObject&gt;()) {
  <span class="hljs-comment">// ...</span>
}
</code></pre>
<p>Did this solve your issue?</p>
`},"compiletime/jsondocument-private":{choices:[{id:"success",label:"Yes",summary:"Using a reference solves the issue",next:"done"},{id:"failure",label:"No",summary:"Using a reference doesn't solve the issue",next:"deadend"}],content:`<p><a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> doesn't support copying.</p>
<p>If you need to pass a <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> to a function, you can use a reference, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">myFunction</span><span class="hljs-params">(JsonDocument&amp; doc)</span>
<span class="hljs-comment">// or</span>
<span class="hljs-type">void</span> <span class="hljs-title">myFunction</span><span class="hljs-params">(<span class="hljs-type">const</span> JsonDocument&amp; doc)</span>
</span></code></pre>
<p>You can even make this function more reusable by allowing it to support any part of a JSON document, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">myFunction</span><span class="hljs-params">(JsonVariant var)</span>
<span class="hljs-comment">// or</span>
<span class="hljs-type">void</span> <span class="hljs-title">myFunction</span><span class="hljs-params">(JsonVariantConst var)</span>
</span></code></pre>
<p>If you must make a copy, use either <a href="/v6/api/dynamicjsondocument/"><code>DynamicJsonDocument</code></a> or <a href="/v6/api/staticjsondocument/"><code>StaticJsonDocument</code></a>.</p>
<p>Did this solve your issue?</p>
`},"compiletime/macro-min":{choices:[{id:"success",label:"Yes",summary:"Defining <code>ARDUINOJSON_ENABLE_STD_STRING</code> and <code>ARDUINOJSON_ENABLE_STD_STREAM</code> to <code>0</code> solves the issue",next:"done"},{id:"failure",label:"No",summary:"Defining <code>ARDUINOJSON_ENABLE_STD_STRING</code> and <code>ARDUINOJSON_ENABLE_STD_STREAM</code> to <code>0</code> doesn't solve the issue",next:"deadend"}],content:`<p>This is a bug in some Arduino cores, most notably the one for <a href="https://en.wikipedia.org/wiki/Atmel_ARM-based_processors#SAM_D">SAMD21</a>.</p>
<p>You can workaround this bug by defining both <a href="/v6/api/config/enable_std_string/"><code>ARDUINOJSON_ENABLE_STD_STRING</code></a> and <a href="/v6/api/config/enable_std_stream/"><code>ARDUINOJSON_ENABLE_STD_STREAM</code></a> to <code>0</code>, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-meta">#<span class="hljs-keyword">define</span> ARDUINOJSON_ENABLE_STD_STRING 0</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> ARDUINOJSON_ENABLE_STD_STREAM 0</span>
<span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;ArduinoJson.h&gt;</span></span>
</code></pre>
<p>You can find more information on this error here: <a href="/v6/error/macro-min-passed-3-arguments-but-takes-just-2/">macro &quot;min&quot; passed 3 arguments, but takes just 2</a></p>
<p>Did this solve your issue?</p>
`},"compiletime/member-write-in-char-ptr":{choices:[{id:"success",label:"Yes",summary:"Passing the buffer size to <code>serializeJson()</code> fixed the issue",next:"done"},{id:"failure",label:"No",summary:"Passing the buffer size to <code>serializeJson()</code> didn't fix the issue",next:"deadend"}],content:`<p>This error occurs when you pass a <code>char*</code> to <a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a> but forget to pass the third argument.
For example:</p>
<pre><code class="hljs language-c++"><span class="hljs-built_in">serializeJson</span>(doc, ptr);  <span class="hljs-comment">// request for member &#x27;write&#x27; in ..., which is of non-class type &#x27;char*&#x27; </span>
</code></pre>
<p>To fix this error, you must pass the size of the destination buffer as the third argument, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-built_in">serializeJson</span>(doc, ptr, size);  <span class="hljs-comment">// OK</span>
</code></pre>
<p>In the examples, you may have seen that I didn't use the size argument; that's because the second argument was not a <code>char*</code> but a <code>char[N]</code>, and <a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a> was able to infer the value of <code>N</code> from the type.</p>
<p>Did this solve your issue?</p>
`},"compiletime/no-matching-deserializejson":{choices:[{id:"success",label:"Yes",summary:"It's the &quot;Most vexing parse&quot;",next:"done"},{id:"failure",label:"No",summary:"It's not the &quot;Most vexing parse&quot;",next:"deadend"}],content:`<p>This error happens when you write the following:</p>
<pre><code class="hljs language-c++"><span class="hljs-function">StaticJsonDocument&lt;200&gt; <span class="hljs-title">doc</span><span class="hljs-params">()</span></span>;
<span class="hljs-built_in">deserializeJson</span>(doc, input);
</code></pre>
<p>Indeed, the first line doesn't declare a <code>StaticJsonDocument&lt;200&gt;</code> but a function that takes no argument and returns a <code>StaticJsonDocument&lt;200&gt;</code>. This is one of the most common pitfalls in C++ and it's called the <a href="https://en.wikipedia.org/wiki/Most_vexing_parse">Most vexing parse</a>.</p>
<p>To fix this program, you must remove the parentheses, like so:</p>
<pre><code class="hljs language-c++">StaticJsonDocument&lt;<span class="hljs-number">200</span>&gt; doc;
<span class="hljs-built_in">deserializeJson</span>(doc, input);
</code></pre>
<p>Did this solve your issue?</p>
`},"compiletime/not-pointer-to-object":{choices:[{id:"success",label:"Yes",summary:"Defining <code>ARDUINOJSON_ENABLE_PROGMEM</code> to <code>0</code> solves the issue",next:"done"},{id:"failure",label:"No",summary:"Defining <code>ARDUINOJSON_ENABLE_PROGMEM</code> to <code>0</code> doesn't solve the issue",next:"deadend"}],content:`<p>This error comes from a bug in the <a href="https://github.com/arduino/ArduinoCore-API">Arduino Core API</a> (or <code>Arduino.h</code>, if you prefer).
This bug was fixed by <a href="https://github.com/arduino/ArduinoCore-API/pull/118">arduino/ArduinoCore-API#118</a> in October 2020, but the fix wasn't immediately propagated to derived projects.</p>
<p>First, make sure that every related piece of software is up-to-date.
Then, if the problem persists, please open an issue in the relevant project and mention <a href="https://github.com/arduino/ArduinoCore-API/pull/118">arduino/ArduinoCore-API#118</a> in the description.</p>
<p>As a workaround, you can disable support for <a href="https://www.arduino.cc/reference/en/language/variables/utilities/progmem/"><code>PROGMEM</code></a> in ArduinoJson (your board doesn't support it anyway) by setting <a href="/v6/api/config/enable_progmem/">ARDUINOJSON_ENABLE_PROGMEM</a> to <code>0</code>, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-meta">#<span class="hljs-keyword">define</span> ARDUINOJSON_ENABLE_PROGMEM 0</span>
<span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;ArduinoJson.h&gt;</span></span>
</code></pre>
<p>Did this fix your issue?</p>
`},"compiletime/objectref":{choices:[{id:"success",label:"Yes",summary:"Replacing <code>JsonObject&amp;</code> with <code>JsonObject</code> fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Replacing <code>JsonObject&amp;</code> with <code>JsonObject</code> doesn't fix the issue",next:"deadend"}],content:`<p>In your program, there is a line that look like this:</p>
<pre><code class="hljs language-c++">JsonObject&amp; obj = doc[<span class="hljs-string">&quot;key&quot;</span>].<span class="hljs-built_in">as</span>&lt;JsonObject&gt;();
</code></pre>
<p>This line produces the following compilation error:</p>
<pre><code class="hljs language-text">cannot bind non-const lvalue reference of type &#x27;ArduinoJson::JsonObject&amp;&#x27; {aka &#x27;ArduinoJson6185_91::ObjectRef&amp;&#x27;} to an rvalue of type &#x27;ArduinoJson6185_91::enable_if&lt;true, ArduinJson6185_91::ObjectRef&gt;::type&#x27; {aka &#x27;ArduinoJson6185_91::ObjectRef&#x27;}
</code></pre>
<p class="code-wrap"></p>
<p>In other words, &quot;cannot assign <code>JsonObject</code> to <code>JsonObject&amp;</code>&quot;.
The compiler refuses to save a reference to a temporary variable because the reference would inevitably dangle.</p>
<p>To fix this issue, you must remove the ampersand (<code>&amp;</code>) after <a href="/v6/api/jsonobject/"><code>JsonObject</code></a>, like so:</p>
<pre><code class="hljs language-c++">JsonObject obj = doc[<span class="hljs-string">&quot;key&quot;</span>].<span class="hljs-built_in">as</span>&lt;JsonObject&gt;();
</code></pre>
<p>Did this solve your issue?</p>
`},"compiletime/passing-volatile-as-this-argument-discards-qualifiers":{choices:[{id:"success",label:"Yes",summary:"Using a queue solves the issue.",next:"done"},{id:"failure",label:"No",summary:"Cannot use a queue.",next:"deadend"}],content:`<p>This error occurs because you declared a <code>volatile</code> variable, but the member function you're trying to call is not qualified as <code>volatile</code>. In other words, the function is not meant to be called from a <code>volatile</code> instance.</p>
<p><a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> is not safe to use on concurrent threads, so its member cannot be qualified as <code>volatile</code>.</p>
<p>To work around this issue, you must remove the <code>volatile</code> qualifier and use a thread-safe synchronization mechanism. For example, you could push values into a <a href="https://www.freertos.org/Embedded-RTOS-Queues.html">FreeRTOS queue</a> and pull them from the main thread.</p>
`},"compiletime/print-ambiguous":{choices:[{id:"success",label:"Yes",summary:"Casting the value or using serializeJson() solves the issue",next:"done"},{id:"failure",label:"No",summary:"Neither casting the value nor using serializeJson() solves the issue",next:"deadend"}],content:`<p>This error usually occurs when you pass a <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a> to <a href="https://www.arduino.cc/reference/en/language/functions/communication/serial/print/"><code>Serial::print()</code></a> or <a href="https://www.arduino.cc/reference/en/language/functions/communication/serial/println/"><code>Serial::print()</code></a>.</p>
<p>To fix it, you need to explicitly cast the <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a> to a type supported by <a href="https://www.arduino.cc/reference/en/language/functions/communication/serial/print/"><code>Serial::print()</code></a>, like so:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">print</span>(doc[<span class="hljs-string">&quot;event&quot;</span>].<span class="hljs-built_in">as</span>&lt;<span class="hljs-type">const</span> <span class="hljs-type">char</span>*&gt;());
</code></pre>
<p><a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a> must contain (or more exactly &quot;point to&quot;) a value of the specified type; otherwise, you'll get a default value, like <code>NULL</code> or <code>0</code>. If you want to support any type of value, you must replace the call to <a href="https://www.arduino.cc/reference/en/language/functions/communication/serial/print/"><code>Serial::print()</code></a> with a call to <a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a>, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-built_in">serializeJson</span>(doc[<span class="hljs-string">&quot;event&quot;</span>], Serial);
</code></pre>
<p>You can find more information on this error here: <a href="/v6/error/call-of-overloaded-println-is-ambiguous/">call of overloaded 'println(...)' is ambiguous</a></p>
<p>Did this solve your issue?</p>
`},"compiletime/unknown-error":{needs_assistance:!0,choices:[],content:`<p>Please click on the &quot;Contact Support&quot; button below and provide the following information:</p>
<ul>
<li>Compiler output (including the error message, of course!)</li>
<li>Target platform</li>
<li>Runtime/core version</li>
</ul>
<p>If you can, please also include an <a href="https://stackoverflow.com/help/mcve">MCVE</a>.</p>
`},"compiletime/unresolved-overloaded-function-type":{choices:[{id:"success",label:"Yes",summary:"Adding the parentheses fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Adding the parentheses doesn't fix the issue",next:"deadend"}],content:`<p>We typically see this error when your program does somethink like this:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">print</span>(doc[<span class="hljs-string">&quot;value&quot;</span>].as&lt;<span class="hljs-type">int</span>&gt;);
</code></pre>
<p>In this case, the compiler produces the following error:</p>
<pre><code class="hljs language-text">no matching function for call to &#x27;HardwareSerial::print(&lt;unresolved overloaded function type&gt;)&#x27;
</code></pre>
<p>This error occurs because <code>as&lt;int&gt;()</code> is a function, so you must call it by appending parenthesis like so:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">print</span>(doc[<span class="hljs-string">&quot;value&quot;</span>].<span class="hljs-built_in">as</span>&lt;<span class="hljs-type">int</span>&gt;());
</code></pre>
<p>Did this solve your issue?</p>
`},compiletime:{choices:[{id:"assignment-of-read-only-location",label:"assignment of read-only location",summary:"Error says &quot;assignment of read-only location&quot;",next:"compiletime/assignment-of-read-only-location"},{id:"ambiguous-string-assign",label:"ambiguous overload for <code>operator=</code> (operand types are <code>String</code> and ...)",summary:"Error says &quot;ambiguous overload for <code>operator=</code> (operand types are <code>String</code> and ...)&quot;",next:"compiletime/ambiguous-string-assign"},{id:"begin-not-found",label:"<code>begin</code>: no matching overloaded function found",summary:"Error says &quot;<code>begin</code>: no matching overloaded function found&quot;",next:"compiletime/iterate-ambiguous"},{id:"print-ambiguous",label:"call of overloaded <code>print(...)</code> is ambiguous",summary:"Error says &quot;call of overloaded <code>print(...)</code> is ambiguous&quot;",next:"compiletime/print-ambiguous"},{id:"cannot-bind-object-ref",label:"cannot bind non-const lvalue reference of type <code>ArduinoJson::JsonObject&amp;</code> ...",summary:"Error says &quot;cannot bind non-const lvalue reference of type <code>ArduinoJson::JsonObject&amp;</code> ...&quot;",next:"compiletime/objectref"},{id:"pointer-to-object",label:"<code>const void*</code> is not a pointer-to-object type",summary:"Error says &quot;<code>const void*</code> is not a pointer-to-object type&quot;",next:"compiletime/not-pointer-to-object"},{id:"doesnt-name-a-type",label:"<code>doc</code> does not name a type",summary:"Error says &quot;<code>doc</code> does not name a type&quot;",next:"compiletime/doesnt-name-a-type"},{id:"dynamicjsonbuffer",label:"<code>DynamicJsonBuffer</code> is a class from ArduinoJson 5",summary:"Error says &quot;<code>DynamicJsonBuffer</code> is a class from ArduinoJson 5&quot;",next:"compiletime/class-from-arduinojson5"},{id:"char-pointer-conversion",label:"invalid conversion from <code>const char*</code> to <code>char*</code> [-fpermissive]",summary:"Error says &quot;invalid conversion from <code>const char*</code> to <code>char*</code> [-fpermissive]&quot;",next:"compiletime/char-pointer-conversion"},{id:"invalid-conversion",label:"invalid use of incomplete type <code>class InvalidConversion&lt;...&gt;</code>",summary:"Error says &quot;invalid use of incomplete type <code>class InvalidConversion&lt;...&gt;</code>&quot;",next:"compiletime/invalid-conversion"},{id:"jsondocument-copy",label:"<code>JsonDocument::JsonDocument(const JsonDocument&amp;)</code> is private",summary:"Error says &quot;<code>JsonDocument::JsonDocument(const JsonDocument&amp;)</code> is private&quot;",next:"compiletime/jsondocument-private"},{id:"macro-min",label:"macro <code>min</code> passed 3 arguments, but takes just 2",summary:"Error says &quot;macro <code>min</code> passed 3 arguments, but takes just 2&quot;",next:"compiletime/macro-min"},{id:"basicjsondocument-default-constructor",label:"no default constructor exists for class <code>BasicJsonDocument</code>",summary:"Error says &quot;no default constructor exists for class <code>BasicJsonDocument</code>&quot;",next:"compiletime/basicjsondocument-default-constructor"},{id:"basicjsondocument-missing-argument",label:"no matching function for call to <code>BasicJsonDocument::BasicJsonDocument()</code>",summary:"Error says &quot;no matching function for call to <code>BasicJsonDocument::BasicJsonDocument()</code>&quot;",next:"compiletime/basicjsondocument-missing-argument"},{id:"converttojson",label:"no matching function for call to <code>convertToJson(...)</code>",summary:"Error says &quot;no matching function for call to <code>convertToJson(...)</code>&quot;",next:"compiletime/converttojson"},{id:"no-matching-deserializejson",label:"no matching function for call to <code>deserializeJson(StaticJsonDocument&lt;200&gt; (&amp;)(), ...)</code>",summary:"Error says &quot;no matching function for call to <code>deserializeJson(StaticJsonDocument&lt;200&gt; (&amp;)(), ...)</code>&quot;",next:"compiletime/no-matching-deserializejson"},{id:"unresolved-overloaded-function-type",label:"no matching function for call to <code>...(&lt;unresolved overloaded function type&gt;)</code>",summary:"Error says &quot;no matching function for call to <code>...(&lt;unresolved overloaded function type&gt;)</code>&quot;",next:"compiletime/unresolved-overloaded-function-type"},{id:"passing-volatile-as-this-argument-discards-qualifiers",label:"passing <code>volatile ...</code> as <code>this</code> argument discards qualifiers [-fpermissive]",summary:"Error says &quot;passing <code>volatile ...</code> as <code>this</code> argument discards qualifiers [-fpermissive]&quot;",next:"compiletime/passing-volatile-as-this-argument-discards-qualifiers"},{id:"cast-void-to-flashstringhelper",label:"reinterpret_cast from <code>const void</code> to <code>const __FlashStringHelper *</code> is not allowed",summary:"Error says &quot;reinterpret_cast from <code>const void</code> to <code>const __FlashStringHelper *</code> is not allowed&quot;",next:"compiletime/cast-void-to-flashstringhelper"},{id:"member-write-in-char-ptr",label:"request for member <code>write</code> in ..., which is of non-class type <code>char*</code>",summary:"Error says &quot;request for member <code>write</code> in ..., which is of non-class type <code>char*</code>&quot;",next:"compiletime/member-write-in-char-ptr"},{id:"staticjsonbuffer",label:"<code>StaticJsonBuffer</code> is a class from ArduinoJson 5",summary:"Error says &quot;<code>StaticJsonBuffer</code> is a class from ArduinoJson 5&quot;",next:"compiletime/class-from-arduinojson5"},{id:"range-based-for-requires-begin",label:"this range-based <code>for</code> statement requires a suitable &quot;begin&quot; function and none was found",summary:"Error says &quot;this range-based <code>for</code> statement requires a suitable &quot;begin&quot; function and none was found&quot;",next:"compiletime/iterate-ambiguous"},{id:"not-in-list",label:"None of the above",summary:"The error is not in the list",next:"compiletime/unknown-error"}],content:`<p>Please look at the <strong>first</strong> error in the compiler output, and tell me what it is...</p>
`},deadend:{needs_assistance:!0,choices:[],content:`<p class="display-4">🤷🏻‍♂️</p>
<p>Well, I'm out of ideas: you need to talk to a human.</p>
<p>Please click the &quot;Contact Support&quot; button below and provide all the relevant information.</p>
`},"deserialization/check-error":{choices:[{id:"ok",label:"<code>Ok</code>",next:"deserialization/noerror",summary:"<code>deserializeJson()</code> returns <code>Ok</code>"},{id:"emptyinput",label:"<code>EmptyInput</code>",next:"deserialization/emptyinput",summary:"<code>deserializeJson()</code> returns <code>EmptyInput</code>"},{id:"incompleteinput",label:"<code>IncompleteInput</code>",next:"deserialization/incompleteinput",summary:"<code>deserializeJson()</code> returns <code>IncompleteInput</code>"},{id:"invalidinput",label:"<code>InvalidInput</code>",next:"deserialization/invalidinput",summary:"<code>deserializeJson()</code> returns <code>InvalidInput</code>"},{id:"nomemory",label:"<code>NoMemory</code>",next:"deserialization/nomemory",summary:"<code>deserializeJson()</code> returns <code>NoMemory</code>"},{id:"notsupported",label:"<code>NotSupported</code>",next:"deserialization/notsupported",summary:"<code>deserializeJson()</code> returns <code>NotSupported</code>"},{id:"toodeep",label:"<code>TooDeep</code>",next:"deserialization/toodeep",summary:"<code>deserializeJson()</code> returns <code>TooDeep</code>"},{id:"crash",label:"I can't tell because the program crashes",next:"deserialization/crash",summary:"The program crashes"}],content:`<p><a href="/v6/api/misc/deserializationerror/"><code>DeserializationError</code></a> is the return type of <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>. It tells whether the operation succeeded and indicates the cause of the error.</p>
<p>Modify your program to show the error code, like so:</p>
<pre><code class="hljs language-c++">DeserializationError error = <span class="hljs-built_in">deserializeJson</span>(doc, input);

Serial.<span class="hljs-built_in">print</span>(<span class="hljs-string">&quot;deserializeJson() returned &quot;</span>);
Serial.<span class="hljs-built_in">println</span>(error.<span class="hljs-built_in">c_str</span>());
</code></pre>
<p>Now, what does it show?</p>
`},"deserialization/crash/after-pointer":{choices:[{id:"success",label:"Yes",summary:"Replacing pointer with a <code>String</code> solves the issue",next:"done"},{id:"failure",label:"No",summary:"Replacing pointer with a <code>String</code> doesn't solve the issue",next:"deadend"}],content:`<p>A programs can also crash after calling <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> because it keeps a pointer to a string stored in the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>.</p>
<p>Indeed, when <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> returns a <code>const char*</code>, it doesn't return a copy of the string, but the address of the string in the memory pool. When the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> is destructed, the memory pool gets released and the pointer dangles. Later, when the program tries to use the string, it reads at an invalid memory location and crashes.</p>
<p>For example, it happens with the following program:</p>
<pre><code class="hljs language-c++"><span class="hljs-type">const</span> <span class="hljs-type">char</span>* username;

<span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">loadConfig</span><span class="hljs-params">()</span> </span>{
  File file = SD.<span class="hljs-built_in">open</span>(filename);
  StaticJsonDocument&lt;<span class="hljs-number">512</span>&gt; doc;
  <span class="hljs-built_in">deserializeJson</span>(doc, file);
  username = doc[<span class="hljs-string">&quot;username&quot;</span>];  <span class="hljs-comment">// ⚠️ stores the pointer</span>
  file.<span class="hljs-built_in">close</span>();
}

<span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">setup</span><span class="hljs-params">()</span> </span>{
  Serial.<span class="hljs-built_in">begin</span>(<span class="hljs-number">9600</span>);
  <span class="hljs-built_in">loadConfig</span>();
  Serial.<span class="hljs-built_in">println</span>(username);  <span class="hljs-comment">// 💥 BOOM!</span>
}
</code></pre>
<p>This is a simplified example; in a real project, things would be more tangled. For example, the variable could be in a <code>struct</code> or a class <code>class</code>, and the program could use the string much later.</p>
<p>To fix this program, you must make a copy of the string and not simply store the pointer. For example, you can use a <a href="https://www.arduino.cc/reference/en/language/variables/data-types/stringobject/"><code>String</code></a>, like so:</p>
<pre><code class="hljs language-c++">String username;

<span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">loadConfig</span><span class="hljs-params">()</span> </span>{
  File file = SD.<span class="hljs-built_in">open</span>(filename);
  StaticJsonDocument&lt;<span class="hljs-number">512</span>&gt; doc;
  <span class="hljs-built_in">deserializeJson</span>(doc, file);
  username = doc[<span class="hljs-string">&quot;username&quot;</span>];  <span class="hljs-comment">// ✅ stores a copy</span>
  file.<span class="hljs-built_in">close</span>();
}

<span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">setup</span><span class="hljs-params">()</span> </span>{
  Serial.<span class="hljs-built_in">begin</span>(<span class="hljs-number">9600</span>);
  <span class="hljs-built_in">loadConfig</span>();
  Serial.<span class="hljs-built_in">println</span>(username);  <span class="hljs-comment">// ✅ reads the copy</span>
}
</code></pre>
<p>Alternatively, you can use a <code>char[]</code> and <a href="https://en.cppreference.com/w/c/string/byte/strcpy"><code>strcpy()</code></a>; I invite you to check the <a href="/v6/example/config/">JsonConfigFile.ino</a> for the details.</p>
<p>Did this solve your issue?</p>
`},"deserialization/crash/after-strcmp":{choices:[{id:"success",label:"Yes",summary:"Replacing <code>strcmp()</code> with <code>==</code> solves the issue",next:"done"},{id:"failure",label:"No",summary:"Replacing <code>strcmp()</code> with <code>==</code> doesn't solve the issue",next:"deadend"}],content:`<p>As per the standard, the behavior of <a href="https://en.cppreference.com/w/c/string/byte/strcmp"><code>strcmp()</code></a> is undefined if one of the two arguments is null.</p>
<p>That's not a problem for you because you don't need to call <a href="https://en.cppreference.com/w/c/string/byte/strcmp"><code>strcmp()</code></a>. Indeed, <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a> supports all comparisons operators (<code>==</code>, <code>&lt;</code>, <code>&gt;</code>, <code>&lt;=</code>, <code>&gt;=</code>), and these operators handle null values correctly.</p>
<p>Please replace calls to <a href="https://en.cppreference.com/w/c/string/byte/strcmp"><code>strcmp()</code></a> with <code>==</code>, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// replace this:</span>
<span class="hljs-keyword">if</span> (<span class="hljs-built_in">strcmp</span>(doc[<span class="hljs-string">&quot;event&quot;</span>], <span class="hljs-string">&quot;wakeup&quot;</span>)) ...

<span class="hljs-comment">// ...with this:</span>
<span class="hljs-keyword">if</span> (doc[<span class="hljs-string">&quot;event&quot;</span>] == <span class="hljs-string">&quot;wakeup&quot;</span>) ...
</code></pre>
<p>Did this solve your issue?</p>
`},"deserialization/crash/after-strcpy":{choices:[{id:"success",label:"Yes",summary:"Providing a non-null default solves the issue",next:"done"},{id:"failure",label:"No",summary:"Providing a non-null default doesn't solve the issue",next:"deadend"}],content:`<p>As per the standard, the behavior of <a href="https://en.cppreference.com/w/c/string/byte/strcpy"><code>strcpy()</code></a> (and <a href="https://en.wikibooks.org/wiki/C_Programming/C_Reference/nonstandard/strlcpy"><code>strlcpy()</code></a>) is undefined if one of the pointers is null.</p>
<p>The simplest solution is to change the default value returned by <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>: instead of returning a null pointer when the value is missing, we can ask it to return an empty string (i.e., <code>&quot;&quot;</code>) or some other replacement (e.g., <code>&quot;&lt;null&gt;&quot;</code>). We can do that with <a href="/v6/api/jsonvariant/or/"><code>operator|</code></a>.</p>
<p>Please provide a non-null default for all calls to <a href="https://en.cppreference.com/w/c/string/byte/strcpy"><code>strcpy()</code></a>, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// replace this:</span>
<span class="hljs-built_in">strcpy</span>(eventName, doc[<span class="hljs-string">&quot;event&quot;</span>]);

<span class="hljs-comment">// ...with this:</span>
<span class="hljs-built_in">strcpy</span>(eventName, doc[<span class="hljs-string">&quot;event&quot;</span>] | <span class="hljs-string">&quot;&lt;null&gt;&quot;</span>);
</code></pre>
<p>Did this solve your issue?</p>
`},"deserialization/crash/after":{choices:[{id:"strcmp",label:"Yes, there is a call to <code>strcmp()</code>",summary:"Program calls <code>strcmp()</code>",next:"deserialization/crash/after-strcmp"},{id:"strcpy",label:"Yes, there is a call to <code>strcpy()</code>",summary:"Program calls <code>strcpy()</code>",next:"deserialization/crash/after-strcpy"},{id:"success",label:"Yes, I found the issue",summary:"Program dereferences a null pointer",next:"done"},{id:"no-usual-suspect",label:"No",summary:"Program calls neither <code>strcmp()</code> nor <code>strcpy()</code>",next:"deserialization/crash/after-pointer"}],content:`<p>Most of the time, when a program crashes after <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>, it's because it dereferences a null pointer returned by the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>.</p>
<p>Take the following statement for example:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// void myFunction(const char*) defined somewhere</span>

<span class="hljs-built_in">myFunction</span>(doc[<span class="hljs-string">&quot;value&quot;</span>]);
</code></pre>
<p>If the key <code>&quot;value&quot;</code> isn't in the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, the function <code>myFunction()</code> will receive a null pointer, which can cause a crash if the function isn't prepared for this.</p>
<p>Do you see something like this in your code?</p>
`},"deserialization/crash/before":{choices:[{id:"static",label:"Yes",summary:"Program uses <code>StaticJsonDocument</code>",next:"deserialization/crash/static"},{id:"no-static",label:"No",summary:"Program doesn't use <code>StaticJsonDocument</code>",next:"deserialization/crash/dynamic"}],content:`<p>A stack-overflow could cause the crash.</p>
<p>Do you use a <a href="/v6/api/staticjsondocument/"><code>StaticJsonDocument</code></a>?</p>
`},"deserialization/crash/dynamic":{choices:[{id:"failure",label:"Yes",summary:"Reducing stack usage doesn't prevent the crash",next:"deadend"},{id:"success",label:"No",summary:"Reducing stack usage prevents the crash",next:"done"}],content:`<p>I still think this could be a stack-overflow.</p>
<p>Please check in your code if there aren't some large variable in the stack. Look for things like:</p>
<ul>
<li><code>char buffer[1024]</code></li>
<li><code>int matrix[64][64][64]</code></li>
<li><code>alloca(1024)</code></li>
</ul>
<p>If you read the JSON from a stream and place it in a buffer, you should instead pass the stream directly to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>. This techniques uses less RAM because ArduioJson will only copy the required parts in the <a href="/v6/api/dynamicjsondocument/"><code>DynamicJsonDocument</code></a> and ignore all the spaces and ponctuation. Of course, you would need a bigger capacity: use the <a href="/v6/assistant/">ArduinoJson Assistant</a> to compute the right value.</p>
<p>If there are other large buffers, you can move them to the heap by using one of the following:</p>
<ul>
<li><a href="https://www.arduino.cc/reference/en/language/variables/data-types/stringobject/"><code>String</code></a></li>
<li><a href="https://en.cppreference.com/w/cpp/container/vector"><code>std::vector</code></a></li>
<li><a href="https://en.cppreference.com/w/cpp/string/basic_string">\`std::string</a></li>
<li><a href="https://en.cppreference.com/w/cpp/memory/unique_ptr"><code>std::unique_ptr</code></a></li>
</ul>
<p>In the last resort, you can use <a href="https://en.cppreference.com/w/c/memory/malloc"><code>malloc()</code></a> and <a href="https://en.cppreference.com/w/c/memory/free"><code>free()</code></a>.</p>
<p>Does your program still crash?</p>
`},"deserialization/crash/inside":{choices:[{id:"no-dangling-ptr",label:"Yes",summary:"Removing dangling pointers doesn't prevent the crash",next:"deserialization/crash/before"},{id:"success",label:"No",summary:"Removing dangling pointers prevents the crash",next:"done"}],content:`<p>Passing a dangling pointer could cause a crash.</p>
<p>If you pass a pointer to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>, make sure that this pointer is still valid. For example, if the pointer is returned by a function, inspect the function to make sure pointer is not refering to a stack variable:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// 💀 DON&#x27;T DO THAT!!!</span>
<span class="hljs-function"><span class="hljs-type">const</span> <span class="hljs-type">char</span>* <span class="hljs-title">readInput</span><span class="hljs-params">()</span> </span>{
  <span class="hljs-type">char</span> buffer[<span class="hljs-number">256</span>];
  <span class="hljs-comment">// yada yada yada...</span>
  <span class="hljs-keyword">return</span> buffer;
}

<span class="hljs-comment">// Possible workaround</span>
<span class="hljs-function">String <span class="hljs-title">readInput</span><span class="hljs-params">()</span> </span>{
  String buffer;
  <span class="hljs-comment">// yada yada yada...</span>
  <span class="hljs-keyword">return</span> buffer;
}
</code></pre>
<p>If you pass <a href="https://www.arduino.cc/reference/en/language/variables/data-types/stringobject/"><code>String</code></a> to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>, make sure you don't call <code>c_str()</code> in between:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// 💀 DON&#x27;T DO THAT!!!</span>
<span class="hljs-type">const</span> <span class="hljs-type">char</span>* input = <span class="hljs-built_in">readInput</span>().<span class="hljs-built_in">c_str</span>();

<span class="hljs-comment">// Possible workaround</span>
String input = <span class="hljs-built_in">readInput</span>();
</code></pre>
<p>Please review your code to make sure you're not passsing a dangling pointer to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>.</p>
<p>Does your program still crash?</p>
`},"deserialization/crash/static-nomemory-capacity-0":{choices:[],content:`<p>Well, that's very bad news. It seems that your microcontroller doesn't have enough RAM to hold the JSON document.</p>
<p>Please read <a href="/v6/how-to/reduce-memory-usage/">How to reduce memory usage</a>; it shows several techniques that you can implement to use less RAM.
Hopefully, you'll make enough room for the memory pool.</p>
<p>If it still doesn't fit, you'll have to upgrade to a bigger microcontroller.</p>
`},"deserialization/crash/static-nomemory-capacity-ok":{choices:[{id:"success",label:"Yes",summary:"Increasing the capacity fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Increasing the capacity doesn't fix the issue",next:"deadend"}],content:`<p>Good news!<br>
The memory allocation succeeds; the <a href="/v6/api/dynamicjsondocument/"><code>DynamicJsonDocument</code></a> is simply too small, so you need to increase its capacity.</p>
<p>Use the <a href="/v6/assistant/">ArduinoJson Assistant</a> to compute the right capacity for your input.</p>
<p>Did this solve your issue?</p>
`},"deserialization/crash/static-nomemory":{choices:[{id:"capacity-0",label:"<code>0</code>",summary:"<code>doc.capacity()</code> returns <code>0</code>",next:"deserialization/crash/static-nomemory-capacity-0"},{id:"capacity-ok",label:"The value passed to the constructor of <code>DynamicJsonDocument</code>",summary:"<code>doc.capacity()</code> returns the right value",next:"deserialization/crash/static-nomemory-capacity-ok"}],content:`<p>It looks like the allocation failed.</p>
<p>Please print the capacity of the <a href="/v6/api/dynamicjsondocument/"><code>DynamicJsonDocument</code></a> to the serial port, like so:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">println</span>(doc.<span class="hljs-built_in">capacity</span>());
</code></pre>
<p>What value does it show?</p>
`},"deserialization/crash/static":{choices:[{id:"success",label:"Yes",summary:"Switching to <code>DynamicJsonDocument</code> prevents the crash",next:"done"},{id:"nomemory",label:"Yes, but now <code>deserializeJson()</code> returns <code>NoMemory</code>",summary:"Switching to <code>DynamicJsonDocument</code> produces <code>NoMemory</code>",next:"deserialization/crash/static-nomemory"},{id:"dynamicjsondocument-too",label:"No",summary:"Switching to <code>DynamicJsonDocument</code> doesn't prevent the crash",next:"deserialization/crash/dynamic"}],content:`<p>A big <a href="/v6/api/staticjsondocument/"><code>StaticJsonDocument</code></a> might indeed overflow the stack.
Even if there is a lot of free memory, you can run out of stack because many platforms limit the size at compile time.</p>
<p>For example, ESP8266 limits to 4096 and ESP32 limits to 8192.
These numbers might seem high, but huge part of the stack is already consumed by callers of your program. As a rule, I recommend never allocating a <a href="/v6/api/staticjsondocument/"><code>StaticJsonDocument</code></a> bigger than the half of the limit (so max 2048 on ESP8266 and 4096 on ESP32). If you need more space in your <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, you need to use a <a href="/v6/api/dynamicjsondocument/"><code>DynamicJsonDocument</code></a> instead.</p>
<p>Does switching to a <a href="/v6/api/dynamicjsondocument/"><code>DynamicJsonDocument</code></a> prevent the crash?</p>
`},"deserialization/crash":{choices:[{id:"before",label:"None: program crashes before calling <code>deserializeJson()</code>",next:"deserialization/crash/before",summary:"Program crashes before calling <code>deserializeJson()</code>"},{id:"inside",label:"Only one: program crashes inside <code>deserializeJson()</code>",next:"deserialization/crash/inside",summary:"Program crashes inside <code>deserializeJson()</code>"},{id:"after",label:"Two traces: program crashes after calling <code>deserializeJson()</code>",next:"deserialization/crash/after",summary:"Program crashes after calling <code>deserializeJson()</code>"}],content:`<p>I need to know for sure if the program crashes before or after calling <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>.</p>
<p>Please add some traces around the call to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> and make sure to flush the Serial buffer. You can use the <a href="https://github.com/bblanchon/ArduinoTrace">ArduinoTrace library</a>, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-built_in">TRACE</span>();
DeserializationError err = <span class="hljs-built_in">deserializeJson</span>(doc, input);
<span class="hljs-built_in">TRACE</span>();
</code></pre>
<p>How many traces can you see in the Serial Monitor?</p>
`},"deserialization/emptyinput/file":{choices:[{id:"success",label:"Yes",summary:"The file was the problem",next:"done"},{id:"failure",label:"No",summary:"The file is not the problem",next:"deadend"}],content:`<p><a href="/v6/api/misc/deserializationerror/#emptyinput"><code>EmptyInput</code></a> in the context of a file usually means:</p>
<ul>
<li>the file is closed</li>
<li>the file is opened in the wrong mode</li>
<li>the file is empty</li>
</ul>
<p>Please, make sure that the file is opened in &quot;read&quot; mode and try to print the content to make sure it's not empty.</p>
<p>You can find an example using the SD library in <a href="/v6/example/config/">JsonConfigFile.ino</a> and one using SPIFFS in <a href="/book/">Mastering ArduinoJson</a>.</p>
<p>Did this sole your issue?</p>
`},"deserialization/emptyinput/http-certificate-insecure":{choices:[],content:`<p>This confirms that the problem comes from the certificate validation.</p>
<p>Disabling certificate validation might represent a risk, though, because your program cannot guarantee the remote server's identity.
Sometimes this is acceptable, but in most cases, you should select an appropriate certificate validation strategy by calling <a href="https://arduino-esp8266.readthedocs.io/en/latest/esp8266wifi/bearssl-client-secure-class.html#setknownkey-const-bearssl-publickey-pk"><code>setKnownKey()</code></a>, <a href="https://arduino-esp8266.readthedocs.io/en/latest/esp8266wifi/bearssl-client-secure-class.html#setfingerprint-const-uint8-t-fp-20-setfingerprint-const-char-fpstr"><code>setFingerprint()</code></a>, or <a href="https://arduino-esp8266.readthedocs.io/en/latest/esp8266wifi/bearssl-client-secure-class.html#settrustanchors-bearssl-x509list-ta"><code>WiFiClientSecure::setTrustAnchors()</code></a>.</p>
<p>Anyway, I'm glad I could help.<br>
I would be very grateful if, in return, you could <a href="https://github.com/bblanchon/ArduinoJson/stargazers">star ArduinoJson on GitHub</a>. ❤</p>
<p>If you want to support the project (and learn a lot of stuff as well), you can purchase <a href="/book/">Mastering ArduinoJson</a>.<br>
Alternatively, you can <a href="https://github.com/sponsors/bblanchon">sponsor the project on GitHub</a>.</p>
<p>Should you have any idea on how I could improve myself, please <a href="https://github.com/bblanchon/ArduinoJson/issues/new">open an issue on GitHub</a>.</p>
<p>Bye!</p>
<p>--<br>
<em>ArduinoJson Troubleshooter</em></p>
`},"deserialization/emptyinput/http-certificate":{choices:[{id:"certificate-problem",label:"Yes",summary:"Calling <code>WiFiClientSecure::setInsecure()</code> solves the issue.",next:"deserialization/emptyinput/http-certificate-insecure"},{id:"certificate-ok",label:"No",summary:"Calling <code>WiFiClientSecure::setInsecure()</code> doesn't solve the issue.",next:"deserialization/emptyinput/http-timeout"}],content:`<p>With HTTPS, <a href="/v6/api/misc/deserializationerror/#emptyinput"><code>EmptyInput</code></a> can be due to an error during the validation of the certificate.</p>
<p>Please try to disable certificate validation by calling <a href="https://arduino-esp8266.readthedocs.io/en/latest/esp8266wifi/bearssl-client-secure-class.html#setinsecure"><code>WiFiClientSecure::setInsecure()</code></a> before starting the HTTP request.</p>
<p>Did this solve your issue?</p>
`},"deserialization/emptyinput/http-redirect":{choices:[{id:"success",label:"Yes",summary:"Following redirection fixes the issue",next:"done"},{id:"no-redirection",label:"No",summary:"It's not a redirection",next:"deserialization/emptyinput/http-certificate"}],content:`<p><a href="/v6/api/misc/deserializationerror/#emptyinput"><code>EmptyInput</code></a> in the context of an HTTP response usually means that the server returns a <a href="https://developer.mozilla.org/en-US/docs/Web/HTTP/Redirections">redirection code</a> (like <a href="https://en.wikipedia.org/wiki/HTTP_301">301 Moved Permanently</a> or <a href="https://en.wikipedia.org/wiki/HTTP_302">302 Found</a>), but the client doesn't follow the redirection.</p>
<p>Please check the HTTP status code in the response. If it's 301 or 302, you must modify your program to handle the redirection.</p>
<p>If you use an HTTP library, make sure that you enabled the &quot;follow redirection&quot; feature.
For example, if you use <code>HTTPClient</code> on ESP8266 or ESP32, you must call the following function before sending the request:</p>
<pre><code class="hljs language-c++">http.<span class="hljs-built_in">setFollowRedirects</span>(HTTPC_FORCE_FOLLOW_REDIRECTS);
</code></pre>
<p>Did this solve your issue?</p>
`},"deserialization/emptyinput/http-timeout":{choices:[{id:"success",label:"Yes",summary:"Increasing the timeout fixes the issue",next:"done"},{id:"timeout-increased",label:"No",summary:"Increasing the timeout doesn't fix the issue",next:"deadend"}],content:`<p><a href="/v6/api/misc/deserializationerror/#emptyinput"><code>EmptyInput</code></a> could be caused by a timeout while reading the response.</p>
<p>Please increase the value of the timeout by calling <a href="https://www.arduino.cc/reference/en/language/functions/communication/stream/streamsettimeout/"><code>Stream::setTimeout()</code></a> before calling <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>. For example:</p>
<pre><code class="hljs language-c++">client.<span class="hljs-built_in">setTimeout</span>(<span class="hljs-number">10000</span>);  <span class="hljs-comment">// 10 seconds</span>
</code></pre>
<p>Did this solve your issue?</p>
`},"deserialization/emptyinput/other-nul":{choices:[],content:`<p>This confirms that the problem comes from the input: it is indeed empty, or at least starts with a NUL character.</p>
<p>You must now find a way to fix your input; I cannot help you with that. Sorry.</p>
`},"deserialization/emptyinput/other-stream":{choices:[{id:"zero",label:"<code>0</code>",summary:"The first character is NUL",next:"deserialization/emptyinput/other-nul"},{id:"non-zero",label:"Something else",summary:"The first character is not NUL",next:"deadend"}],content:`<p>Please print the ASCII code for the first character, like so:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">println</span>(stream.<span class="hljs-built_in">peek</span>());
</code></pre>
<p>What do you see?</p>
`},"deserialization/emptyinput/other-string":{choices:[{id:"zero",label:"<code>0</code>",summary:"The first character is NUL",next:"deserialization/emptyinput/other-nul"},{id:"non-zero",label:"Something else",summary:"The first character is not NUL",next:"deadend"}],content:`<p>Please print the ASCII code for the first character, like so:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">println</span>((<span class="hljs-type">int</span>)input[<span class="hljs-number">0</span>]);
</code></pre>
<p>What do you see?</p>
`},"deserialization/emptyinput/other":{choices:[{id:"string",label:"A string (<code>const char*</code>, <code>String</code>, <code>std::string</code>...)",summary:"Input type is a string",next:"deserialization/emptyinput/other-string"},{id:"stream",label:"A stream (<code>Serial</code>, <code>WiFiClient</code>, <code>File</code>...)",summary:"Input type is a stream",next:"deserialization/emptyinput/other-stream"}],content:`<p>The input is probably starting with a NUL character.</p>
<p>What type of input are you passing to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>?</p>
`},"deserialization/emptyinput/serial-crlf":{choices:[{id:"success",label:"Yes",summary:"Removing CRLF fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Removing CRLF doesn't fix the issue",next:"deadend"}],content:`<p><a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> also returns <a href="/v6/api/misc/deserializationerror/#emptyinput"><code>EmptyInput</code></a> when the input contains only spaces.</p>
<p>For example, this can happen when you call <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> repeatedly and there are spaces (or line breaks) between the documents. Suppose you use the following program to parse JSON input from the serial port:</p>
<pre><code class="hljs language-c++"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">loop</span><span class="hljs-params">()</span> </span>{
  <span class="hljs-comment">// wait from an incomming message</span>
  <span class="hljs-keyword">while</span> (Serial.<span class="hljs-built_in">available</span>() == <span class="hljs-number">0</span>)
    <span class="hljs-built_in">delay</span>(<span class="hljs-number">100</span>);
    
  StaticJsonDocument&lt;<span class="hljs-number">1024</span>&gt; doc;
  DeserializationError err = <span class="hljs-built_in">deserializeJson</span>(doc, Serial);
  
  ...
}
</code></pre>
<p>If you use the default settings of the Arduino Serial Monitor, <code>err</code> will contains <a href="/v6/api/misc/deserializationerror/#ok"><code>Ok</code></a> and then <a href="/v6/api/misc/deserializationerror/#emptyinput"><code>EmptyInput</code></a> each time you press &quot;Send&quot;.</p>
<p>Indeed, by default the Arduino Serial Monitor appends <a href="https://fr.wikipedia.org/wiki/Carriage_Return_Line_Feed">CRLF</a> at the end of the message, so when you enter <code>{&quot;hello&quot;:&quot;world&quot;}</code> in the input box, what is really sent is <code>{&quot;hello&quot;:&quot;world&quot;}\\r\\n</code>.
Since <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> stops reading immediately at the end of the object, the <code>\\r\\n</code> remains in the serial buffer.
Therefore, <a href="https://www.arduino.cc/reference/en/language/functions/communication/serial/available/"><code>Serial::available()</code></a> returns <code>2</code> and the program calls <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> again.
<a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> reads <code>\\r\\n</code>, which are just white spaces for him, so it continues reading until a timeout occurs (1s by default), and it finally returns <a href="/v6/api/misc/deserializationerror/#emptyinput"><code>EmptyInput</code></a>.</p>
<p>You can fix this problem by changing the configuration of the Serial Monitor to &quot;No line ending&quot;.
If you cannot remove the CR+LF at the end of the message, you must add a flush loop after <code>deserializeJson(doc, Serial)</code>, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-keyword">while</span> (<span class="hljs-built_in">isspace</span>(Serial.<span class="hljs-built_in">peek</span>())
  Serial.<span class="hljs-built_in">read</span>();
</code></pre>
<p>Did this solve your issue?</p>
`},"deserialization/emptyinput/serial-timeout":{choices:[{id:"success",label:"Yes",summary:"Adding a wait loop fixes the issue",next:"done"},{id:"available",label:"No",summary:"Adding a wait loop doesn't fix the issue",next:"deserialization/emptyinput/serial-crlf"}],content:`<p><a href="/v6/api/misc/deserializationerror/#emptyinput"><code>EmptyInput</code></a> can be caused by a timeout while waiting for the input.</p>
<p>In that case, the solution is to wait until some data is available before calling <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>. A simple loop can do the trick:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// wait from an incomming message</span>
<span class="hljs-keyword">while</span> (Serial.<span class="hljs-built_in">available</span>() == <span class="hljs-number">0</span>)
  <span class="hljs-built_in">delay</span>(<span class="hljs-number">100</span>);

<span class="hljs-built_in">deserializeJson</span>(doc, Serial);
</code></pre>
<p>Did this solve your issue?</p>
`},"deserialization/emptyinput":{choices:[{id:"http",label:"An HTTP response",summary:"Input comes from an HTTP response",next:"deserialization/emptyinput/http-redirect"},{id:"file",label:"A file",summary:"Input comes from a file",next:"deserialization/emptyinput/file"},{id:"serial",label:"A serial port",summary:"Input comes from a serial port",next:"deserialization/emptyinput/serial-timeout"},{id:"other",label:"Something else",summary:"Input comes neither from an HTTP response, nor a file, nor a serial port",next:"deserialization/emptyinput/other"}],content:`<p><a href="/v6/api/misc/deserializationerror/#emptyinput"><code>EmptyInput</code></a> means that the input was empty or contained only spaces or comments.</p>
<p>Where does your JSON input come from?</p>
`},"deserialization/incompleteinput/stream-buffer":{choices:[{id:"success",label:"Yes",summary:"Adding a buffer solves the issue",next:"done"},{id:"same-with-buffer",label:"No",summary:"Adding a buffer doesn't solve the issue",next:"deserialization/incompleteinput/stream-log"}],content:`<p><a href="/v6/api/misc/deserializationerror/#incompleteinput"><code>IncompleteInput</code></a> can be caused by an interruped connection. For example, this problem happens when the client reads to slowly.</p>
<p>Indeed, because it reads bytes one by one, <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> can be slow with some implementations of <a href="https://www.arduino.cc/reference/en/language/functions/communication/stream/"><code>Stream</code></a>. To speed up the reading, we must add a buffer between the <a href="https://www.arduino.cc/reference/en/language/functions/communication/stream/"><code>Stream</code></a> and <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>. The easiest way to do this is to use <code>ReadBufferingStream</code> from the <a href="https://github.com/bblanchon/ArduinoStreamUtils">StreamUtils library</a>:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// replace the following line:</span>
<span class="hljs-built_in">deserializeJson</span>(doc, stream);

<span class="hljs-comment">// with these two lines:</span>
<span class="hljs-function">ReadBufferingStream <span class="hljs-title">bufferedStream</span><span class="hljs-params">(stream, <span class="hljs-number">64</span>)</span></span>;
<span class="hljs-built_in">deserializeJson</span>(doc, bufferedStream);
</code></pre>
<p>Thanks to <code>ReadBufferingStream</code>, the program will read the input in chunks of 64 bytes, which will be much faster.
Hopefully, this will keep the connection opened till the end of the message.</p>
<p>Did this solve your issue?</p>
`},"deserialization/incompleteinput/stream-log":{choices:[{id:"success",label:"Yes",summary:"Logging helped fixing the issue",next:"done"},{id:"failure",label:"No",summary:"Logging didn't help fixing the issue",next:"deadend"}],content:`<p>Maybe seeing where the input stops will give you an idea of what's going wrong.</p>
<p>We can log the characters read by <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> by inserting a <code>ReadLoggingStream</code>, another class from the <a href="https://github.com/bblanchon/ArduinoStreamUtils">StreamUtils library</a>:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// replace the following line</span>
<span class="hljs-built_in">deserializeJson</span>(doc, wifiClient);

<span class="hljs-comment">// with these two lines:</span>
<span class="hljs-function">ReadLoggingStream <span class="hljs-title">loggingStream</span><span class="hljs-params">(stream, Serial)</span></span>;
<span class="hljs-built_in">deserializeJson</span>(doc, loggingStream);
</code></pre>
<p>Thanks to <code>ReadLoggingStream</code>, this program prints every character read by <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>. Therefore, you'll be able to see where the input stops; hopefully, this will give you a better understanding of the problem.</p>
<p>When using <code>ReadLoggingStream</code>, it's crucial that you configure the serial port with a very high baud rate; otherwise the log will slow down the reading process, which is a problem as I explained previously.</p>
<p>Did this information help you solve your issue?</p>
`},"deserialization/incompleteinput/stream-timeout":{choices:[{id:"success",label:"Yes",summary:"Inreasing the timeout solves the issue",next:"done"},{id:"timeout-increased",label:"No",summary:"Inreasing the timeout doesn't solve the issue",next:"deserialization/incompleteinput/stream-buffer"}],content:`<p><a href="/v6/api/misc/deserializationerror/#incompleteinput"><code>IncompleteInput</code></a> can be caused by a timeout.</p>
<p>Please increase the value of the timeout by calling <a href="https://www.arduino.cc/reference/en/language/functions/communication/stream/streamsettimeout/"><code>Stream::setTimeout()</code></a> before calling <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>. For example:</p>
<pre><code class="hljs language-c++">stream.<span class="hljs-built_in">setTimeout</span>(<span class="hljs-number">10000</span>);  <span class="hljs-comment">// 10 seconds</span>
</code></pre>
<p>Did this solve your issue?</p>
`},"deserialization/incompleteinput/string-object":{choices:[{id:"success",label:"Yes",summary:"Calling <code>String::reserve()</code> solves the issue",next:"done"},{id:"same-with-reserve",label:"No",summary:"Calling <code>String::reserve()</code> doesn't solve the issue",next:"deserialization/incompleteinput/string-stream"}],content:`<p>Please print the content of the string to the serial port, like so:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">println</span>(input);
</code></pre>
<p>You should see that the JSON document is truncated.
This can be caused by a lack of RAM: the string object failed to allocate a buffer large enough for the whole document.</p>
<p>This issue may come from <a href="https://cpp4arduino.com/2018/11/06/what-is-heap-fragmentation.html">heap fragmentated</a>.
Fixing this problem is very hard because it involve fixing the whole program, not just the JSON deserialization.</p>
<p>Yet, you can try to preallocate the buffer by calling <a href="https://www.arduino.cc/reference/en/language/variables/data-types/string/functions/reserve/"><code>String::reserve()</code></a> before loading the content of the input, like so:</p>
<pre><code class="hljs language-c++">input.<span class="hljs-built_in">reserve</span>(<span class="hljs-number">1024</span>);  <span class="hljs-comment">// adapt the value to the size of your input</span>
</code></pre>
<p>Hopefully, you'll be able to reserve a buffer large enough to store the whole string.</p>
<p>Did this solve your issue?</p>
`},"deserialization/incompleteinput/string-pointer":{choices:[{id:"success",label:"Yes",summary:"Increasing the buffer size solves the issue",next:"done"},{id:"larger-buffer",label:"No",summary:"Increasing the buffer size doesn't solve the issue",next:"deserialization/incompleteinput/string-stream"}],content:`<p>I'll assume that your input is stored in a buffer similar to this one:</p>
<pre><code class="hljs language-c++"><span class="hljs-type">char</span> input[<span class="hljs-number">1024</span>];
<span class="hljs-type">size_t</span> input_size;
</code></pre>
<p>Please print the content of the string to the serial port, like so:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">write</span>(input, input_size);
</code></pre>
<p>You should see that the JSON document is truncated.
This is probably caused by the size of the buffer: it's too small to store the complete document.</p>
<p>Please increase the capacity of the buffer, but remember that the size of the stack is limited so you may need to move your buffer to the heap.</p>
<p>Did this solve your issue?</p>
`},"deserialization/incompleteinput/string-stream":{choices:[{id:"success",label:"Yes",summary:"Reading from stream fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Reading from stream doesn't fix the issue",next:"deadend"}],content:`<p>To reduce the memory usage, you should try to pass the source (file, connection, stream, etc) directly to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>, either via the <a href="https://www.arduino.cc/reference/en/language/functions/communication/stream/"><code>Stream</code></a> interface or via a <a href="/news/2019/11/01/version-6-13-0/">custom reader</a>. This will avoid the need for an intermediate buffer.</p>
<p>As usual, make sure the capacity of the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> is adapted to your needs by using the <a href="/v6/assistant/">ArduinoJson Assistant</a></p>
<p>Additionaly, you can add a <a href="/news/2020/03/22/version-6-15-0/">filter</a> to remove all unneeded information from the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>.</p>
<p>You can see examples here:</p>
<ul>
<li><a href="/v6/example/config/">JsonConfigFile.ino</a></li>
<li><a href="/v6/example/http-client/">JsonHttpClient.ino</a></li>
<li><a href="/v6/doc/deserialization/">Deserialization tutorial</a></li>
<li><a href="/book/">Mastering ArduinoJson</a></li>
</ul>
<p>Did this solve your issue?</p>
`},"deserialization/incompleteinput":{choices:[{id:"stream",label:"A stream",summary:"Input comes from a stream",next:"deserialization/incompleteinput/stream-timeout"},{id:"string",label:"A string object (like <code>String</code>)",summary:"Input comes from a string object",next:"deserialization/incompleteinput/string-object"},{id:"pointer",label:"A pointer (like <code>char*</code> or <code>const char*</code>)",summary:"Input comes from a pointer",next:"deserialization/incompleteinput/string-pointer"}],content:`<p><a href="/v6/api/misc/deserializationerror/#incompleteinput"><code>IncompleteInput</code></a> means that <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> managed to parse the beginning of the JSON document, but the end was missing.</p>
<p>What type of input do you pass to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>?</p>
`},"deserialization/invalidinput/array-twice-cast":{choices:[{id:"success",label:"Yes",summary:"Casting the pointer fixes the issue",next:"done"},{id:"cast",label:"No",summary:"Casting the pointer doesn't fix the issue",next:"deserialization/invalidinput/string-jsonlint"}],content:`<p>If you must keep the two calls to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>, you need to disable the zero-copy mode by passing a read-only input buffer. Casting the pointer will do the trick:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// replace this:</span>
<span class="hljs-built_in">deserializeJson</span>(doc, input, inputSize);

<span class="hljs-comment">// ...with this:</span>
<span class="hljs-built_in">deserializeJson</span>(doc, (<span class="hljs-type">const</span> <span class="hljs-type">char</span>*)input, inputSize);
</code></pre>
<p>Now that the zero-copy mode is disabled, you probably need a larger <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, so don't forget to update the capacity. As usual, use the <a href="/v6/assistant/">ArduinoJson Assistant</a> to compute the right capacity for your project.</p>
<p>Did this solve your issue?</p>
`},"deserialization/invalidinput/array-twice":{choices:[{id:"success",label:"Yes, and it fixes the issue",summary:"Removing the second call fixes the issue",next:"done"},{id:"second-removed",label:"Yes, but it doesn't fix the issue",summary:"Removing the second call doesn't fix the issue",next:"deserialization/invalidinput/string-jsonlint"},{id:"cannot-remove",label:"No",summary:"The second call cannot be removed",next:"deserialization/invalidinput/array-twice-cast"}],content:`<p>When you pass a writeable buffer as the input of <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>, ArduinoJson uses the zero-copy mode. Instead of copying the strings from the input into the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, it stores pointers to the input buffer.</p>
<p>While doing this, the parser modifies the input buffer to unescape special characters and add string terminator.
When <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> returns the input buffer doesn't contain a valid JSON document anymore.
For this reason, you cannot call <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> twice with the same input buffer (if the input is writable).</p>
<p>I don't see any reason for calling <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> twice with the same input, so I recommend that your remove the second call.</p>
<p>Can you avoid duplicate calls to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>?</p>
`},"deserialization/invalidinput/array":{choices:[{id:"two-calls",label:"Yes",summary:"Program calls <code>deserializeJson()</code> twice",next:"deserialization/invalidinput/array-twice"},{id:"one-call",label:"No",summary:"Program doesn't call <code>deserializeJson()</code> twice",next:"deserialization/invalidinput/string-jsonlint"}],content:`<p>Did you call <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> twice with the same input buffer?</p>
`},"deserialization/invalidinput/bom-none":{needs_assistance:!0,choices:[],content:`<p>So, the JSON document is valid and not preceded by anything; yet, <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> returns <a href="/v6/api/misc/deserializationerror/#invalidinput"><code>InvalidInput</code></a>.</p>
<p>Well... I'm out of idea :-(</p>
`},"deserialization/invalidinput/bom-utf16":{choices:[{id:"success",label:"Yes",summary:"<code>Accept: application/json;charset=utf-8</code> solves the issue",next:"done"},{id:"failure",label:"No",summary:"<code>Accept: application/json;charset=utf-8</code> doesn't solve the issue",next:"deadend"}],content:`<p><code>254</code> or <code>255</code> is the first byte of the UTF-16 <a href="https://en.wikipedia.org/wiki/Byte_order_mark">Byte Order Mark</a> (BOM). ArduinoJson doesn't support UTF-16; it only supports ASCII and UTF-8. As a workaround, we can ask the server to encode the response with UTF-8.</p>
<p>To do this, add the following header to the HTTP request:</p>
<pre><code class="hljs language-http"><span class="hljs-attribute">Accept</span><span class="hljs-punctuation">: </span>application/json;charset=utf-8
</code></pre>
<p>Did this solve your problem?</p>
`},"deserialization/invalidinput/bom-utf8":{choices:[{id:"success",label:"Yes",summary:"Skipping the first 3 bytes solves the issue",next:"done"},{id:"failure",label:"No",summary:"Skipping the first 3 bytes doesn't solve the issue",next:"deadend"}],content:`<p><code>239</code> is the first by of the UTF-8 <a href="https://en.wikipedia.org/wiki/Byte_order_mark">Byte Order Mark</a> (BOM).</p>
<p>ArduinoJson doesn't skip the BOM, you need to do it in your program by adding the following lines <strong>before</strong> calling <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// skip BOM</span>
stream.<span class="hljs-built_in">read</span>();  <span class="hljs-comment">// 0xEF</span>
stream.<span class="hljs-built_in">read</span>();  <span class="hljs-comment">// 0xBB</span>
stream.<span class="hljs-built_in">read</span>();  <span class="hljs-comment">// 0xBF</span>
</code></pre>
<p>Admitedly, this is quite hacky, so I recommend that you try to fix the server which should not include a BOM anyway.</p>
<p>Did this fix your problem?</p>
`},"deserialization/invalidinput/file-bom":{choices:[{id:"utf8",label:"<code>239</code>",summary:"Input's first byte suggests a UTF-8 BOM",next:"deserialization/invalidinput/bom-utf8"},{id:"utf16",label:"<code>254</code> or <code>255</code>",summary:"Input's first byte suggests a UTF-16 BOM",next:"deserialization/invalidinput/bom-utf16"},{id:"no-bom",label:"Something else",summary:"Input's first byte doesn't suggest a BOM",next:"deserialization/invalidinput/bom-none"}],content:`<p>We must check that the JSON document is not preceded by a <a href="https://en.wikipedia.org/wiki/Byte_order_mark">Byte Order Mark</a> (BOM).</p>
<p>To do this, we'll peek at the first byte in the file and print the value to the serial port. Insert the following line <em>before</em> the call to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">println</span>(file.<span class="hljs-built_in">peek</span>());
</code></pre>
<p>What value does it print?</p>
`},"deserialization/invalidinput/file-jsonlint":{summary:null,choices:[{id:"valid",label:"Yes",summary:"<code>jsonlint</code> says the document is valid",next:"deserialization/invalidinput/file-bom"},{id:"invalid",label:"No",summary:"<code>jsonlint</code> says the document is invalid",next:"deserialization/invalidinput/jsonlint-bad"}],content:`<p>We need to make sure that the JSON document is valid.
To do that, please print the input to the serial port, like so:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">print</span>(file.<span class="hljs-built_in">readString</span>());
</code></pre>
<p>Then, copy the content and paste it in a JSON linter, like <a href="https://jsonlint.com/">jsonlint.com</a>.</p>
<p>Is the JSON document valid?</p>
`},"deserialization/invalidinput/http-library":{needs_assistance:!0,choices:[],content:`<p>Unfortunately, I only know how to use the following HTTP libraries:</p>
<ul>
<li><a href="https://github.com/esp8266/Arduino/tree/master/libraries/ESP8266HTTPClient">ESP8266HTTPClient</a></li>
<li><a href="https://github.com/espressif/arduino-esp32/tree/master/libraries/HTTPClient">HTTPClient for ESP32</a></li>
</ul>
`},"deserialization/invalidinput/http-manual-headers-warning":{choices:[{id:"success",label:"Yes",summary:"Using a <code>char[]</code> fixes the warning",next:"done"},{id:"failure",label:"No",summary:"Using a <code>char[]</code> doesn't fix the warning",next:"deadend"}],content:`<p>On some Arduino core (most notably <a href="https://github.com/arduino/ArduinoCore-avr/blob/master/cores/arduino/Stream.h">AVR core</a>), <a href="https://www.arduino.cc/reference/en/language/functions/communication/stream/streamfind/"><code>Stream::find()</code></a> takes a <code>char*</code> instead of a <code>const char*</code>. In this case, you'll get a compiler warning, which you can fix by extracting a char array, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-type">char</span> endOfHeaders[] = <span class="hljs-string">&quot;\\r\\n\\r\\n&quot;</span>;
client.<span class="hljs-built_in">find</span>(endOfHeaders);
</code></pre>
<p>Did this fix your problem?</p>
`},"deserialization/invalidinput/http-manual-headers":{choices:[{id:"success",label:"Yes",summary:"Skipping HTTP headers solves the issue",next:"done"},{id:"warning",label:"Yes, but there is a compiler warning about <code>client.find()</code>",summary:"Skipping HTTP headers solves the issue, but there is a warning",next:"deserialization/invalidinput/http-manual-headers-warning"},{id:"header-skipped",label:"No",summary:"Skipping HTTP headers doesn't solve the issue",next:"deserialization/invalidinput/http-manual-http10"}],content:`<p>Just like the HTTP request, the response contains some headers, followed by an empty line, and then followed by the body.
The body is the part that contains the JSON document; therefore, before calling <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>, you must skip the headers.</p>
<p>To skip the headers, call <a href="https://www.arduino.cc/reference/en/language/functions/communication/stream/streamfind/"><code>Stream::find()</code></a>, like so:</p>
<pre><code class="hljs language-c++">client.<span class="hljs-built_in">find</span>(<span class="hljs-string">&quot;\\r\\n\\r\\n&quot;</span>);
</code></pre>
<p>This function consumes the input stream until it finds the empty line (<code>\\r\\n\\r\\n</code>).</p>
<p>See the example <a href="/v6/example/http-client/">JsonHttpClient.ino</a> for a complete implementation.</p>
<p>Did this fix your problem?</p>
`},"deserialization/invalidinput/http-manual-http10":{choices:[{id:"success",label:"Yes",summary:"Using <code>HTTP/1.0</code> solves the issue",next:"done"},{id:"http10",label:"No",summary:"Using <code>HTTP/1.0</code> doesn't solve the issue",next:"deserialization/invalidinput/inputtype"}],content:`<p>When you use HTTP 1.1, the server can send the response with <a href="https://en.wikipedia.org/wiki/Chunked_transfer_encoding">Chunked Transfer Encoding</a>.
As a result, the response's body may contain some hexadecimal number at the begining of each chunk.
The most straightforward workaround is to downgrade the connection to HTTP 1.0.</p>
<p>To do this, replace <code>HTTP/1.1</code> with <code>HTTP/1.0</code> in the first line of the request.</p>
<p>Did this solve your problem?</p>
`},"deserialization/invalidinput/http-manual":{choices:[{id:"headers-skipped",label:"Yes",summary:"HTTP headers are skipped",next:"deserialization/invalidinput/http-manual-http10"},{id:"headers-not-skipped",label:"No",summary:"HTTP headers are not skipped",next:"deserialization/invalidinput/http-manual-headers"}],content:`<p>Before calling <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>, did you skip the HTTP headers?</p>
`},"deserialization/invalidinput/http":{choices:[{id:"httpclient",label:"Yes, I'm using <code>HTTPClient</code>",summary:"Program uses <code>HTTPClient</code>",next:"deserialization/invalidinput/httpclient"},{id:"http-library",label:"Yes, but it's not <code>HTTPClient</code>",summary:"Program uses an unknown HTTP library",next:"deserialization/invalidinput/http-library"},{id:"no-library",label:"No",summary:"Program doesn't use an HTTP library",next:"deserialization/invalidinput/http-manual"}],content:`<p>Do you use an HTTP library?</p>
`},"deserialization/invalidinput/httpclient-readstring":{choices:[{id:"success",label:"Yes",summary:"Calling <code>HTTPClient::getString()</code> solves the issue",next:"done"},{id:"getstring",label:"No",summary:"Calling <code>HTTPClient::getString()</code> doesn't solves the issue",next:"deserialization/invalidinput/string-jsonlint"}],content:`<p>You're calling the wrong function:</p>
<ul>
<li><code>HTTPClient::getString()</code> extracts the response's body</li>
<li><code>HTTPClient::readString()</code> is inherited from <a href="https://www.arduino.cc/reference/en/language/functions/communication/stream/"><code>Stream</code></a> and reads the complete response</li>
</ul>
<p>Unless you have very good reasons, you should call <code>getString()</code> not <code>readString()</code>.</p>
<p>Do calling <code>HTTPClient::getString()</code> solves your issue?</p>
`},"deserialization/invalidinput/httpclient-stream-http10":{choices:[{id:"success",label:"Yes",summary:"Calling <code>HTTPClient::useHTTP10(true)</code> solves the issue",next:"done"},{id:"changed",label:"No",summary:"Calling <code>HTTPClient::useHTTP10(true)</code> doesn't solve the issue",next:"deserialization/invalidinput/httpclient-stream-jsonlint"}],content:`<p>When you call <code>HTTPClient::getStream()</code>, you bypass the part that handles <a href="https://en.wikipedia.org/wiki/Chunked_transfer_encoding">Chunked Transfer Encoding</a>.
As a result, the response's body may contain some hexadecimal number at the begining of each chunk.
The most straightforward workaround is to downgrade the connection to HTTP 1.0.</p>
<p>To do this, add the following line <strong>before</strong> sending the request:</p>
<pre><code class="hljs language-c++">http.<span class="hljs-built_in">useHTTP10</span>(<span class="hljs-literal">true</span>);  <span class="hljs-comment">// avoid Chunked Transfer Encoding</span>
</code></pre>
<p>Did this solve your problem?</p>
`},"deserialization/invalidinput/httpclient-stream-jsonlint":{choices:[{id:"valid",label:"Yes",summary:"jsonlint says the document is valid",next:"deserialization/invalidinput/stream-bom"},{id:"invalid",label:"No",summary:"jsonlint says the document is invalid",next:"deserialization/invalidinput/jsonlint-bad"}],content:`<p>We need to make sure that the JSON document is valid.
To do that, please print the content of the response to the serial port like so:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">print</span>(http.<span class="hljs-built_in">getStream</span>().<span class="hljs-built_in">readString</span>());
</code></pre>
<p>Then, copy the response and paste it in a JSON linter, like <a href="https://jsonlint.com/">jsonlint.com</a>.</p>
<p>Is the JSON document valid?</p>
`},"deserialization/invalidinput/httpclient-stream":{choices:[{id:"http10-true",label:"Yes",summary:"Program calls <code>HTTPClient::useHTTP10(true)</code>",next:"deserialization/invalidinput/httpclient-stream-jsonlint"},{id:"http10-false",label:"No",summary:"Program doesn't call <code>HTTPClient::useHTTP10(true)</code>",next:"deserialization/invalidinput/httpclient-stream-http10"}],content:`<p>Did you call <code>HTTPClient::useHTTP10(true)</code>?</p>
`},"deserialization/invalidinput/httpclient":{choices:[{id:"getstream",label:"<code>getStream()</code>",summary:"Response comes from <code>HTTPClient::getStream()</code>",next:"deserialization/invalidinput/httpclient-stream"},{id:"getstring",label:"<code>getString()</code>",summary:"Response comes from <code>HTTPClient::getString()</code>",next:"deserialization/invalidinput/string-jsonlint"},{id:"readstring",label:"<code>readString()</code>",summary:"Response comes from <code>HTTPClient::readString()</code>",next:"deserialization/invalidinput/httpclient-readstring"}],content:`<p>Which function of <code>HTTPClient</code> do you call to get the response?</p>
`},"deserialization/invalidinput/inputtype":{choices:[{id:"stream",label:"A stream",summary:"The input is a stream",next:"deserialization/invalidinput/stream-bom"},{id:"string",label:"A string",summary:"The input is a string",next:"deserialization/invalidinput/string-bom"}],content:`<p>What's the type of the input that you pass to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a></p>
`},"deserialization/invalidinput/jsonlint-bad":{choices:[{id:"comments",label:"It contains comments",summary:"The document contains comments",next:"deserialization/invalidinput/jsonlint-comments"},{id:"other-error",label:"Something else",summary:"The document doesn't contain comments",next:"deserialization/invalidinput/jsonlint-other"}],content:`<p>What is wrong with this JSON document?</p>
`},"deserialization/invalidinput/jsonlint-comments":{choices:[{id:"success",label:"Yes",summary:"Setting ARDUINOJSON_ENABLE_COMMENTS to 1 solves the issue",next:"done"},{id:"failure",label:"No",summary:"Setting ARDUINOJSON_ENABLE_COMMENTS to 1 doesn't solve the issue",next:"deadend"}],content:`<p><a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> can read JSON documents that contain comments, but the support is disabled by default to reduce the footprint of the library.</p>
<p>To enable the support for comments, define <a href="/v6/api/config/enable_comments/"><code>ARDUINOJSON_ENABLE_COMMENTS</code></a> to <code>1</code> before including <code>ArduinoJson.h</code>, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-meta">#<span class="hljs-keyword">define</span> ARDUINOJSON_ENABLE_COMMENTS 1</span>
<span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;ArduinoJson.h&gt;</span></span>
</code></pre>
<p>Did this solve your issue?</p>
`},"deserialization/invalidinput/jsonlint-other":{choices:[],content:`<p>Well, if the JSON document is invalid, you have to fix it, otherwise ArduinoJson won't be able to parse it.</p>
<p>The ball is in your court; there is nothing more I can do.</p>
`},"deserialization/invalidinput/serial-buffer":{choices:[{id:"success",label:"Yes",summary:"Increasing serial buffer size solves the issue",next:"done"},{id:"larger-buffer",label:"No",summary:"Increasing serial buffer size doesn't solves the issue",next:"deserialization/invalidinput/stream-bom"}],content:`<p>Sometimes, the input is invalid because some bytes were dropped when receiving the document.</p>
<p>This problem occurs when the sender writes bytes faster than the receiver reads them, filling up the receiver's serial buffer until it overflows and drops incoming bytes.
For example, this problem happens when the receiver is busy doing some other task while the sender is transmitting.
It also occurs when the receiver logs incoming data at a slower rate; that's why it's crucial to get the &quot;debug&quot; serial port running much faster than the &quot;communication&quot; serial.</p>
<p>You could solve this issue by increasing the serial buffer size; the details depend on each platform.
For Arduino UNO, the default is 64 and can be changed by defining the <code>SERIAL_RX_BUFFER_SIZE</code> macro.
For <a href="https://en.wikipedia.org/wiki/ESP8266">ESP8266</a> and <a href="https://en.wikipedia.org/wiki/ESP32">ESP32</a>, the default is 256 and can be changed by calling <code>setRxBufferSize()</code>.</p>
<p>If your JSON document is significantly larger than the buffer, please try to increase the buffer size.</p>
<p>Did this solve your issue?</p>
`},"deserialization/invalidinput/serial-cable":{choices:[{id:"success",label:"Yes",summary:"Upgrading the cables solves the issue",next:"done"},{id:"improved",label:"Somehow",summary:"Upgrading the cables improves the situation",next:"deserialization/invalidinput/serial-hamming"},{id:"no-improvment",label:"No",summary:"Upgrading the cables doesn't solve the issue",next:"deserialization/invalidinput/serial-jsonlint"}],content:`<p>Any electric wire acts as an antenna; the longer the wire, the stronger the effect. This antenna picks up every electromagnetic field in the environment, which induces a current in the wire. On long wires, this current is strong enough to introduce errors in the communication.</p>
<p>Reducing the length of the cable certainly improve the error ratio, but is rarely applicable.
We can, however, improve the quality of the cable.
For example, you can replace the wires with a coaxial cable: the shielding will prevent the inner wire from acting as an antenna.</p>
<p>Please upgrade or shorten your cable.</p>
<p>Did this solve your issue?</p>
`},"deserialization/invalidinput/serial-flush":{choices:[{id:"success",label:"Yes",summary:"Flushing the serial buffer fixes the issue",next:"done"},{id:"flush",label:"No",summary:"Flushing the serial buffer doesn't fix the issue",next:"deserialization/invalidinput/serial-voltage"}],content:`<p><a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> may return <a href="/v6/api/misc/deserializationerror/#invalidinput"><code>InvalidInput</code></a> because it starts reading the input mid-stream.</p>
<p>For example, it can happen if your program calls <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> in a loop like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">loop</span><span class="hljs-params">()</span> </span>{
  <span class="hljs-keyword">if</span> (Serial1.<span class="hljs-built_in">available</span>()) {
    StaticJsonDocument&lt;<span class="hljs-number">64</span>&gt; doc;
    DeserializationError err = <span class="hljs-built_in">deserializeJson</span>(doc, Serial1);

    <span class="hljs-keyword">if</span> (err) {
      Serial.<span class="hljs-built_in">println</span>(err.<span class="hljs-built_in">c_str</span>());
      <span class="hljs-keyword">return</span>;
    }
}
</code></pre>
<p>The problem with this program is that, if <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> returns an error (such as <a href="/v6/api/misc/deserializationerror/#nomemory"><code>NoMemory</code></a>), any subsequent call to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> will return <a href="/v6/api/misc/deserializationerror/#invalidinput"><code>InvalidInput</code></a>. Indeed, <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> stops reading as soon as it encounters an error, so the remainder of the document is still in the serial buffer.</p>
<p>The solution is to flush the serial buffer any time an error is detected:</p>
<pre><code class="hljs language-c++"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">loop</span><span class="hljs-params">()</span> </span>{
  <span class="hljs-keyword">if</span> (Serial1.<span class="hljs-built_in">available</span>()) {
    StaticJsonDocument&lt;<span class="hljs-number">64</span>&gt; doc;
    DeserializationError err = <span class="hljs-built_in">deserializeJson</span>(doc, Serial1);

    <span class="hljs-keyword">if</span> (err) {
      Serial.<span class="hljs-built_in">println</span>(err.<span class="hljs-built_in">c_str</span>());

      <span class="hljs-keyword">while</span> (Serial1.<span class="hljs-built_in">available</span>() &gt; <span class="hljs-number">0</span>)
        Serial1.<span class="hljs-built_in">read</span>();

      <span class="hljs-keyword">return</span>;
    }
}
</code></pre>
<p>Did this solve your issue?</p>
`},"deserialization/invalidinput/serial-hamming":{choices:[{id:"success",label:"Yes",summary:"Error-correction codes fix the issue",next:"done"},{id:"ecc",label:"No",summary:"Error-correction codes doesn't fix the issue",next:"deserialization/invalidinput/serial-jsonlint"}],content:`<p><a href="https://en.wikipedia.org/wiki/Error_correction_code">Error-correction codes (ECC)</a> are a way of transmitting the data with redundant information that allows the receiver to fix most of the errors.
The most basic error-correction code is <a href="https://en.wikipedia.org/wiki/Hamming(7,4)">Hamming(7,4)</a>, which transmits 7 bits for every 4 bits of actual data.
In other words, it adds 3 bits of redundancy for every 4 bits that you send.
The magic with this code is that it can correct any 1-bit error in the 7 bits.</p>
<p>The simplest way to implement <a href="https://en.wikipedia.org/wiki/Hamming(7,4)">Hamming(7,4)</a> on Arduino is to use the <code>HammingStream</code> class from the <a href="https://github.com/bblanchon/ArduinoStreamUtils">StreamUtils</a> library:</p>
<pre><code class="hljs language-c++"><span class="hljs-function">HammingStream&lt;7, 4&gt; <span class="hljs-title">eccLinkSerial</span><span class="hljs-params">(linkSerial)</span></span>;
</code></pre>
<p>Now, you can use <code>eccSerial1</code> in place of the original <code>Serial1</code>; it will automatically encode and decode the information.</p>
<p>As <a href="https://en.wikipedia.org/wiki/Hamming(7,4)">Hamming(7,4)</a> only transmits 7 bits of data, you can safely downgrade the serial link from 8 to 7 bits.
You can do this by passing <code>SERIAL_7N1</code> as the second argument of <a href="https://www.arduino.cc/reference/en/language/functions/communication/serial/begin/"><code>Serial::begin()</code></a>.
This feature is not supported by <a href="https://www.arduino.cc/en/Reference/SoftwareSerial"><code>SoftwareSerial</code></a>, which is yet another reason to avoid it.</p>
<p>Error-correction codes are very powerful, but they'll never eliminate errors completely. For example, <a href="https://en.wikipedia.org/wiki/Hamming(7,4)">Hamming(7,4)</a> can only fix a 1-bit error, so if two or more bits are swapped, it will not fix them.
To get more confidence in the integrity of the received data, the ultimate solution is to add an error detection scheme, like a checksum.</p>
<p>Did this solve your issue?</p>
`},"deserialization/invalidinput/serial-jsonlint":{choices:[{id:"valid",label:"Yes",summary:"<code>jsonlint</code> says the document is valid",next:"deserialization/invalidinput/serial-buffer"},{id:"invalid",label:"No",summary:"<code>jsonlint</code> says the document is invalid",next:"deserialization/invalidinput/jsonlint-bad"}],content:`<p>We need to make sure that the JSON document is valid.
To do that, please print the input to the serial port, like so:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">print</span>(Serial1.<span class="hljs-built_in">readString</span>());
</code></pre>
<p>Note that <a href="https://www.arduino.cc/reference/en/language/functions/communication/serial/readstring/"><code>Serial1.readString()</code></a> will wait for a timeout before returning, so the statement may appear frozen. Be patient.</p>
<p>Then, copy the response and paste it in a JSON linter, like <a href="https://jsonlint.com/">jsonlint.com</a>.</p>
<p>Is the JSON document valid?</p>
`},"deserialization/invalidinput/serial-rate":{choices:[{id:"success",label:"Yes",summary:"Reducing baud rate fixes the issue",next:"done"},{id:"better",label:"Somehow",summary:"Reducing baud rate improves the situation",next:"deserialization/invalidinput/serial-cable"},{id:"not-better",label:"No",summary:"Reducing baud rate doesn't fix the issue",next:"deserialization/invalidinput/serial-jsonlint"}],content:`<p>Any kind of communication is subject to errors: sometimes you send a <code>1</code> and receive a <code>0</code>.
The problem with the serial port is that there is no built-in way to fix these errors.
However, we can take some measures to reduce the error ratio to something acceptable.</p>
<p>The faster the communication, the higher the error ratio, so the first thing to try is to reduce the communication speed.
Please try to reduce the baud rate.</p>
<p class="short-warning">If your program receives from one port and logs to another one, <strong>make sure the latter runs at a much higher speed</strong>. Logging must be at least ten times faster, or it will slow down the receiving port, which may drop incoming bytes.</p>
<p>Did this solve your issue?</p>
`},"deserialization/invalidinput/serial-software":{choices:[{id:"success",label:"Yes",summary:"Removing <code>SoftwareSerial</code> solves the issue",next:"done"},{id:"removed",label:"No",summary:"Removing <code>SoftwareSerial</code> doesn't solve the issue",next:"deserialization/invalidinput/serial-voltage"}],content:`<p>The <a href="https://en.wikipedia.org/wiki/AVR_microcontrollers">AVR</a> implementation of <a href="https://www.arduino.cc/en/Reference/SoftwareSerial"><code>SoftwareSerial</code></a> is <strong>notoriously unreliable</strong> 😱.
The main problem is that it disables interrupts when sending data, which causes many issues like dropping incoming bytes on the regular <a href="https://www.arduino.cc/reference/en/language/functions/communication/serial/"><code>Serial</code></a>.</p>
<p>You may consider the following alternative libraries, but none of them is perfect:</p>
<ul>
<li><a href="https://github.com/PaulStoffregen/AltSoftSerial">AltSoftSerial</a></li>
<li><a href="https://github.com/MustBeArt/SlowSoftSerial">SlowSoftSerial</a></li>
<li><a href="https://github.com/SlashDevin/NeoSWSerial">NeoSoftSerial</a></li>
</ul>
<p>However, I highly recommend that you use a hardware implementation (such as <code>Serial</code> or <code>Serial1</code>). Don't hesitate to upgrade your board to get one with more than one <a href="https://en.wikipedia.org/wiki/Universal_asynchronous_receiver-transmitter">UART</a>.</p>
<p>For example, if you're currently using an Arduino UNO, which has only one UART, you can upgrade to the Arduino Leonardo, which has three.</p>
<p>Did this solve your issue?</p>
`},"deserialization/invalidinput/serial-voltage":{choices:[{id:"success",label:"Yes",summary:"The issue is a voltage mismatch",next:"done"},{id:"voltage-ok",label:"No",summary:"The issue is not a voltage mismatch",next:"deserialization/invalidinput/serial-rate"}],content:`<p>Not all microcontrollers use the same voltage for the serial port.
Some use 5V logic; others use 3.3V.</p>
<p>For example, the Arduino UNO uses 5V whereas the ESP8266 uses 3.3V.</p>
<p>If you need to wire two devices with different voltages, you need a <a href="https://www.amazon.com/dp/B00NAY2BBY?tag=bblanchon0b-20">logic-level converter</a>.</p>
<p>Did this solve your issue?</p>
`},"deserialization/invalidinput/serial":{choices:[{id:"software",label:"Yes",summary:"Program uses <code>SoftwareSerial</code>",next:"deserialization/invalidinput/serial-software"},{id:"hardware",label:"No",summary:"Program doesn't use <code>SoftwareSerial</code>",next:"deserialization/invalidinput/serial-flush"}],content:`<p>Are you using <a href="https://www.arduino.cc/en/Reference/SoftwareSerial"><code>SoftwareSerial</code></a>?</p>
`},"deserialization/invalidinput/stream-bom":{choices:[{id:"utf8",label:"<code>239</code>",summary:"Input's first byte suggests a UTF-8 BOM",next:"deserialization/invalidinput/bom-utf8"},{id:"utf16",label:"<code>254</code> or <code>255</code>",summary:"Input's first byte suggests a UTF-16 BOM",next:"deserialization/invalidinput/bom-utf16"},{id:"no-bom",label:"Something else",summary:"Input's first byte doesn't suggest a BOM",next:"deserialization/invalidinput/bom-none"}],content:`<p>We must check that the JSON document is not preceded by a  <a href="https://en.wikipedia.org/wiki/Byte_order_mark">Byte Order Mark</a> (BOM).</p>
<p>To do this, we'll peek at the first byte in the stream and print the value to the serial. Insert the following line before the call to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">println</span>(stream.<span class="hljs-built_in">peek</span>());
</code></pre>
<p>What value does it print?</p>
`},"deserialization/invalidinput/stream-buffer":{choices:[{id:"success",label:"Yes",summary:"Adding a buffer solves the issue",next:"done"},{id:"buffer",label:"No",summary:"Adding a buffer doesn't solve the issue",next:"deserialization/invalidinput/stream-bom"}],content:`<p>Sometimes, the input is invalid because some bytes were dropped when receiving the document.
Usually, this happens when the receiver reads the stream too slowly, which overflows a buffer somewhere in the path.</p>
<p>Indeed, because it reads bytes one by one, <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> can be slow with some implementations of <a href="https://www.arduino.cc/reference/en/language/functions/communication/stream/"><code>Stream</code></a>. To speed up the reading, we must add a buffer between the <a href="https://www.arduino.cc/reference/en/language/functions/communication/stream/"><code>Stream</code></a> and <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>. The easiest way to do this is to use <code>ReadBufferingStream</code> from the <a href="https://github.com/bblanchon/ArduinoStreamUtils">StreamUtils library</a>:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// replace the following line:</span>
<span class="hljs-built_in">deserializeJson</span>(doc, stream);

<span class="hljs-comment">// with these two lines:</span>
<span class="hljs-function">ReadBufferingStream <span class="hljs-title">bufferedStream</span><span class="hljs-params">(stream, <span class="hljs-number">64</span>)</span></span>;
<span class="hljs-built_in">deserializeJson</span>(doc, bufferedStream);
</code></pre>
<p>Thanks to <code>ReadBufferingStream</code>, the program will read the input in chunks of 64 bytes, which will be much faster.
Hopefully, it will be fast enough to read the whole message without droping any byte.</p>
<p>Did this solve your issue?</p>
`},"deserialization/invalidinput/stream-jsonlint":{summary:null,choices:[{id:"valid",label:"Yes",summary:"<code>jsonlint</code> says the document is valid",next:"deserialization/invalidinput/stream-buffer"},{id:"invalid",label:"No",summary:"<code>jsonlint</code> says the document is invalid",next:"deserialization/invalidinput/jsonlint-bad"}],content:`<p>We need to make sure that the JSON document is valid.
To do that, please print the input to the serial port, like so:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">print</span>(stream.<span class="hljs-built_in">readString</span>());
</code></pre>
<p>Then, copy the response and paste it in a JSON linter, like <a href="https://jsonlint.com/">jsonlint.com</a>.</p>
<p>Is the JSON document valid?</p>
`},"deserialization/invalidinput/string-bom":{choices:[{id:"utf8",label:"<code>239</code>",summary:"Input's first byte suggests a UTF-8 BOM",next:"deserialization/invalidinput/bom-utf8"},{id:"utf16",label:"<code>254</code> or <code>255</code>",summary:"Input's first byte suggests a UTF-16 BOM",next:"deserialization/invalidinput/bom-utf16"},{id:"no-bom",label:"Something else",summary:"Input's first byte doesn't suggest a BOM",next:"deserialization/invalidinput/bom-none"}],content:`<p>We must check that the JSON document is not preceded by a  <a href="https://en.wikipedia.org/wiki/Byte_order_mark">Byte Order Mark</a> (BOM).</p>
<p>To do this, we'll print the first byte to the serial port. Insert the following line before the call to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">println</span>(input[<span class="hljs-number">0</span>]);
</code></pre>
<p>What value does it print?</p>
`},"deserialization/invalidinput/string-jsonlint":{summary:null,choices:[{id:"valid",label:"Yes",summary:"<code>jsonlint</code> says the document is valid",next:"deserialization/invalidinput/string-bom"},{id:"invalid",label:"No",summary:"<code>jsonlint</code> says the document is invalid",next:"deserialization/invalidinput/jsonlint-bad"}],content:`<p>We need to make sure that the JSON document is valid.
To do that, please print the content of the string to the serial port, like so:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">print</span>(input);
</code></pre>
<p>Then, copy the response and paste it in a JSON linter, like <a href="https://jsonlint.com/">jsonlint.com</a>.</p>
<p>Is the JSON document valid?</p>
`},"deserialization/invalidinput":{choices:[{id:"http",label:"An HTTP response",summary:"Input comes from HTTP",next:"deserialization/invalidinput/http"},{id:"serial",label:"A serial port",summary:"Input comes from a serial port",next:"deserialization/invalidinput/serial"},{id:"file",label:"A file",summary:"Input comes from a file",next:"deserialization/invalidinput/file-jsonlint"},{id:"stream",label:"A stream",summary:"Input comes from a stream",next:"deserialization/invalidinput/stream-jsonlint"},{id:"string",label:"A string (like <code>String</code> or <code>const char*</code>)",summary:"Input comes from a string",next:"deserialization/invalidinput/string-jsonlint"},{id:"char-ptr",label:"A char array or pointer (like <code>char[]</code> or <code>char*</code>)",summary:"Input comes from a char array",next:"deserialization/invalidinput/array"}],content:`<p>Where does the input come from?</p>
`},"deserialization/noerror/firstchar-brace":{choices:[{id:"success",label:"Yes",summary:"Garbage characters follow the input",next:"done"},{id:"lint",label:"No",summary:"No garbage characters follow the input",next:"deserialization/noerror/jsonlint"}],content:`<p><a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> stops reading as soon as the JSON document ends.
In your case, it will read the input until it reaches the matching <code>}</code> and ignore any remaining characters.</p>
<p>Suppose the input looks like this:</p>
<pre><code class="hljs language-json"><span class="hljs-punctuation">{</span><span class="hljs-attr">&quot;key&quot;</span><span class="hljs-punctuation">:</span><span class="hljs-string">&quot;value&quot;</span><span class="hljs-punctuation">}</span>GARBAGE
</code></pre>
<p>Here, <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> reads the JSON object <code>{&quot;key&quot;:&quot;value&quot;}</code> and returns <code>Ok</code> ignoring the <code>GARBAGE</code> part.</p>
<p>This feature enables <a href="/v6/how-to/deserialize-a-very-large-document/#deserialization/in-chunks">deserializing in chunks</a> and allows non-zero-terminated input strings.</p>
<p>Did this solve your issue?</p>
`},"deserialization/noerror/firstchar-bracket":{choices:[{id:"success",label:"Yes",summary:"Garbage characters follow the input",next:"done"},{id:"lint",label:"No",summary:"No garbage characters follow the input",next:"deserialization/noerror/jsonlint"}],content:`<p><a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> stops reading as soon as the JSON document ends.
In your case, it will read the input until it reaches the matching <code>]</code> and ignore any remaining characters.</p>
<p>Suppose the input looks like this:</p>
<pre><code class="hljs language-json"><span class="hljs-punctuation">[</span><span class="hljs-number">1</span><span class="hljs-punctuation">,</span><span class="hljs-number">2</span><span class="hljs-punctuation">,</span><span class="hljs-number">3</span><span class="hljs-punctuation">]</span>GARBAGE
</code></pre>
<p>Here, <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> reads the JSON array <code>[1,2,3]</code> and returns <code>Ok</code> ignoring the <code>GARBAGE</code> part.</p>
<p>This feature enables <a href="/v6/how-to/deserialize-a-very-large-document/#deserialization/in-chunks">deserializing in chunks</a> and allows non-zero-terminated input strings.</p>
<p>Did this solve your issue?</p>
`},"deserialization/noerror/firstchar-quote":{choices:[{id:"success",label:"Yes",summary:"Garbage characters follow the input",next:"done"},{id:"lint",label:"No",summary:"No garbage characters follow the input",next:"deserialization/noerror/jsonlint"}],content:`<p><a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> stops reading as soon as the JSON document ends.
In your case, it will read the input until it reaches the matching <code>&quot;</code> and ignore any remaining characters.</p>
<p>Suppose the input looks like this:</p>
<pre><code class="hljs language-json"><span class="hljs-string">&quot;hello&quot;</span>GARBAGE
</code></pre>
<p>Here, <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> reads the JSON string <code>&quot;hello&quot;</code> and returns <code>Ok</code> ignoring the <code>GARBAGE</code> part.</p>
<p>This feature enables <a href="/v6/how-to/deserialize-a-very-large-document/#deserialization/in-chunks">deserializing in chunks</a> and allows non-zero-terminated input strings.</p>
<p>Did this solve your issue?</p>
`},"deserialization/noerror/firstchar":{choices:[{id:"brace",label:"An opening brace: <code>{</code>",next:"deserialization/noerror/firstchar-brace",summary:"The first character is <code>{</code>"},{id:"bracket",label:"An opening bracket: <code>[</code>",next:"deserialization/noerror/firstchar-bracket",summary:"The first character is <code>[</code>"},{id:"quote",label:"A quote: <code>&quot;</code>",next:"deserialization/noerror/firstchar-quote",summary:"The first character is <code>&quot;</code>"},{id:"other",label:"None of the above",next:"deserialization/noerror/unacceptable",summary:"The first character is neither <code>{</code>, nor <code>[</code>, nor <code>&quot;</code>"}],content:`<p>What's the first character of your input?</p>
`},"deserialization/noerror/garbage-zerocopy-cast":{choices:[{id:"success",label:"Yes",summary:"Casting the pointer fixes the issue",next:"done"},{id:"cast",label:"No",summary:"Casting the pointer doesn't fix the issue",next:"deadend"}],content:`<p>The easiest solution is to disable the zero-copy mode by passing a read-only input buffer. Casting the pointer will do the trick:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// replace this:</span>
<span class="hljs-built_in">deserializeJson</span>(doc, input, inputSize);

<span class="hljs-comment">// ...with this:</span>
<span class="hljs-built_in">deserializeJson</span>(doc, (<span class="hljs-type">const</span> <span class="hljs-type">char</span>*)input, inputSize);
</code></pre>
<p>Now that the zero-copy mode is disabled, you probably need a larger <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, so don't forget to update the capacity. As usual, use the <a href="/v6/assistant/">ArduinoJson Assistant</a> to compute the right capacity for your project.</p>
<p>Did this solve your issue?</p>
`},"deserialization/noerror/garbage-zerocopy-stream":{choices:[{id:"success",label:"Yes",summary:"Passing the stream to <code>deserializeJson()</code> fixes the issue",next:"done"},{id:"cast",label:"No",summary:"Passing the stream to <code>deserializeJson()</code> doesn't fix the issue",next:"deadend"}],content:`<p>The easiest solution is to remove the buffer and pass the input stream directly to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>.</p>
<p>For example, assuming the JSON document comes from a file:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// replace this:</span>
<span class="hljs-function">std::unique_ptr&lt;<span class="hljs-type">char</span>[]&gt; <span class="hljs-title">buf</span><span class="hljs-params">(<span class="hljs-keyword">new</span> <span class="hljs-type">char</span>[size])</span></span>;
file.<span class="hljs-built_in">readBytes</span>(buf.<span class="hljs-built_in">get</span>(), size);
<span class="hljs-built_in">deserializeJson</span>(doc, buf.<span class="hljs-built_in">get</span>());

<span class="hljs-comment">// ...with this:</span>
<span class="hljs-built_in">deserializeJson</span>(doc, file);
</code></pre>
<p>Now that the zero-copy mode is disabled, you probably need a larger <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, so don't forget to update the capacity. As usual, use the <a href="/v6/assistant/">ArduinoJson Assistant</a> to compute the right capacity for your project.</p>
<p>Did this solve your issue?</p>
`},"deserialization/noerror/garbage-zerocopy":{choices:[{id:"success",label:"Yes, the input comes from a stream",summary:"Input comes from a stream",next:"deserialization/noerror/garbage-zerocopy-stream"},{id:"cast",label:"No, it doesn't",summary:"Input doesn't come from a stream",next:"deserialization/noerror/garbage-zerocopy-cast"}],content:`<p>When you pass a writeable buffer as the input of <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>, ArduinoJson uses the zero-copy mode. Instead of copying the strings from the input into the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, it stores pointers to the input buffer.
Therefore, the input buffer must stay alive during the whole lifetime of the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>; otherwise, all the strings in the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> will be dangling pointers.</p>
<p>Does your program fills this buffer from a <a href="https://www.arduino.cc/reference/en/language/functions/communication/stream/"><code>Stream</code></a> (like <a href="https://www.arduino.cc/en/Reference/SD"><code>File</code></a>, <a href="https://www.arduino.cc/reference/en/language/functions/communication/serial/"><code>Serial</code></a>, <a href="https://www.arduino.cc/en/Reference/EthernetClient"><code>EthernetClient</code></a>, <a href="https://www.arduino.cc/en/Reference/WiFiClient"><code>WifiClient</code></a>...)?</p>
`},"deserialization/noerror/garbage":{choices:[{id:"char-ptr",label:"<code>char*</code> (or <code>char[]</code>)",summary:"Input type is <code>char*</code>",next:"deserialization/noerror/garbage-zerocopy"},{id:"const-char-ptr",label:"<code>const char*</code>",summary:"Input type is <code>const char*</code>",next:"deadend"},{id:"string",label:"<code>String</code> (or <code>std::string</code>)",summary:"Input type is <code>String</code>",next:"deadend"},{id:"stream",label:"<code>Stream</code> (or <code>std::istream)</code>",summary:"Input type is <code>Stream</code>",next:"deadend"}],content:`<p>What is the type of the second argument passed to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>?</p>
`},"deserialization/noerror/input-modified":{choices:[{id:"success",label:"Yes",summary:"Casting the pointer fixes the issue",next:"done"},{id:"cast",label:"No",summary:"Casting the pointer doesn't fix the issue",next:"deadend"}],content:`<p>When you pass a writeable buffer as the input of <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>, ArduinoJson uses the zero-copy mode. Instead of copying the strings from the input into the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, it stores pointers to the input buffer.</p>
<p>While doing this, the parser modifies the input buffer to unescape special characters and add string terminator.
When <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> returns the input buffer doesn't contain a valid JSON document anymore.</p>
<p>For more information, please see <a href="/v6/issues/altered-input/">Why is the input modified?</a></p>
<p>If you must preserve the input as it is, you must disable the zero-copy mode by passing a read-only input buffer. Casting the pointer will do the trick:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// replace this:</span>
<span class="hljs-built_in">deserializeJson</span>(doc, input, inputSize);

<span class="hljs-comment">// ...with this:</span>
<span class="hljs-built_in">deserializeJson</span>(doc, (<span class="hljs-type">const</span> <span class="hljs-type">char</span>*)input, inputSize);
</code></pre>
<p>Now that the zero-copy mode is disabled, you probably need a larger <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, so don't forget to update the capacity. As usual, use the <a href="/v6/assistant/">ArduinoJson Assistant</a> to compute the right capacity for your project.</p>
<p>Did this solve your issue?</p>
`},"deserialization/noerror/jsonlint-invalid":{choices:[{id:"acceptable",label:"Yes",next:"done",summary:"<code>deserializeJson()</code> tolerates the error, and that's OK"},{id:"unacceptable",label:"No",next:"deserialization/noerror/unacceptable",summary:"<code>deserializeJson()</code> should not let this error pass"}],content:`<blockquote>
<p>ArduinoJson isn't very picky about the input: its implementation favors code size and speed over strict conformance.
After all, size and speed are what matter the most for embedded software, right?</p>
<p>This means that ArduinoJson's parser may accept documents that would be rejected by other parsers.</p>
<p>For example, <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> tolerates the following substitutions:</p>
<ul>
<li><code>'hello'</code> instead of <code>&quot;hello&quot;</code> (single quotes)</li>
<li><code>nULL</code> or <code>n0n3</code> instead if <code>null</code> (only checks first character and length)</li>
<li><code>tRUE</code> or <code>t0t0</code> instead of <code>true</code> (ditto)</li>
<li><code>fALSE</code> or <code>fAkk3</code> instead of <code>false</code> (ditto)</li>
</ul>
</blockquote>
<p>It also ignores the follwing UTF-8 errors:</p>
<ul>
<li><code>&quot;\\ud83d&quot;</code> (a leading surrogate without a trailing surrogate)</li>
<li><code>&quot;\\udda4&quot;</code> (a trailing surrogate without a leading surrogate)</li>
<li><code>&quot;\\ud83d\\ud83d&quot;</code> (two leading surrogates)</li>
</ul>
<p>Lastly, it supports C++-style comments when <a href="/v6/api/config/enable_comments/">ARDUINOSJSON_ENABLE_COMMENTS</a> is set to <code>1</code>.</p>
<p>As a result, you cannot use <a href="/v6/api/json/deserializejson/"><code>deserializeJson</code></a> as a JSON validator because you'd get many false-negatives.</p>
<p>Did this solve your issue?</p>
`},"deserialization/noerror/jsonlint-valid":{choices:[],content:`<p>So, <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> returns <code>Ok</code>, and jsonlint confirms that the input is valid.</p>
<p>I don't see any issue here; maybe you should <a href="/v6/troubleshooter/">start over</a>?</p>
`},"deserialization/noerror/jsonlint":{choices:[{id:"valid",label:"Document is valid",next:"deserialization/noerror/jsonlint-valid",summary:"jsonlint says the document is valid"},{id:"invalid",label:"Document is invalid",next:"deserialization/noerror/jsonlint-invalid",summary:"jsonlint says the document is invalid"}],content:`<p>Let's make sure that your input is actually invalid.</p>
<p>Please go to <a href="https://jsonlint.com/">jsonlint.com</a>, paste your input, and press &quot;Validate JSON.&quot;</p>
<p>What does it say?</p>
`},"deserialization/noerror/novalue/confusion":{choices:[{id:"success",label:"Yes",summary:"Issue is a confusion between array and object",next:"done"},{id:"array-object",label:"No",summary:"Issue is not a confusion between array and object",next:"deadend"}],content:`<p>When a program fails to extract the values from a <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, it's often because there is a confusion between arrays and objects.</p>
<p>For example, a common mistake is to write:</p>
<pre><code class="hljs language-c++"><span class="hljs-type">const</span> <span class="hljs-type">char</span>* temperature = doc[<span class="hljs-string">&quot;list&quot;</span>][<span class="hljs-string">&quot;main&quot;</span>][<span class="hljs-string">&quot;temp&quot;</span>];
</code></pre>
<p>instead of</p>
<pre><code class="hljs language-c++"><span class="hljs-type">const</span> <span class="hljs-type">char</span>* temperature = doc[<span class="hljs-string">&quot;list&quot;</span>][<span class="hljs-number">0</span>][<span class="hljs-string">&quot;main&quot;</span>][<span class="hljs-string">&quot;temp&quot;</span>];
</code></pre>
<p>You can find other examples in <a href="/v6/issues/cannot-get-values/">deserializeJson() succeeds but I cannot read any value</a>, but the simplest solution is to use the <a href="/v6/assistant/">ArduinoJson Assistant</a> because the last step shows how to extract the values.</p>
<p>Did this solve your issue?</p>
`},"deserialization/noerror/novalue/filter":{choices:[{id:"success",label:"Yes",summary:"Fixing the filter solves the issue",next:"done"},{id:"filter-ok",label:"No",summary:"Fixing the filter doesn't solve the issue",next:"deserialization/noerror/novalue/print"}],content:`<p>Maybe the filter excludes the part that it should keep. Let's verify that it's doing its job correctly:</p>
<p>Please print the filter to the serial port like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-built_in">serializeJsonPretty</span>(filter, Serial);
</code></pre>
<p>Then, open the <a href="/v6/assistant/">ArduinoJson Assistant</a>:</p>
<ul>
<li>in step 1: choose &quot;Deserialize and filter&quot;</li>
<li>in step 2:
<ul>
<li>in the &quot;input&quot; column, paste a sample input</li>
<li>in the &quot;filter&quot; column, paste the filter that your program just printed</li>
</ul>
</li>
</ul>
<p>The column &quot;filtered input&quot; shows the result of applying the filter to the input.<br>
Make sure the result is what you expect, and adjust the filter as needed.<br>
Once happy with the result, you can move to step 4 and copy the block that fills the filter.</p>
<p>Did this solve your issue?</p>
`},"deserialization/noerror/novalue/json-or-msgpack":{choices:[{id:"json",label:"<code>deserializeJson()</code>",summary:"Program calls <code>deserializeJson()</code>",next:"deadend"},{id:"msgpack",label:"<code>deserializeMsgPack()</code>",summary:"Program calls <code>deserializeMsgPack()</code>",next:"deserialization/noerror/novalue/msgpack"}],content:`<p>Which function do you call to deserialize the input?</p>
`},"deserialization/noerror/novalue/msgpack":{choices:[{id:"success",label:"Yes",summary:"The input was incorrect",next:"done"},{id:"not-garbage",label:"No",summary:"The input is correct",next:"deadend"}],content:`<p>It seems that your program fed <a href="/v6/api/msgpack/deserializemsgpack/"><code>deserializeMsgPack()</code></a> with garbage input.</p>
<p>Indeed, any byte in the following ranges is a valid one-byte MessagePack document:</p>
<ul>
<li><code>0x00</code>-<code>0x7f</code>: positive fixint</li>
<li><code>0xc0</code>: nil</li>
<li><code>0xc2</code>: false</li>
<li><code>0xc3</code>: true</li>
<li><code>0xe0</code>-<code>0xff</code>: negative fixint</li>
</ul>
<p>As you can see, these values cover about 63% of all possible values for a random byte; that's why they are very likely to occur.</p>
<p>When the first byte in the input is one of these, <a href="/v6/api/msgpack/deserializemsgpack"><code>deserializeMsgPack()</code></a> saves the corresponding value in the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> and immediately returns <a href="/v6/api/misc/deserializationerror/#ok"><code>Ok</code></a>, which is the correct behavior.</p>
<p>To fix this issue, you must repair the input.</p>
<p>Did this solve your issue?</p>
`},"deserialization/noerror/novalue/print":{choices:[{id:"quote",label:"A quote (<code>&quot;</code>)",summary:"Serialized document starts with a quote",next:"deserialization/noerror/novalue/quote"},{id:"bracket",label:"A square bracket (<code>[</code>)",summary:"Serialized document starts with a square bracket",next:"deserialization/noerror/novalue/confusion"},{id:"brace",label:"A curly brace (<code>{</code>)",summary:"Serialized document starts with a curly brace",next:"deserialization/noerror/novalue/confusion"},{id:"not-in-list",label:"None of the above",summary:"Serialized document starts with neither a quote, a bracket, nor a brace.",next:"deserialization/noerror/novalue/json-or-msgpack"}],content:`<p>Please print the content of the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> to the serial port like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-built_in">serializeJson</span>(doc, Serial);
Serial.<span class="hljs-built_in">println</span>(); <span class="hljs-comment">// adds a line break (optional)</span>
</code></pre>
<p>What's the first character?</p>
`},"deserialization/noerror/novalue/quote-keep":{choices:[{id:"success",label:"Yes",summary:"Calling <code>deserializeJson()</code> twice fixes the issue",next:"done"},{id:"call-twice",label:"No",summary:"Calling <code>deserializeJson()</code> twice doesn't fix the issue",next:"deadend"}],content:`<p>If you cannot fix the server, you can at least reverse the double serialization (stringification) to get back the original object.</p>
<p>To do this, you must call <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> twice, like so:</p>
<pre><code class="hljs language-c++">StaticJsonDocument&lt;<span class="hljs-number">512</span>&gt; doc1, doc2;
<span class="hljs-built_in">deserializeJson</span>(doc1, input);
<span class="hljs-built_in">deserializeJson</span>(doc2, doc1.<span class="hljs-built_in">as</span>&lt;<span class="hljs-type">const</span> <span class="hljs-type">char</span>*&gt;());
String value = doc2[<span class="hljs-string">&quot;hello&quot;</span>];
</code></pre>
<p>In this snippet, <code>doc1</code> is only used during the second deserialization and can be safely discarded after that.
The cleanest way to do this is to wrap the double deserialization in a function, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-function">DeserilizationError <span class="hljs-title">deserializeJsonTwice</span><span class="hljs-params">(JsonDocument&amp; doc, Stream&amp; input)</span> </span>{
  StaticJsonDocument&lt;<span class="hljs-number">512</span>&gt; tmp;
  DeserializationError err = <span class="hljs-built_in">deserializeJson</span>(tmp, input);
  <span class="hljs-keyword">if</span> (err) <span class="hljs-keyword">return</span> err;
  <span class="hljs-keyword">return</span> <span class="hljs-built_in">deserializeJson</span>(doc, tmp.<span class="hljs-built_in">as</span>&lt;<span class="hljs-type">const</span> <span class="hljs-type">char</span>*&gt;());
}
</code></pre>
<p>Of course, if you need to use the <a href="/news/2020/03/22/version-6-15-0/">filtering feature</a>, you must pass <code>DeserializationOption::Filter</code> to the second call to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>.</p>
<p>Did this solve your issue?</p>
`},"deserialization/noerror/novalue/quote-server":{choices:[{id:"success",label:"Yes",summary:"Fixing the server fixes the issue",next:"done"},{id:"server-ok",label:"No",summary:"Fixing the server doesn't fix the issue",next:"deserialization/noerror/novalue/quote-keep"}],content:`<p>In the server implementation, please look at the view that returns the faulty JSON.
You will probably find that this function explicitly stringifies the response to JSON, which is incorrect because the underlying framework already does it for you.</p>
<p>Of course the details will depend on the framework you used to implement the server, but in any case, you'll have to remove a call to a function that stringifies to JSON. Here are some example:</p>
<ul>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/JSON/stringify"><code>JSON.stringify()</code></a> in JavaScript</li>
<li><a href="https://www.php.net/manual/en/function.json-encode.php"><code>json_encode()</code></a> in PHP</li>
<li><a href="https://docs.python.org/3/library/json.html"><code>json.dumps()</code></a> in Python</li>
<li><a href="https://ruby-doc.org/stdlib-2.6.3/libdoc/json/rdoc/JSON.html"><code>JSON.generate()</code></a> in Ruby</li>
<li><a href="https://docs.microsoft.com/en-us/dotnet/api/system.text.json.jsonserializer.serialize"><code>JsonSerializer.Serialize()</code></a> on .NET</li>
<li><a href="https://www.javadoc.io/doc/com.google.code.gson/gson/latest/com.google.gson/com/google/gson/Gson.html"><code>gson.toJson()</code></a> in Java</li>
<li><a href="https://golang.org/pkg/encoding/json/"><code>json.Marshal()</code></a> in Go</li>
</ul>
<p>Did this solve your issue?</p>
`},"deserialization/noerror/novalue/quote":{choices:[{id:"server-fixable",label:"Yes",summary:"User can modify the server",next:"deserialization/noerror/novalue/quote-server"},{id:"server-unfixable",label:"No",summary:"User cannot modify the server",next:"deserialization/noerror/novalue/quote-keep"}],content:`<p>If you see a double-quote at the beginning of the JSON input, it means that the whole document is neither an object or an array; it's a string. This happens when the input was incorrectly generated, by serializing (stringifying) to JSON twice.</p>
<p>For example, the following document:</p>
<pre><code class="hljs language-json"><span class="hljs-string">&quot;{\\&quot;hello\\&quot;:\\&quot;world\\&quot;}&quot;</span>
</code></pre>
<p>...is not a JSON <em>object</em> but a <em>string</em>.
It's the result of the JSON serialization (stringification) of the following JSON document:</p>
<pre><code class="hljs language-json"><span class="hljs-punctuation">{</span><span class="hljs-attr">&quot;hello&quot;</span><span class="hljs-punctuation">:</span><span class="hljs-string">&quot;world&quot;</span><span class="hljs-punctuation">}</span>
</code></pre>
<p>...which was alread serialized (stringified) to JSON.</p>
<p>So, the bug isn't in the Arduino code, but on the other size, most likely a server.</p>
<p>Can you modify the code of the server?</p>
`},"deserialization/noerror/novalue":{choices:[{id:"filter",label:"Yes",summary:"Program uses <code>DeserializationOption::Filter</code>",next:"deserialization/noerror/novalue/filter"},{id:"no-filter",label:"No",summary:"Program doesn't use <code>DeserializationOption::Filter</code>",next:"deserialization/noerror/novalue/print"}],content:`<p>Do you use a filter (with <code>DeserializationOption::Filter</code>)?</p>
`},"deserialization/noerror/slow":{choices:[{id:"success",label:"Yes",summary:"<code>ReadBufferingStream</code> solves the issue",next:"done"},{id:"buffered",label:"No",summary:"<code>ReadBufferingStream</code> doesn't solve the issue",next:"deadend"}],content:`<p>Indeed, <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> can be pretty slow with unbuffered implementations of <a href="https://www.arduino.cc/reference/en/language/functions/communication/stream/"><code>Stream</code></a> (such as <code>File</code> and <code>WiFiClient</code>) because it reads characters one by one.</p>
<p>Why does it reads bytes one by one?<br>
Because it has to stop exactly at the end of the document to support <a href="https://en.wikipedia.org/wiki/JSON_streaming">JSON streaming</a> and similar techniques.</p>
<p>To speed up the process, we need to insert a buffer between the <a href="https://www.arduino.cc/reference/en/language/functions/communication/stream/"><code>Stream</code></a> and <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>.  The easiest way to do this is to use <code>ReadBufferingStream</code> from the <a href="https://github.com/bblanchon/ArduinoStreamUtils">StreamUtils library</a>:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// replace the following line:</span>
<span class="hljs-built_in">deserializeJson</span>(doc, stream);

<span class="hljs-comment">// with these two lines:</span>
<span class="hljs-function">ReadBufferingStream <span class="hljs-title">bufferedStream</span><span class="hljs-params">(stream, <span class="hljs-number">64</span>)</span></span>;
<span class="hljs-built_in">deserializeJson</span>(doc, bufferedStream);
</code></pre>
<p>Thanks to <code>ReadBufferingStream</code>, the program will read the input in chunks of 64 bytes, which will be much faster.</p>
<p>Did this solve your issue?</p>
`},"deserialization/noerror/truncatedstrings/http-httpclient-stream":{choices:[{id:"success",label:"Yes",summary:"Calling <code>HTTPClient::useHTTP10(true)</code> solves the issue",next:"done"},{id:"http10",label:"No",summary:"Calling <code>HTTPClient::useHTTP10(true)</code> doesn't solve the issue",next:"deadend"}],content:`<p>When you call <code>HTTPClient::getStream()</code>, you bypass the part that handles <a href="https://en.wikipedia.org/wiki/Chunked_transfer_encoding">Chunked Transfer Encoding</a>.
As a result, the response's body may be split in multiple chunks, making some strings look like they are truncated (in reality, they contain a line-break).</p>
<p>The most straightforward workaround is to downgrade the connection to HTTP 1.0.
To do this, add the following line <strong>before</strong> sending the request:</p>
<pre><code class="hljs language-c++">http.<span class="hljs-built_in">useHTTP10</span>(<span class="hljs-literal">true</span>);  <span class="hljs-comment">// avoid Chunked Transfer Encoding</span>
</code></pre>
<p>Did this solve your problem?</p>
`},"deserialization/noerror/truncatedstrings/http-httpclient":{choices:[{id:"getstream",label:"<code>getStream()</code>",summary:"Response comes from <code>HTTPClient::getStream()</code>",next:"deserialization/noerror/truncatedstrings/http-httpclient-stream"},{id:"getstring",label:"<code>getString()</code>",summary:"Response comes from <code>HTTPClient::getString()</code>",next:"deadend"}],content:`<p>Which function of <code>HTTPClient</code> do you call to get the response?</p>
`},"deserialization/noerror/truncatedstrings/http-manual":{choices:[{id:"success",label:"Yes",summary:"Using <code>HTTP/1.0</code> solves the issue",next:"done"},{id:"http10",label:"No",summary:"Using <code>HTTP/1.0</code> doesn't solve the issue",next:"deadend"}],content:`<p>When you use HTTP 1.1, the server can send the response with <a href="https://en.wikipedia.org/wiki/Chunked_transfer_encoding">Chunked Transfer Encoding</a>.
As a result, the response's body may be split in multiple chunks, making some strings look like they are truncated (in reality, they contain a line-break).</p>
<p>The most straightforward workaround is to downgrade the connection to HTTP 1.0.
To do this, replace <code>HTTP/1.1</code> with <code>HTTP/1.0</code> in the first line of the request.</p>
<p>Did this solve your problem?</p>
`},"deserialization/noerror/truncatedstrings/http-otherlib":{needs_assistance:!0,choices:[],content:`<p>Unfortunately, I only know how to use the following HTTP libraries:</p>
<ul>
<li><a href="https://github.com/esp8266/Arduino/tree/master/libraries/ESP8266HTTPClient">ESP8266HTTPClient</a></li>
<li><a href="https://github.com/espressif/arduino-esp32/tree/master/libraries/HTTPClient">HTTPClient for ESP32</a></li>
</ul>
`},"deserialization/noerror/truncatedstrings/http":{choices:[{id:"httpclient",label:"Yes, I'm using <code>HTTPClient</code>",summary:"Program uses <code>HTTPClient</code>",next:"deserialization/noerror/truncatedstrings/http-httpclient"},{id:"http-library",label:"Yes, but it's not <code>HTTPClient</code>",summary:"Program uses an unknown HTTP library",next:"deserialization/noerror/truncatedstrings/http-otherlib"},{id:"no-library",label:"No",summary:"Program doesn't use an HTTP library",next:"deserialization/noerror/truncatedstrings/http-manual"}],content:`<p>Do you use an HTTP library?</p>
`},"deserialization/noerror/truncatedstrings/input":{choices:[{id:"http",label:"Yes",summary:"The input comes from an HTTP response",next:"deserialization/noerror/truncatedstrings/http"},{id:"no-nul",label:"No",summary:"The input doesn't come from an HTTP response",next:"deadend"}],content:`<p>Does the JSON input come from an HTTP response?</p>
`},"deserialization/noerror/truncatedstrings":{choices:[{id:"nul",label:"Yes",summary:"The input contains <code>\\u0000</code>",next:"nul-not-supported"},{id:"no-nul",label:"No",summary:"The input doesn't contain <code>\\u0000</code>",next:"deserialization/noerror/truncatedstrings/input"}],content:`<p>Does the JSON input contain the Unicode character <code>\\u0000</code>?</p>
`},"deserialization/noerror/unacceptable":{needs_assistance:!0,choices:[],content:`<p><a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> accepted a JSON document that was clearly invalid.<br>
You need to talk to a human about that.</p>
`},"deserialization/noerror":{choices:[{id:"slow",label:"It's slow",summary:"Deserialisation is slow",next:"deserialization/noerror/slow"},{id:"invalid",label:"I expected <code>InvalidInput</code> instead of <code>Ok</code>",summary:"<code>Ok</code> is incorrect; it should be <code>InvalidInput</code>",next:"deserialization/noerror/firstchar"},{id:"crash",label:"My program crashes after calling <code>deserializeJson()</code>",summary:"Program crashes after calling <code>deserializeJson()</code>",next:"deserialization/crash/after"},{id:"empty",label:"<code>JsonDocument</code> returns empty/null values",summary:"Program fails to extract values from the <code>JsonDocument</code>",next:"deserialization/noerror/novalue"},{id:"garbage",label:"<code>JsonDocument</code> returns garbage",summary:"<code>JsonDocument</code> returns garbage",next:"deserialization/noerror/garbage"},{id:"changing-strings",label:"The strings in the <code>JsonDocument</code> change for no reason",summary:"The strings in the <code>JsonDocument</code> change for no reason",next:"deserialization/noerror/garbage"},{id:"altered",label:"<code>deserializeJson()</code> alters the content of the input",summary:"Input buffer is modified",next:"deserialization/noerror/input-modified"},{id:"truncated-strings",label:"Some strings are truncated",summary:"Some strings are truncated",next:"deserialization/noerror/truncatedstrings"}],content:`<p>So, what's the problem then?</p>
`},"deserialization/nomemory/assistant-settings":{choices:[{id:"success",label:"Yes",summary:"Changing the Assistant settings solves the issue",next:"done"},{id:"failure",label:"No",summary:"Changing the Assistant settings doesn't solve the issue",next:"deadend"}],content:`<p>I'm pretty confident the Assistant computes the right capacity; however, some settings can affect the result:</p>
<ol>
<li><em>Input type</em>, in step 1, should match the type of the argument passed to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a></li>
<li><em>Deduplicate values when measuring the capacity</em>, in step 3, should be unchecked, most likely.</li>
<li><em>Deduplicate keys when measuring the capacity</em>, in step 3, it should be unchecked if the keys are dynamic or if you used placeholders.</li>
</ol>
<p>Please verify that these settings are correct.</p>
<p>Did this solve your issue?</p>
`},"deserialization/nomemory/document-basic-capacity-0-global":{choices:[{id:"success",label:"Yes",summary:"Making <code>doc</code> local fixes the issue.",next:"done"},{id:"failure",label:"No",summary:"Making <code>doc</code> local doesn't fix the issue.",next:"deadend"}],content:`<p><code>doc.capacity()</code> returns <code>0</code>, which means <code>allocate()</code> returned <code>NULL</code>.</p>
<p>I think that, because <code>doc</code> is declared global, <code>allocate()</code> is called too early and the heap isn't ready.
I know this happens on ESP32 with the <code>SpiRamJsonDocument</code> shown in <a href="/v6/how-to/use-external-ram-on-esp32/">How to use external RAM on ESP32?</a>.</p>
<p>Please try to make <code>doc</code> a local variable.</p>
<p>Did this solve your issue?</p>
`},"deserialization/nomemory/document-basic-capacity-0":{choices:[{id:"global",label:"Yes",summary:"<code>doc</code> is a global variable",next:"deserialization/nomemory/document-basic-capacity-0-global"},{id:"local",label:"No",summary:"<code>doc</code> is not a global variable",next:"deserialization/nomemory/assistant-settings"}],content:`<p>Is <code>doc</code> a global variable?</p>
`},"deserialization/nomemory/document-basic":{choices:[{id:"capacity-0",label:"<code>0</code>",summary:"<code>doc.capacity()</code> returns <code>0</code>",next:"deserialization/nomemory/document-basic-capacity-0"},{id:"capacity-ok",label:"The value passed to the constructor of <code>DynamicJsonDocument</code>",summary:"<code>doc.capacity()</code> returns the right value",next:"deserialization/nomemory/assistant-settings"}],content:`<p>Let's verify that the memory allocation succeeded.</p>
<p>Please print the capacity of the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> to the serial port, like so:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">println</span>(doc.<span class="hljs-built_in">capacity</span>());
</code></pre>
<p>What value does it show?</p>
`},"deserialization/nomemory/document-dynamic-toobig":{choices:[{id:"success",label:"Yes",summary:"Reducing memory usage fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Reducing memory usage doesn't fix the issue",next:"deadend"}],content:`<p>Well, that's very bad news. It seems that your microcontroller doesn't have enough RAM to hold the JSON document.</p>
<p>Please read <a href="/v6/how-to/reduce-memory-usage/">How to reduce memory usage</a>.
It shows several techniques you can use to use less RAM.
Hopefully, you'll make enough room for the memory pool.</p>
<p>Did this solve your issue?</p>
`},"deserialization/nomemory/document-dynamic":{choices:[{id:"capacity-0",label:"<code>0</code>",summary:"<code>doc.capacity()</code> returns <code>0</code>",next:"deserialization/nomemory/document-dynamic-toobig"},{id:"capacity-ok",label:"The value passed to the constructor of <code>DynamicJsonDocument</code>",summary:"<code>doc.capacity()</code> returns the right value",next:"deserialization/nomemory/assistant-settings"}],content:`<p>It's possible that the <a href="/v6/api/dynamicjsondocument/"><code>DynamicJsonDocument</code></a> failed to allocate the memory pool.</p>
<p>Please print the capacity of the <a href="/v6/api/dynamicjsondocument/"><code>DynamicJsonDocument</code></a> to the serial port, like so:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">println</span>(doc.<span class="hljs-built_in">capacity</span>());
</code></pre>
<p>What value does it show?</p>
`},"deserialization/nomemory/document":{choices:[{id:"basic",label:"<code>BasicJsonDocument&lt;T&gt;</code>",summary:"The document is a <code>BasicJsonDocument&lt;T&gt;</code>",next:"deserialization/nomemory/document-basic"},{id:"dynamicjsondocument",label:"<code>DynamicJsonDocument</code>",summary:"The <code>JsonDocument</code> is a <code>DynamicJsonDocument</code>",next:"deserialization/nomemory/document-dynamic"},{id:"staticjsondocument",label:"<code>StaticJsonDocument</code>",summary:"The <code>JsonDocument</code> is a <code>StaticJsonDocument</code>",next:"deserialization/nomemory/assistant-settings"}],content:`<p>What kind of <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> are you using?</p>
`},"deserialization/nomemory":{choices:[{id:"success",label:"Yes",summary:"Increasing the capacity of the <code>JsonDocument</code> fixes the issue",next:"done"},{id:"increased-capacity",label:"No",summary:"Increasing the capacity of the <code>JsonDocument</code> doesn't fix the issue",next:"deserialization/nomemory/document"}],content:`<p><a href="/v6/api/misc/deserializationerror/#nomemory"><code>NoMemory</code></a> means that the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> is too small to hold the entire document.</p>
<p>Please use the <a href="/v6/assistant/">ArduinoJson Assistant</a> to compute the right capacity for your document.
Use the recommended capacity when creating <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> and retry.</p>
<p>Did this solve your issue?</p>
`},"deserialization/notsupported/msgpack":{choices:[{id:"success",label:"Yes",summary:"Upgrading solve the issue",next:"done"},{id:"upgraded",label:"No",summary:"Upgrading doesn't solve the issue",next:"deadend"}],content:`<p>So, I guess that you're not using <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> but <a href="/v6/api/msgpack/deserializemsgpack/"><code>deserializeMsgPack()</code></a>, right?</p>
<p>Indeed, before <a href="/news/2021/05/04/version-6-18-0/">ArduinoJson 6.18</a>, <a href="/v6/api/msgpack/deserializemsgpack/"><code>deserializeMsgPack()</code></a> returned <a href="/v6/api/misc/deserializationerror/#notsupported"><code>NotSupported</code></a> as soon as the input contains an unsupported MsgPack value type:</p>
<ul>
<li>a binary value</li>
<li>an extension value</li>
<li>an object key that is not a string</li>
</ul>
<p>If that's your case, then you must upgrade the library. Instead of returning an error, newer versions simply ignore the unsupported values and replace them with nulls.</p>
<p>Did this solve your issue?</p>
`},"deserialization/notsupported/unicode":{choices:[{id:"success",label:"Yes",summary:"Defining <code>ARDUINOJSON_DECODE_UNICODE</code> solves the issue",next:"done"},{id:"decode-unicode-set",label:"No",summary:"Defining <code>ARDUINOJSON_DECODE_UNICODE</code> doesn't solve the issue",next:"deserialization/notsupported/msgpack"}],content:`<p><a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> returns <a href="/v6/api/misc/deserializationerror/#notsupported"><code>NotSupported</code></a> when is read an UTF-16 escape sequence but support is disabled.</p>
<p>To fix this issue, you must enable support UTF-16 escape sequences by defining <a href="/v6/api/config/decode_unicode/"><code>ARDUINOJSON_DECODE_UNICODE</code></a> to <code>1</code> before including <code>ArduinoJson.h</code>, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-meta">#<span class="hljs-keyword">define</span> ARDUINOJSON_DECODE_UNICODE 1</span>
<span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;ArduinoJson.h&gt;</span></span>
</code></pre>
<p>Did this solve your issue?</p>
`},"deserialization/notsupported/upgrade-duplicates":{choices:[{id:"success",label:"Yes",summary:"Removing duplicates fixes the issue",next:"done"},{id:"duplicates-removed",label:"No",summary:"Removing duplicates doesn't fix the issue",next:"deserialization/notsupported/unicode"}],content:`<p>If you're still seeing <code>NotSupported</code> after upgrading the library, it means that your code is not using the upgraded library.</p>
<p>Several versions of ArduinoJson are installed on your machine; you must either:</p>
<ul>
<li>removed the duplicates</li>
<li>upgrade the duplicates</li>
<li>make sure your project uses the right one</li>
</ul>
<p>To find the duplicates, run a disk search for a file named <code>ArduinoJson.h</code>.</p>
<p>Did this solve your issue?</p>
`},"deserialization/notsupported/upgrade":{choices:[{id:"success",label:"Yes",summary:"Upgrading solves the issue",next:"done"},{id:"updraded",label:"No",summary:"Upgrading doesn't solves the issue",next:"deserialization/notsupported/upgrade-duplicates"}],content:`<p>Please upgrade ArduinoJson to the latest version.<br>
You may need to consult the <a href="https://github.com/bblanchon/ArduinoJson/blob/6.x/CHANGELOG.md">change log</a> to make sure there aren't any breaking changes that could break your existing code.</p>
<p>Did this solve your issue?</p>
`},"deserialization/notsupported":{choices:[{id:"can-uprade",label:"Yes",summary:"Library can be upgraded",next:"deserialization/notsupported/upgrade"},{id:"cannot-upgrade",label:"No",summary:"Library cannot be upgraded",next:"deserialization/notsupported/unicode"}],content:`<p><a href="/v6/api/misc/deserializationerror/#notsupported"><code>NotSupported</code></a> means that the document was valid but contained features not supported by the library.</p>
<p>This error code was removed in <a href="/news/2021/05/04/version-6-18-0/">ArduinoJson 6.18</a>, so you should not see it anymore.</p>
<p>Can you upgrade the library?</p>
`},"deserialization/toodeep":{choices:[{id:"success",label:"Yes",summary:"Increasing the limit fixes the issue",next:"done"},{id:"limit-increased",label:"No",summary:"Increasing the limit doesn't fix the issue",next:"deadend"}],content:`<p><a href="/v6/api/misc/deserializationerror/#toodeep"><code>TooDeep</code></a> means that the input document has too many levels of nesting.</p>
<p><a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> is implemented with a recursive function: it's fast, small, and efficient, but it can cause a stack-overflow if the input document has too many levels. To protect your program against malicious input that could trigger a stack-overflow on purpose, ArduinoJson sets a limit on how many levels it accepts.</p>
<p>The default nesting limit is configured by <a href="/v6/api/config/default_nesting_limit/"><code>ARDUINOJSON_DEFAULT_NESTING_LIMIT</code></a>, which is set to <code>10</code> by default. To increase the limit, you can change this setting, or you can pass an extra argument to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-built_in">deserializeJson</span>(doc, input, DeserializationOption::<span class="hljs-built_in">NestingLimit</span>(<span class="hljs-number">20</span>));
</code></pre>
<p>The <a href="/v6/assistant/">ArduinoJson Assistant</a> shows an alert when your input overpasses the default limit and includes the appropriate <code>DeserializationOption::NestingLimit</code> in the sample program.</p>
<p>Did this solve your issue?</p>
`},deserialization:{choices:[{id:"ok",label:"<code>Ok</code>",next:"deserialization/noerror",summary:"<code>deserializeJson()</code> returns <code>Ok</code>"},{id:"emptyinput",label:"<code>EmptyInput</code>",next:"deserialization/emptyinput",summary:"<code>deserializeJson()</code> returns <code>EmptyInput</code>"},{id:"incompleteinput",label:"<code>IncompleteInput</code>",next:"deserialization/incompleteinput",summary:"<code>deserializeJson()</code> returns <code>IncompleteInput</code>"},{id:"invalidinput",label:"<code>InvalidInput</code>",next:"deserialization/invalidinput",summary:"<code>deserializeJson()</code> returns <code>InvalidInput</code>"},{id:"nomemory",label:"<code>NoMemory</code>",next:"deserialization/nomemory",summary:"<code>deserializeJson()</code> returns <code>NoMemory</code>"},{id:"notsupported",label:"<code>NotSupported</code>",next:"deserialization/notsupported",summary:"<code>deserializeJson()</code> returns <code>NotSupported</code>"},{id:"toodeep",label:"<code>TooDeep</code>",next:"deserialization/toodeep",summary:"<code>deserializeJson()</code> returns <code>TooDeep</code>"},{id:"crash",label:"I can't tell because the program crashes",next:"deserialization/crash",summary:"The program crashes"},{id:"unknown",label:"I don't know what you're talking about",next:"deserialization/check-error",summary:"The program doesn't check the error"}],content:`<p>What is the value of <a href="/v6/api/misc/deserializationerror/"><code>DeserializationError</code></a>?</p>
`},done:{choices:[],content:`<p class="display-4">🙋🏻‍♂️</p>
<p>Awesome! I'm glad I could help.</p>
<p>I would be very grateful if, in return, you could <a href="https://github.com/bblanchon/ArduinoJson/stargazers">star ArduinoJson on GitHub</a>. ❤</p>
<p>If you want to support the project (and learn a lot of stuff as well), you can purchase <a href="/book/">Mastering ArduinoJson</a>.<br>
Alternatively, you can <a href="https://github.com/sponsors/bblanchon">sponsor the project on GitHub</a>.</p>
<p>Should you have any idea on how I could improve myself, please <a href="https://github.com/bblanchon/ArduinoJson/issues/new">open an issue on GitHub</a>.</p>
<p>Bye!</p>
<p>--<br>
<em>ArduinoJson Troubleshooter</em></p>
`},intro:{choices:[],content:`<p>Hi!</p>
<p>I'm the <em>ArduinoJson Troubleshooter</em>, and I'm here to help you fix your problem.<br>
I'll ask you a series of questions and give you some instructions along the way.</p>
<p>Ready? Here we go!</p>
`},"nul-not-supported":{choices:[{id:"success",label:"Yes",summary:"Using <code>JsonString</code> solves the issue",next:"done"},{id:"failure",label:"No",summary:"Using <code>JsonString</code> doesn't solves the issue",next:"deadend"}],content:`<p>First, ArduinoJson only supports NUL characters since <a href="/news/2022/01/08/arduinojson-6-19-0/">version 6.19</a>, so make sure you are up-to-date.</p>
<p>Next, the Arduino <a href="https://www.arduino.cc/reference/en/language/variables/data-types/stringobject/"><code>String</code></a> class doesn't support NUL characters, so you have to use another string type, like <a href="/v6/api/jsonstring/"><code>JsonString</code></a>, like this:</p>
<pre><code class="hljs language-c++">JsonString myString = doc[<span class="hljs-string">&quot;myString&quot;</span>];
<span class="hljs-type">const</span> <span class="hljs-type">char</span>* myStringData = myString.<span class="hljs-built_in">c_str</span>();
<span class="hljs-type">size_t</span> myStringSize = myString.<span class="hljs-built_in">size</span>();
</code></pre>
<p>Did this solve your issue?</p>
`},runtime:{choices:[{id:"serialization",label:"Serialization",next:"serialization",summary:"The issue concerns serialization"},{id:"deserialization",label:"Deserialization",next:"deserialization",summary:"The issue concerns deserialization"}],content:`<p>Does your issue concern serialization or deserialization?</p>
`},"serialization/crash/charptr-comment":{choices:[{id:"crash-with-literals",label:"Yes",summary:"Replacing string pointers with literals doesn't fix the crash",next:"serialization/crash/output-type"},{id:"no-crash-with-literals",label:"No",summary:"Replacing string pointers with literals fixes the crash",next:"serialization/crash/charptr-print"}],content:`<p>Please replace all those string pointers with literals.</p>
<p>For example, replace:</p>
<pre><code class="hljs language-c++">doc[<span class="hljs-string">&quot;name&quot;</span>] = name;
</code></pre>
<p>with:</p>
<pre><code class="hljs language-c++">doc[<span class="hljs-string">&quot;name&quot;</span>] = <span class="hljs-string">&quot;name&quot;</span>;
</code></pre>
<p>Does your program still crash?</p>
`},"serialization/crash/charptr-print":{choices:[{id:"success",label:"Yes",summary:"Printing the string reveals the faulty pointer",next:"done"},{id:"no-faulty-pointer",label:"No",summary:"Printing the string doesn't show any faulty pointer",next:"deadend"}],content:`<p>One of these string pointers is probably dangling or points to a non-zero-terminated string.</p>
<p>Please print them all to the serial port to find the faulty pointer.</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">print</span>(name);
doc[<span class="hljs-string">&quot;name&quot;</span>] = <span class="hljs-string">&quot;name&quot;</span>;
</code></pre>
<p>Can you locate the faulty string in the output?</p>
`},"serialization/crash/charptr":{choices:[{id:"charptr",label:"Yes",summary:"Program inserts string pointers into the <code>JsonDocument</code>",next:"serialization/crash/charptr-comment"},{id:"no-charptr",label:"No",summary:"Program doesn't insert string pointers into the <code>JsonDocument</code>",next:"serialization/crash/output-type"}],content:`<p>Does your program insert string pointers in the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>?<br>
(for example, <code>doc[&quot;name&quot;] = name</code> where <code>name</code> is a <code>const char*</code>)</p>
`},"serialization/crash/output-char-array":{choices:[{id:"success",label:"Yes",summary:"Switching to <code>String</code> solves the issue",next:"done"},{id:"failure",label:"No",summary:"Switching to <code>String</code> doesn't solve the issue",next:"deadend"}],content:`<p>I think this output buffer could cause a <a href="https://en.wikipedia.org/wiki/Stack_buffer_overflow">stack overflow</a>.</p>
<p>Please try with a <a href="https://www.arduino.cc/reference/en/language/variables/data-types/stringobject/"><code>String</code></a> instead of a <code>char[]</code>.</p>
<p>Did this solve your issue?</p>
`},"serialization/crash/output-type":{choices:[{id:"char-array",label:"<code>char[]</code>",summary:"Output type is <code>char[]</code>",next:"serialization/crash/output-char-array"},{id:"char-ptr",label:"<code>char*</code>",summary:"Output type is <code>char*</code>",next:"deadend"},{id:"arduino-string",label:"<code>String</code>",summary:"Output type is <code>String</code>",next:"deadend"},{id:"arduino-stream",label:"<code>Stream</code>",summary:"Output type is <code>Stream</code>",next:"deadend"}],content:`<p>What is the type of the second argument passed to <a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a>?</p>
`},"serialization/crash/progmem-cast":{choices:[{id:"success",label:"Yes",summary:"Casting the pointer solves the issue",next:"done"},{id:"failure",label:"No",summary:"Casting the pointer doesn't solve the issue",next:"deadend"}],content:`<p>When you use <code>PROGMEM</code> with ArduinoJson (and any other Arduino code, really), you must make sure that the addresses in Flash memory aren't interpreted as addresses in RAM.</p>
<p>Indeed, <a href="https://en.wikipedia.org/wiki/Harvard_architecture">Harvard architectures</a> (like <a href="https://en.wikipedia.org/wiki/AVR_microcontrollers">AVR</a> and <a href="https://en.wikipedia.org/wiki/ESP8266">ESP8266</a>) use different addresses spaces for RAM and Flash. This means that the same address can refer to either Flash or RAM, so the CPU has no way to tell that some pointers target RAM and some others target Flash. That's why the program needs to treat the two areas separately.</p>
<p>Getting back to your program, you must make sure that ArduinoJson can clearly identify Flash strings as such. The <code>PROGMEM</code> attribute itself isn't sufficient because it doesn't alter the C++ type of the string. That's why ArduinoJson needs you to pass a pointer of type <code>const __FlashStringHelper*</code> when you refer to a Flash string.</p>
<p>For example, the following program is incorrect and very likely to crash:</p>
<pre><code class="hljs language-c++"><span class="hljs-type">const</span> <span class="hljs-type">char</span> PROGMEM key[] = <span class="hljs-string">&quot;the answer&quot;</span>;

doc[key] = <span class="hljs-number">42</span>; <span class="hljs-comment">// &lt;- BOOOOOM!!!!</span>
</code></pre>
<p>Because, it saw a string of type <code>const char*</code>, ArduinoJson assumed the pointer was refering to the RAM address space.
To fix this issue, you must cast the pointer to \`\`const __FlashStringHelper*\`, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-type">const</span> <span class="hljs-type">char</span> PROGMEM key_data[] = <span class="hljs-string">&quot;the answer&quot;</span>;
<span class="hljs-type">const</span> __FlashStringHelper* key = (<span class="hljs-type">const</span> __FlashStringHelper*)key_data;

doc[key] = <span class="hljs-number">42</span>;
</code></pre>
<p>As you can see, this is quite ugly. Fortunately, Arduino provides the F(), which declares the Flash string and return the appropriate type:</p>
<pre><code class="hljs language-c++">doc[<span class="hljs-built_in">F</span>(<span class="hljs-string">&quot;the answer&quot;</span>)] = <span class="hljs-number">42</span>;
</code></pre>
<p>There is one drawback with the <code>F()</code>, though: it doesn't perform <a href="https://cpp4arduino.com/2018/10/23/what-is-string-interning.html">string interning</a>.
This means that every call to the macro creates a new string in the program memory, even if the same string was already present.
So, be careful with this macro; otherwise, you'll end up with multiple copies of the same string, and the program will be bigger than it should.
If you need to use the Flash string in several places, you better define a variable, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-keyword">auto</span> key = <span class="hljs-built_in">F</span>(<span class="hljs-string">&quot;the answer&quot;</span>);

doc[key] = <span class="hljs-number">42</span>;
</code></pre>
<p>Did this solve your issue?</p>
`},"serialization/crash/progmem":{choices:[{id:"progmem",label:"Yes",summary:"Program uses <code>PROGMEM</code>",next:"serialization/crash/progmem-cast"},{id:"no-progmem",label:"No",summary:"Program doesn't use <code>PROGMEM</code>",next:"serialization/crash/charptr"}],content:`<p>Do you use <code>PROGMEM</code> (Flash memory) in your program?</p>
`},"serialization/crash/static":{choices:[{id:"success",label:"Yes",summary:"Switching to <code>DynamicJsonDocument</code> fixes the issue",next:"done"},{id:"dynamicjsondocument-tried",label:"No",summary:"Switching to <code>DynamicJsonDocument</code> doesn't fix the issue",next:"serialization/crash/progmem"}],content:`<p>Because a <a href="/v6/api/staticjsondocument/"><code>StaticJsonDocument</code></a> resides on the stack, it may cause a <a href="https://en.wikipedia.org/wiki/Stack_buffer_overflow">stack-overflow</a>.</p>
<p>Some platforms can detect stack overflows, in which case they raise an exception; others let the program do nothing and let the program crash on its own.
The tricky part with stack overflows is that the program doesn't always crash; it can also expose all kind of weird behavior.</p>
<p>If you have no idea of what I'm talking about, please have a look a &quot;The missing C++ course&quot; in <a href="/book/">Mastering ArduioJson</a>, it explains the roles of the different areas of memory, as well as many other important stuffs.</p>
<p>Please switch to a <a href="/v6/api/dynamicjsondocument/"><code>DynamicJsonDocument</code></a></p>
<p>Did this solve your issue?</p>
`},"serialization/crash":{choices:[{id:"dynamicjsondocument",label:"<code>DynamicJsonDocument</code>",summary:"Program uses <code>DynamicJsonDocument</code>",next:"serialization/crash/progmem"},{id:"staticjsondocument",label:"<code>StaticJsonDocument</code>",summary:"Program uses <code>StaticJsonDocument</code>",next:"serialization/crash/static"}],content:`<p>Do you use <a href="/v6/api/dynamicjsondocument/"><code>DynamicJsonDocument</code></a> or <a href="/v6/api/staticjsondocument/"><code>StaticJsonDocument</code></a>?</p>
`},"serialization/empty/capacity-0-basic-global":{choices:[{id:"success",label:"Yes",summary:"Making <code>doc</code> local fixes the issue.",next:"done"},{id:"failure",label:"No",summary:"Making <code>doc</code> local doesn't fix the issue.",next:"deadend"}],content:`<p><code>doc.capacity()</code> returns <code>0</code>, which means <code>allocate()</code> returned <code>NULL</code>.</p>
<p>I think that, because <code>doc</code> is declared global, <code>allocate()</code> is called too early and the heap isn't ready.
I know this happens on ESP32 with the <code>SpiRamJsonDocument</code> shown in <a href="/v6/how-to/use-external-ram-on-esp32/">How to use external RAM on ESP32?</a>.</p>
<p>Please try to make <code>doc</code> a local variable.</p>
<p>Did this solve your issue?</p>
`},"serialization/empty/capacity-0-basic":{choices:[{id:"global",label:"Yes",summary:"<code>doc</code> is a global variable",next:"serialization/empty/capacity-0-basic-global"},{id:"local",label:"No",summary:"<code>doc</code> is not a global variable",next:"serialization/empty/capacity-0-oom"}],content:`<p>Is <code>doc</code> a global variable?</p>
`},"serialization/empty/capacity-0-oom":{choices:[{id:"success",label:"Yes",summary:"Reducing memory usage fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Reducing memory usage doesn't fix the issue",next:"deadend"}],content:`<p>Well, that's very bad news. It seems that your microcontroller doesn't have enough RAM to hold the JSON document.</p>
<p>Please read <a href="/v6/how-to/reduce-memory-usage/">How to reduce memory usage</a>.
It shows several techniques you can use to use less RAM.
Hopefully, you'll make enough room for the memory pool.</p>
<p>Did this solve your issue?</p>
`},"serialization/empty/capacity-0":{choices:[{id:"basic",label:"<code>BasicJsonDocument&lt;T&gt;</code>",summary:"The document is a <code>BasicJsonDocument&lt;T&gt;</code>",next:"serialization/empty/capacity-0-basic"},{id:"dynamic",label:"<code>DynamicJsonDocument</code>",summary:"The document is a <code>DynamicJsonDocument</code>",next:"serialization/empty/capacity-0-oom"},{id:"static",label:"<code>StaticJsonDocument</code>",summary:"The document is a <code>StaticJsonDocument</code>",next:"deadend"}],content:`<p>What kind of <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> do you use?</p>
`},"serialization/empty":{choices:[{id:"capacity-0",label:"<code>0</code>",summary:"<code>doc.capacity()</code> returns <code>0</code>",next:"serialization/empty/capacity-0"},{id:"capacity-ok",label:"a positive integer",summary:"<code>doc.capacity()</code> looks good",next:"deadend"}],content:`<p>Please print the capacity of the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> to the serial port, like so:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">println</span>(doc.<span class="hljs-built_in">capacity</span>());
</code></pre>
<p>What value does it show?</p>
`},"serialization/emptystrings/deserialize-zerocopy":{choices:[{id:"success",label:"Yes",summary:"Casting the pointer fixes the issue",next:"done"},{id:"casting-fails",label:"No",summary:"Casting the pointer doesn't fix the issue",next:"deadend"}],content:`<p>When you pass a writeable buffer as the input of <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>, ArduinoJson uses the <strong>zero-copy mode</strong>. Instead of copying the strings from the input into the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, it stores pointers to the input buffer.
Therefore, the input buffer must stay alive during the whole lifetime of the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>; otherwise, all the strings in the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> will be dangling pointers.
Usually, this produces grabage in the output, but it can sometimes produce empty strings.</p>
<p>The easiest solution is to disable the zero-copy mode by passing a read-only input buffer. Casting the pointer will do the trick:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// replace this:</span>
<span class="hljs-built_in">deserializeJson</span>(doc, input, inputSize);

<span class="hljs-comment">// ...with this:</span>
<span class="hljs-built_in">deserializeJson</span>(doc, (<span class="hljs-type">const</span> <span class="hljs-type">char</span>*)input, inputSize);
</code></pre>
<p>Now that the zero-copy mode is disabled, you probably need a larger <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, so don't forget to update the capacity. As usual, use the <a href="/v6/assistant/">ArduinoJson Assistant</a> to compute the right capacity for your project.</p>
<p>Did this solve your issue?</p>
`},"serialization/emptystrings/deserialize":{choices:[{id:"char-ptr",label:"<code>char*</code> (or <code>char[]</code>)",summary:"Input type is <code>char*</code>",next:"serialization/emptystrings/deserialize-zerocopy"},{id:"const-char-ptr",label:"<code>const char*</code>",summary:"Input type is <code>const char*</code>",next:"deadend"},{id:"string",label:"<code>String</code> (or <code>std::string</code>)",summary:"Input type is <code>String</code>",next:"deadend"},{id:"stream",label:"<code>Stream</code> (or <code>std::istream)</code>",summary:"Input type is <code>Stream</code>",next:"deadend"}],content:`<p>What's the type of the second argument passed to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>?</p>
`},"serialization/emptystrings":{choices:[{id:"deserialize-yes",label:"Yes",summary:"Program calls <code>deserializeJson()</code>",next:"serialization/emptystrings/deserialize"},{id:"deserialize-no",label:"No",summary:"Program doesn't call <code>deserializeJson()</code>",next:"deadend"}],content:`<p>Do you call <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> before calling <a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a>? (with the same <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>)</p>
`},"serialization/floats/double-rounded":{choices:[{id:"success",label:"Yes",summary:"Defining <code>ARDUINOJSON_USE_DOUBLE</code> to <code>0</code> fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Defining <code>ARDUINOJSON_USE_DOUBLE</code> to <code>0</code> doesn't fix the issue",next:"deadend"}],content:`<p>I'm surprised that the previous solution didn't work.</p>
<p>We can try to change ArduinoJson's storage type to see if it solves your issue.</p>
<p>Please define <a href="/v6/api/config/use_double/">ARDUINOJSON_USE_DOUBLE</a> to <code>0</code> before including <code>ArduinoJson.h</code>, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-meta">#<span class="hljs-keyword">define</span> ARDUINOJSON_USE_DOUBLE 0</span>
<span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;ArduinoJson.h&gt;</span></span>
</code></pre>
<p>Did this solve your issue?</p>
`},"serialization/floats/double":{choices:[{id:"success",label:"Yes",summary:"Rounding the value fixes the issue",next:"done"},{id:"double",label:"No",summary:"Rounding the value doesn't fix the issue",next:"serialization/floats/double-rounded"}],content:`<p>Please add the following function somewhere in your program:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// rounds a number to 2 decimal places</span>
<span class="hljs-comment">// example: round(3.14159) -&gt; 3.14</span>
<span class="hljs-function"><span class="hljs-type">double</span> <span class="hljs-title">round2</span><span class="hljs-params">(<span class="hljs-type">double</span> value)</span> </span>{
   <span class="hljs-keyword">return</span> (<span class="hljs-type">int</span>)(value * <span class="hljs-number">100</span> + <span class="hljs-number">0.5</span>) / <span class="hljs-number">100.0</span>;
}
</code></pre>
<p>Now, call this function when inserting the value in the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>; for example:</p>
<pre><code class="hljs language-c++">doc[<span class="hljs-string">&quot;value&quot;</span>] = <span class="hljs-built_in">round2</span>(value);
</code></pre>
<p>Did this solve your issue?</p>
`},"serialization/floats":{choices:[{id:"success",label:"Yes",summary:"Replacing <code>float</code> with <code>double</code> fixes the issue",next:"done"},{id:"double",label:"No",summary:"Replacing <code>float</code> with <code>double</code> doesn't fix the issue",next:"serialization/floats/double"}],content:`<p>Supposing that the JSON output looks like this:</p>
<pre><code class="hljs language-json"><span class="hljs-punctuation">{</span><span class="hljs-attr">&quot;value&quot;</span><span class="hljs-punctuation">:</span><span class="hljs-number">23.79999924</span><span class="hljs-punctuation">}</span>
</code></pre>
<p>I'm assuming you expect it to look like that:</p>
<pre><code class="hljs language-json"><span class="hljs-punctuation">{</span><span class="hljs-attr">&quot;value&quot;</span><span class="hljs-punctuation">:</span><span class="hljs-number">24.8</span><span class="hljs-punctuation">}</span>
</code></pre>
<p>Please change the type of the variable from <code>float</code> to <code>double</code> and try again.</p>
<p>Did this solve your issue?</p>
`},"serialization/garbage/deserialize-zerocopy":{choices:[{id:"success",label:"Yes",summary:"Casting the pointer fixes the issue",next:"done"},{id:"casting-fails",label:"No",summary:"Casting the pointer doesn't fix the issue",next:"serialization/garbage/serialize"}],content:`<p>When you pass a writeable buffer as the input of <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>, ArduinoJson uses the <strong>zero-copy mode</strong>. Instead of copying the strings from the input into the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, it stores pointers to the input buffer.
Therefore, the input buffer must stay alive during the whole lifetime of the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>; otherwise, all the strings in the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> will be dangling pointers.</p>
<p>The easiest solution is to disable the zero-copy mode by passing a read-only input buffer. Casting the pointer will do the trick:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// replace this:</span>
<span class="hljs-built_in">deserializeJson</span>(doc, input, inputSize);

<span class="hljs-comment">// ...with this:</span>
<span class="hljs-built_in">deserializeJson</span>(doc, (<span class="hljs-type">const</span> <span class="hljs-type">char</span>*)input, inputSize);
</code></pre>
<p>Now that the zero-copy mode is disabled, you probably need a larger <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, so don't forget to update the capacity. As usual, use the <a href="/v6/assistant/">ArduinoJson Assistant</a> to compute the right capacity for your project.</p>
<p>Did this solve your issue?</p>
`},"serialization/garbage/deserialize":{choices:[{id:"char-ptr",label:"<code>char*</code> (or <code>char[]</code>)",summary:"Input type is <code>char*</code>",next:"serialization/garbage/deserialize-zerocopy"},{id:"const-char-ptr",label:"<code>const char*</code>",summary:"Input type is <code>const char*</code>",next:"serialization/garbage/serialize"},{id:"string",label:"<code>String</code> (or <code>std::string</code>)",summary:"Input type is <code>String</code>",next:"serialization/garbage/serialize"},{id:"stream",label:"<code>Stream</code> (or <code>std::istream)</code>",summary:"Input type is <code>Stream</code>",next:"serialization/garbage/serialize"}],content:`<p>What's the type of the second argument passed to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>?</p>
`},"serialization/garbage/document-dynamic-string":{choices:[{id:"success",label:"Yes",summary:"Removing <code>.c_str()</code> fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Removing <code>.c_str()</code> doesn't fix the issue",next:"deadend"}],content:`<p>You can insert <a href="https://www.arduino.cc/reference/en/language/variables/data-types/stringobject/"><code>String</code></a> objects in a <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, but make sure you don't store the result of <a href="https://www.arduino.cc/reference/en/language/variables/data-types/string/functions/c_str/"><code>String::c_str()</code></a>.</p>
<p>The pointer returned by <a href="https://www.arduino.cc/reference/en/language/variables/data-types/string/functions/c_str/"><code>String::c_str()</code></a> is valid only if you don't modify or destroy the string.
Indeed, this function returns the address of the internal buffer of the <a href="https://www.arduino.cc/reference/en/language/variables/data-types/stringobject/"><code>String</code></a> instance.
This buffer is freed by <a href="https://www.arduino.cc/reference/en/language/variables/data-types/stringobject/"><code>String</code></a>'s destructor and may be reallocated each time the  <a href="https://www.arduino.cc/reference/en/language/variables/data-types/stringobject/"><code>String</code></a> changes. It's crucial that you don't save the pointer returned of <a href="https://www.arduino.cc/reference/en/language/variables/data-types/string/functions/c_str/"><code>String::c_str()</code></a>; otherwise, the pointer may be dangling.</p>
<p>For example, the following lines create a dangling pointer:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// DON&#x27;T DO THAT!!!  💀</span>
doc[<span class="hljs-string">&quot;address&quot;</span>] = address.<span class="hljs-built_in">toString</span>().<span class="hljs-built_in">c_str</span>();
<span class="hljs-comment">// ...</span>
<span class="hljs-built_in">serializeJson</span>(doc, Serial);  <span class="hljs-comment">// &lt;- likely to produce garbage</span>
</code></pre>
<p>Here, <code>address.toString()</code> returns a temporary <a href="https://www.arduino.cc/reference/en/language/variables/data-types/stringobject/"><code>String</code></a> which is destructed after executing the first statement. When <a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a> runs, the pointer stored in the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> will be pointing to an invalid location. It may work by accident, but if the memory location is reused by another variable, it will print garbage to the serial port.</p>
<p>The solution is to duplicate string instead of saving a pointer.
Since ArduinoJson automatically duplicates <a href="https://www.arduino.cc/reference/en/language/variables/data-types/stringobject/"><code>String</code></a> instances, all you need to do is  removing the call to <a href="https://www.arduino.cc/reference/en/language/variables/data-types/string/functions/c_str/"><code>String::c_str()</code></a>, like so:</p>
<pre><code class="hljs language-c++">doc[<span class="hljs-string">&quot;address&quot;</span>] = address.<span class="hljs-built_in">toString</span>(); <span class="hljs-comment">// &lt;- duplicates</span>
<span class="hljs-comment">// ...</span>
<span class="hljs-built_in">serializeJson</span>(doc, Serial);
</code></pre>
<p>Now, ArduinoJson sees a <a href="https://www.arduino.cc/reference/en/language/variables/data-types/stringobject/"><code>String</code></a> and knows that it needs to make a copy of the string in the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>.</p>
<p>Did this solve your issue?</p>
`},"serialization/garbage/document-dynamic":{choices:[{id:"string",label:"Yes",summary:"Program uses <code>String</code>",next:"serialization/garbage/document-dynamic-string"},{id:"no-string",label:"No",summary:"Program doesn't use <code>String</code>",next:"deadend"}],content:`<p>Do you use <a href="https://www.arduino.cc/reference/en/language/variables/data-types/stringobject/"><code>String</code></a> in your program?</p>
`},"serialization/garbage/document-static":{choices:[{id:"success",label:"Yes",summary:"Switching to <code>DynamicJsonDocument</code> fixes the issue",next:"done"},{id:"dynamic",label:"No",summary:"Switching to <code>DynamicJsonDocument</code> doesn't fix the issue",next:"serialization/garbage/document-dynamic"}],content:`<p><a href="/v6/api/staticjsondocument/"><code>StaticJsonDocument</code></a> stores its memory pool in the stack.
A large memory pool can overflow the stack a produce all kinds of strange behavior.
In particural, a <a href="https://en.wikipedia.org/wiki/Stack_buffer_overflow">stack overflow</a> can cause garbage in <a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a>'s output.</p>
<p>Please switch to a <a href="/v6/api/dynamicjsondocument/"><code>DynamicJsonDocument</code></a>.</p>
<p>Did this solve your issue?</p>
`},"serialization/garbage/document":{choices:[{id:"dynamic",label:"<code>DynamicJsonDocument</code>",summary:"Program uses <code>DynamicJsonDocument</code>",next:"serialization/garbage/document-dynamic"},{id:"static",label:"<code>StaticJsonDocument</code>",summary:"Program uses <code>StaticJsonDocument</code>",next:"serialization/garbage/document-static"}],content:`<p>Which kind of <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> do you use?</p>
`},"serialization/garbage/json":{choices:[{id:"deserialize-yes",label:"Yes",summary:"Program calls <code>deserializeJson()</code>",next:"serialization/garbage/deserialize"},{id:"deserialize-no",label:"No",summary:"Program doesn't call <code>deserializeJson()</code>",next:"serialization/garbage/serialize"}],content:`<p>Do you call <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> before calling <a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a>? (with the same <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>)</p>
`},"serialization/garbage/jsonarray":{choices:[{id:"success",label:"Yes",summary:"Extending the lifetime of the <code>JsonDocument</code> solves the issue",next:"done"},{id:"not-destroyed",label:"No",summary:"Extending the lifetime of the <code>JsonDocument</code> doesn't solve the issue",next:"serialization/garbage/reference"}],content:`<p><a href="/v6/api/jsonarray/"><code>JsonArray</code></a> doesn't contain any data: it is a reference to an object stored in the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>. It becomes invalid as soon as the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> is destroyed; this could explain the garbage you see in the output.</p>
<p>For example, here is a function that creates a dangling <a href="/v6/api/jsonarray/"><code>JsonArray</code></a>:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// DON&#x27;T DO THAT!!!  💀</span>
<span class="hljs-function">JsonArray <span class="hljs-title">createArray</span><span class="hljs-params">()</span> </span>{
  StaticJsonDocument&lt;<span class="hljs-number">200</span>&gt; doc;
  JsonArray arr = doc.<span class="hljs-built_in">to</span>&lt;JsonArray&gt;();
  arr.<span class="hljs-built_in">add</span>(<span class="hljs-string">&quot;hello&quot;</span>);
  arr.<span class="hljs-built_in">add</span>(<span class="hljs-string">&quot;world&quot;</span>);
  <span class="hljs-keyword">return</span> arr;
}
</code></pre>
<p>The <a href="/v6/api/jsonarray/"><code>JsonArray</code></a> returned by this function points to a destructed <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, and therefore is likely to produce garbage or crash the program.</p>
<p>The best way to fix this function is to pass the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> as an argument:</p>
<pre><code class="hljs language-c++"><span class="hljs-function">JsonArray <span class="hljs-title">createArray</span><span class="hljs-params">(JsonDocument&amp; doc)</span> </span>{
  JsonArray arr = doc.<span class="hljs-built_in">to</span>&lt;JsonArray&gt;();
  arr.<span class="hljs-built_in">add</span>(<span class="hljs-string">&quot;hello&quot;</span>);
  arr.<span class="hljs-built_in">add</span>(<span class="hljs-string">&quot;world&quot;</span>);
  <span class="hljs-keyword">return</span> arr;
}
</code></pre>
<p>This way, you can keep the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> alive when you call <a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a></p>
<p>Did this solve your issue?</p>
`},"serialization/garbage/jsonobject":{choices:[{id:"success",label:"Yes",summary:"Extending the lifetime of the <code>JsonDocument</code> solves the issue",next:"done"},{id:"not-destroyed",label:"No",summary:"Extending the lifetime of the <code>JsonDocument</code> doesn't solve the issue",next:"serialization/garbage/reference"}],content:`<p><a href="/v6/api/jsonobject/"><code>JsonObject</code></a> doesn't contain any data: it is a reference to an object stored in the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>. It becomes invalid as soon as the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> is destroyed; this could explain the garbage you see in the output.</p>
<p>For example, here is a function that creates a dangling <a href="/v6/api/jsonobject/"><code>JsonObject</code></a>:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// DON&#x27;T DO THAT!!!  💀</span>
<span class="hljs-function">JsonObject <span class="hljs-title">createObject</span><span class="hljs-params">()</span> </span>{
  StaticJsonDocument&lt;<span class="hljs-number">200</span>&gt; doc;
  JsonObject obj = doc.<span class="hljs-built_in">to</span>&lt;JsonObject&gt;();
  obj[<span class="hljs-string">&quot;hello&quot;</span>] = <span class="hljs-string">&quot;world&quot;</span>;
  <span class="hljs-keyword">return</span> obj;
}
</code></pre>
<p>The <a href="/v6/api/jsonobject/"><code>JsonObject</code></a> returned by this function points to a destructed <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, and therefore is likely to produce garbage or crash the program.</p>
<p>The best way to fix this function is to pass the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> as an argument:</p>
<pre><code class="hljs language-c++"><span class="hljs-function">JsonObject <span class="hljs-title">createObject</span><span class="hljs-params">(JsonDocument&amp; doc)</span> </span>{
  JsonObject obj = doc.<span class="hljs-built_in">to</span>&lt;JsonObject&gt;();
  obj[<span class="hljs-string">&quot;hello&quot;</span>] = <span class="hljs-string">&quot;world&quot;</span>;
  <span class="hljs-keyword">return</span> obj;
}
</code></pre>
<p>This way, you can keep the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> alive when you call <a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a></p>
<p>Did this solve your issue?</p>
`},"serialization/garbage/jsonvariant":{choices:[{id:"success",label:"Yes",summary:"Extending the lifetime of the <code>JsonDocument</code> solves the issue",next:"done"},{id:"not-destroyed",label:"No",summary:"Extending the lifetime of the <code>JsonDocument</code> doesn't solve the issue",next:"serialization/garbage/reference"}],content:`<p><a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a> doesn't contain any data: it is a reference to an object stored in the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>. It becomes invalid as soon as the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> is destroyed; this could explain the garbage you see in the output.</p>
<p>For example, here is a function that creates a dangling <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a>:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// DON&#x27;T DO THAT!!!  💀</span>
<span class="hljs-function">JsonVariant <span class="hljs-title">createVariant</span><span class="hljs-params">()</span> </span>{
  StaticJsonDocument&lt;<span class="hljs-number">200</span>&gt; doc;
  JsonVariant var = doc.<span class="hljs-built_in">to</span>&lt;JsonVariant&gt;();
  var[<span class="hljs-string">&quot;hello&quot;</span>] = <span class="hljs-string">&quot;world&quot;</span>;
  <span class="hljs-keyword">return</span> var;
}
</code></pre>
<p>The <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a> returned by this function points to a destructed <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, and therefore is likely to produce garbage or crash the program.</p>
<p>The best way to fix this function is to pass the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> as an argument:</p>
<pre><code class="hljs language-c++"><span class="hljs-function">JsonVariant <span class="hljs-title">createVariant</span><span class="hljs-params">(JsonDocument&amp; doc)</span> </span>{
  JsonVariant var = doc.<span class="hljs-built_in">to</span>&lt;JsonVariant&gt;();
  var[<span class="hljs-string">&quot;hello&quot;</span>] = <span class="hljs-string">&quot;world&quot;</span>;
  <span class="hljs-keyword">return</span> var;
}
</code></pre>
<p>This way, you can keep the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> alive when you call <a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a></p>
<p>Did this solve your issue?</p>
`},"serialization/garbage/msgpack-only":{needs_assistance:!0,choices:[],content:`<p><a href="/v6/api/msgpack/serializemsgpack/"><code>serializeMsgPack()</code></a> share a lot of code with <a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a>. There is no reason you should produce garbage with one and not the other. You may have found a bug.</p>
`},"serialization/garbage/msgpack":{choices:[{id:"json",label:"Yes, <code>serializeJson()</code> produces garbage too",summary:"<code>serializeJson()</code> produces garbage too",next:"serialization/garbage/json"},{id:"deserialize-no",label:"No, <code>serializeJson()</code> doesn't produces garbage",summary:"<code>serializeJson()</code> doesn't produces garbage",next:"serialization/garbage/msgpack-only"}],content:`<p><a href="https://msgpack.org/">MessagePack</a> is a binary format, so if you print a document to the serial port, the result would look weird.</p>
<p>For example, the following program:</p>
<pre><code class="hljs language-c++">StaticJsonDocument&lt;<span class="hljs-number">256</span>&gt; doc;
doc[<span class="hljs-string">&quot;val&quot;</span>] = <span class="hljs-number">42</span>;
<span class="hljs-built_in">serializeMsgPack</span>(doc, Serial);
</code></pre>
<p>produces:</p>
<pre><code class="hljs language-text">⸮⸮val*
</code></pre>
<p>Yes. This is the expected result.</p>
<p>Now that this question is cleared, let's move on with the diagnostic.</p>
<p>I need you to edit your program to replace <a href="/v6/api/msgpack/serializemsgpack/"><code>serializeMsgPack()</code></a> with <a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a>.</p>
<p>Do you see the garbage in the JSON output?</p>
`},"serialization/garbage/reference-garbagecollect":{choices:[{id:"success",label:"Yes",summary:"Reassigning references fixes the issue",next:"done"},{id:"reassigned",label:"No",summary:"Reassigning references doesn't fix the issue",next:"serialization/garbage/document"}],content:`<p><a href="/v6/api/jsondocument/garbagecollect/"><code>JsonDocument::garbageCollect()</code></a> invalidates all previously acquired <a href="/v6/api/jsonarray/"><code>JsonArray</code></a>, <a href="/v6/api/jsonobject/"><code>JsonObject</code></a>, and <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a>.</p>
<p>After you call it, make sure to reassign  every references, like so:</p>
<pre><code class="hljs language-c++">JsonObject cfg = doc.<span class="hljs-built_in">createNestedObject</span>(<span class="hljs-string">&quot;config&quot;</span>);
<span class="hljs-comment">// ...</span>
doc.<span class="hljs-built_in">garbageCollect</span>();
cfg = doc[<span class="hljs-string">&quot;config&quot;</span>];
</code></pre>
<p>Did this solve your issue?</p>
`},"serialization/garbage/reference-shrinktofit":{choices:[{id:"success",label:"Yes",summary:"Reassigning references fixes the issue",next:"done"},{id:"reassigned",label:"No",summary:"Reassigning references doesn't fix the issue",next:"serialization/garbage/document"}],content:`<p><a href="/v6/api/basicjsondocument/shrinktofit/"><code>JsonDocument::shrinkToFit()</code></a> invalidates all previously acquired <a href="/v6/api/jsonarray/"><code>JsonArray</code></a>, <a href="/v6/api/jsonobject/"><code>JsonObject</code></a>, and <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a>.</p>
<p>After you call it, make sure to reassign  every references, like so:</p>
<pre><code class="hljs language-c++">JsonObject cfg = doc.<span class="hljs-built_in">createNestedObject</span>(<span class="hljs-string">&quot;config&quot;</span>);
<span class="hljs-comment">// ...</span>
doc.<span class="hljs-built_in">shrinkToFit</span>();
cfg = doc[<span class="hljs-string">&quot;config&quot;</span>];
</code></pre>
<p>Did this solve your issue?</p>
`},"serialization/garbage/reference":{choices:[{id:"garbagecollect",label:"<code>JsonDocument::garbageCollect()</code>",summary:"Program calls <code>JsonDocument::garbageCollect()</code>",next:"serialization/garbage/reference-garbagecollect"},{id:"shrinktofit",label:"<code>JsonDocument::shrinkToFit()</code>",summary:"Program calls <code>JsonDocument::shrinkToFit()</code>",next:"serialization/garbage/reference-shrinktofit"},{id:"none",label:"No",summary:"Program doesn't call an invalidating function",next:"serialization/garbage/document"}],content:`<p>Do you use one of the following function?</p>
`},"serialization/garbage/serialize":{choices:[{id:"document",label:"<code>JsonDocument</code>",summary:"Program calls <code>serializeJson(const JsonDocument&amp;, ...)</code>",next:"serialization/garbage/document"},{id:"array",label:"<code>JsonArray</code> (or <code>JsonArrayConst</code>)",summary:"Program calls <code>serializeJson(JsonArrayConst, ...)</code>",next:"serialization/garbage/jsonarray"},{id:"object",label:"<code>JsonObject</code> (or <code>JsonObjectConst</code>)",summary:"Program calls <code>serializeJson(JsonObjectConst, ...)</code>",next:"serialization/garbage/jsonobject"},{id:"variant",label:"<code>JsonVariant</code> (or <code>JsonVariantConst</code>)",summary:"Program calls <code>serializeJson(JsonVariantConst, ...)</code>",next:"serialization/garbage/jsonvariant"}],content:`<p>What is the type of the first argument passed to <a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a>?</p>
`},"serialization/garbage":{choices:[{id:"json",label:"<code>serializeJson()</code> (or <code>serializeJsonPretty()</code>)",summary:"<code>serializeJson()</code> produces garbage",next:"serialization/garbage/json"},{id:"msg",label:"<code>serializeMsgPack()</code>",summary:"<code>serializeMsgPack()</code> produces garbage",next:"serialization/garbage/msgpack"}],content:`<p>What function produces the garbage?</p>
`},"serialization/incomplete/loop-outside-clear":{choices:[{id:"success",label:"Yes",summary:"Calling <code>JsonDocument::garbageCollect()</code> solves the issue",next:"done"},{id:"gc",label:"No",summary:"Calling <code>JsonDocument::garbageCollect()</code> doesn't solve the issue",next:"serialization/incomplete/noloop"}],content:`<p>Please replace the call to <a href="/v6/api/jsondocument/clear/"><code>JsonDocument::clear()</code></a> with <a href="/v6/api/jsondocument/garbagecollect/"><code>JsonDocument::garbageCollect()</code></a>.</p>
<p>Did this solve your issue?</p>
`},"serialization/incomplete/loop-outside":{choices:[{id:"success",label:"Yes",summary:"Calling <code>JsonDocument::clear()</code> solves the issue",next:"done"},{id:"clear",label:"No",summary:"Calling <code>JsonDocument::clear()</code> doesn't solve the issue",next:"serialization/incomplete/noloop"},{id:"missing",label:"Yes, but now some values are missing",summary:"Calling <code>JsonDocument::clear()</code> solves the issue but removes other values",next:"serialization/incomplete/loop-outside-clear"}],content:`<p>We can try to clear the content of the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>.</p>
<p>Please call <a href="/v6/api/jsondocument/clear/"><code>JsonDocument::clear()</code></a> at the beginning of the loop.</p>
<p>Did this solve your issue?</p>
`},"serialization/incomplete/loop":{choices:[{id:"success",label:"Yes",summary:"Moving the <code>JsonDocument</code> inside the loop solves the issue",next:"done"},{id:"inside",label:"No",summary:"Moving the <code>JsonDocument</code> inside the loop doesn't solve the issue",next:"serialization/incomplete/noloop"},{id:"outside",label:"I cannot move the declaration inside the loop",summary:"The <code>JsonDocument</code> cannot be moved inside the loop",next:"serialization/incomplete/loop-outside"}],content:`<p>Calling <a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a> is safe, but you'll run into issues if you modify the same <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> in a loop.</p>
<p>The best workaround is to move the declaration of the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> inside the loop.</p>
<p>For example:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// Before</span>
StaticJsonDocument&lt;<span class="hljs-number">128</span>&gt; doc;
<span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i=<span class="hljs-number">0</span>; i&lt;<span class="hljs-number">10</span>; i++) {
  doc[<span class="hljs-string">&quot;userid&quot;</span>] = <span class="hljs-built_in">String</span>(<span class="hljs-string">&quot;user&quot;</span>) + i;
  <span class="hljs-built_in">serializeJson</span>(doc, Serial);
}

<span class="hljs-comment">// After</span>
<span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i=<span class="hljs-number">0</span>; i&lt;<span class="hljs-number">10</span>; i++) {
  StaticJsonDocument&lt;<span class="hljs-number">128</span>&gt; doc;
  doc[<span class="hljs-string">&quot;userid&quot;</span>] = <span class="hljs-built_in">String</span>(<span class="hljs-string">&quot;user&quot;</span>) + i;
  <span class="hljs-built_in">serializeJson</span>(doc, Serial);
}
</code></pre>
<p>Please read <a href="/v6/issues/memory-leak/">I found a memory leak in the library!</a> for an explanation.</p>
<p>Did this solve your issue?</p>
`},"serialization/incomplete/noloop":{choices:[{id:"overflowed-1",label:"<code>1</code> (or <code>true</code>)",summary:"<code>JsonDocument::overflowed()</code> returns <code>true</code>",next:"serialization/incomplete/overflowed"},{id:"overflowed-0",label:"<code>0</code> (or <code>false</code>)",summary:"<code>JsonDocument::overflowed()</code> returns <code>false</code>",next:"serialization/incomplete/nul-char"}],content:`<p>Please print the value of <a href="/v6/api/jsondocument/overflowed/"><code>JsonDocument::overflowed()</code></a>, like so:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">println</span>(doc.<span class="hljs-built_in">overflowed</span>());
</code></pre>
<p>What value does it print?</p>
`},"serialization/incomplete/nul-char":{choices:[{id:"nul",label:"Yes",summary:"One or more strings contain a NUL",next:"nul-not-supported"},{id:"no-nul",label:"No",summary:"No string contains a NUL",next:"deadend"}],content:`<p>Does one of the strings in the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> contains a NUL (i.e.,  ASCII code 0, or <code>\\u0000</code>)?</p>
`},"serialization/incomplete/overflowed":{choices:[{id:"success",label:"Yes",summary:"Increasing the capacity of the <code>JsonDocument</code> solves the issue.",next:"done"},{id:"capacity-increased",label:"No",summary:"Increasing the capacity of the <code>JsonDocument</code> doesn't solve the issue.",next:"deadend"}],content:`<p><a href="/v6/api/jsondocument/overflowed/"><code>JsonDocument::overflowed()</code></a> returns <code>true</code> when you tried to insert a value in the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> but there isn't enough room to store it.</p>
<p>The solution is to increase the capacity of the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>. Don't forget to account for the size of the duplicated strings. As usual, use the <a href="/v6/assistant/">ArduinoJson Assistant</a> to compute the right capacity for your project.</p>
<p>For more explanations on this issue, please see <a href="/v6/issues/incomplete-output/">Why is the output incomplete?</a>.</p>
<p>Did this solve your issue?</p>
`},"serialization/incomplete":{choices:[{id:"loop",label:"Yes",summary:"Program calls <code>serializeJson()</code> in a loop",next:"serialization/incomplete/loop"},{id:"no-loop",label:"No",summary:"Program doesn't call <code>serializeJson()</code> in a loop",next:"serialization/incomplete/noloop"}],content:`<p>Does your program call <a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a> in a loop?</p>
`},"serialization/slow":{choices:[{id:"success",label:"Yes",summary:"Adding <code>WriteBufferingStream</code> fixes the issue",next:"serialization/incomplete/overflowed"},{id:"failure",label:"No",summary:"Adding <code>WriteBufferingStream</code> doesn't fix the issue",next:"deadend"}],content:`<p><a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a> writes the JSON document mostly one character at a time, which can be pretty slow with unbuffered streams (such as <a href="https://www.arduino.cc/en/Reference/EthernetClient"><code>EthernetClient</code></a>, <a href="https://www.arduino.cc/en/Reference/WiFiClient"><code>WifiClient</code></a>, <a href="https://www.arduino.cc/en/Reference/SD"><code>File</code></a>, and <a href="https://github.com/knolleary/pubsubclient/">PubSubClient</a>).</p>
<p>To speed up the serialization process, you must insert a buffer between the stream and <a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a>.
The easiest way to do so it by using the <code>WriteBufferingStream</code> from the <a href="https://github.com/bblanchon/ArduinoStreamUtils">StreamUtils</a> library.</p>
<p>Replace the following:</p>
<pre><code class="hljs language-c++"><span class="hljs-built_in">serializeJson</span>(doc, stream);
</code></pre>
<p>with</p>
<pre><code class="hljs language-c++"><span class="hljs-function">WriteBufferingStream <span class="hljs-title">bufferedStream</span><span class="hljs-params">(stream, <span class="hljs-number">64</span>)</span></span>;
<span class="hljs-built_in">serializeJson</span>(doc, bufferedStream);
bufferedStream.<span class="hljs-built_in">flush</span>();
</code></pre>
<p>The first line creates a new stream <code>bufferedStream</code> that implements buffering on top of the original (this is the <a href="https://en.wikipedia.org/wiki/Decorator_pattern">decorator pattern</a>).<br>
The second line writes the JSON document to the <a href="https://www.arduino.cc/en/Reference/WiFiClient"><code>WiFiClient</code></a> through the buffer.<br>
The last line flushes the buffer to make sure we send the end of the document.</p>
`},serialization:{choices:[{id:"crash",label:"The program crashes",summary:"Program crashes",next:"serialization/crash"},{id:"empty",label:"The output is empty (e.g. <code>{}</code>, <code>[]</code>, or <code>null</code>)",summary:"Output is empty",next:"serialization/empty"},{id:"incomplete",label:"The output is incomplete",summary:"Output is incomplete",next:"serialization/incomplete"},{id:"garbage",label:"The output contains garbage",summary:"Output contains garbage",next:"serialization/garbage"},{id:"empty-strings",label:"The output contains empty strings",summary:"Output contains empty strings",next:"serialization/emptystrings"},{id:"null",label:"The output contains <code>null</code>",summary:"Output contains null",next:"serialization/incomplete"},{id:"float",label:"Floating-point values contain too many decimal digits",summary:"Floating-point values contain too many decimal digits",next:"serialization/floats"},{id:"slow",label:"It's slow",summary:"Serialization is slow",next:"serialization/slow"}],content:`<p>What's the problem?</p>
`},start:{choices:[{id:"compiletime",label:"At compile time",next:"compiletime",summary:"The issue happens at compile time"},{id:"runtime",label:"At run time",next:"runtime",summary:"The issue happens at run time"}],content:`<p>When does your issue happen?</p>
`}},Ss=(e,s)=>{const n=e.__vccOpts||e;for(const[t,o]of s)n[t]=o;return n},Dr=En({props:{report:{type:String,required:!0}},methods:{createIssue(){const e=new FormData(this.$refs.issueForm),s=e.get("title");let n="";const t=e.get("description");t&&(n+=`**Description**
${t}

`),n+=`**Troubleshooter's report**
${this.report}

`;const o=e.get("mcu"),a=e.get("framework"),i=e.get("ide");(o||a||i)&&(n+=`**Environment**
`,o&&(n+="* Microcontroller: "+o+`
`),a&&(n+="* Core/Framework: "+a+`
`),i&&(n+="* IDE: "+i+`
`),n+=`
`);const r=e.get("repro");r&&(n+="**Reproduction code**\n```c++\n",n+=r,n+="\n```\n\n");const l=e.get("remarks");l&&(n+=`**Remarks**
${l}

`);const p="https://github.com/bblanchon/ArduinoJson/issues/new?"+new URLSearchParams({title:s,body:n}).toString();console.log("URL",p),window.open(p,"_blank"),$("#assistance-modal").modal("hide")}}}),Ar={class:"modal fade"},Nr={class:"modal-dialog modal-dialog-scrollable modal-lg"},Ir=[qi('<div class="modal-header bg-primary text-white"><h5 class="modal-title">Contact support</h5><button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">×</span></button></div><div class="modal-body"><div class="alert alert-info"> This form simplifies the creation of an issue on ArduinoJson&#39;s GitHub project page. It ensures you provide all the required information and prefills the issue&#39;s text. After that, you just need to submit the issue.<br><b>A GitHub account is required.</b></div><div class="form-group"><label for="title-input">Title</label><input type="text" name="title" class="form-control" id="title-input" aria-describedby="title-help" required><small id="title-help" class="form-text text-muted">Summarize your issue in one sentence.</small></div><div class="form-group"><label for="description-input">Description</label><textarea name="description" rows="3" class="form-control" id="description-input" aria-describedby="description-help" required placeholder="When I do ..., I expect ..., but instead I get ..."></textarea><small id="description-help" class="form-text text-muted">Describe your issue with a few sentences.</small></div><div class="form-group"><label for="mcu-input">Microcontroller</label><input type="text" name="mcu" class="form-control" id="mcu-input" aria-describedby="mcu-help" placeholder="ESP8266" required><small id="mcu-help" class="form-text text-muted">Which processor or with board do you use?</small></div><div class="form-group"><label for="framework-input">Arduino Core / Framework</label><input type="text" name="framework" class="form-control" id="framework-input" aria-describedby="framework-help" placeholder="ESP8266 core for Arduino v3.0.2"><small id="framework-help" class="form-text text-muted">Please include version number.</small></div><div class="form-group"><label for="ide-input">IDE</label><input type="text" name="ide" class="form-control" id="ide-input" aria-describedby="ide-help" placeholder="Arduino IDE 1.8.16"><small id="ide-help" class="form-text text-muted">Please include version number.</small></div><div class="form-group"><label for="repro-input">Reproduction code</label><textarea name="repro" rows="10" class="form-control text-monospace" style="font-size:80%;" id="repro-input" aria-describedby="repro-help" placeholder="DynamicJsonDocuemnt doc(1024);\\n..."></textarea><small id="repro-help" class="form-text text-muted">Write a few lines of code that demonstrate the issue.</small></div><div class="form-group"><label for="remarks-input">Remarks</label><textarea name="remarks" rows="4" class="form-control" id="remarks-input" aria-describedby="remarks-help"></textarea><small id="remarks-help" class="form-text text-muted">Anything else you need to tell us?</small></div></div><div class="modal-footer"><div class="text-right"><button type="submit" class="btn btn-primary">Submit</button><small class="form-text text-muted"> This button redirects you to GitHub. </small></div></div>',3)];function Cr(e,s,n,t,o,a){return le(),ze("div",Ar,[be("div",Nr,[be("form",{class:"modal-content",ref:"issueForm",onSubmit:s[0]||(s[0]=Jr((...i)=>e.createIssue&&e.createIssue(...i),["prevent"]))},Ir,544)])])}const Or=Ss(Dr,[["render",Cr]]),qr=En({inject:["debug"],emits:["check"],props:{choice:{type:Object,required:!0}}}),Pr={class:"form-check"},Er=["id","checked","disabled"],Rr=["for","innerHTML"],Mr=["innerHTML"];function Fr(e,s,n,t,o,a){return le(),ze("div",Pr,[be("input",{type:"radio",id:e.choice.inputId,class:"form-check-input",checked:e.choice.selected,onClick:s[0]||(s[0]=i=>e.$emit("check")),disabled:e.choice.missing},null,8,Er),be("label",{class:"form-check-label",for:e.choice.inputId,innerHTML:e.choice.label},null,8,Rr),e.debug?(le(),ze("div",{key:0,class:"d-block mb-2 small text-muted",innerHTML:e.choice.summary},null,8,Mr)):$n("",!0)])}const Hr=Ss(qr,[["render",Fr]]),Yr={inject:["debug"],props:{step:{type:Object,required:!0}}},Ur=["title"],Br={class:"bg-secondary text-white rounded-circle"},Lr={key:0,class:"sr-only"};function Wr(e,s,n,t,o,a){return le(),ze("h2",{class:"troubleshooter-step-number",title:a.debug&&n.step.slug},[be("div",Br,[bo(cn(n.step.number)+" ",1),a.debug?(le(),ze("span",Lr,": "+cn(n.step.slug),1)):$n("",!0)])],8,Ur)}const $r=Ss(Yr,[["render",Wr]]),fc="",Vr=En({inject:["debug"],emits:["choose"],props:{step:{type:Object,required:!0}},components:{TroubleshooterStepChoice:Hr,TroubleshooterStepNumber:$r}}),Kr={key:0,class:"troubleshooter-step mb-4"},Gr=["innerHTML"],Xr={class:"troubleshooter-step-choices"},Zr=["innerHTML"];function Qr(e,s,n,t,o,a){const i=Xs("TroubleshooterStepNumber"),r=Xs("TroubleshooterStepChoice");return e.step.choices.length?(le(),ze("div",Kr,[de(i,{step:e.step},null,8,["step"]),be("div",{innerHTML:e.step.content,class:"troubleshooter-step-content"},null,8,Gr),be("div",Xr,[(le(!0),ze(pe,null,Qt(e.step.choices,l=>(le(),Wn(r,{key:l.hash,choice:l,onClick:u=>e.$emit("choose",l)},null,8,["choice","onClick"]))),128))])])):(le(),ze("div",{key:1,innerHTML:e.step.content,class:"troubleshooter-step-content"},null,8,Zr))}const ec=Ss(Vr,[["render",Qr]]),gc="",Ho=e=>new Promise(s=>setTimeout(s,e));function Yo(e,s,n){var o;const t=et[e];return t?{...t,number:n,slug:e,hash:s||"#",id:(s||"#start").substring(1),choices:(o=t.choices)==null?void 0:o.map(a=>({...a,inputId:(s?s.substring(1)+"/":"")+a.id,hash:(s?s+"/":"#")+a.id,missing:!et[a.next]}))}:null}const sc={components:{AssistanceModal:Or,TroubleshooterStep:ec},setup(){return{intro:et.intro}},data(){return{reportCopied:!1,hash:""}},mounted(){this.hash=location.hash,window.addEventListener("hashchange",()=>this.hash=location.hash)},computed:{currentStep(){return this.steps[this.steps.length-1]},needsAssistance(){return!this.currentStep||this.currentStep.needs_assistance},steps(){const e=[Yo("start",void 0,1)];if(this.hash){let s=e[0];for(let n of this.hash.substring(1).split("/")){const t=s.choices.filter(o=>o.id===n)[0];if(!t){console.error(`Choice "${n}" not found`);break}if(t.selected=!0,s.selectedChoice=t,s=Yo(t.next,t.hash,s.number+1),!s)break;e.push(s)}}return e},report(){return this.steps.filter(e=>e.selectedChoice).map((e,s)=>`${s+1}. ${e.selectedChoice.summary}`).join(`
`)}},watch:{async hash(e){const s=e.substring(1);await Ho(300);const n=document.getElementById(s);n?n.scrollIntoView({behavior:"smooth"}):console.error(`Element with id "${s}" not found`)}},methods:{choose(e){document.location.assign(e.hash),ga("set","page",document.location.pathname+document.location.hash),ga("send","pageview")},async copyReport(){await navigator.clipboard.writeText(this.report),this.reportCopied=!0,await Ho(2e3),this.reportCopied=!1}}},nc=["innerHTML"],tc={key:0},oc=be("button",{type:"button",class:"btn btn-primary","data-toggle":"modal","data-target":"#assistance-modal"}," Contact support ",-1),ac=["disabled"];function ic(e,s,n,t,o,a){const i=Xs("TroubleshooterStep"),r=Xs("AssistanceModal");return le(),ze("div",null,[be("div",{innerHTML:t.intro.content},null,8,nc),de(yr,{name:"fade",mode:"out-in"},{default:Mt(()=>[(le(!0),ze(pe,null,Qt(a.steps,l=>(le(),Wn(i,{key:l.hash,id:l.id,step:l,onChoose:a.choose},null,8,["id","step","onChoose"]))),128))]),_:1}),a.needsAssistance?(le(),ze("div",tc,[oc,be("button",{class:Ds(["btn",{"btn-outline-primary":!o.reportCopied,"btn-success":o.reportCopied}]),disabled:o.reportCopied,onClick:s[0]||(s[0]=(...l)=>a.copyReport&&a.copyReport(...l))},cn(o.reportCopied?"✓ Report copied":"Copy troubleshooter's report"),11,ac),de(r,{id:"assistance-modal",report:a.report},null,8,["report"])])):$n("",!0)])}Tr(Ss(sc,[["render",ic]])).provide("debug",!1).mount("#troubleshooter-app")})();
