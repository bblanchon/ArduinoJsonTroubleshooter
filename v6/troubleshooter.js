(function(){"use strict";function ln(e,s){const n=Object.create(null),t=e.split(",");for(let a=0;a<t.length;a++)n[t[a]]=!0;return s?a=>!!n[a.toLowerCase()]:a=>!!n[a]}function cn(e){if(D(e)){const s={};for(let n=0;n<e.length;n++){const t=e[n],a=Z(t)?Xa(t):cn(t);if(a)for(const o in a)s[o]=a[o]}return s}else{if(Z(e))return e;if(L(e))return e}}const Va=/;(?![^(]*\))/g,Ka=/:([^]+)/,Ga=/\/\*.*?\*\//gs;function Xa(e){const s={};return e.replace(Ga,"").split(Va).forEach(n=>{if(n){const t=n.split(Ka);t.length>1&&(s[t[0].trim()]=t[1].trim())}}),s}function Ds(e){let s="";if(Z(e))s=e;else if(D(e))for(let n=0;n<e.length;n++){const t=Ds(e[n]);t&&(s+=t+" ")}else if(L(e))for(const n in e)e[n]&&(s+=n+" ");return s.trim()}const Za=ln("itemscope,allowfullscreen,formnovalidate,ismap,nomodule,novalidate,readonly");function tt(e){return!!e||e===""}const As=e=>Z(e)?e:e==null?"":D(e)||L(e)&&(e.toString===rt||!I(e.toString))?JSON.stringify(e,at,2):String(e),at=(e,s)=>s&&s.__v_isRef?at(e,s.value):os(s)?{[`Map(${s.size})`]:[...s.entries()].reduce((n,[t,a])=>(n[`${t} =>`]=a,n),{})}:ot(s)?{[`Set(${s.size})`]:[...s.values()]}:L(s)&&!D(s)&&!lt(s)?String(s):s,B={},as=[],ve=()=>{},Qa=()=>!1,eo=/^on[^a-z]/,Ns=e=>eo.test(e),dn=e=>e.startsWith("onUpdate:"),Q=Object.assign,un=(e,s)=>{const n=e.indexOf(s);n>-1&&e.splice(n,1)},so=Object.prototype.hasOwnProperty,q=(e,s)=>so.call(e,s),D=Array.isArray,os=e=>Is(e)==="[object Map]",ot=e=>Is(e)==="[object Set]",I=e=>typeof e=="function",Z=e=>typeof e=="string",pn=e=>typeof e=="symbol",L=e=>e!==null&&typeof e=="object",it=e=>L(e)&&I(e.then)&&I(e.catch),rt=Object.prototype.toString,Is=e=>rt.call(e),no=e=>Is(e).slice(8,-1),lt=e=>Is(e)==="[object Object]",hn=e=>Z(e)&&e!=="NaN"&&e[0]!=="-"&&""+parseInt(e,10)===e,Cs=ln(",key,ref,ref_for,ref_key,onVnodeBeforeMount,onVnodeMounted,onVnodeBeforeUpdate,onVnodeUpdated,onVnodeBeforeUnmount,onVnodeUnmounted"),Os=e=>{const s=Object.create(null);return n=>s[n]||(s[n]=e(n))},to=/-(\w)/g,Je=Os(e=>e.replace(to,(s,n)=>n?n.toUpperCase():"")),ao=/\B([A-Z])/g,is=Os(e=>e.replace(ao,"-$1").toLowerCase()),Ps=Os(e=>e.charAt(0).toUpperCase()+e.slice(1)),mn=Os(e=>e?`on${Ps(e)}`:""),qs=(e,s)=>!Object.is(e,s),fn=(e,s)=>{for(let n=0;n<e.length;n++)e[n](s)},Es=(e,s,n)=>{Object.defineProperty(e,s,{configurable:!0,enumerable:!1,value:n})},gn=e=>{const s=parseFloat(e);return isNaN(s)?e:s};let ct;const oo=()=>ct||(ct=typeof globalThis<"u"?globalThis:typeof self<"u"?self:typeof window<"u"?window:typeof global<"u"?global:{});let Se;class io{constructor(s=!1){this.detached=s,this.active=!0,this.effects=[],this.cleanups=[],this.parent=Se,!s&&Se&&(this.index=(Se.scopes||(Se.scopes=[])).push(this)-1)}run(s){if(this.active){const n=Se;try{return Se=this,s()}finally{Se=n}}}on(){Se=this}off(){Se=this.parent}stop(s){if(this.active){let n,t;for(n=0,t=this.effects.length;n<t;n++)this.effects[n].stop();for(n=0,t=this.cleanups.length;n<t;n++)this.cleanups[n]();if(this.scopes)for(n=0,t=this.scopes.length;n<t;n++)this.scopes[n].stop(!0);if(!this.detached&&this.parent&&!s){const a=this.parent.scopes.pop();a&&a!==this&&(this.parent.scopes[this.index]=a,a.index=this.index)}this.parent=void 0,this.active=!1}}}function ro(e,s=Se){s&&s.active&&s.effects.push(e)}const yn=e=>{const s=new Set(e);return s.w=0,s.n=0,s},dt=e=>(e.w&Re)>0,ut=e=>(e.n&Re)>0,lo=({deps:e})=>{if(e.length)for(let s=0;s<e.length;s++)e[s].w|=Re},co=e=>{const{deps:s}=e;if(s.length){let n=0;for(let t=0;t<s.length;t++){const a=s[t];dt(a)&&!ut(a)?a.delete(e):s[n++]=a,a.w&=~Re,a.n&=~Re}s.length=n}},bn=new WeakMap;let fs=0,Re=1;const vn=30;let je;const $e=Symbol(""),jn=Symbol("");class wn{constructor(s,n=null,t){this.fn=s,this.scheduler=n,this.active=!0,this.deps=[],this.parent=void 0,ro(this,t)}run(){if(!this.active)return this.fn();let s=je,n=Fe;for(;s;){if(s===this)return;s=s.parent}try{return this.parent=je,je=this,Fe=!0,Re=1<<++fs,fs<=vn?lo(this):pt(this),this.fn()}finally{fs<=vn&&co(this),Re=1<<--fs,je=this.parent,Fe=n,this.parent=void 0,this.deferStop&&this.stop()}}stop(){je===this?this.deferStop=!0:this.active&&(pt(this),this.onStop&&this.onStop(),this.active=!1)}}function pt(e){const{deps:s}=e;if(s.length){for(let n=0;n<s.length;n++)s[n].delete(e);s.length=0}}let Fe=!0;const ht=[];function rs(){ht.push(Fe),Fe=!1}function ls(){const e=ht.pop();Fe=e===void 0?!0:e}function pe(e,s,n){if(Fe&&je){let t=bn.get(e);t||bn.set(e,t=new Map);let a=t.get(n);a||t.set(n,a=yn()),mt(a)}}function mt(e,s){let n=!1;fs<=vn?ut(e)||(e.n|=Re,n=!dt(e)):n=!e.has(je),n&&(e.add(je),je.deps.push(e))}function Ce(e,s,n,t,a,o){const i=bn.get(e);if(!i)return;let r=[];if(s==="clear")r=[...i.values()];else if(n==="length"&&D(e)){const c=gn(t);i.forEach((u,p)=>{(p==="length"||p>=c)&&r.push(u)})}else switch(n!==void 0&&r.push(i.get(n)),s){case"add":D(e)?hn(n)&&r.push(i.get("length")):(r.push(i.get($e)),os(e)&&r.push(i.get(jn)));break;case"delete":D(e)||(r.push(i.get($e)),os(e)&&r.push(i.get(jn)));break;case"set":os(e)&&r.push(i.get($e));break}if(r.length===1)r[0]&&xn(r[0]);else{const c=[];for(const u of r)u&&c.push(...u);xn(yn(c))}}function xn(e,s){const n=D(e)?e:[...e];for(const t of n)t.computed&&ft(t);for(const t of n)t.computed||ft(t)}function ft(e,s){(e!==je||e.allowRecurse)&&(e.scheduler?e.scheduler():e.run())}const uo=ln("__proto__,__v_isRef,__isVue"),gt=new Set(Object.getOwnPropertyNames(Symbol).filter(e=>e!=="arguments"&&e!=="caller").map(e=>Symbol[e]).filter(pn)),po=_n(),ho=_n(!1,!0),mo=_n(!0),yt=fo();function fo(){const e={};return["includes","indexOf","lastIndexOf"].forEach(s=>{e[s]=function(...n){const t=E(this);for(let o=0,i=this.length;o<i;o++)pe(t,"get",o+"");const a=t[s](...n);return a===-1||a===!1?t[s](...n.map(E)):a}}),["push","pop","shift","unshift","splice"].forEach(s=>{e[s]=function(...n){rs();const t=E(this)[s].apply(this,n);return ls(),t}}),e}function _n(e=!1,s=!1){return function(t,a,o){if(a==="__v_isReactive")return!e;if(a==="__v_isReadonly")return e;if(a==="__v_isShallow")return s;if(a==="__v_raw"&&o===(e?s?No:St:s?Jt:zt).get(t))return t;const i=D(t);if(!e&&i&&q(yt,a))return Reflect.get(yt,a,o);const r=Reflect.get(t,a,o);return(pn(a)?gt.has(a):uo(a))||(e||pe(t,"get",a),s)?r:ie(r)?i&&hn(a)?r:r.value:L(r)?e?Tt(r):Sn(r):r}}const go=bt(),yo=bt(!0);function bt(e=!1){return function(n,t,a,o){let i=n[t];if(gs(i)&&ie(i)&&!ie(a))return!1;if(!e&&(!kn(a)&&!gs(a)&&(i=E(i),a=E(a)),!D(n)&&ie(i)&&!ie(a)))return i.value=a,!0;const r=D(n)&&hn(t)?Number(t)<n.length:q(n,t),c=Reflect.set(n,t,a,o);return n===E(o)&&(r?qs(a,i)&&Ce(n,"set",t,a):Ce(n,"add",t,a)),c}}function bo(e,s){const n=q(e,s);e[s];const t=Reflect.deleteProperty(e,s);return t&&n&&Ce(e,"delete",s,void 0),t}function vo(e,s){const n=Reflect.has(e,s);return(!pn(s)||!gt.has(s))&&pe(e,"has",s),n}function jo(e){return pe(e,"iterate",D(e)?"length":$e),Reflect.ownKeys(e)}const vt={get:po,set:go,deleteProperty:bo,has:vo,ownKeys:jo},wo={get:mo,set(e,s){return!0},deleteProperty(e,s){return!0}},xo=Q({},vt,{get:ho,set:yo}),zn=e=>e,Rs=e=>Reflect.getPrototypeOf(e);function Fs(e,s,n=!1,t=!1){e=e.__v_raw;const a=E(e),o=E(s);n||(s!==o&&pe(a,"get",s),pe(a,"get",o));const{has:i}=Rs(a),r=t?zn:n?An:Dn;if(i.call(a,s))return r(e.get(s));if(i.call(a,o))return r(e.get(o));e!==a&&e.get(s)}function Ms(e,s=!1){const n=this.__v_raw,t=E(n),a=E(e);return s||(e!==a&&pe(t,"has",e),pe(t,"has",a)),e===a?n.has(e):n.has(e)||n.has(a)}function Hs(e,s=!1){return e=e.__v_raw,!s&&pe(E(e),"iterate",$e),Reflect.get(e,"size",e)}function jt(e){e=E(e);const s=E(this);return Rs(s).has.call(s,e)||(s.add(e),Ce(s,"add",e,e)),this}function wt(e,s){s=E(s);const n=E(this),{has:t,get:a}=Rs(n);let o=t.call(n,e);o||(e=E(e),o=t.call(n,e));const i=a.call(n,e);return n.set(e,s),o?qs(s,i)&&Ce(n,"set",e,s):Ce(n,"add",e,s),this}function xt(e){const s=E(this),{has:n,get:t}=Rs(s);let a=n.call(s,e);a||(e=E(e),a=n.call(s,e)),t&&t.call(s,e);const o=s.delete(e);return a&&Ce(s,"delete",e,void 0),o}function _t(){const e=E(this),s=e.size!==0,n=e.clear();return s&&Ce(e,"clear",void 0,void 0),n}function Ys(e,s){return function(t,a){const o=this,i=o.__v_raw,r=E(i),c=s?zn:e?An:Dn;return!e&&pe(r,"iterate",$e),i.forEach((u,p)=>t.call(a,c(u),c(p),o))}}function Us(e,s,n){return function(...t){const a=this.__v_raw,o=E(a),i=os(o),r=e==="entries"||e===Symbol.iterator&&i,c=e==="keys"&&i,u=a[e](...t),p=n?zn:s?An:Dn;return!s&&pe(o,"iterate",c?jn:$e),{next(){const{value:y,done:b}=u.next();return b?{value:y,done:b}:{value:r?[p(y[0]),p(y[1])]:p(y),done:b}},[Symbol.iterator](){return this}}}}function Me(e){return function(...s){return e==="delete"?!1:this}}function _o(){const e={get(o){return Fs(this,o)},get size(){return Hs(this)},has:Ms,add:jt,set:wt,delete:xt,clear:_t,forEach:Ys(!1,!1)},s={get(o){return Fs(this,o,!1,!0)},get size(){return Hs(this)},has:Ms,add:jt,set:wt,delete:xt,clear:_t,forEach:Ys(!1,!0)},n={get(o){return Fs(this,o,!0)},get size(){return Hs(this,!0)},has(o){return Ms.call(this,o,!0)},add:Me("add"),set:Me("set"),delete:Me("delete"),clear:Me("clear"),forEach:Ys(!0,!1)},t={get(o){return Fs(this,o,!0,!0)},get size(){return Hs(this,!0)},has(o){return Ms.call(this,o,!0)},add:Me("add"),set:Me("set"),delete:Me("delete"),clear:Me("clear"),forEach:Ys(!0,!0)};return["keys","values","entries",Symbol.iterator].forEach(o=>{e[o]=Us(o,!1,!1),n[o]=Us(o,!0,!1),s[o]=Us(o,!1,!0),t[o]=Us(o,!0,!0)}),[e,n,s,t]}const[zo,Jo,So,To]=_o();function Jn(e,s){const n=s?e?To:So:e?Jo:zo;return(t,a,o)=>a==="__v_isReactive"?!e:a==="__v_isReadonly"?e:a==="__v_raw"?t:Reflect.get(q(n,a)&&a in t?n:t,a,o)}const ko={get:Jn(!1,!1)},Do={get:Jn(!1,!0)},Ao={get:Jn(!0,!1)},zt=new WeakMap,Jt=new WeakMap,St=new WeakMap,No=new WeakMap;function Io(e){switch(e){case"Object":case"Array":return 1;case"Map":case"Set":case"WeakMap":case"WeakSet":return 2;default:return 0}}function Co(e){return e.__v_skip||!Object.isExtensible(e)?0:Io(no(e))}function Sn(e){return gs(e)?e:Tn(e,!1,vt,ko,zt)}function Oo(e){return Tn(e,!1,xo,Do,Jt)}function Tt(e){return Tn(e,!0,wo,Ao,St)}function Tn(e,s,n,t,a){if(!L(e)||e.__v_raw&&!(s&&e.__v_isReactive))return e;const o=a.get(e);if(o)return o;const i=Co(e);if(i===0)return e;const r=new Proxy(e,i===2?t:n);return a.set(e,r),r}function cs(e){return gs(e)?cs(e.__v_raw):!!(e&&e.__v_isReactive)}function gs(e){return!!(e&&e.__v_isReadonly)}function kn(e){return!!(e&&e.__v_isShallow)}function kt(e){return cs(e)||gs(e)}function E(e){const s=e&&e.__v_raw;return s?E(s):e}function Dt(e){return Es(e,"__v_skip",!0),e}const Dn=e=>L(e)?Sn(e):e,An=e=>L(e)?Tt(e):e;function Po(e){Fe&&je&&(e=E(e),mt(e.dep||(e.dep=yn())))}function qo(e,s){e=E(e),e.dep&&xn(e.dep)}function ie(e){return!!(e&&e.__v_isRef===!0)}function Eo(e){return ie(e)?e.value:e}const Ro={get:(e,s,n)=>Eo(Reflect.get(e,s,n)),set:(e,s,n,t)=>{const a=e[s];return ie(a)&&!ie(n)?(a.value=n,!0):Reflect.set(e,s,n,t)}};function At(e){return cs(e)?e:new Proxy(e,Ro)}var Nt;class Fo{constructor(s,n,t,a){this._setter=n,this.dep=void 0,this.__v_isRef=!0,this[Nt]=!1,this._dirty=!0,this.effect=new wn(s,()=>{this._dirty||(this._dirty=!0,qo(this))}),this.effect.computed=this,this.effect.active=this._cacheable=!a,this.__v_isReadonly=t}get value(){const s=E(this);return Po(s),(s._dirty||!s._cacheable)&&(s._dirty=!1,s._value=s.effect.run()),s._value}set value(s){this._setter(s)}}Nt="__v_isReadonly";function Mo(e,s,n=!1){let t,a;const o=I(e);return o?(t=e,a=ve):(t=e.get,a=e.set),new Fo(t,a,o||!a,n)}function pl(e,...s){}function He(e,s,n,t){let a;try{a=t?e(...t):e()}catch(o){Bs(o,s,n)}return a}function ge(e,s,n,t){if(I(e)){const o=He(e,s,n,t);return o&&it(o)&&o.catch(i=>{Bs(i,s,n)}),o}const a=[];for(let o=0;o<e.length;o++)a.push(ge(e[o],s,n,t));return a}function Bs(e,s,n,t=!0){const a=s?s.vnode:null;if(s){let o=s.parent;const i=s.proxy,r=n;for(;o;){const u=o.ec;if(u){for(let p=0;p<u.length;p++)if(u[p](e,i,r)===!1)return}o=o.parent}const c=s.appContext.config.errorHandler;if(c){He(c,null,10,[e,i,r]);return}}Ho(e,n,a,t)}function Ho(e,s,n,t=!0){console.error(e)}let ys=!1,Nn=!1;const te=[];let Te=0;const ds=[];let Oe=null,Ve=0;const It=Promise.resolve();let In=null;function Yo(e){const s=In||It;return e?s.then(this?e.bind(this):e):s}function Uo(e){let s=Te+1,n=te.length;for(;s<n;){const t=s+n>>>1;bs(te[t])<e?s=t+1:n=t}return s}function Cn(e){(!te.length||!te.includes(e,ys&&e.allowRecurse?Te+1:Te))&&(e.id==null?te.push(e):te.splice(Uo(e.id),0,e),Ct())}function Ct(){!ys&&!Nn&&(Nn=!0,In=It.then(qt))}function Bo(e){const s=te.indexOf(e);s>Te&&te.splice(s,1)}function Lo(e){D(e)?ds.push(...e):(!Oe||!Oe.includes(e,e.allowRecurse?Ve+1:Ve))&&ds.push(e),Ct()}function Ot(e,s=ys?Te+1:0){for(;s<te.length;s++){const n=te[s];n&&n.pre&&(te.splice(s,1),s--,n())}}function Pt(e){if(ds.length){const s=[...new Set(ds)];if(ds.length=0,Oe){Oe.push(...s);return}for(Oe=s,Oe.sort((n,t)=>bs(n)-bs(t)),Ve=0;Ve<Oe.length;Ve++)Oe[Ve]();Oe=null,Ve=0}}const bs=e=>e.id==null?1/0:e.id,Wo=(e,s)=>{const n=bs(e)-bs(s);if(n===0){if(e.pre&&!s.pre)return-1;if(s.pre&&!e.pre)return 1}return n};function qt(e){Nn=!1,ys=!0,te.sort(Wo);const s=ve;try{for(Te=0;Te<te.length;Te++){const n=te[Te];n&&n.active!==!1&&He(n,null,14)}}finally{Te=0,te.length=0,Pt(),ys=!1,In=null,(te.length||ds.length)&&qt()}}function $o(e,s,...n){if(e.isUnmounted)return;const t=e.vnode.props||B;let a=n;const o=s.startsWith("update:"),i=o&&s.slice(7);if(i&&i in t){const p=`${i==="modelValue"?"model":i}Modifiers`,{number:y,trim:b}=t[p]||B;b&&(a=n.map(T=>Z(T)?T.trim():T)),y&&(a=n.map(gn))}let r,c=t[r=mn(s)]||t[r=mn(Je(s))];!c&&o&&(c=t[r=mn(is(s))]),c&&ge(c,e,6,a);const u=t[r+"Once"];if(u){if(!e.emitted)e.emitted={};else if(e.emitted[r])return;e.emitted[r]=!0,ge(u,e,6,a)}}function Et(e,s,n=!1){const t=s.emitsCache,a=t.get(e);if(a!==void 0)return a;const o=e.emits;let i={},r=!1;if(!I(e)){const c=u=>{const p=Et(u,s,!0);p&&(r=!0,Q(i,p))};!n&&s.mixins.length&&s.mixins.forEach(c),e.extends&&c(e.extends),e.mixins&&e.mixins.forEach(c)}return!o&&!r?(L(e)&&t.set(e,null),null):(D(o)?o.forEach(c=>i[c]=null):Q(i,o),L(e)&&t.set(e,i),i)}function Ls(e,s){return!e||!Ns(s)?!1:(s=s.slice(2).replace(/Once$/,""),q(e,s[0].toLowerCase()+s.slice(1))||q(e,is(s))||q(e,s))}let ye=null,Rt=null;function Ws(e){const s=ye;return ye=e,Rt=e&&e.type.__scopeId||null,s}function Ft(e,s=ye,n){if(!s||e._n)return e;const t=(...a)=>{t._d&&ma(-1);const o=Ws(s);let i;try{i=e(...a)}finally{Ws(o),t._d&&ma(1)}return i};return t._n=!0,t._c=!0,t._d=!0,t}function hl(){}function On(e){const{type:s,vnode:n,proxy:t,withProxy:a,props:o,propsOptions:[i],slots:r,attrs:c,emit:u,render:p,renderCache:y,data:b,setupState:T,ctx:O,inheritAttrs:k}=e;let V,R;const le=Ws(e);try{if(n.shapeFlag&4){const Y=a||t;V=ke(p.call(Y,Y,y,o,T,b,O)),R=c}else{const Y=s;V=ke(Y.length>1?Y(o,{attrs:c,slots:r,emit:u}):Y(o,null)),R=s.props?c:Vo(c)}}catch(Y){xs.length=0,Bs(Y,e,1),V=ue(we)}let N=V;if(R&&k!==!1){const Y=Object.keys(R),{shapeFlag:se}=N;Y.length&&se&7&&(i&&Y.some(dn)&&(R=Ko(R,i)),N=Ye(N,R))}return n.dirs&&(N=Ye(N),N.dirs=N.dirs?N.dirs.concat(n.dirs):n.dirs),n.transition&&(N.transition=n.transition),V=N,Ws(le),V}const Vo=e=>{let s;for(const n in e)(n==="class"||n==="style"||Ns(n))&&((s||(s={}))[n]=e[n]);return s},Ko=(e,s)=>{const n={};for(const t in e)(!dn(t)||!(t.slice(9)in s))&&(n[t]=e[t]);return n};function Go(e,s,n){const{props:t,children:a,component:o}=e,{props:i,children:r,patchFlag:c}=s,u=o.emitsOptions;if(s.dirs||s.transition)return!0;if(n&&c>=0){if(c&1024)return!0;if(c&16)return t?Mt(t,i,u):!!i;if(c&8){const p=s.dynamicProps;for(let y=0;y<p.length;y++){const b=p[y];if(i[b]!==t[b]&&!Ls(u,b))return!0}}}else return(a||r)&&(!r||!r.$stable)?!0:t===i?!1:t?i?Mt(t,i,u):!0:!!i;return!1}function Mt(e,s,n){const t=Object.keys(s);if(t.length!==Object.keys(e).length)return!0;for(let a=0;a<t.length;a++){const o=t[a];if(s[o]!==e[o]&&!Ls(n,o))return!0}return!1}function Xo({vnode:e,parent:s},n){for(;s&&s.subTree===e;)(e=s.vnode).el=n,s=s.parent}const Zo=e=>e.__isSuspense;function Qo(e,s){s&&s.pendingBranch?D(e)?s.effects.push(...e):s.effects.push(e):Lo(e)}function ei(e,s){if(ee){let n=ee.provides;const t=ee.parent&&ee.parent.provides;t===n&&(n=ee.provides=Object.create(t)),n[e]=s}}function $s(e,s,n=!1){const t=ee||ye;if(t){const a=t.parent==null?t.vnode.appContext&&t.vnode.appContext.provides:t.parent.provides;if(a&&e in a)return a[e];if(arguments.length>1)return n&&I(s)?s.call(t.proxy):s}}const Vs={};function Pn(e,s,n){return Ht(e,s,n)}function Ht(e,s,{immediate:n,deep:t,flush:a,onTrack:o,onTrigger:i}=B){const r=ee;let c,u=!1,p=!1;if(ie(e)?(c=()=>e.value,u=kn(e)):cs(e)?(c=()=>e,t=!0):D(e)?(p=!0,u=e.some(N=>cs(N)||kn(N)),c=()=>e.map(N=>{if(ie(N))return N.value;if(cs(N))return us(N);if(I(N))return He(N,r,2)})):I(e)?s?c=()=>He(e,r,2):c=()=>{if(!(r&&r.isUnmounted))return y&&y(),ge(e,r,3,[b])}:c=ve,s&&t){const N=c;c=()=>us(N())}let y,b=N=>{y=R.onStop=()=>{He(N,r,4)}},T;if(zs)if(b=ve,s?n&&ge(s,r,3,[c(),p?[]:void 0,b]):c(),a==="sync"){const N=Vi();T=N.__watcherHandles||(N.__watcherHandles=[])}else return ve;let O=p?new Array(e.length).fill(Vs):Vs;const k=()=>{if(R.active)if(s){const N=R.run();(t||u||(p?N.some((Y,se)=>qs(Y,O[se])):qs(N,O)))&&(y&&y(),ge(s,r,3,[N,O===Vs?void 0:p&&O[0]===Vs?[]:O,b]),O=N)}else R.run()};k.allowRecurse=!!s;let V;a==="sync"?V=k:a==="post"?V=()=>ce(k,r&&r.suspense):(k.pre=!0,r&&(k.id=r.uid),V=()=>Cn(k));const R=new wn(c,V);s?n?k():O=R.run():a==="post"?ce(R.run.bind(R),r&&r.suspense):R.run();const le=()=>{R.stop(),r&&r.scope&&un(r.scope.effects,R)};return T&&T.push(le),le}function si(e,s,n){const t=this.proxy,a=Z(e)?e.includes(".")?Yt(t,e):()=>t[e]:e.bind(t,t);let o;I(s)?o=s:(o=s.handler,n=s);const i=ee;ps(this);const r=Ht(a,o.bind(t),n);return i?ps(i):Qe(),r}function Yt(e,s){const n=s.split(".");return()=>{let t=e;for(let a=0;a<n.length&&t;a++)t=t[n[a]];return t}}function us(e,s){if(!L(e)||e.__v_skip||(s=s||new Set,s.has(e)))return e;if(s.add(e),ie(e))us(e.value,s);else if(D(e))for(let n=0;n<e.length;n++)us(e[n],s);else if(ot(e)||os(e))e.forEach(n=>{us(n,s)});else if(lt(e))for(const n in e)us(e[n],s);return e}function Ut(){const e={isMounted:!1,isLeaving:!1,isUnmounting:!1,leavingVNodes:new Map};return $t(()=>{e.isMounted=!0}),Kt(()=>{e.isUnmounting=!0}),e}const be=[Function,Array],ni={name:"BaseTransition",props:{mode:String,appear:Boolean,persisted:Boolean,onBeforeEnter:be,onEnter:be,onAfterEnter:be,onEnterCancelled:be,onBeforeLeave:be,onLeave:be,onAfterLeave:be,onLeaveCancelled:be,onBeforeAppear:be,onAppear:be,onAfterAppear:be,onAppearCancelled:be},setup(e,{slots:s}){const n=ba(),t=Ut();let a;return()=>{const o=s.default&&En(s.default(),!0);if(!o||!o.length)return;let i=o[0];if(o.length>1){for(const k of o)if(k.type!==we){i=k;break}}const r=E(e),{mode:c}=r;if(t.isLeaving)return qn(i);const u=Lt(i);if(!u)return qn(i);const p=vs(u,r,t,n);js(u,p);const y=n.subTree,b=y&&Lt(y);let T=!1;const{getTransitionKey:O}=u.type;if(O){const k=O();a===void 0?a=k:k!==a&&(a=k,T=!0)}if(b&&b.type!==we&&(!Ze(u,b)||T)){const k=vs(b,r,t,n);if(js(b,k),c==="out-in")return t.isLeaving=!0,k.afterLeave=()=>{t.isLeaving=!1,n.update.active!==!1&&n.update()},qn(i);c==="in-out"&&u.type!==we&&(k.delayLeave=(V,R,le)=>{const N=Bt(t,b);N[String(b.key)]=b,V._leaveCb=()=>{R(),V._leaveCb=void 0,delete p.delayedLeave},p.delayedLeave=le})}return i}}};function Bt(e,s){const{leavingVNodes:n}=e;let t=n.get(s.type);return t||(t=Object.create(null),n.set(s.type,t)),t}function vs(e,s,n,t){const{appear:a,mode:o,persisted:i=!1,onBeforeEnter:r,onEnter:c,onAfterEnter:u,onEnterCancelled:p,onBeforeLeave:y,onLeave:b,onAfterLeave:T,onLeaveCancelled:O,onBeforeAppear:k,onAppear:V,onAfterAppear:R,onAppearCancelled:le}=s,N=String(e.key),Y=Bt(n,e),se=(C,K)=>{C&&ge(C,t,9,K)},We=(C,K)=>{const U=K[1];se(C,K),D(C)?C.every(oe=>oe.length<=1)&&U():C.length<=1&&U()},Ae={mode:o,persisted:i,beforeEnter(C){let K=r;if(!n.isMounted)if(a)K=k||r;else return;C._leaveCb&&C._leaveCb(!0);const U=Y[N];U&&Ze(e,U)&&U.el._leaveCb&&U.el._leaveCb(),se(K,[C])},enter(C){let K=c,U=u,oe=p;if(!n.isMounted)if(a)K=V||c,U=R||u,oe=le||p;else return;let z=!1;const W=C._enterCb=me=>{z||(z=!0,me?se(oe,[C]):se(U,[C]),Ae.delayedLeave&&Ae.delayedLeave(),C._enterCb=void 0)};K?We(K,[C,W]):W()},leave(C,K){const U=String(e.key);if(C._enterCb&&C._enterCb(!0),n.isUnmounting)return K();se(y,[C]);let oe=!1;const z=C._leaveCb=W=>{oe||(oe=!0,K(),W?se(O,[C]):se(T,[C]),C._leaveCb=void 0,Y[U]===e&&delete Y[U])};Y[U]=e,b?We(b,[C,z]):z()},clone(C){return vs(C,s,n,t)}};return Ae}function qn(e){if(Gs(e))return e=Ye(e),e.children=null,e}function Lt(e){return Gs(e)?e.children?e.children[0]:void 0:e}function js(e,s){e.shapeFlag&6&&e.component?js(e.component.subTree,s):e.shapeFlag&128?(e.ssContent.transition=s.clone(e.ssContent),e.ssFallback.transition=s.clone(e.ssFallback)):e.transition=s}function En(e,s=!1,n){let t=[],a=0;for(let o=0;o<e.length;o++){let i=e[o];const r=n==null?i.key:String(n)+String(i.key!=null?i.key:o);i.type===he?(i.patchFlag&128&&a++,t=t.concat(En(i.children,s,r))):(s||i.type!==we)&&t.push(r!=null?Ye(i,{key:r}):i)}if(a>1)for(let o=0;o<t.length;o++)t[o].patchFlag=-2;return t}function Rn(e){return I(e)?{setup:e,name:e.name}:e}const Ks=e=>!!e.type.__asyncLoader,Gs=e=>e.type.__isKeepAlive;function ti(e,s){Wt(e,"a",s)}function ai(e,s){Wt(e,"da",s)}function Wt(e,s,n=ee){const t=e.__wdc||(e.__wdc=()=>{let a=n;for(;a;){if(a.isDeactivated)return;a=a.parent}return e()});if(Xs(s,t,n),n){let a=n.parent;for(;a&&a.parent;)Gs(a.parent.vnode)&&oi(t,s,n,a),a=a.parent}}function oi(e,s,n,t){const a=Xs(s,e,t,!0);Gt(()=>{un(t[s],a)},n)}function Xs(e,s,n=ee,t=!1){if(n){const a=n[e]||(n[e]=[]),o=s.__weh||(s.__weh=(...i)=>{if(n.isUnmounted)return;rs(),ps(n);const r=ge(s,n,e,i);return Qe(),ls(),r});return t?a.unshift(o):a.push(o),o}}const Pe=e=>(s,n=ee)=>(!zs||e==="sp")&&Xs(e,(...t)=>s(...t),n),ii=Pe("bm"),$t=Pe("m"),ri=Pe("bu"),Vt=Pe("u"),Kt=Pe("bum"),Gt=Pe("um"),li=Pe("sp"),ci=Pe("rtg"),di=Pe("rtc");function ui(e,s=ee){Xs("ec",e,s)}function Ke(e,s,n,t){const a=e.dirs,o=s&&s.dirs;for(let i=0;i<a.length;i++){const r=a[i];o&&(r.oldValue=o[i].value);let c=r.dir[t];c&&(rs(),ge(c,n,8,[e.el,r,e,s]),ls())}}const Xt="components";function Zs(e,s){return hi(Xt,e,!0,s)||e}const pi=Symbol();function hi(e,s,n=!0,t=!1){const a=ye||ee;if(a){const o=a.type;if(e===Xt){const r=Bi(o,!1);if(r&&(r===s||r===Je(s)||r===Ps(Je(s))))return o}const i=Zt(a[e]||o[e],s)||Zt(a.appContext[e],s);return!i&&t?o:i}}function Zt(e,s){return e&&(e[s]||e[Je(s)]||e[Ps(Je(s))])}function Qt(e,s,n,t){let a;const o=n&&n[t];if(D(e)||Z(e)){a=new Array(e.length);for(let i=0,r=e.length;i<r;i++)a[i]=s(e[i],i,void 0,o&&o[i])}else if(typeof e=="number"){a=new Array(e);for(let i=0;i<e;i++)a[i]=s(i+1,i,void 0,o&&o[i])}else if(L(e))if(e[Symbol.iterator])a=Array.from(e,(i,r)=>s(i,r,void 0,o&&o[r]));else{const i=Object.keys(e);a=new Array(i.length);for(let r=0,c=i.length;r<c;r++){const u=i[r];a[r]=s(e[u],u,r,o&&o[r])}}else a=[];return n&&(n[t]=a),a}const Fn=e=>e?va(e)?Gn(e)||e.proxy:Fn(e.parent):null,ws=Q(Object.create(null),{$:e=>e,$el:e=>e.vnode.el,$data:e=>e.data,$props:e=>e.props,$attrs:e=>e.attrs,$slots:e=>e.slots,$refs:e=>e.refs,$parent:e=>Fn(e.parent),$root:e=>Fn(e.root),$emit:e=>e.emit,$options:e=>Yn(e),$forceUpdate:e=>e.f||(e.f=()=>Cn(e.update)),$nextTick:e=>e.n||(e.n=Yo.bind(e.proxy)),$watch:e=>si.bind(e)}),Mn=(e,s)=>e!==B&&!e.__isScriptSetup&&q(e,s),mi={get({_:e},s){const{ctx:n,setupState:t,data:a,props:o,accessCache:i,type:r,appContext:c}=e;let u;if(s[0]!=="$"){const T=i[s];if(T!==void 0)switch(T){case 1:return t[s];case 2:return a[s];case 4:return n[s];case 3:return o[s]}else{if(Mn(t,s))return i[s]=1,t[s];if(a!==B&&q(a,s))return i[s]=2,a[s];if((u=e.propsOptions[0])&&q(u,s))return i[s]=3,o[s];if(n!==B&&q(n,s))return i[s]=4,n[s];Hn&&(i[s]=0)}}const p=ws[s];let y,b;if(p)return s==="$attrs"&&pe(e,"get",s),p(e);if((y=r.__cssModules)&&(y=y[s]))return y;if(n!==B&&q(n,s))return i[s]=4,n[s];if(b=c.config.globalProperties,q(b,s))return b[s]},set({_:e},s,n){const{data:t,setupState:a,ctx:o}=e;return Mn(a,s)?(a[s]=n,!0):t!==B&&q(t,s)?(t[s]=n,!0):q(e.props,s)||s[0]==="$"&&s.slice(1)in e?!1:(o[s]=n,!0)},has({_:{data:e,setupState:s,accessCache:n,ctx:t,appContext:a,propsOptions:o}},i){let r;return!!n[i]||e!==B&&q(e,i)||Mn(s,i)||(r=o[0])&&q(r,i)||q(t,i)||q(ws,i)||q(a.config.globalProperties,i)},defineProperty(e,s,n){return n.get!=null?e._.accessCache[s]=0:q(n,"value")&&this.set(e,s,n.value,null),Reflect.defineProperty(e,s,n)}};let Hn=!0;function fi(e){const s=Yn(e),n=e.proxy,t=e.ctx;Hn=!1,s.beforeCreate&&ea(s.beforeCreate,e,"bc");const{data:a,computed:o,methods:i,watch:r,provide:c,inject:u,created:p,beforeMount:y,mounted:b,beforeUpdate:T,updated:O,activated:k,deactivated:V,beforeDestroy:R,beforeUnmount:le,destroyed:N,unmounted:Y,render:se,renderTracked:We,renderTriggered:Ae,errorCaptured:C,serverPrefetch:K,expose:U,inheritAttrs:oe,components:z,directives:W,filters:me}=s;if(u&&gi(u,t,null,e.appContext.config.unwrapInjectedRef),i)for(const G in i){const M=i[G];I(M)&&(t[G]=M.bind(n))}if(a){const G=a.call(n,n);L(G)&&(e.data=Sn(G))}if(Hn=!0,o)for(const G in o){const M=o[G],ns=I(M)?M.bind(n,n):I(M.get)?M.get.bind(n,n):ve,on=!I(M)&&I(M.set)?M.set.bind(n):ve,ts=Wi({get:ns,set:on});Object.defineProperty(t,G,{enumerable:!0,configurable:!0,get:()=>ts.value,set:Ne=>ts.value=Ne})}if(r)for(const G in r)sa(r[G],t,n,G);if(c){const G=I(c)?c.call(n):c;Reflect.ownKeys(G).forEach(M=>{ei(M,G[M])})}p&&ea(p,e,"c");function ne(G,M){D(M)?M.forEach(ns=>G(ns.bind(n))):M&&G(M.bind(n))}if(ne(ii,y),ne($t,b),ne(ri,T),ne(Vt,O),ne(ti,k),ne(ai,V),ne(ui,C),ne(di,We),ne(ci,Ae),ne(Kt,le),ne(Gt,Y),ne(li,K),D(U))if(U.length){const G=e.exposed||(e.exposed={});U.forEach(M=>{Object.defineProperty(G,M,{get:()=>n[M],set:ns=>n[M]=ns})})}else e.exposed||(e.exposed={});se&&e.render===ve&&(e.render=se),oe!=null&&(e.inheritAttrs=oe),z&&(e.components=z),W&&(e.directives=W)}function gi(e,s,n=ve,t=!1){D(e)&&(e=Un(e));for(const a in e){const o=e[a];let i;L(o)?"default"in o?i=$s(o.from||a,o.default,!0):i=$s(o.from||a):i=$s(o),ie(i)&&t?Object.defineProperty(s,a,{enumerable:!0,configurable:!0,get:()=>i.value,set:r=>i.value=r}):s[a]=i}}function ea(e,s,n){ge(D(e)?e.map(t=>t.bind(s.proxy)):e.bind(s.proxy),s,n)}function sa(e,s,n,t){const a=t.includes(".")?Yt(n,t):()=>n[t];if(Z(e)){const o=s[e];I(o)&&Pn(a,o)}else if(I(e))Pn(a,e.bind(n));else if(L(e))if(D(e))e.forEach(o=>sa(o,s,n,t));else{const o=I(e.handler)?e.handler.bind(n):s[e.handler];I(o)&&Pn(a,o,e)}}function Yn(e){const s=e.type,{mixins:n,extends:t}=s,{mixins:a,optionsCache:o,config:{optionMergeStrategies:i}}=e.appContext,r=o.get(s);let c;return r?c=r:!a.length&&!n&&!t?c=s:(c={},a.length&&a.forEach(u=>Qs(c,u,i,!0)),Qs(c,s,i)),L(s)&&o.set(s,c),c}function Qs(e,s,n,t=!1){const{mixins:a,extends:o}=s;o&&Qs(e,o,n,!0),a&&a.forEach(i=>Qs(e,i,n,!0));for(const i in s)if(!(t&&i==="expose")){const r=yi[i]||n&&n[i];e[i]=r?r(e[i],s[i]):s[i]}return e}const yi={data:na,props:Ge,emits:Ge,methods:Ge,computed:Ge,beforeCreate:re,created:re,beforeMount:re,mounted:re,beforeUpdate:re,updated:re,beforeDestroy:re,beforeUnmount:re,destroyed:re,unmounted:re,activated:re,deactivated:re,errorCaptured:re,serverPrefetch:re,components:Ge,directives:Ge,watch:vi,provide:na,inject:bi};function na(e,s){return s?e?function(){return Q(I(e)?e.call(this,this):e,I(s)?s.call(this,this):s)}:s:e}function bi(e,s){return Ge(Un(e),Un(s))}function Un(e){if(D(e)){const s={};for(let n=0;n<e.length;n++)s[e[n]]=e[n];return s}return e}function re(e,s){return e?[...new Set([].concat(e,s))]:s}function Ge(e,s){return e?Q(Q(Object.create(null),e),s):s}function vi(e,s){if(!e)return s;if(!s)return e;const n=Q(Object.create(null),e);for(const t in s)n[t]=re(e[t],s[t]);return n}function ji(e,s,n,t=!1){const a={},o={};Es(o,nn,1),e.propsDefaults=Object.create(null),ta(e,s,a,o);for(const i in e.propsOptions[0])i in a||(a[i]=void 0);n?e.props=t?a:Oo(a):e.type.props?e.props=a:e.props=o,e.attrs=o}function wi(e,s,n,t){const{props:a,attrs:o,vnode:{patchFlag:i}}=e,r=E(a),[c]=e.propsOptions;let u=!1;if((t||i>0)&&!(i&16)){if(i&8){const p=e.vnode.dynamicProps;for(let y=0;y<p.length;y++){let b=p[y];if(Ls(e.emitsOptions,b))continue;const T=s[b];if(c)if(q(o,b))T!==o[b]&&(o[b]=T,u=!0);else{const O=Je(b);a[O]=Bn(c,r,O,T,e,!1)}else T!==o[b]&&(o[b]=T,u=!0)}}}else{ta(e,s,a,o)&&(u=!0);let p;for(const y in r)(!s||!q(s,y)&&((p=is(y))===y||!q(s,p)))&&(c?n&&(n[y]!==void 0||n[p]!==void 0)&&(a[y]=Bn(c,r,y,void 0,e,!0)):delete a[y]);if(o!==r)for(const y in o)(!s||!q(s,y))&&(delete o[y],u=!0)}u&&Ce(e,"set","$attrs")}function ta(e,s,n,t){const[a,o]=e.propsOptions;let i=!1,r;if(s)for(let c in s){if(Cs(c))continue;const u=s[c];let p;a&&q(a,p=Je(c))?!o||!o.includes(p)?n[p]=u:(r||(r={}))[p]=u:Ls(e.emitsOptions,c)||(!(c in t)||u!==t[c])&&(t[c]=u,i=!0)}if(o){const c=E(n),u=r||B;for(let p=0;p<o.length;p++){const y=o[p];n[y]=Bn(a,c,y,u[y],e,!q(u,y))}}return i}function Bn(e,s,n,t,a,o){const i=e[n];if(i!=null){const r=q(i,"default");if(r&&t===void 0){const c=i.default;if(i.type!==Function&&I(c)){const{propsDefaults:u}=a;n in u?t=u[n]:(ps(a),t=u[n]=c.call(null,s),Qe())}else t=c}i[0]&&(o&&!r?t=!1:i[1]&&(t===""||t===is(n))&&(t=!0))}return t}function aa(e,s,n=!1){const t=s.propsCache,a=t.get(e);if(a)return a;const o=e.props,i={},r=[];let c=!1;if(!I(e)){const p=y=>{c=!0;const[b,T]=aa(y,s,!0);Q(i,b),T&&r.push(...T)};!n&&s.mixins.length&&s.mixins.forEach(p),e.extends&&p(e.extends),e.mixins&&e.mixins.forEach(p)}if(!o&&!c)return L(e)&&t.set(e,as),as;if(D(o))for(let p=0;p<o.length;p++){const y=Je(o[p]);oa(y)&&(i[y]=B)}else if(o)for(const p in o){const y=Je(p);if(oa(y)){const b=o[p],T=i[y]=D(b)||I(b)?{type:b}:Object.assign({},b);if(T){const O=la(Boolean,T.type),k=la(String,T.type);T[0]=O>-1,T[1]=k<0||O<k,(O>-1||q(T,"default"))&&r.push(y)}}}const u=[i,r];return L(e)&&t.set(e,u),u}function oa(e){return e[0]!=="$"}function ia(e){const s=e&&e.toString().match(/^\s*function (\w+)/);return s?s[1]:e===null?"null":""}function ra(e,s){return ia(e)===ia(s)}function la(e,s){return D(s)?s.findIndex(n=>ra(n,e)):I(s)&&ra(s,e)?0:-1}const ca=e=>e[0]==="_"||e==="$stable",Ln=e=>D(e)?e.map(ke):[ke(e)],xi=(e,s,n)=>{if(s._n)return s;const t=Ft((...a)=>Ln(s(...a)),n);return t._c=!1,t},da=(e,s,n)=>{const t=e._ctx;for(const a in e){if(ca(a))continue;const o=e[a];if(I(o))s[a]=xi(a,o,t);else if(o!=null){const i=Ln(o);s[a]=()=>i}}},ua=(e,s)=>{const n=Ln(s);e.slots.default=()=>n},_i=(e,s)=>{if(e.vnode.shapeFlag&32){const n=s._;n?(e.slots=E(s),Es(s,"_",n)):da(s,e.slots={})}else e.slots={},s&&ua(e,s);Es(e.slots,nn,1)},zi=(e,s,n)=>{const{vnode:t,slots:a}=e;let o=!0,i=B;if(t.shapeFlag&32){const r=s._;r?n&&r===1?o=!1:(Q(a,s),!n&&r===1&&delete a._):(o=!s.$stable,da(s,a)),i=s}else s&&(ua(e,s),i={default:1});if(o)for(const r in a)!ca(r)&&!(r in i)&&delete a[r]};function pa(){return{app:null,config:{isNativeTag:Qa,performance:!1,globalProperties:{},optionMergeStrategies:{},errorHandler:void 0,warnHandler:void 0,compilerOptions:{}},mixins:[],components:{},directives:{},provides:Object.create(null),optionsCache:new WeakMap,propsCache:new WeakMap,emitsCache:new WeakMap}}let Ji=0;function Si(e,s){return function(t,a=null){I(t)||(t=Object.assign({},t)),a!=null&&!L(a)&&(a=null);const o=pa(),i=new Set;let r=!1;const c=o.app={_uid:Ji++,_component:t,_props:a,_container:null,_context:o,_instance:null,version:Ki,get config(){return o.config},set config(u){},use(u,...p){return i.has(u)||(u&&I(u.install)?(i.add(u),u.install(c,...p)):I(u)&&(i.add(u),u(c,...p))),c},mixin(u){return o.mixins.includes(u)||o.mixins.push(u),c},component(u,p){return p?(o.components[u]=p,c):o.components[u]},directive(u,p){return p?(o.directives[u]=p,c):o.directives[u]},mount(u,p,y){if(!r){const b=ue(t,a);return b.appContext=o,p&&s?s(b,u):e(b,u,y),r=!0,c._container=u,u.__vue_app__=c,Gn(b.component)||b.component.proxy}},unmount(){r&&(e(null,c._container),delete c._container.__vue_app__)},provide(u,p){return o.provides[u]=p,c}};return c}}function Wn(e,s,n,t,a=!1){if(D(e)){e.forEach((b,T)=>Wn(b,s&&(D(s)?s[T]:s),n,t,a));return}if(Ks(t)&&!a)return;const o=t.shapeFlag&4?Gn(t.component)||t.component.proxy:t.el,i=a?null:o,{i:r,r:c}=e,u=s&&s.r,p=r.refs===B?r.refs={}:r.refs,y=r.setupState;if(u!=null&&u!==c&&(Z(u)?(p[u]=null,q(y,u)&&(y[u]=null)):ie(u)&&(u.value=null)),I(c))He(c,r,12,[i,p]);else{const b=Z(c),T=ie(c);if(b||T){const O=()=>{if(e.f){const k=b?q(y,c)?y[c]:p[c]:c.value;a?D(k)&&un(k,o):D(k)?k.includes(o)||k.push(o):b?(p[c]=[o],q(y,c)&&(y[c]=p[c])):(c.value=[o],e.k&&(p[e.k]=c.value))}else b?(p[c]=i,q(y,c)&&(y[c]=i)):T&&(c.value=i,e.k&&(p[e.k]=i))};i?(O.id=-1,ce(O,n)):O()}}}const ce=Qo;function Ti(e){return ki(e)}function ki(e,s){const n=oo();n.__VUE__=!0;const{insert:t,remove:a,patchProp:o,createElement:i,createText:r,createComment:c,setText:u,setElementText:p,parentNode:y,nextSibling:b,setScopeId:T=ve,insertStaticContent:O}=e,k=(l,d,h,f=null,m=null,j=null,x=!1,v=null,w=!!d.dynamicChildren)=>{if(l===d)return;l&&!Ze(l,d)&&(f=rn(l),Ne(l,m,j,!0),l=null),d.patchFlag===-2&&(w=!1,d.dynamicChildren=null);const{type:g,ref:J,shapeFlag:_}=d;switch(g){case en:V(l,d,h,f);break;case we:R(l,d,h,f);break;case sn:l==null&&le(d,h,f,x);break;case he:z(l,d,h,f,m,j,x,v,w);break;default:_&1?se(l,d,h,f,m,j,x,v,w):_&6?W(l,d,h,f,m,j,x,v,w):(_&64||_&128)&&g.process(l,d,h,f,m,j,x,v,w,hs)}J!=null&&m&&Wn(J,l&&l.ref,j,d||l,!d)},V=(l,d,h,f)=>{if(l==null)t(d.el=r(d.children),h,f);else{const m=d.el=l.el;d.children!==l.children&&u(m,d.children)}},R=(l,d,h,f)=>{l==null?t(d.el=c(d.children||""),h,f):d.el=l.el},le=(l,d,h,f)=>{[l.el,l.anchor]=O(l.children,d,h,f,l.el,l.anchor)},N=({el:l,anchor:d},h,f)=>{let m;for(;l&&l!==d;)m=b(l),t(l,h,f),l=m;t(d,h,f)},Y=({el:l,anchor:d})=>{let h;for(;l&&l!==d;)h=b(l),a(l),l=h;a(d)},se=(l,d,h,f,m,j,x,v,w)=>{x=x||d.type==="svg",l==null?We(d,h,f,m,j,x,v,w):K(l,d,m,j,x,v,w)},We=(l,d,h,f,m,j,x,v)=>{let w,g;const{type:J,props:_,shapeFlag:S,transition:A,dirs:P}=l;if(w=l.el=i(l.type,j,_&&_.is,_),S&8?p(w,l.children):S&16&&C(l.children,w,null,f,m,j&&J!=="foreignObject",x,v),P&&Ke(l,null,f,"created"),_){for(const F in _)F!=="value"&&!Cs(F)&&o(w,F,null,_[F],j,l.children,f,m,Ee);"value"in _&&o(w,"value",null,_.value),(g=_.onVnodeBeforeMount)&&De(g,f,l)}Ae(w,l,l.scopeId,x,f),P&&Ke(l,null,f,"beforeMount");const H=(!m||m&&!m.pendingBranch)&&A&&!A.persisted;H&&A.beforeEnter(w),t(w,d,h),((g=_&&_.onVnodeMounted)||H||P)&&ce(()=>{g&&De(g,f,l),H&&A.enter(w),P&&Ke(l,null,f,"mounted")},m)},Ae=(l,d,h,f,m)=>{if(h&&T(l,h),f)for(let j=0;j<f.length;j++)T(l,f[j]);if(m){let j=m.subTree;if(d===j){const x=m.vnode;Ae(l,x,x.scopeId,x.slotScopeIds,m.parent)}}},C=(l,d,h,f,m,j,x,v,w=0)=>{for(let g=w;g<l.length;g++){const J=l[g]=v?Ue(l[g]):ke(l[g]);k(null,J,d,h,f,m,j,x,v)}},K=(l,d,h,f,m,j,x)=>{const v=d.el=l.el;let{patchFlag:w,dynamicChildren:g,dirs:J}=d;w|=l.patchFlag&16;const _=l.props||B,S=d.props||B;let A;h&&Xe(h,!1),(A=S.onVnodeBeforeUpdate)&&De(A,h,d,l),J&&Ke(d,l,h,"beforeUpdate"),h&&Xe(h,!0);const P=m&&d.type!=="foreignObject";if(g?U(l.dynamicChildren,g,v,h,f,P,j):x||M(l,d,v,null,h,f,P,j,!1),w>0){if(w&16)oe(v,d,_,S,h,f,m);else if(w&2&&_.class!==S.class&&o(v,"class",null,S.class,m),w&4&&o(v,"style",_.style,S.style,m),w&8){const H=d.dynamicProps;for(let F=0;F<H.length;F++){const X=H[F],ze=_[X],ms=S[X];(ms!==ze||X==="value")&&o(v,X,ze,ms,m,l.children,h,f,Ee)}}w&1&&l.children!==d.children&&p(v,d.children)}else!x&&g==null&&oe(v,d,_,S,h,f,m);((A=S.onVnodeUpdated)||J)&&ce(()=>{A&&De(A,h,d,l),J&&Ke(d,l,h,"updated")},f)},U=(l,d,h,f,m,j,x)=>{for(let v=0;v<d.length;v++){const w=l[v],g=d[v],J=w.el&&(w.type===he||!Ze(w,g)||w.shapeFlag&70)?y(w.el):h;k(w,g,J,null,f,m,j,x,!0)}},oe=(l,d,h,f,m,j,x)=>{if(h!==f){if(h!==B)for(const v in h)!Cs(v)&&!(v in f)&&o(l,v,h[v],null,x,d.children,m,j,Ee);for(const v in f){if(Cs(v))continue;const w=f[v],g=h[v];w!==g&&v!=="value"&&o(l,v,g,w,x,d.children,m,j,Ee)}"value"in f&&o(l,"value",h.value,f.value)}},z=(l,d,h,f,m,j,x,v,w)=>{const g=d.el=l?l.el:r(""),J=d.anchor=l?l.anchor:r("");let{patchFlag:_,dynamicChildren:S,slotScopeIds:A}=d;A&&(v=v?v.concat(A):A),l==null?(t(g,h,f),t(J,h,f),C(d.children,h,J,m,j,x,v,w)):_>0&&_&64&&S&&l.dynamicChildren?(U(l.dynamicChildren,S,h,m,j,x,v),(d.key!=null||m&&d===m.subTree)&&ha(l,d,!0)):M(l,d,h,J,m,j,x,v,w)},W=(l,d,h,f,m,j,x,v,w)=>{d.slotScopeIds=v,l==null?d.shapeFlag&512?m.ctx.activate(d,h,f,x,w):me(d,h,f,m,j,x,w):Ts(l,d,w)},me=(l,d,h,f,m,j,x)=>{const v=l.component=Fi(l,f,m);if(Gs(l)&&(v.ctx.renderer=hs),Mi(v),v.asyncDep){if(m&&m.registerDep(v,ne),!l.el){const w=v.subTree=ue(we);R(null,w,d,h)}return}ne(v,l,d,h,m,j,x)},Ts=(l,d,h)=>{const f=d.component=l.component;if(Go(l,d,h))if(f.asyncDep&&!f.asyncResolved){G(f,d,h);return}else f.next=d,Bo(f.update),f.update();else d.el=l.el,f.vnode=d},ne=(l,d,h,f,m,j,x)=>{const v=()=>{if(l.isMounted){let{next:J,bu:_,u:S,parent:A,vnode:P}=l,H=J,F;Xe(l,!1),J?(J.el=P.el,G(l,J,x)):J=P,_&&fn(_),(F=J.props&&J.props.onVnodeBeforeUpdate)&&De(F,A,J,P),Xe(l,!0);const X=On(l),ze=l.subTree;l.subTree=X,k(ze,X,y(ze.el),rn(ze),l,m,j),J.el=X.el,H===null&&Xo(l,X.el),S&&ce(S,m),(F=J.props&&J.props.onVnodeUpdated)&&ce(()=>De(F,A,J,P),m)}else{let J;const{el:_,props:S}=d,{bm:A,m:P,parent:H}=l,F=Ks(d);if(Xe(l,!1),A&&fn(A),!F&&(J=S&&S.onVnodeBeforeMount)&&De(J,H,d),Xe(l,!0),_&&nt){const X=()=>{l.subTree=On(l),nt(_,l.subTree,l,m,null)};F?d.type.__asyncLoader().then(()=>!l.isUnmounted&&X()):X()}else{const X=l.subTree=On(l);k(null,X,h,f,l,m,j),d.el=X.el}if(P&&ce(P,m),!F&&(J=S&&S.onVnodeMounted)){const X=d;ce(()=>De(J,H,X),m)}(d.shapeFlag&256||H&&Ks(H.vnode)&&H.vnode.shapeFlag&256)&&l.a&&ce(l.a,m),l.isMounted=!0,d=h=f=null}},w=l.effect=new wn(v,()=>Cn(g),l.scope),g=l.update=()=>w.run();g.id=l.uid,Xe(l,!0),g()},G=(l,d,h)=>{d.component=l;const f=l.vnode.props;l.vnode=d,l.next=null,wi(l,d.props,f,h),zi(l,d.children,h),rs(),Ot(),ls()},M=(l,d,h,f,m,j,x,v,w=!1)=>{const g=l&&l.children,J=l?l.shapeFlag:0,_=d.children,{patchFlag:S,shapeFlag:A}=d;if(S>0){if(S&128){on(g,_,h,f,m,j,x,v,w);return}else if(S&256){ns(g,_,h,f,m,j,x,v,w);return}}A&8?(J&16&&Ee(g,m,j),_!==g&&p(h,_)):J&16?A&16?on(g,_,h,f,m,j,x,v,w):Ee(g,m,j,!0):(J&8&&p(h,""),A&16&&C(_,h,f,m,j,x,v,w))},ns=(l,d,h,f,m,j,x,v,w)=>{l=l||as,d=d||as;const g=l.length,J=d.length,_=Math.min(g,J);let S;for(S=0;S<_;S++){const A=d[S]=w?Ue(d[S]):ke(d[S]);k(l[S],A,h,null,m,j,x,v,w)}g>J?Ee(l,m,j,!0,!1,_):C(d,h,f,m,j,x,v,w,_)},on=(l,d,h,f,m,j,x,v,w)=>{let g=0;const J=d.length;let _=l.length-1,S=J-1;for(;g<=_&&g<=S;){const A=l[g],P=d[g]=w?Ue(d[g]):ke(d[g]);if(Ze(A,P))k(A,P,h,null,m,j,x,v,w);else break;g++}for(;g<=_&&g<=S;){const A=l[_],P=d[S]=w?Ue(d[S]):ke(d[S]);if(Ze(A,P))k(A,P,h,null,m,j,x,v,w);else break;_--,S--}if(g>_){if(g<=S){const A=S+1,P=A<J?d[A].el:f;for(;g<=S;)k(null,d[g]=w?Ue(d[g]):ke(d[g]),h,P,m,j,x,v,w),g++}}else if(g>S)for(;g<=_;)Ne(l[g],m,j,!0),g++;else{const A=g,P=g,H=new Map;for(g=P;g<=S;g++){const fe=d[g]=w?Ue(d[g]):ke(d[g]);fe.key!=null&&H.set(fe.key,g)}let F,X=0;const ze=S-P+1;let ms=!1,La=0;const ks=new Array(ze);for(g=0;g<ze;g++)ks[g]=0;for(g=A;g<=_;g++){const fe=l[g];if(X>=ze){Ne(fe,m,j,!0);continue}let Ie;if(fe.key!=null)Ie=H.get(fe.key);else for(F=P;F<=S;F++)if(ks[F-P]===0&&Ze(fe,d[F])){Ie=F;break}Ie===void 0?Ne(fe,m,j,!0):(ks[Ie-P]=g+1,Ie>=La?La=Ie:ms=!0,k(fe,d[Ie],h,null,m,j,x,v,w),X++)}const Wa=ms?Di(ks):as;for(F=Wa.length-1,g=ze-1;g>=0;g--){const fe=P+g,Ie=d[fe],$a=fe+1<J?d[fe+1].el:f;ks[g]===0?k(null,Ie,h,$a,m,j,x,v,w):ms&&(F<0||g!==Wa[F]?ts(Ie,h,$a,2):F--)}}},ts=(l,d,h,f,m=null)=>{const{el:j,type:x,transition:v,children:w,shapeFlag:g}=l;if(g&6){ts(l.component.subTree,d,h,f);return}if(g&128){l.suspense.move(d,h,f);return}if(g&64){x.move(l,d,h,hs);return}if(x===he){t(j,d,h);for(let _=0;_<w.length;_++)ts(w[_],d,h,f);t(l.anchor,d,h);return}if(x===sn){N(l,d,h);return}if(f!==2&&g&1&&v)if(f===0)v.beforeEnter(j),t(j,d,h),ce(()=>v.enter(j),m);else{const{leave:_,delayLeave:S,afterLeave:A}=v,P=()=>t(j,d,h),H=()=>{_(j,()=>{P(),A&&A()})};S?S(j,P,H):H()}else t(j,d,h)},Ne=(l,d,h,f=!1,m=!1)=>{const{type:j,props:x,ref:v,children:w,dynamicChildren:g,shapeFlag:J,patchFlag:_,dirs:S}=l;if(v!=null&&Wn(v,null,h,l,!0),J&256){d.ctx.deactivate(l);return}const A=J&1&&S,P=!Ks(l);let H;if(P&&(H=x&&x.onVnodeBeforeUnmount)&&De(H,d,l),J&6)dl(l.component,h,f);else{if(J&128){l.suspense.unmount(h,f);return}A&&Ke(l,null,d,"beforeUnmount"),J&64?l.type.remove(l,d,h,m,hs,f):g&&(j!==he||_>0&&_&64)?Ee(g,d,h,!1,!0):(j===he&&_&384||!m&&J&16)&&Ee(w,d,h),f&&Ua(l)}(P&&(H=x&&x.onVnodeUnmounted)||A)&&ce(()=>{H&&De(H,d,l),A&&Ke(l,null,d,"unmounted")},h)},Ua=l=>{const{type:d,el:h,anchor:f,transition:m}=l;if(d===he){cl(h,f);return}if(d===sn){Y(l);return}const j=()=>{a(h),m&&!m.persisted&&m.afterLeave&&m.afterLeave()};if(l.shapeFlag&1&&m&&!m.persisted){const{leave:x,delayLeave:v}=m,w=()=>x(h,j);v?v(l.el,j,w):w()}else j()},cl=(l,d)=>{let h;for(;l!==d;)h=b(l),a(l),l=h;a(d)},dl=(l,d,h)=>{const{bum:f,scope:m,update:j,subTree:x,um:v}=l;f&&fn(f),m.stop(),j&&(j.active=!1,Ne(x,l,d,h)),v&&ce(v,d),ce(()=>{l.isUnmounted=!0},d),d&&d.pendingBranch&&!d.isUnmounted&&l.asyncDep&&!l.asyncResolved&&l.suspenseId===d.pendingId&&(d.deps--,d.deps===0&&d.resolve())},Ee=(l,d,h,f=!1,m=!1,j=0)=>{for(let x=j;x<l.length;x++)Ne(l[x],d,h,f,m)},rn=l=>l.shapeFlag&6?rn(l.component.subTree):l.shapeFlag&128?l.suspense.next():b(l.anchor||l.el),Ba=(l,d,h)=>{l==null?d._vnode&&Ne(d._vnode,null,null,!0):k(d._vnode||null,l,d,null,null,null,h),Ot(),Pt(),d._vnode=l},hs={p:k,um:Ne,m:ts,r:Ua,mt:me,mc:C,pc:M,pbc:U,n:rn,o:e};let st,nt;return s&&([st,nt]=s(hs)),{render:Ba,hydrate:st,createApp:Si(Ba,st)}}function Xe({effect:e,update:s},n){e.allowRecurse=s.allowRecurse=n}function ha(e,s,n=!1){const t=e.children,a=s.children;if(D(t)&&D(a))for(let o=0;o<t.length;o++){const i=t[o];let r=a[o];r.shapeFlag&1&&!r.dynamicChildren&&((r.patchFlag<=0||r.patchFlag===32)&&(r=a[o]=Ue(a[o]),r.el=i.el),n||ha(i,r)),r.type===en&&(r.el=i.el)}}function Di(e){const s=e.slice(),n=[0];let t,a,o,i,r;const c=e.length;for(t=0;t<c;t++){const u=e[t];if(u!==0){if(a=n[n.length-1],e[a]<u){s[t]=a,n.push(t);continue}for(o=0,i=n.length-1;o<i;)r=o+i>>1,e[n[r]]<u?o=r+1:i=r;u<e[n[o]]&&(o>0&&(s[t]=n[o-1]),n[o]=t)}}for(o=n.length,i=n[o-1];o-- >0;)n[o]=i,i=s[i];return n}const Ai=e=>e.__isTeleport,he=Symbol(void 0),en=Symbol(void 0),we=Symbol(void 0),sn=Symbol(void 0),xs=[];let xe=null;function de(e=!1){xs.push(xe=e?null:[])}function Ni(){xs.pop(),xe=xs[xs.length-1]||null}let _s=1;function ma(e){_s+=e}function fa(e){return e.dynamicChildren=_s>0?xe||as:null,Ni(),_s>0&&xe&&xe.push(e),e}function _e(e,s,n,t,a,o){return fa(ae(e,s,n,t,a,o,!0))}function $n(e,s,n,t,a){return fa(ue(e,s,n,t,a,!0))}function Ii(e){return e?e.__v_isVNode===!0:!1}function Ze(e,s){return e.type===s.type&&e.key===s.key}const nn="__vInternal",ya=({key:e})=>e??null,tn=({ref:e,ref_key:s,ref_for:n})=>e!=null?Z(e)||ie(e)||I(e)?{i:ye,r:e,k:s,f:!!n}:e:null;function ae(e,s=null,n=null,t=0,a=null,o=e===he?0:1,i=!1,r=!1){const c={__v_isVNode:!0,__v_skip:!0,type:e,props:s,key:s&&ya(s),ref:s&&tn(s),scopeId:Rt,slotScopeIds:null,children:n,component:null,suspense:null,ssContent:null,ssFallback:null,dirs:null,transition:null,el:null,anchor:null,target:null,targetAnchor:null,staticCount:0,shapeFlag:o,patchFlag:t,dynamicProps:a,dynamicChildren:null,appContext:null,ctx:ye};return r?(Kn(c,n),o&128&&e.normalize(c)):n&&(c.shapeFlag|=Z(n)?8:16),_s>0&&!i&&xe&&(c.patchFlag>0||o&6)&&c.patchFlag!==32&&xe.push(c),c}const ue=Ci;function Ci(e,s=null,n=null,t=0,a=null,o=!1){if((!e||e===pi)&&(e=we),Ii(e)){const r=Ye(e,s,!0);return n&&Kn(r,n),_s>0&&!o&&xe&&(r.shapeFlag&6?xe[xe.indexOf(e)]=r:xe.push(r)),r.patchFlag|=-2,r}if(Li(e)&&(e=e.__vccOpts),s){s=Oi(s);let{class:r,style:c}=s;r&&!Z(r)&&(s.class=Ds(r)),L(c)&&(kt(c)&&!D(c)&&(c=Q({},c)),s.style=cn(c))}const i=Z(e)?1:Zo(e)?128:Ai(e)?64:L(e)?4:I(e)?2:0;return ae(e,s,n,t,a,i,o,!0)}function Oi(e){return e?kt(e)||nn in e?Q({},e):e:null}function Ye(e,s,n=!1){const{props:t,ref:a,patchFlag:o,children:i}=e,r=s?qi(t||{},s):t;return{__v_isVNode:!0,__v_skip:!0,type:e.type,props:r,key:r&&ya(r),ref:s&&s.ref?n&&a?D(a)?a.concat(tn(s)):[a,tn(s)]:tn(s):a,scopeId:e.scopeId,slotScopeIds:e.slotScopeIds,children:i,target:e.target,targetAnchor:e.targetAnchor,staticCount:e.staticCount,shapeFlag:e.shapeFlag,patchFlag:s&&e.type!==he?o===-1?16:o|16:o,dynamicProps:e.dynamicProps,dynamicChildren:e.dynamicChildren,appContext:e.appContext,dirs:e.dirs,transition:e.transition,component:e.component,suspense:e.suspense,ssContent:e.ssContent&&Ye(e.ssContent),ssFallback:e.ssFallback&&Ye(e.ssFallback),el:e.el,anchor:e.anchor,ctx:e.ctx}}function an(e=" ",s=0){return ue(en,null,e,s)}function Pi(e,s){const n=ue(sn,null,e);return n.staticCount=s,n}function Vn(e="",s=!1){return s?(de(),$n(we,null,e)):ue(we,null,e)}function ke(e){return e==null||typeof e=="boolean"?ue(we):D(e)?ue(he,null,e.slice()):typeof e=="object"?Ue(e):ue(en,null,String(e))}function Ue(e){return e.el===null&&e.patchFlag!==-1||e.memo?e:Ye(e)}function Kn(e,s){let n=0;const{shapeFlag:t}=e;if(s==null)s=null;else if(D(s))n=16;else if(typeof s=="object")if(t&65){const a=s.default;a&&(a._c&&(a._d=!1),Kn(e,a()),a._c&&(a._d=!0));return}else{n=32;const a=s._;!a&&!(nn in s)?s._ctx=ye:a===3&&ye&&(ye.slots._===1?s._=1:(s._=2,e.patchFlag|=1024))}else I(s)?(s={default:s,_ctx:ye},n=32):(s=String(s),t&64?(n=16,s=[an(s)]):n=8);e.children=s,e.shapeFlag|=n}function qi(...e){const s={};for(let n=0;n<e.length;n++){const t=e[n];for(const a in t)if(a==="class")s.class!==t.class&&(s.class=Ds([s.class,t.class]));else if(a==="style")s.style=cn([s.style,t.style]);else if(Ns(a)){const o=s[a],i=t[a];i&&o!==i&&!(D(o)&&o.includes(i))&&(s[a]=o?[].concat(o,i):i)}else a!==""&&(s[a]=t[a])}return s}function De(e,s,n,t=null){ge(e,s,7,[n,t])}const Ei=pa();let Ri=0;function Fi(e,s,n){const t=e.type,a=(s?s.appContext:e.appContext)||Ei,o={uid:Ri++,vnode:e,type:t,parent:s,appContext:a,root:null,next:null,subTree:null,effect:null,update:null,scope:new io(!0),render:null,proxy:null,exposed:null,exposeProxy:null,withProxy:null,provides:s?s.provides:Object.create(a.provides),accessCache:null,renderCache:[],components:null,directives:null,propsOptions:aa(t,a),emitsOptions:Et(t,a),emit:null,emitted:null,propsDefaults:B,inheritAttrs:t.inheritAttrs,ctx:B,data:B,props:B,attrs:B,slots:B,refs:B,setupState:B,setupContext:null,suspense:n,suspenseId:n?n.pendingId:0,asyncDep:null,asyncResolved:!1,isMounted:!1,isUnmounted:!1,isDeactivated:!1,bc:null,c:null,bm:null,m:null,bu:null,u:null,um:null,bum:null,da:null,a:null,rtg:null,rtc:null,ec:null,sp:null};return o.ctx={_:o},o.root=s?s.root:o,o.emit=$o.bind(null,o),e.ce&&e.ce(o),o}let ee=null;const ba=()=>ee||ye,ps=e=>{ee=e,e.scope.on()},Qe=()=>{ee&&ee.scope.off(),ee=null};function va(e){return e.vnode.shapeFlag&4}let zs=!1;function Mi(e,s=!1){zs=s;const{props:n,children:t}=e.vnode,a=va(e);ji(e,n,a,s),_i(e,t);const o=a?Hi(e,s):void 0;return zs=!1,o}function Hi(e,s){const n=e.type;e.accessCache=Object.create(null),e.proxy=Dt(new Proxy(e.ctx,mi));const{setup:t}=n;if(t){const a=e.setupContext=t.length>1?Ui(e):null;ps(e),rs();const o=He(t,e,0,[e.props,a]);if(ls(),Qe(),it(o)){if(o.then(Qe,Qe),s)return o.then(i=>{ja(e,i,s)}).catch(i=>{Bs(i,e,0)});e.asyncDep=o}else ja(e,o,s)}else xa(e,s)}function ja(e,s,n){I(s)?e.type.__ssrInlineRender?e.ssrRender=s:e.render=s:L(s)&&(e.setupState=At(s)),xa(e,n)}let wa;function xa(e,s,n){const t=e.type;if(!e.render){if(!s&&wa&&!t.render){const a=t.template||Yn(e).template;if(a){const{isCustomElement:o,compilerOptions:i}=e.appContext.config,{delimiters:r,compilerOptions:c}=t,u=Q(Q({isCustomElement:o,delimiters:r},i),c);t.render=wa(a,u)}}e.render=t.render||ve}ps(e),rs(),fi(e),ls(),Qe()}function Yi(e){return new Proxy(e.attrs,{get(s,n){return pe(e,"get","$attrs"),s[n]}})}function Ui(e){const s=t=>{e.exposed=t||{}};let n;return{get attrs(){return n||(n=Yi(e))},slots:e.slots,emit:e.emit,expose:s}}function Gn(e){if(e.exposed)return e.exposeProxy||(e.exposeProxy=new Proxy(At(Dt(e.exposed)),{get(s,n){if(n in s)return s[n];if(n in ws)return ws[n](e)},has(s,n){return n in s||n in ws}}))}function Bi(e,s=!0){return I(e)?e.displayName||e.name:e.name||s&&e.__name}function Li(e){return I(e)&&"__vccOpts"in e}const Wi=(e,s)=>Mo(e,s,zs),$i=Symbol(""),Vi=()=>$s($i),Ki="3.2.45",Gi="http://www.w3.org/2000/svg",es=typeof document<"u"?document:null,_a=es&&es.createElement("template"),Xi={insert:(e,s,n)=>{s.insertBefore(e,n||null)},remove:e=>{const s=e.parentNode;s&&s.removeChild(e)},createElement:(e,s,n,t)=>{const a=s?es.createElementNS(Gi,e):es.createElement(e,n?{is:n}:void 0);return e==="select"&&t&&t.multiple!=null&&a.setAttribute("multiple",t.multiple),a},createText:e=>es.createTextNode(e),createComment:e=>es.createComment(e),setText:(e,s)=>{e.nodeValue=s},setElementText:(e,s)=>{e.textContent=s},parentNode:e=>e.parentNode,nextSibling:e=>e.nextSibling,querySelector:e=>es.querySelector(e),setScopeId(e,s){e.setAttribute(s,"")},insertStaticContent(e,s,n,t,a,o){const i=n?n.previousSibling:s.lastChild;if(a&&(a===o||a.nextSibling))for(;s.insertBefore(a.cloneNode(!0),n),!(a===o||!(a=a.nextSibling)););else{_a.innerHTML=t?`<svg>${e}</svg>`:e;const r=_a.content;if(t){const c=r.firstChild;for(;c.firstChild;)r.appendChild(c.firstChild);r.removeChild(c)}s.insertBefore(r,n)}return[i?i.nextSibling:s.firstChild,n?n.previousSibling:s.lastChild]}};function Zi(e,s,n){const t=e._vtc;t&&(s=(s?[s,...t]:[...t]).join(" ")),s==null?e.removeAttribute("class"):n?e.setAttribute("class",s):e.className=s}function Qi(e,s,n){const t=e.style,a=Z(n);if(n&&!a){for(const o in n)Xn(t,o,n[o]);if(s&&!Z(s))for(const o in s)n[o]==null&&Xn(t,o,"")}else{const o=t.display;a?s!==n&&(t.cssText=n):s&&e.removeAttribute("style"),"_vod"in e&&(t.display=o)}}const za=/\s*!important$/;function Xn(e,s,n){if(D(n))n.forEach(t=>Xn(e,s,t));else if(n==null&&(n=""),s.startsWith("--"))e.setProperty(s,n);else{const t=er(e,s);za.test(n)?e.setProperty(is(t),n.replace(za,""),"important"):e[t]=n}}const Ja=["Webkit","Moz","ms"],Zn={};function er(e,s){const n=Zn[s];if(n)return n;let t=Je(s);if(t!=="filter"&&t in e)return Zn[s]=t;t=Ps(t);for(let a=0;a<Ja.length;a++){const o=Ja[a]+t;if(o in e)return Zn[s]=o}return s}const Sa="http://www.w3.org/1999/xlink";function sr(e,s,n,t,a){if(t&&s.startsWith("xlink:"))n==null?e.removeAttributeNS(Sa,s.slice(6,s.length)):e.setAttributeNS(Sa,s,n);else{const o=Za(s);n==null||o&&!tt(n)?e.removeAttribute(s):e.setAttribute(s,o?"":n)}}function nr(e,s,n,t,a,o,i){if(s==="innerHTML"||s==="textContent"){t&&i(t,a,o),e[s]=n??"";return}if(s==="value"&&e.tagName!=="PROGRESS"&&!e.tagName.includes("-")){e._value=n;const c=n??"";(e.value!==c||e.tagName==="OPTION")&&(e.value=c),n==null&&e.removeAttribute(s);return}let r=!1;if(n===""||n==null){const c=typeof e[s];c==="boolean"?n=tt(n):n==null&&c==="string"?(n="",r=!0):c==="number"&&(n=0,r=!0)}try{e[s]=n}catch{}r&&e.removeAttribute(s)}function tr(e,s,n,t){e.addEventListener(s,n,t)}function ar(e,s,n,t){e.removeEventListener(s,n,t)}function or(e,s,n,t,a=null){const o=e._vei||(e._vei={}),i=o[s];if(t&&i)i.value=t;else{const[r,c]=ir(s);if(t){const u=o[s]=cr(t,a);tr(e,r,u,c)}else i&&(ar(e,r,i,c),o[s]=void 0)}}const Ta=/(?:Once|Passive|Capture)$/;function ir(e){let s;if(Ta.test(e)){s={};let t;for(;t=e.match(Ta);)e=e.slice(0,e.length-t[0].length),s[t[0].toLowerCase()]=!0}return[e[2]===":"?e.slice(3):is(e.slice(2)),s]}let Qn=0;const rr=Promise.resolve(),lr=()=>Qn||(rr.then(()=>Qn=0),Qn=Date.now());function cr(e,s){const n=t=>{if(!t._vts)t._vts=Date.now();else if(t._vts<=n.attached)return;ge(dr(t,n.value),s,5,[t])};return n.value=e,n.attached=lr(),n}function dr(e,s){if(D(s)){const n=e.stopImmediatePropagation;return e.stopImmediatePropagation=()=>{n.call(e),e._stopped=!0},s.map(t=>a=>!a._stopped&&t&&t(a))}else return s}const ka=/^on[a-z]/,ur=(e,s,n,t,a=!1,o,i,r,c)=>{s==="class"?Zi(e,t,a):s==="style"?Qi(e,n,t):Ns(s)?dn(s)||or(e,s,n,t,i):(s[0]==="."?(s=s.slice(1),!0):s[0]==="^"?(s=s.slice(1),!1):pr(e,s,t,a))?nr(e,s,t,o,i,r,c):(s==="true-value"?e._trueValue=t:s==="false-value"&&(e._falseValue=t),sr(e,s,t,a))};function pr(e,s,n,t){return t?!!(s==="innerHTML"||s==="textContent"||s in e&&ka.test(s)&&I(n)):s==="spellcheck"||s==="draggable"||s==="translate"||s==="form"||s==="list"&&e.tagName==="INPUT"||s==="type"&&e.tagName==="TEXTAREA"||ka.test(s)&&Z(n)?!1:s in e}const Be="transition",Js="animation",Da={name:String,type:String,css:{type:Boolean,default:!0},duration:[String,Number,Object],enterFromClass:String,enterActiveClass:String,enterToClass:String,appearFromClass:String,appearActiveClass:String,appearToClass:String,leaveFromClass:String,leaveActiveClass:String,leaveToClass:String},hr=Q({},ni.props,Da),ss=(e,s=[])=>{D(e)?e.forEach(n=>n(...s)):e&&e(...s)},Aa=e=>e?D(e)?e.some(s=>s.length>1):e.length>1:!1;function mr(e){const s={};for(const z in e)z in Da||(s[z]=e[z]);if(e.css===!1)return s;const{name:n="v",type:t,duration:a,enterFromClass:o=`${n}-enter-from`,enterActiveClass:i=`${n}-enter-active`,enterToClass:r=`${n}-enter-to`,appearFromClass:c=o,appearActiveClass:u=i,appearToClass:p=r,leaveFromClass:y=`${n}-leave-from`,leaveActiveClass:b=`${n}-leave-active`,leaveToClass:T=`${n}-leave-to`}=e,O=fr(a),k=O&&O[0],V=O&&O[1],{onBeforeEnter:R,onEnter:le,onEnterCancelled:N,onLeave:Y,onLeaveCancelled:se,onBeforeAppear:We=R,onAppear:Ae=le,onAppearCancelled:C=N}=s,K=(z,W,me)=>{Le(z,W?p:r),Le(z,W?u:i),me&&me()},U=(z,W)=>{z._isLeaving=!1,Le(z,y),Le(z,T),Le(z,b),W&&W()},oe=z=>(W,me)=>{const Ts=z?Ae:le,ne=()=>K(W,z,me);ss(Ts,[W,ne]),Na(()=>{Le(W,z?c:o),qe(W,z?p:r),Aa(Ts)||Ia(W,t,k,ne)})};return Q(s,{onBeforeEnter(z){ss(R,[z]),qe(z,o),qe(z,i)},onBeforeAppear(z){ss(We,[z]),qe(z,c),qe(z,u)},onEnter:oe(!1),onAppear:oe(!0),onLeave(z,W){z._isLeaving=!0;const me=()=>U(z,W);qe(z,y),qa(),qe(z,b),Na(()=>{z._isLeaving&&(Le(z,y),qe(z,T),Aa(Y)||Ia(z,t,V,me))}),ss(Y,[z,me])},onEnterCancelled(z){K(z,!1),ss(N,[z])},onAppearCancelled(z){K(z,!0),ss(C,[z])},onLeaveCancelled(z){U(z),ss(se,[z])}})}function fr(e){if(e==null)return null;if(L(e))return[et(e.enter),et(e.leave)];{const s=et(e);return[s,s]}}function et(e){return gn(e)}function qe(e,s){s.split(/\s+/).forEach(n=>n&&e.classList.add(n)),(e._vtc||(e._vtc=new Set)).add(s)}function Le(e,s){s.split(/\s+/).forEach(t=>t&&e.classList.remove(t));const{_vtc:n}=e;n&&(n.delete(s),n.size||(e._vtc=void 0))}function Na(e){requestAnimationFrame(()=>{requestAnimationFrame(e)})}let gr=0;function Ia(e,s,n,t){const a=e._endId=++gr,o=()=>{a===e._endId&&t()};if(n)return setTimeout(o,n);const{type:i,timeout:r,propCount:c}=Ca(e,s);if(!i)return t();const u=i+"end";let p=0;const y=()=>{e.removeEventListener(u,b),o()},b=T=>{T.target===e&&++p>=c&&y()};setTimeout(()=>{p<c&&y()},r+1),e.addEventListener(u,b)}function Ca(e,s){const n=window.getComputedStyle(e),t=O=>(n[O]||"").split(", "),a=t(`${Be}Delay`),o=t(`${Be}Duration`),i=Oa(a,o),r=t(`${Js}Delay`),c=t(`${Js}Duration`),u=Oa(r,c);let p=null,y=0,b=0;s===Be?i>0&&(p=Be,y=i,b=o.length):s===Js?u>0&&(p=Js,y=u,b=c.length):(y=Math.max(i,u),p=y>0?i>u?Be:Js:null,b=p?p===Be?o.length:c.length:0);const T=p===Be&&/\b(transform|all)(,|$)/.test(t(`${Be}Property`).toString());return{type:p,timeout:y,propCount:b,hasTransform:T}}function Oa(e,s){for(;e.length<s.length;)e=e.concat(e);return Math.max(...s.map((n,t)=>Pa(n)+Pa(e[t])))}function Pa(e){return Number(e.slice(0,-1).replace(",","."))*1e3}function qa(){return document.body.offsetHeight}const Ea=new WeakMap,Ra=new WeakMap,yr={name:"TransitionGroup",props:Q({},hr,{tag:String,moveClass:String}),setup(e,{slots:s}){const n=ba(),t=Ut();let a,o;return Vt(()=>{if(!a.length)return;const i=e.moveClass||`${e.name||"v"}-move`;if(!wr(a[0].el,n.vnode.el,i))return;a.forEach(br),a.forEach(vr);const r=a.filter(jr);qa(),r.forEach(c=>{const u=c.el,p=u.style;qe(u,i),p.transform=p.webkitTransform=p.transitionDuration="";const y=u._moveCb=b=>{b&&b.target!==u||(!b||/transform$/.test(b.propertyName))&&(u.removeEventListener("transitionend",y),u._moveCb=null,Le(u,i))};u.addEventListener("transitionend",y)})}),()=>{const i=E(e),r=mr(i);let c=i.tag||he;a=o,o=s.default?En(s.default()):[];for(let u=0;u<o.length;u++){const p=o[u];p.key!=null&&js(p,vs(p,r,t,n))}if(a)for(let u=0;u<a.length;u++){const p=a[u];js(p,vs(p,r,t,n)),Ea.set(p,p.el.getBoundingClientRect())}return ue(c,null,o)}}};function br(e){const s=e.el;s._moveCb&&s._moveCb(),s._enterCb&&s._enterCb()}function vr(e){Ra.set(e,e.el.getBoundingClientRect())}function jr(e){const s=Ea.get(e),n=Ra.get(e),t=s.left-n.left,a=s.top-n.top;if(t||a){const o=e.el.style;return o.transform=o.webkitTransform=`translate(${t}px,${a}px)`,o.transitionDuration="0s",e}}function wr(e,s,n){const t=e.cloneNode();e._vtc&&e._vtc.forEach(i=>{i.split(/\s+/).forEach(r=>r&&t.classList.remove(r))}),n.split(/\s+/).forEach(i=>i&&t.classList.add(i)),t.style.display="none";const a=s.nodeType===1?s:s.parentNode;a.appendChild(t);const{hasTransform:o}=Ca(t);return a.removeChild(t),o}const xr=["ctrl","shift","alt","meta"],_r={stop:e=>e.stopPropagation(),prevent:e=>e.preventDefault(),self:e=>e.target!==e.currentTarget,ctrl:e=>!e.ctrlKey,shift:e=>!e.shiftKey,alt:e=>!e.altKey,meta:e=>!e.metaKey,left:e=>"button"in e&&e.button!==0,middle:e=>"button"in e&&e.button!==1,right:e=>"button"in e&&e.button!==2,exact:(e,s)=>xr.some(n=>e[`${n}Key`]&&!s.includes(n))},zr=(e,s)=>(n,...t)=>{for(let a=0;a<s.length;a++){const o=_r[s[a]];if(o&&o(n,s))return}return e(n,...t)},Jr=Q({patchProp:ur},Xi);let Fa;function Sr(){return Fa||(Fa=Ti(Jr))}const Tr=(...e)=>{const s=Sr().createApp(...e),{mount:n}=s;return s.mount=t=>{const a=kr(t);if(!a)return;const o=s._component;!I(o)&&!o.render&&!o.template&&(o.template=a.innerHTML),a.innerHTML="";const i=n(a,!1,a instanceof SVGElement);return a instanceof Element&&(a.removeAttribute("v-cloak"),a.setAttribute("data-v-app","")),i},s};function kr(e){return Z(e)?document.querySelector(e):e}const Ss=(e,s)=>{const n=e.__vccOpts||e;for(const[t,a]of s)n[t]=a;return n},Dr=Rn({props:{report:{type:String,required:!0}},methods:{createIssue(){const e=new FormData(this.$refs.issueForm),s=e.get("title");let n="";const t=e.get("description");t&&(n+=`**Description**
${t}

`),n+=`**Troubleshooter's report**
${this.report}

`;const a=e.get("mcu"),o=e.get("framework"),i=e.get("ide");(a||o||i)&&(n+=`**Environment**
`,a&&(n+="* Microcontroller: "+a+`
`),o&&(n+="* Core/Framework: "+o+`
`),i&&(n+="* IDE: "+i+`
`),n+=`
`);const r=e.get("repro");r&&(n+="**Reproduction code**\n```c++\n",n+=r,n+="\n```\n\n");const c=e.get("remarks");c&&(n+=`**Remarks**
${c}

`);const p="https://github.com/bblanchon/ArduinoJson/issues/new?"+new URLSearchParams({title:s,body:n}).toString();console.log("URL",p),window.open(p,"_blank"),$("#assistance-modal").modal("hide")}}}),Ar={class:"modal fade"},Nr={class:"modal-dialog modal-dialog-scrollable modal-lg"},Ir=[Pi('<div class="modal-header bg-primary text-white"><h5 class="modal-title">Contact support</h5><button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">×</span></button></div><div class="modal-body"><div class="alert alert-info"> This form simplifies the creation of an issue on ArduinoJson&#39;s GitHub project page. It ensures you provide all the required information and prefills the issue&#39;s text. After that, you just need to submit the issue.<br><b>A GitHub account is required.</b></div><div class="form-group"><label for="title-input">Title</label><input type="text" name="title" class="form-control" id="title-input" aria-describedby="title-help" required><small id="title-help" class="form-text text-muted">Summarize your issue in one sentence.</small></div><div class="form-group"><label for="description-input">Description</label><textarea name="description" rows="3" class="form-control" id="description-input" aria-describedby="description-help" required placeholder="When I do ..., I expect ..., but instead I get ..."></textarea><small id="description-help" class="form-text text-muted">Describe your issue with a few sentences.</small></div><div class="form-group"><label for="mcu-input">Microcontroller</label><input type="text" name="mcu" class="form-control" id="mcu-input" aria-describedby="mcu-help" placeholder="ESP8266" required><small id="mcu-help" class="form-text text-muted">Which processor or with board do you use?</small></div><div class="form-group"><label for="framework-input">Arduino Core / Framework</label><input type="text" name="framework" class="form-control" id="framework-input" aria-describedby="framework-help" placeholder="ESP8266 core for Arduino v3.0.2"><small id="framework-help" class="form-text text-muted">Please include version number.</small></div><div class="form-group"><label for="ide-input">IDE</label><input type="text" name="ide" class="form-control" id="ide-input" aria-describedby="ide-help" placeholder="Arduino IDE 1.8.16"><small id="ide-help" class="form-text text-muted">Please include version number.</small></div><div class="form-group"><label for="repro-input">Reproduction code</label><textarea name="repro" rows="10" class="form-control text-monospace" style="font-size:80%;" id="repro-input" aria-describedby="repro-help" placeholder="DynamicJsonDocuemnt doc(1024);\\n..."></textarea><small id="repro-help" class="form-text text-muted">Write a few lines of code that demonstrate the issue.</small></div><div class="form-group"><label for="remarks-input">Remarks</label><textarea name="remarks" rows="4" class="form-control" id="remarks-input" aria-describedby="remarks-help"></textarea><small id="remarks-help" class="form-text text-muted">Anything else you need to tell us?</small></div></div><div class="modal-footer"><div class="text-right"><button type="submit" class="btn btn-primary">Submit</button><small class="form-text text-muted"> This button redirects you to GitHub. </small></div></div>',3)];function Cr(e,s,n,t,a,o){return de(),_e("div",Ar,[ae("div",Nr,[ae("form",{class:"modal-content",ref:"issueForm",onSubmit:s[0]||(s[0]=zr((...i)=>e.createIssue&&e.createIssue(...i),["prevent"]))},Ir,544)])])}const Or=Ss(Dr,[["render",Cr]]),Pr=Rn({inject:["debug"],emits:["check"],props:{choice:{type:Object,required:!0}}}),qr={class:"form-check"},Er=["id","checked","disabled"],Rr=["for","innerHTML"],Fr={key:0,class:"d-block mb-2 small text-muted"};function Mr(e,s,n,t,a,o){return de(),_e("div",qr,[ae("input",{type:"radio",id:e.choice.inputId,class:"form-check-input",checked:e.choice.selected,onClick:s[0]||(s[0]=i=>e.$emit("check")),disabled:e.choice.missing},null,8,Er),ae("label",{class:"form-check-label",for:e.choice.inputId,innerHTML:e.choice.label},null,8,Rr),e.debug?(de(),_e("div",Fr,As(e.choice.summary),1)):Vn("",!0)])}const Hr=Ss(Pr,[["render",Mr]]),Yr={inject:["debug"],props:{step:{type:Object,required:!0}}},Ur=["title"],Br={class:"bg-secondary text-white rounded-circle"},Lr={key:0,class:"sr-only"};function Wr(e,s,n,t,a,o){return de(),_e("h2",{class:"troubleshooter-step-number",title:o.debug&&n.step.slug},[ae("div",Br,[an(As(n.step.number)+" ",1),o.debug?(de(),_e("span",Lr,": "+As(n.step.slug),1)):Vn("",!0)])],8,Ur)}const $r=Ss(Yr,[["render",Wr]]),yl="",Vr=Rn({inject:["debug"],emits:["choose"],props:{step:{type:Object,required:!0}},components:{TroubleshooterStepChoice:Hr,TroubleshooterStepNumber:$r}}),Kr={key:0,class:"troubleshooter-step mb-4"},Gr=["innerHTML"],Xr={class:"troubleshooter-step-choices"},Zr=["innerHTML"];function Qr(e,s,n,t,a,o){const i=Zs("TroubleshooterStepNumber"),r=Zs("TroubleshooterStepChoice");return e.step.choices?(de(),_e("div",Kr,[ue(i,{step:e.step},null,8,["step"]),ae("div",{innerHTML:e.step.content,class:"troubleshooter-step-content"},null,8,Gr),ae("div",Xr,[(de(!0),_e(he,null,Qt(e.step.choices,c=>(de(),$n(r,{key:c.hash,choice:c,onClick:u=>e.$emit("choose",c)},null,8,["choice","onClick"]))),128))])])):(de(),_e("div",{key:1,innerHTML:e.step.content,class:"troubleshooter-step-content"},null,8,Zr))}const el=Ss(Vr,[["render",Qr]]),Ma={"compiletime/ambiguous-string-assign":{content:`<p>This error occurs when you try to assign a <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a> to a <a href="https://www.arduino.cc/reference/en/language/variables/data-types/stringobject/"><code>String</code></a>.</p>
<p>Indeed, due to the way these two classes are defined, you can <em>construct</em> a <a href="https://www.arduino.cc/reference/en/language/variables/data-types/stringobject/"><code>String</code></a> from a <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a>, but you cannot <em>assign</em> a <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a> to an existing string.</p>
<pre><code class="hljs">String name <span class="hljs-operator">=</span> doc[<span class="hljs-string">&quot;name&quot;</span>]<span class="hljs-comment">;  // contruction -&gt; works</span>
<span class="hljs-attribute">name</span> <span class="hljs-operator">=</span> doc[<span class="hljs-string">&quot;name&quot;</span>]<span class="hljs-comment">;         // assignment  -&gt; fails</span>
</code></pre>
<p>You can easily workaround this problem by explicitly casting the <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a>, like so:</p>
<pre><code class="hljs"><span class="hljs-attribute">name</span> <span class="hljs-operator">=</span> doc[<span class="hljs-string">&quot;name&quot;</span>].as&lt;String&gt;()<span class="hljs-comment">;</span>
</code></pre>
<p>Please see <a href="/v6/error/ambiguous-overload-for-operator-equal/">error: ambiguous overload for 'operator=' (operand types are 'String' and ...)</a> for details.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Casting the `JsonVariant` fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Casting the `JsonVariant` doesn't fix the issue",next:"deadend"}]},"compiletime/assignment-of-read-only-location":{content:`<p>This error occurs when you pass a string to <a href="/v6/api/jsonarray/subscript/"><code>JsonArray::operator[]</code></a>; i.e., when you use an array like an object.</p>
<p>For example, if you try to compile the following code:</p>
<pre><code class="hljs language-c++">StaticJsonDocument&lt;<span class="hljs-number">128</span>&gt; doc;
JsonArray array = doc.<span class="hljs-built_in">createNestedArray</span>(<span class="hljs-string">&quot;array&quot;</span>);
array[<span class="hljs-string">&quot;key&quot;</span>] = <span class="hljs-string">&quot;value&quot;</span>;  <span class="hljs-comment">// ERROR</span>
</code></pre>
<p>...you'll get the following error message:</p>
<pre><code class="hljs language-text">error: assignment of read-only location &#x27;*(((const char*)&quot;key&quot;) + ((sizetype)data.ArduinoJson6194_F1::ArrayRef::&lt;anonymous&gt;.ArduinoJson6194_F1::ArrayRefBase&lt;ArduinoJson6194_F1::CollectionData&gt;::operator bool()))&#x27;
   38 |   data[&quot;key&quot;] = &quot;value&quot;;
      |   ~~~~~~~~~~~~^~~~~~~~~
</code></pre>
<p>The solution is to use the array correctly or to switch to an object.</p>
<p>So do either this:</p>
<pre><code class="hljs language-c++">StaticJsonDocument&lt;<span class="hljs-number">128</span>&gt; doc;
JsonArray array = doc.<span class="hljs-built_in">createNestedArray</span>(<span class="hljs-string">&quot;array&quot;</span>);
array[<span class="hljs-number">0</span>] = <span class="hljs-string">&quot;value&quot;</span>;
</code></pre>
<p>...or that:</p>
<pre><code class="hljs language-c++">StaticJsonDocument&lt;<span class="hljs-number">128</span>&gt; doc;
JsonArray obj = doc.<span class="hljs-built_in">createNestedObject</span>(<span class="hljs-string">&quot;obj&quot;</span>);
obj[<span class="hljs-string">&quot;key&quot;</span>] = <span class="hljs-string">&quot;value&quot;</span>;
</code></pre>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Using an integer or switching to an object fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Neither using an integer nor switching to an object fixes the issue",next:"deadend"}]},"compiletime/basicjsondocument-default-constructor":{content:`<p>This is a linting error you may have in Visual Studio Code if your program contains something like this:</p>
<pre><code class="hljs language-c++">DynamicJsonDocument doc;
</code></pre>
<p>If you try to compile this code, you will get the following error:</p>
<pre><code class="hljs language-text">no matching function for call to &#x27;ArduinoJson6194_F1::BasicJsonDocument&lt;ArduinoJson6194_F1::DefaultAllocator&gt;::BasicJsonDocument()&#x27;
</code></pre>
<p>These errors occur when you forget to pass the capacity to the constructor of <a href="/v6/api/dynamicjsondocument/"><code>DynamicJsonDocument</code></a>.</p>
<p>To fix them, you must specify the capacity of the memory pool, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-function">DynamicJsonDocument <span class="hljs-title">doc</span><span class="hljs-params">(<span class="hljs-number">2048</span>)</span></span>;
</code></pre>
<p>As usual, you can use the <a href="/v6/assistant/">ArduinoJson Assistant</a> to compute the right capacity for your project.</p>
<p>For more information, please read <a href="/v6/error/no-matching-function-for-call-to-basicjsondocument-basicjsondocument/">no matching function for call to 'BasicJsonDocument::BasicJsonDocument()'</a>.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Passing the capacity to the constructor fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Passing the capacity to the constructor doesn't fix the issue",next:"deadend"}]},"compiletime/cast-void-to-flashstringhelper":{content:`<p>The error should look like this:</p>
<pre><code class="hljs language-text">.../src/ArduinoJson/Polyfills/pgmspace_generic.hpp:14:10: error: reinterpret_cast from &#x27;const void&#x27; to &#x27;const __FlashStringHelper *&#x27; is not allowed
  return reinterpret_cast&lt;T&gt;(pgm_read_ptr(p));
         ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
</code></pre>
<p>This error indicate that the return type of <code>pgm_read_ptr()</code> is incorrect: it should be <code>const void*</code>, but the error message shows it's <code>const void</code>.</p>
<p>This is a common bug in cores (or &quot;framework&quot;) that emulate the original <code>pgmspace.h</code> header.
It already poped up in:</p>
<ul>
<li>ArduinoFake (issue <a href="https://github.com/bblanchon/ArduinoJson/issues/1676">#1676</a>)</li>
<li>ESP8266 (issue <a href="https://github.com/bblanchon/ArduinoJson/issues/1442">#1442</a>)</li>
<li>Industruino (issue <a href="https://github.com/bblanchon/ArduinoJson/issues/1519">#1519</a>)</li>
<li>Particle Argon (issue <a href="https://github.com/bblanchon/ArduinoJson/issues/1591">#1591</a>)</li>
<li>Teknic ClearCore (issue <a href="https://github.com/bblanchon/ArduinoJson/issues/1381">#1381</a>)</li>
<li>Others (issue <a href="https://github.com/bblanchon/ArduinoJson/issues/1536">#1536</a>)</li>
</ul>
<p>The root of this bug was fixed in October 2020 in <a href="https://github.com/arduino/ArduinoCore-API/pull/118">arduino/ArduinoCore-API#118</a>, but many clones are still using the old code.</p>
<p>To fix this issue, update your Arduino Core (or &quot;framework&quot;) to the newest version.</p>
<p>If the same error still occurs, please contact the relevant support team.
In the meantime, you can work around this issue by disabling PROGMEM support in ArduinoJson by setting <a href="/v6/api/config/enable_progmem/"><code>ARDUINOJSON_ENABLE_PROGMEM</code></a> to <code>0</code>, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-meta">#<span class="hljs-keyword">define</span> ARDUINOJSON_ENABLE_PROGMEM 0</span>
<span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;ArduinoJson.h&gt;</span></span>
</code></pre>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Setting `ARDUINOJSON_ENABLE_PROGMEM` to `0` fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Setting `ARDUINOJSON_ENABLE_PROGMEM` to `0` doesn't fix the issue",next:"deadend"}]},"compiletime/char-pointer-conversion":{content:`<p>This error occurs when you try to store a pointer of type <code>const char*</code> into a variable of type <code>char*</code>, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-type">char</span>* eventName = doc[<span class="hljs-string">&quot;event&quot;</span>];
</code></pre>
<p>Indeed, <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a> returns a <code>const char*</code>, not a <code>char*</code>. You must change the type of the pointer, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-type">const</span> <span class="hljs-type">char</span>* eventName = doc[<span class="hljs-string">&quot;event&quot;</span>];
</code></pre>
<p>There are other similar situations where this error can occur. For more information, please read <a href="/v6/error/invalid-conversion-from-const-char-to-char/">invalid conversion from 'const char*' to 'char*' [-fpermissive]</a>.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Changing the pointer type fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Changing the pointer type doesn't fix the issue",next:"deadend"}]},"compiletime/class-from-arduinojson5-keep":{content:`<p>I recommend that you download <a href="https://github.com/bblanchon/ArduinoJson/releases/download/v5.13.5/ArduinoJson-v5.13.5.h"><code>ArduinoJson-v5.13.5.h</code></a> and save it among the project files, this way you're sure that the project is distributed with the right version of the library.</p>
<p>You can choose to keep the name <code>ArduinoJson-v5.13.5.h</code> and update the <code>#include</code> directive, or you can rename the file to <code>ArduinoJson.h</code>; either way is fine, as long as the header is in the same folder as the file that includes it.</p>
<p>Please see <a href="/v5/doc/installation/">installation instructions</a> for details</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Downloading ArduinoJson 5 fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Downloading ArduinoJson 5 doesn't fix the issue",next:"deadend"}]},"compiletime/class-from-arduinojson5-upgrade":{content:`<p>Upgrading from v5 to v6 isn't trivial but isn't complicated either.<br>
Please follow the <a href="/v6/doc/upgrade/">upgrade instructions</a>.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Upgrading fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Upgrading doesn't fix the issue",next:"deadend"}]},"compiletime/class-from-arduinojson5":{content:`<p>This error occurs when you try to use ArduinoJson 6 with a program written for ArduinoJson 5.</p>
<p>Can you upgrade the program or do you prefer keeping it as it is?</p>
`,needs_assistance:!1,choices:[{id:"upgrade",label:"I'd like to upgrade to ArduinoJson 6",summary:"User prefers upgrading to ArduinoJson 6",next:"compiletime/class-from-arduinojson5-upgrade"},{id:"keep-v5",label:"I prefer not touching the program",summary:"User prefers keeping ArduinoJson 5",next:"compiletime/class-from-arduinojson5-keep"}]},"compiletime/doesnt-name-a-type":{content:`<p>This error usually occurs when you write statements at the global scope, which isn't possible in C++.</p>
<p>Unlike many popular languages like Python or JavaScript, the C++ language doesn't allow statements at the global scope.
Instead, you must move all statements into a function.
The entry point for a regular C++ program is the <code>main()</code> function, but in an Arduino program, it's the <code>setup()</code> function.</p>
<p>For example, if your program is like this:</p>
<pre><code class="hljs language-c++">StaticJsonDocument&lt;<span class="hljs-number">200</span>&gt; doc;
doc[<span class="hljs-string">&quot;hello&quot;</span>] = <span class="hljs-string">&quot;world&quot;</span>;
<span class="hljs-built_in">serializeJson</span>(doc, Serial);
</code></pre>
<p>...you must rewrite it like this:</p>
<pre><code class="hljs language-c++"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">setup</span><span class="hljs-params">()</span> </span>{
  StaticJsonDocument&lt;<span class="hljs-number">200</span>&gt; doc;
  doc[<span class="hljs-string">&quot;hello&quot;</span>] = <span class="hljs-string">&quot;world&quot;</span>;
  <span class="hljs-built_in">serializeJson</span>(doc, Serial);
}
</code></pre>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Moving statement to `setup()` fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Moving statement to `setup()` doesn't fix the issue",next:"deadend"}]},compiletime:{content:`<p>Please look at the <strong>first</strong> error in the compiler output, and tell me what it is...</p>
`,needs_assistance:!1,choices:[{id:"assignment-of-read-only-location",label:"assignment of read-only location",summary:'Error says "assignment of read-only location"',next:"compiletime/assignment-of-read-only-location"},{id:"ambiguous-string-assign",label:"ambiguous overload for <code>operator=</code> (operand types are <code>String</code> and ...)",summary:'Error says "ambiguous overload for `operator=` (operand types are `String` and ...)"',next:"compiletime/ambiguous-string-assign"},{id:"begin-not-found",label:"<code>begin</code>: no matching overloaded function found",summary:'Error says "`begin`: no matching overloaded function found"',next:"compiletime/iterate-ambiguous"},{id:"print-ambiguous",label:"call of overloaded <code>print(...)</code> is ambiguous",summary:'Error says "call of overloaded `print(...)` is ambiguous"',next:"compiletime/print-ambiguous"},{id:"cannot-bind-object-ref",label:"cannot bind non-const lvalue reference of type <code>ArduinoJson::JsonObject&amp;</code> ...",summary:'Error says "cannot bind non-const lvalue reference of type `ArduinoJson::JsonObject&` ..."',next:"compiletime/objectref"},{id:"pointer-to-object",label:"<code>const void*</code> is not a pointer-to-object type",summary:'Error says "`const void*` is not a pointer-to-object type"',next:"compiletime/not-pointer-to-object"},{id:"doesnt-name-a-type",label:"<code>doc</code> does not name a type",summary:'Error says "`doc` does not name a type"',next:"compiletime/doesnt-name-a-type"},{id:"dynamicjsonbuffer",label:"<code>DynamicJsonBuffer</code> is a class from ArduinoJson 5",summary:'Error says "`DynamicJsonBuffer` is a class from ArduinoJson 5"',next:"compiletime/class-from-arduinojson5"},{id:"char-pointer-conversion",label:"invalid conversion from <code>const char*</code> to <code>char*</code> [-fpermissive]",summary:'Error says "invalid conversion from `const char*` to `char*` [-fpermissive]"',next:"compiletime/char-pointer-conversion"},{id:"invalid-conversion",label:"invalid use of incomplete type <code>class InvalidConversion&lt;...&gt;</code>",summary:'Error says "invalid use of incomplete type `class InvalidConversion<...>`"',next:"compiletime/invalid-conversion"},{id:"jsondocument-copy",label:"<code>JsonDocument::JsonDocument(const JsonDocument&amp;)</code> is private",summary:'Error says "`JsonDocument::JsonDocument(const JsonDocument&)` is private"',next:"compiletime/jsondocument-private"},{id:"macro-min",label:"macro <code>min</code> passed 3 arguments, but takes just 2",summary:'Error says "macro `min` passed 3 arguments, but takes just 2"',next:"compiletime/macro-min"},{id:"basicjsondocument-default-constructor",label:"no default constructor exists for class <code>BasicJsonDocument</code>",summary:'Error says "no default constructor exists for class `BasicJsonDocument`"',next:"compiletime/basicjsondocument-default-constructor"},{id:"no-matching-function",label:"no matching function for call to ...",summary:'Error says "no matching function for call to ..."',next:"compiletime/no-matching-function"},{id:"passing-volatile-as-this-argument-discards-qualifiers",label:"passing <code>volatile ...</code> as <code>this</code> argument discards qualifiers [-fpermissive]",summary:'Error says "passing `volatile ...` as `this` argument discards qualifiers [-fpermissive]"',next:"compiletime/passing-volatile-as-this-argument-discards-qualifiers"},{id:"cast-void-to-flashstringhelper",label:"reinterpret_cast from <code>const void</code> to <code>const __FlashStringHelper *</code> is not allowed",summary:'Error says "reinterpret_cast from `const void` to `const __FlashStringHelper *` is not allowed"',next:"compiletime/cast-void-to-flashstringhelper"},{id:"member-write-in-char-ptr",label:"request for member <code>write</code> in ..., which is of non-class type <code>char*</code>",summary:'Error says "request for member `write` in ..., which is of non-class type `char*`"',next:"compiletime/member-write-in-char-ptr"},{id:"staticjsonbuffer",label:"<code>StaticJsonBuffer</code> is a class from ArduinoJson 5",summary:'Error says "`StaticJsonBuffer` is a class from ArduinoJson 5"',next:"compiletime/class-from-arduinojson5"},{id:"range-based-for-requires-begin",label:"this range-based <code>for</code> statement requires a suitable &quot;begin&quot; function and none was found",summary:'Error says "this range-based `for` statement requires a suitable "begin" function and none was found"',next:"compiletime/iterate-ambiguous"},{id:"not-in-list",label:"None of the above",summary:"The error is not in the list",next:"compiletime/unknown-error"}]},"compiletime/invalid-conversion-array":{content:`<p>Somewhere in your program, there is a conversion from <a href="/v6/api/jsonvariantconst/"><code>JsonVariantConst</code></a> to <a href="/v6/api/jsonarray/"><code>JsonArray</code></a>. This conversion is invalid because it would convert a <em>read-only</em> reference to a <em>read-write</em> reference.</p>
<p>To fix this issue, you must use <a href="/v6/api/jsonarrayconst/"><code>JsonArrayConst</code></a> in place of <a href="/v6/api/jsonarray/"><code>JsonArray</code></a>.</p>
<p>For example, if your program contains the expression <code>variant.as&lt;JsonArray&gt;()</code>, you must replace it with <code>variant.as&lt;JsonArrayConst&gt;()</code>. Alternatively, if your program contains a statement like <code>JsonArray array = variant</code>, you must replace it with <code>JsonArrayConst array = variant</code>.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Replacing `JsonArray` with `JsonArrayConst` fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Replacing `JsonArray` with `JsonArrayConst` doesn't fix the issue",next:"deadend"}]},"compiletime/invalid-conversion-object":{content:`<p>Somewhere in your program, there is a conversion from <a href="/v6/api/jsonvariantconst/"><code>JsonVariantConst</code></a> to <a href="/v6/api/jsonobject/"><code>JsonObject</code></a>. This conversion is invalid because it would convert a <em>read-only</em> reference to a <em>read-write</em> reference.</p>
<p>To fix this issue, you must use <a href="/v6/api/jsonobjectconst/"><code>JsonObjectConst</code></a> in place of <a href="/v6/api/jsonobject/"><code>JsonObject</code></a>.</p>
<p>For example, if your program contains the expression <code>variant.as&lt;JsonObject&gt;()</code>, you must replace it with <code>variant.as&lt;JsonObjectConst&gt;()</code>. Alternatively, if your program contains a statement like <code>JsonObject obj = variant</code>, you must replace it with <code>JsonObjectConst obj = variant</code>.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Replacing `JsonObject` with `JsonObjectConst` fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Replacing `JsonObject` with `JsonObjectConst` doesn't fix the issue",next:"deadend"}]},"compiletime/invalid-conversion-variant":{content:`<p>Somewhere in your program, there is a conversion from <a href="/v6/api/jsonvariantconst/"><code>JsonVariantConst</code></a> to <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a>. This conversion is invalid because it would convert a <em>read-only</em> reference to a <em>read-write</em> reference.</p>
<p>To fix this issue, you must use <a href="/v6/api/jsonvariantconst/"><code>JsonVariantConst</code></a> in place of <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a>.</p>
<p>For example, if your program contains the expression <code>variant.as&lt;JsonVariant&gt;()</code>, you must replace it with <code>variant.as&lt;JsonVariantConst&gt;()</code>. Alternatively, if your program contains a statement like <code>JsonVariant v = variant</code>, you must replace it with <code>JsonVariantConst v = variant</code>.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Replacing `JsonVariant` with `JsonVariantConst` fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Replacing `JsonVariant` with `JsonVariantConst` doesn't fix the issue",next:"deadend"}]},"compiletime/invalid-conversion":{content:`<p>Assuming we removed the namespace <code>ArduinoJson6XXX</code> from the error message, what type is incomplete?</p>
`,needs_assistance:!1,choices:[{id:"array",label:"<code>class InvalidConversion&lt;VariantConstRef, ArrayRef&gt;</code>",summary:"Specialization is `InvalidConversion<VariantConstRef, ArrayRef>`",next:"compiletime/invalid-conversion-array"},{id:"object",label:"<code>class InvalidConversion&lt;VariantConstRef, ObjectRef&gt;</code>",summary:"Specialization is `InvalidConversion<VariantConstRef, ObjectRef>`",next:"compiletime/invalid-conversion-object"},{id:"variant",label:"<code>class InvalidConversion&lt;VariantConstRef, VariantRef&gt;</code>",summary:"Specialization is `InvalidConversion<VariantConstRef, VariantRef>`",next:"compiletime/invalid-conversion-variant"}]},"compiletime/iterate-ambiguous":{content:`<p>You get this error when you try to iterate over a <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> or a <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a>.</p>
<p>For example, it happens if you do this:</p>
<pre><code class="hljs language-c++"><span class="hljs-keyword">for</span> (JsonPair p : doc[<span class="hljs-string">&quot;config&quot;</span>]) {
  <span class="hljs-comment">// ...</span>
}
</code></pre>
<p>Indeed, you cannot iterate a <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a> like so because there is an ambiguity: the <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a> could point to either an array or an object, and this information is only available at runtime.</p>
<p>For this reason, we need to tell the compiler which type of structure we expect by casting the <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a> to a <a href="/v6/api/jsonarray/"><code>JsonArray</code></a> or <a href="/v6/api/jsonobject/"><code>JsonObject</code></a>.
For example, we could solve the above snippet like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-keyword">for</span> (JsonPair p : doc[<span class="hljs-string">&quot;config&quot;</span>].<span class="hljs-built_in">as</span>&lt;JsonObject&gt;()) {
  <span class="hljs-comment">// ...</span>
}
</code></pre>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Calling `as<JsonObject>()` or `as<JsonArray>()` solves the issue",next:"done"},{id:"failure",label:"No",summary:"Calling `as<JsonObject>()` or `as<JsonArray>()` doesn't solve the issue",next:"deadend"}]},"compiletime/jsondocument-private":{content:`<p><a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> doesn't support copying.</p>
<p>If you need to pass a <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> to a function, you can use a reference, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">myFunction</span><span class="hljs-params">(JsonDocument&amp; doc)</span>
<span class="hljs-comment">// or</span>
<span class="hljs-type">void</span> <span class="hljs-title">myFunction</span><span class="hljs-params">(<span class="hljs-type">const</span> JsonDocument&amp; doc)</span>
</span></code></pre>
<p>You can even make this function more reusable by allowing it to support any part of a JSON document, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">myFunction</span><span class="hljs-params">(JsonVariant var)</span>
<span class="hljs-comment">// or</span>
<span class="hljs-type">void</span> <span class="hljs-title">myFunction</span><span class="hljs-params">(JsonVariantConst var)</span>
</span></code></pre>
<p>If you must make a copy, use either <a href="/v6/api/dynamicjsondocument/"><code>DynamicJsonDocument</code></a> or <a href="/v6/api/staticjsondocument/"><code>StaticJsonDocument</code></a>.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Using a reference solves the issue",next:"done"},{id:"failure",label:"No",summary:"Using a reference doesn't solve the issue",next:"deadend"}]},"compiletime/macro-min":{content:`<p>This is a bug in some Arduino cores, most notably the one for <a href="https://en.wikipedia.org/wiki/Atmel_ARM-based_processors#SAM_D">SAMD21</a>.</p>
<p>You can workaround this bug by defining both <a href="/v6/api/config/enable_std_string/"><code>ARDUINOJSON_ENABLE_STD_STRING</code></a> and <a href="/v6/api/config/enable_std_stream/"><code>ARDUINOJSON_ENABLE_STD_STREAM</code></a> to <code>0</code>, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-meta">#<span class="hljs-keyword">define</span> ARDUINOJSON_ENABLE_STD_STRING 0</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> ARDUINOJSON_ENABLE_STD_STREAM 0</span>
<span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;ArduinoJson.h&gt;</span></span>
</code></pre>
<p>You can find more information on this error here: <a href="/v6/error/macro-min-passed-3-arguments-but-takes-just-2/">macro &quot;min&quot; passed 3 arguments, but takes just 2</a></p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Defining `ARDUINOJSON_ENABLE_STD_STRING` and `ARDUINOJSON_ENABLE_STD_STREAM` to `0` solves the issue",next:"done"},{id:"failure",label:"No",summary:"Defining `ARDUINOJSON_ENABLE_STD_STRING` and `ARDUINOJSON_ENABLE_STD_STREAM` to `0` doesn't solve the issue",next:"deadend"}]},"compiletime/member-write-in-char-ptr":{content:`<p>This error occurs when you pass a <code>char*</code> to <a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a> but forget to pass the third argument.
For example:</p>
<pre><code class="hljs language-c++"><span class="hljs-built_in">serializeJson</span>(doc, ptr);  <span class="hljs-comment">// request for member &#x27;write&#x27; in ..., which is of non-class type &#x27;char*&#x27; </span>
</code></pre>
<p>To fix this error, you must pass the size of the destination buffer as the third argument, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-built_in">serializeJson</span>(doc, ptr, size);  <span class="hljs-comment">// OK</span>
</code></pre>
<p>In the examples, you may have seen that I didn't use the size argument; that's because the second argument was not a <code>char*</code> but a <code>char[N]</code>, and <a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a> was able to infer the value of <code>N</code> from the type.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Passing the buffer size to `serializeJson()` fixed the issue",next:"done"},{id:"failure",label:"No",summary:"Passing the buffer size to `serializeJson()` didn't fix the issue",next:"deadend"}]},"compiletime/no-matching-function/as-char-ptr":{content:`<p>This error happens when you write the following:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">println</span>(doc[<span class="hljs-string">&quot;key&quot;</span>].<span class="hljs-built_in">as</span>&lt;<span class="hljs-type">char</span>*&gt;());
<span class="hljs-comment">// or</span>
<span class="hljs-type">char</span>* value = doc[<span class="hljs-string">&quot;key&quot;</span>];
</code></pre>
<p>Indeed, support for <code>char*</code> was deprecated in <a href="/news/2021/05/04/version-6-18-0/">6.18</a> and removed in <a href="/news/2022/12/26/arduinojson-6-20-0/">6.20</a>.</p>
<p>To fix this error, you must replace <code>char*</code> with <code>const char*</code>, like so:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">println</span>(doc[<span class="hljs-string">&quot;key&quot;</span>].<span class="hljs-built_in">as</span>&lt;<span class="hljs-type">const</span> <span class="hljs-type">char</span>*&gt;());
<span class="hljs-comment">// or</span>
<span class="hljs-type">const</span> <span class="hljs-type">char</span>* value = doc[<span class="hljs-string">&quot;key&quot;</span>];
</code></pre>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Replacing `char*` with `const char*` fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Replacing `char*` with `const char*` doesn't fix the issue",next:"deadend"}]},"compiletime/no-matching-function/as-char":{content:`<p>This error happens when you write the following:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">println</span>(doc[<span class="hljs-string">&quot;key&quot;</span>].<span class="hljs-built_in">as</span>&lt;<span class="hljs-type">char</span>*&gt;());
<span class="hljs-comment">// or</span>
<span class="hljs-type">char</span> value = doc[<span class="hljs-string">&quot;key&quot;</span>];
</code></pre>
<p>Indeed, support for <code>char</code> was deprecated in <a href="/news/2021/05/04/version-6-18-0/">6.18</a> and removed in <a href="/news/2022/12/26/arduinojson-6-20-0/">6.20</a>.</p>
<p>To fix this error, you must replace <code>char</code> with <code>int8_t</code>, <code>uint8_t</code>, or any other integral type, like so:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">println</span>(doc[<span class="hljs-string">&quot;key&quot;</span>].<span class="hljs-built_in">as</span>&lt;<span class="hljs-type">int8_t</span>&gt;());
<span class="hljs-comment">// or</span>
<span class="hljs-type">int8_t</span> value = doc[<span class="hljs-string">&quot;key&quot;</span>];
</code></pre>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Replacing `char` with `int8_t` fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Replacing `char` with `int8_t` doesn't fix the issue",next:"deadend"}]},"compiletime/no-matching-function/basicjsondocument":{content:`<p>This error occurs when you forget to pass the capacity to the constructor of <a href="/v6/api/dynamicjsondocument/"><code>DynamicJsonDocument</code></a>, like so:</p>
<pre><code class="hljs language-c++">DynamicJsonDocument doc;
</code></pre>
<p>Instead, you need to specify the capacity of the memory pool, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-function">DynamicJsonDocument <span class="hljs-title">doc</span><span class="hljs-params">(<span class="hljs-number">2048</span>)</span></span>;
</code></pre>
<p>As usual, you can use the <a href="/v6/assistant/">ArduinoJson Assistant</a> to compute the right capacity for your project.</p>
<p>For more information, please read <a href="/v6/error/no-matching-function-for-call-to-basicjsondocument-basicjsondocument/">no matching function for call to 'BasicJsonDocument::BasicJsonDocument()'</a>.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Passing the capacity to the constructor fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Passing the capacity to the constructor doesn't fix the issue",next:"deadend"}]},"compiletime/no-matching-function/converttojson":{content:`<p>This error occurs when you try to insert an unsupported value type in a <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>.</p>
<p>To fix this error, you must either:</p>
<ul>
<li>change the type of value to a supported one (<code>const char*</code>, <code>int</code>, <code>float</code>, etc.)</li>
<li>write a <a href="/news/2021/05/04/version-6-18-0/">custom converter</a> for this type</li>
</ul>
<p>For more information, see <a href="/v6/error/no-matching-function-for-call-to-converttojson/">error: no matching function for call to 'convertToJson(...)'</a>.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Converting the value fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Converting the value doesn't fix the issue",next:"deadend"}]},"compiletime/no-matching-function/deserializejson":{content:`<p>This error happens when you write the following:</p>
<pre><code class="hljs language-c++"><span class="hljs-function">StaticJsonDocument&lt;200&gt; <span class="hljs-title">doc</span><span class="hljs-params">()</span></span>;
<span class="hljs-built_in">deserializeJson</span>(doc, input);
</code></pre>
<p>Indeed, the first line doesn't declare a <code>StaticJsonDocument&lt;200&gt;</code> but a function that takes no argument and returns a <code>StaticJsonDocument&lt;200&gt;</code>. This is one of the most common pitfalls in C++ and it's called the <a href="https://en.wikipedia.org/wiki/Most_vexing_parse">Most vexing parse</a>.</p>
<p>To fix this program, you must remove the parentheses, like so:</p>
<pre><code class="hljs language-c++">StaticJsonDocument&lt;<span class="hljs-number">200</span>&gt; doc;
<span class="hljs-built_in">deserializeJson</span>(doc, input);
</code></pre>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:`It's the "Most vexing parse"`,next:"done"},{id:"failure",label:"No",summary:`It's not the "Most vexing parse"`,next:"deadend"}]},"compiletime/no-matching-function":{content:`<p>Which error is it?</p>
`,needs_assistance:!1,choices:[{id:"basicjsondocument",label:"no matching function for call to <code>BasicJsonDocument::BasicJsonDocument()</code>",summary:'Error says "no matching function for call to `BasicJsonDocument::BasicJsonDocument()`"',next:"compiletime/no-matching-function/basicjsondocument"},{id:"converttojson",label:"no matching function for call to <code>convertToJson(...)</code>",summary:'Error says "no matching function for call to `convertToJson(...)`"',next:"compiletime/no-matching-function/converttojson"},{id:"deserializejson",label:"no matching function for call to <code>deserializeJson(StaticJsonDocument&lt;200&gt; (&amp;)(), ...)</code>",summary:'Error says "no matching function for call to `deserializeJson(StaticJsonDocument<200> (&)(), ...)`"',next:"compiletime/no-matching-function/deserializejson"},{id:"unresolved-overloaded-function-type",label:"no matching function for call to <code>...(&lt;unresolved overloaded function type&gt;)</code>",summary:'Error says "no matching function for call to `...(<unresolved overloaded function type>)`"',next:"compiletime/no-matching-function/unresolved-overloaded-function-type"},{id:"as-char",label:"no matching function for call to <code>...as&lt;char&gt;() const</code>",summary:'Error says "no matching function for call to `...as<char>()` const"',next:"compiletime/no-matching-function/as-char"},{id:"as-char-ptr",label:"no matching function for call to <code>...as&lt;char*&gt;() const</code>",summary:'Error says "no matching function for call to `...as<char*>()` const"',next:"compiletime/no-matching-function/as-char-ptr"},{id:"not-in-list",label:"None of the above",summary:"The error is not in the list",next:"compiletime/unknown-error"}]},"compiletime/no-matching-function/unresolved-overloaded-function-type":{content:`<p>We typically see this error when your program does somethink like this:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">print</span>(doc[<span class="hljs-string">&quot;value&quot;</span>].as&lt;<span class="hljs-type">int</span>&gt;);
</code></pre>
<p>In this case, the compiler produces the following error:</p>
<pre><code class="hljs language-text">no matching function for call to &#x27;HardwareSerial::print(&lt;unresolved overloaded function type&gt;)&#x27;
</code></pre>
<p>This error occurs because <code>as&lt;int&gt;()</code> is a function, so you must call it by appending parenthesis like so:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">print</span>(doc[<span class="hljs-string">&quot;value&quot;</span>].<span class="hljs-built_in">as</span>&lt;<span class="hljs-type">int</span>&gt;());
</code></pre>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Adding the parentheses fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Adding the parentheses doesn't fix the issue",next:"deadend"}]},"compiletime/not-pointer-to-object":{content:`<p>This error comes from a bug in the <a href="https://github.com/arduino/ArduinoCore-API">Arduino Core API</a> (or <code>Arduino.h</code>, if you prefer).
This bug was fixed by <a href="https://github.com/arduino/ArduinoCore-API/pull/118">arduino/ArduinoCore-API#118</a> in October 2020, but the fix wasn't immediately propagated to derived projects.</p>
<p>First, make sure that every related piece of software is up-to-date.
Then, if the problem persists, please open an issue in the relevant project and mention <a href="https://github.com/arduino/ArduinoCore-API/pull/118">arduino/ArduinoCore-API#118</a> in the description.</p>
<p>As a workaround, you can disable support for <a href="https://www.arduino.cc/reference/en/language/variables/utilities/progmem/"><code>PROGMEM</code></a> in ArduinoJson (your board doesn't support it anyway) by setting <a href="/v6/api/config/enable_progmem/">ARDUINOJSON_ENABLE_PROGMEM</a> to <code>0</code>, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-meta">#<span class="hljs-keyword">define</span> ARDUINOJSON_ENABLE_PROGMEM 0</span>
<span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;ArduinoJson.h&gt;</span></span>
</code></pre>
<p>Did this fix your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Defining `ARDUINOJSON_ENABLE_PROGMEM` to `0` solves the issue",next:"done"},{id:"failure",label:"No",summary:"Defining `ARDUINOJSON_ENABLE_PROGMEM` to `0` doesn't solve the issue",next:"deadend"}]},"compiletime/objectref":{content:`<p>In your program, there is a line that look like this:</p>
<pre><code class="hljs language-c++">JsonObject&amp; obj = doc[<span class="hljs-string">&quot;key&quot;</span>].<span class="hljs-built_in">as</span>&lt;JsonObject&gt;();
</code></pre>
<p>This line produces the following compilation error:</p>
<pre><code class="hljs language-text">cannot bind non-const lvalue reference of type &#x27;ArduinoJson::JsonObject&amp;&#x27; {aka &#x27;ArduinoJson6185_91::ObjectRef&amp;&#x27;} to an rvalue of type &#x27;ArduinoJson6185_91::enable_if&lt;true, ArduinJson6185_91::ObjectRef&gt;::type&#x27; {aka &#x27;ArduinoJson6185_91::ObjectRef&#x27;}
</code></pre>
<p class="code-wrap"></p>
<p>In other words, &quot;cannot assign <code>JsonObject</code> to <code>JsonObject&amp;</code>&quot;.
The compiler refuses to save a reference to a temporary variable because the reference would inevitably dangle.</p>
<p>To fix this issue, you must remove the ampersand (<code>&amp;</code>) after <a href="/v6/api/jsonobject/"><code>JsonObject</code></a>, like so:</p>
<pre><code class="hljs language-c++">JsonObject obj = doc[<span class="hljs-string">&quot;key&quot;</span>].<span class="hljs-built_in">as</span>&lt;JsonObject&gt;();
</code></pre>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Replacing `JsonObject&` with `JsonObject` fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Replacing `JsonObject&` with `JsonObject` doesn't fix the issue",next:"deadend"}]},"compiletime/passing-volatile-as-this-argument-discards-qualifiers":{content:`<p>This error occurs because you declared a <code>volatile</code> variable, but the member function you're trying to call is not qualified as <code>volatile</code>. In other words, the function is not meant to be called from a <code>volatile</code> instance.</p>
<p><a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> is not safe to use on concurrent threads, so its member cannot be qualified as <code>volatile</code>.</p>
<p>To work around this issue, you must remove the <code>volatile</code> qualifier and use a thread-safe synchronization mechanism. For example, you could push values into a <a href="https://www.freertos.org/Embedded-RTOS-Queues.html">FreeRTOS queue</a> and pull them from the main thread.</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Using a queue solves the issue.",next:"done"},{id:"failure",label:"No",summary:"Cannot use a queue.",next:"deadend"}]},"compiletime/print-ambiguous":{content:`<p>This error usually occurs when you pass a <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a> to <a href="https://www.arduino.cc/reference/en/language/functions/communication/serial/print/"><code>Serial::print()</code></a> or <a href="https://www.arduino.cc/reference/en/language/functions/communication/serial/println/"><code>Serial::print()</code></a>.</p>
<p>To fix it, you need to explicitly cast the <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a> to a type supported by <a href="https://www.arduino.cc/reference/en/language/functions/communication/serial/print/"><code>Serial::print()</code></a>, like so:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">print</span>(doc[<span class="hljs-string">&quot;event&quot;</span>].<span class="hljs-built_in">as</span>&lt;<span class="hljs-type">const</span> <span class="hljs-type">char</span>*&gt;());
</code></pre>
<p><a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a> must contain (or more exactly &quot;point to&quot;) a value of the specified type; otherwise, you'll get a default value, like <code>NULL</code> or <code>0</code>. If you want to support any type of value, you must replace the call to <a href="https://www.arduino.cc/reference/en/language/functions/communication/serial/print/"><code>Serial::print()</code></a> with a call to <a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a>, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-built_in">serializeJson</span>(doc[<span class="hljs-string">&quot;event&quot;</span>], Serial);
</code></pre>
<p>You can find more information on this error here: <a href="/v6/error/call-of-overloaded-println-is-ambiguous/">call of overloaded 'println(...)' is ambiguous</a></p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Casting the value or using `serializeJson()` solves the issue",next:"done"},{id:"failure",label:"No",summary:"Neither casting the value nor using `serializeJson()` solves the issue",next:"deadend"}]},"compiletime/unknown-error":{content:`<p>Please click on the &quot;Contact Support&quot; button below and provide the following information:</p>
<ul>
<li>Compiler output (including the error message, of course!)</li>
<li>Target platform</li>
<li>Runtime/core version</li>
</ul>
<p>If you can, please also include an <a href="https://stackoverflow.com/help/mcve">MCVE</a>.</p>
`,needs_assistance:!0,choices:void 0},deadend:{content:`<p class="display-4">🤷🏻‍♂️</p>
<p>Well, I'm out of ideas: you need to talk to a human.</p>
<p>Please click the &quot;Contact Support&quot; button below and provide all the relevant information.</p>
`,needs_assistance:!0,choices:void 0},"deserialization/check-error":{content:`<p><a href="/v6/api/misc/deserializationerror/"><code>DeserializationError</code></a> is the return type of <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>. It tells whether the operation succeeded and indicates the cause of the error.</p>
<p>Modify your program to show the error code, like so:</p>
<pre><code class="hljs language-c++">DeserializationError error = <span class="hljs-built_in">deserializeJson</span>(doc, input);

Serial.<span class="hljs-built_in">print</span>(<span class="hljs-string">&quot;deserializeJson() returned &quot;</span>);
Serial.<span class="hljs-built_in">println</span>(error.<span class="hljs-built_in">c_str</span>());
</code></pre>
<p>Now, what does it show?</p>
`,needs_assistance:!1,choices:[{id:"ok",label:"<code>Ok</code>",next:"deserialization/noerror",summary:"`deserializeJson()` returns `Ok`"},{id:"emptyinput",label:"<code>EmptyInput</code>",next:"deserialization/emptyinput",summary:"`deserializeJson()` returns `EmptyInput`"},{id:"incompleteinput",label:"<code>IncompleteInput</code>",next:"deserialization/incompleteinput",summary:"`deserializeJson()` returns `IncompleteInput`"},{id:"invalidinput",label:"<code>InvalidInput</code>",next:"deserialization/invalidinput",summary:"`deserializeJson()` returns `InvalidInput`"},{id:"nomemory",label:"<code>NoMemory</code>",next:"deserialization/nomemory",summary:"`deserializeJson()` returns `NoMemory`"},{id:"notsupported",label:"<code>NotSupported</code>",next:"deserialization/notsupported",summary:"`deserializeJson()` returns `NotSupported`"},{id:"toodeep",label:"<code>TooDeep</code>",next:"deserialization/toodeep",summary:"`deserializeJson()` returns `TooDeep`"},{id:"crash",label:"I can't tell because the program crashes",next:"deserialization/crash",summary:"The program crashes"}]},"deserialization/crash/after-pointer":{content:`<p>A programs can also crash after calling <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> because it keeps a pointer to a string stored in the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>.</p>
<p>Indeed, when <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> returns a <code>const char*</code>, it doesn't return a copy of the string, but the address of the string in the memory pool. When the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> is destructed, the memory pool gets released and the pointer dangles. Later, when the program tries to use the string, it reads at an invalid memory location and crashes.</p>
<p>For example, it happens with the following program:</p>
<pre><code class="hljs language-c++"><span class="hljs-type">const</span> <span class="hljs-type">char</span>* username;

<span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">loadConfig</span><span class="hljs-params">()</span> </span>{
  File file = SD.<span class="hljs-built_in">open</span>(filename);
  StaticJsonDocument&lt;<span class="hljs-number">512</span>&gt; doc;
  <span class="hljs-built_in">deserializeJson</span>(doc, file);
  username = doc[<span class="hljs-string">&quot;username&quot;</span>];  <span class="hljs-comment">// ⚠️ stores the pointer</span>
  file.<span class="hljs-built_in">close</span>();
}

<span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">setup</span><span class="hljs-params">()</span> </span>{
  Serial.<span class="hljs-built_in">begin</span>(<span class="hljs-number">9600</span>);
  <span class="hljs-built_in">loadConfig</span>();
  Serial.<span class="hljs-built_in">println</span>(username);  <span class="hljs-comment">// 💥 BOOM!</span>
}
</code></pre>
<p>This is a simplified example; in a real project, things would be more tangled. For example, the variable could be in a <code>struct</code> or a class <code>class</code>, and the program could use the string much later.</p>
<p>To fix this program, you must make a copy of the string and not simply store the pointer. For example, you can use a <a href="https://www.arduino.cc/reference/en/language/variables/data-types/stringobject/"><code>String</code></a>, like so:</p>
<pre><code class="hljs language-c++">String username;

<span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">loadConfig</span><span class="hljs-params">()</span> </span>{
  File file = SD.<span class="hljs-built_in">open</span>(filename);
  StaticJsonDocument&lt;<span class="hljs-number">512</span>&gt; doc;
  <span class="hljs-built_in">deserializeJson</span>(doc, file);
  username = doc[<span class="hljs-string">&quot;username&quot;</span>];  <span class="hljs-comment">// ✅ stores a copy</span>
  file.<span class="hljs-built_in">close</span>();
}

<span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">setup</span><span class="hljs-params">()</span> </span>{
  Serial.<span class="hljs-built_in">begin</span>(<span class="hljs-number">9600</span>);
  <span class="hljs-built_in">loadConfig</span>();
  Serial.<span class="hljs-built_in">println</span>(username);  <span class="hljs-comment">// ✅ reads the copy</span>
}
</code></pre>
<p>Alternatively, you can use a <code>char[]</code> and <a href="https://en.cppreference.com/w/c/string/byte/strcpy"><code>strcpy()</code></a>; I invite you to check the <a href="/v6/example/config/">JsonConfigFile.ino</a> for the details.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Replacing pointer with a `String` solves the issue",next:"done"},{id:"failure",label:"No",summary:"Replacing pointer with a `String` doesn't solve the issue",next:"deadend"}]},"deserialization/crash/after-strcmp":{content:`<p>As per the standard, the behavior of <a href="https://en.cppreference.com/w/c/string/byte/strcmp"><code>strcmp()</code></a> is undefined if one of the two arguments is null.</p>
<p>That's not a problem for you because you don't need to call <a href="https://en.cppreference.com/w/c/string/byte/strcmp"><code>strcmp()</code></a>. Indeed, <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a> supports all comparisons operators (<code>==</code>, <code>&lt;</code>, <code>&gt;</code>, <code>&lt;=</code>, <code>&gt;=</code>), and these operators handle null values correctly.</p>
<p>Please replace calls to <a href="https://en.cppreference.com/w/c/string/byte/strcmp"><code>strcmp()</code></a> with <code>==</code>, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// replace this:</span>
<span class="hljs-keyword">if</span> (<span class="hljs-built_in">strcmp</span>(doc[<span class="hljs-string">&quot;event&quot;</span>], <span class="hljs-string">&quot;wakeup&quot;</span>)) ...

<span class="hljs-comment">// ...with this:</span>
<span class="hljs-keyword">if</span> (doc[<span class="hljs-string">&quot;event&quot;</span>] == <span class="hljs-string">&quot;wakeup&quot;</span>) ...
</code></pre>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Replacing `strcmp()` with `==` solves the issue",next:"done"},{id:"failure",label:"No",summary:"Replacing `strcmp()` with `==` doesn't solve the issue",next:"deadend"}]},"deserialization/crash/after-strcpy":{content:`<p>As per the standard, the behavior of <a href="https://en.cppreference.com/w/c/string/byte/strcpy"><code>strcpy()</code></a> (and <a href="https://en.wikibooks.org/wiki/C_Programming/C_Reference/nonstandard/strlcpy"><code>strlcpy()</code></a>) is undefined if one of the pointers is null.</p>
<p>The simplest solution is to change the default value returned by <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>: instead of returning a null pointer when the value is missing, we can ask it to return an empty string (i.e., <code>&quot;&quot;</code>) or some other replacement (e.g., <code>&quot;&lt;null&gt;&quot;</code>). We can do that with <a href="/v6/api/jsonvariant/or/"><code>operator|</code></a>.</p>
<p>Please provide a non-null default for all calls to <a href="https://en.cppreference.com/w/c/string/byte/strcpy"><code>strcpy()</code></a>, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// replace this:</span>
<span class="hljs-built_in">strcpy</span>(eventName, doc[<span class="hljs-string">&quot;event&quot;</span>]);

<span class="hljs-comment">// ...with this:</span>
<span class="hljs-built_in">strcpy</span>(eventName, doc[<span class="hljs-string">&quot;event&quot;</span>] | <span class="hljs-string">&quot;&lt;null&gt;&quot;</span>);
</code></pre>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Providing a non-null default solves the issue",next:"done"},{id:"failure",label:"No",summary:"Providing a non-null default doesn't solve the issue",next:"deadend"}]},"deserialization/crash/after":{content:`<p>Most of the time, when a program crashes after <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>, it's because it dereferences a null pointer returned by the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>.</p>
<p>Take the following statement for example:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// void myFunction(const char*) defined somewhere</span>

<span class="hljs-built_in">myFunction</span>(doc[<span class="hljs-string">&quot;value&quot;</span>]);
</code></pre>
<p>If the key <code>&quot;value&quot;</code> isn't in the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, the function <code>myFunction()</code> will receive a null pointer, which can cause a crash if the function isn't prepared for this.</p>
<p>Do you see something like this in your code?</p>
`,needs_assistance:!1,choices:[{id:"strcmp",label:"Yes, there is a call to <code>strcmp()</code>",summary:"Program calls `strcmp()`",next:"deserialization/crash/after-strcmp"},{id:"strcpy",label:"Yes, there is a call to <code>strcpy()</code>",summary:"Program calls `strcpy()`",next:"deserialization/crash/after-strcpy"},{id:"success",label:"Yes, I found the issue",summary:"Program dereferences a null pointer",next:"done"},{id:"no-usual-suspect",label:"No",summary:"Program calls neither `strcmp()` nor `strcpy()`",next:"deserialization/crash/after-pointer"}]},"deserialization/crash/before":{content:`<p>A stack-overflow could cause the crash.</p>
<p>Do you use a <a href="/v6/api/staticjsondocument/"><code>StaticJsonDocument</code></a>?</p>
`,needs_assistance:!1,choices:[{id:"static",label:"Yes",summary:"Program uses `StaticJsonDocument`",next:"deserialization/crash/static"},{id:"no-static",label:"No",summary:"Program doesn't use `StaticJsonDocument`",next:"deserialization/crash/dynamic"}]},"deserialization/crash/dynamic":{content:`<p>I still think this could be a stack-overflow.</p>
<p>Please check in your code if there aren't some large variable in the stack. Look for things like:</p>
<ul>
<li><code>char buffer[1024]</code></li>
<li><code>int matrix[64][64][64]</code></li>
<li><code>alloca(1024)</code></li>
</ul>
<p>If you read the JSON from a stream and place it in a buffer, you should instead pass the stream directly to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>. This techniques uses less RAM because ArduioJson will only copy the required parts in the <a href="/v6/api/dynamicjsondocument/"><code>DynamicJsonDocument</code></a> and ignore all the spaces and ponctuation. Of course, you would need a bigger capacity: use the <a href="/v6/assistant/">ArduinoJson Assistant</a> to compute the right value.</p>
<p>If there are other large buffers, you can move them to the heap by using one of the following:</p>
<ul>
<li><a href="https://www.arduino.cc/reference/en/language/variables/data-types/stringobject/"><code>String</code></a></li>
<li><a href="https://en.cppreference.com/w/cpp/container/vector"><code>std::vector</code></a></li>
<li><a href="https://en.cppreference.com/w/cpp/string/basic_string">\`std::string</a></li>
<li><a href="https://en.cppreference.com/w/cpp/memory/unique_ptr"><code>std::unique_ptr</code></a></li>
</ul>
<p>In the last resort, you can use <a href="https://en.cppreference.com/w/c/memory/malloc"><code>malloc()</code></a> and <a href="https://en.cppreference.com/w/c/memory/free"><code>free()</code></a>.</p>
<p>Does your program still crash?</p>
`,needs_assistance:!1,choices:[{id:"failure",label:"Yes",summary:"Reducing stack usage doesn't prevent the crash",next:"deadend"},{id:"success",label:"No",summary:"Reducing stack usage prevents the crash",next:"done"}]},"deserialization/crash/inside":{content:`<p>Passing a dangling pointer could cause a crash.</p>
<p>If you pass a pointer to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>, make sure that this pointer is still valid. For example, if the pointer is returned by a function, inspect the function to make sure pointer is not refering to a stack variable:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// 💀 DON&#x27;T DO THAT!!!</span>
<span class="hljs-function"><span class="hljs-type">const</span> <span class="hljs-type">char</span>* <span class="hljs-title">readInput</span><span class="hljs-params">()</span> </span>{
  <span class="hljs-type">char</span> buffer[<span class="hljs-number">256</span>];
  <span class="hljs-comment">// yada yada yada...</span>
  <span class="hljs-keyword">return</span> buffer;
}

<span class="hljs-comment">// Possible workaround</span>
<span class="hljs-function">String <span class="hljs-title">readInput</span><span class="hljs-params">()</span> </span>{
  String buffer;
  <span class="hljs-comment">// yada yada yada...</span>
  <span class="hljs-keyword">return</span> buffer;
}
</code></pre>
<p>If you pass <a href="https://www.arduino.cc/reference/en/language/variables/data-types/stringobject/"><code>String</code></a> to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>, make sure you don't call <code>c_str()</code> in between:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// 💀 DON&#x27;T DO THAT!!!</span>
<span class="hljs-type">const</span> <span class="hljs-type">char</span>* input = <span class="hljs-built_in">readInput</span>().<span class="hljs-built_in">c_str</span>();

<span class="hljs-comment">// Possible workaround</span>
String input = <span class="hljs-built_in">readInput</span>();
</code></pre>
<p>Please review your code to make sure you're not passsing a dangling pointer to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>.</p>
<p>Does your program still crash?</p>
`,needs_assistance:!1,choices:[{id:"no-dangling-ptr",label:"Yes",summary:"Removing dangling pointers doesn't prevent the crash",next:"deserialization/crash/before"},{id:"success",label:"No",summary:"Removing dangling pointers prevents the crash",next:"done"}]},"deserialization/crash/static-nomemory-capacity-0":{content:`<p>Well, that's very bad news. It seems that your microcontroller doesn't have enough RAM to hold the JSON document.</p>
<p>Please read <a href="/v6/how-to/reduce-memory-usage/">How to reduce memory usage</a>; it shows several techniques that you can implement to use less RAM.
Hopefully, you'll make enough room for the memory pool.</p>
<p>If it still doesn't fit, you'll have to upgrade to a bigger microcontroller.</p>
`,needs_assistance:!1,choices:void 0},"deserialization/crash/static-nomemory-capacity-ok":{content:`<p>Good news!<br>
The memory allocation succeeds; the <a href="/v6/api/dynamicjsondocument/"><code>DynamicJsonDocument</code></a> is simply too small, so you need to increase its capacity.</p>
<p>Use the <a href="/v6/assistant/">ArduinoJson Assistant</a> to compute the right capacity for your input.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Increasing the capacity fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Increasing the capacity doesn't fix the issue",next:"deadend"}]},"deserialization/crash/static-nomemory":{content:`<p>It looks like the allocation failed.</p>
<p>Please print the capacity of the <a href="/v6/api/dynamicjsondocument/"><code>DynamicJsonDocument</code></a> to the serial port, like so:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">println</span>(doc.<span class="hljs-built_in">capacity</span>());
</code></pre>
<p>What value does it show?</p>
`,needs_assistance:!1,choices:[{id:"capacity-0",label:"<code>0</code>",summary:"`doc.capacity()` returns `0`",next:"deserialization/crash/static-nomemory-capacity-0"},{id:"capacity-ok",label:"The value passed to the constructor of <code>DynamicJsonDocument</code>",summary:"`doc.capacity()` returns the right value",next:"deserialization/crash/static-nomemory-capacity-ok"}]},"deserialization/crash/static":{content:`<p>A big <a href="/v6/api/staticjsondocument/"><code>StaticJsonDocument</code></a> might indeed overflow the stack.
Even if there is a lot of free memory, you can run out of stack because many platforms limit the size at compile time.</p>
<p>For example, ESP8266 limits to 4096 and ESP32 limits to 8192.
These numbers might seem high, but huge part of the stack is already consumed by callers of your program. As a rule, I recommend never allocating a <a href="/v6/api/staticjsondocument/"><code>StaticJsonDocument</code></a> bigger than the half of the limit (so max 2048 on ESP8266 and 4096 on ESP32). If you need more space in your <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, you need to use a <a href="/v6/api/dynamicjsondocument/"><code>DynamicJsonDocument</code></a> instead.</p>
<p>Does switching to a <a href="/v6/api/dynamicjsondocument/"><code>DynamicJsonDocument</code></a> prevent the crash?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Switching to `DynamicJsonDocument` prevents the crash",next:"done"},{id:"nomemory",label:"Yes, but now <code>deserializeJson()</code> returns <code>NoMemory</code>",summary:"Switching to `DynamicJsonDocument` produces `NoMemory`",next:"deserialization/crash/static-nomemory"},{id:"dynamicjsondocument-too",label:"No",summary:"Switching to `DynamicJsonDocument` doesn't prevent the crash",next:"deserialization/crash/dynamic"}]},"deserialization/crash":{content:`<p>I need to know for sure if the program crashes before or after calling <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>.</p>
<p>Please add some traces around the call to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> and make sure to flush the Serial buffer. You can use the <a href="https://github.com/bblanchon/ArduinoTrace">ArduinoTrace library</a>, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-built_in">TRACE</span>();
DeserializationError err = <span class="hljs-built_in">deserializeJson</span>(doc, input);
<span class="hljs-built_in">TRACE</span>();
</code></pre>
<p>How many traces can you see in the Serial Monitor?</p>
`,needs_assistance:!1,choices:[{id:"before",label:"None: program crashes before calling <code>deserializeJson()</code>",next:"deserialization/crash/before",summary:"Program crashes before calling `deserializeJson()`"},{id:"inside",label:"Only one: program crashes inside <code>deserializeJson()</code>",next:"deserialization/crash/inside",summary:"Program crashes inside `deserializeJson()`"},{id:"after",label:"Two traces: program crashes after calling <code>deserializeJson()</code>",next:"deserialization/crash/after",summary:"Program crashes after calling `deserializeJson()`"}]},"deserialization/emptyinput/file":{content:`<p><a href="/v6/api/misc/deserializationerror/#emptyinput"><code>EmptyInput</code></a> in the context of a file usually means:</p>
<ul>
<li>the file is closed</li>
<li>the file is opened in the wrong mode</li>
<li>the file is empty</li>
</ul>
<p>Please, make sure that the file is opened in &quot;read&quot; mode and try to print the content to make sure it's not empty.</p>
<p>You can find an example using the SD library in <a href="/v6/example/config/">JsonConfigFile.ino</a> and one using SPIFFS in <a href="/book/">Mastering ArduinoJson</a>.</p>
<p>Did this sole your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"The file was the problem",next:"done"},{id:"failure",label:"No",summary:"The file is not the problem",next:"deadend"}]},"deserialization/emptyinput/http-certificate-insecure":{content:`<p>This confirms that the problem comes from the certificate validation.</p>
<p>Disabling certificate validation might represent a risk, though, because your program cannot guarantee the remote server's identity.
Sometimes this is acceptable, but in most cases, you should select an appropriate certificate validation strategy by calling <a href="https://arduino-esp8266.readthedocs.io/en/latest/esp8266wifi/bearssl-client-secure-class.html#setknownkey-const-bearssl-publickey-pk"><code>setKnownKey()</code></a>, <a href="https://arduino-esp8266.readthedocs.io/en/latest/esp8266wifi/bearssl-client-secure-class.html#setfingerprint-const-uint8-t-fp-20-setfingerprint-const-char-fpstr"><code>setFingerprint()</code></a>, or <a href="https://arduino-esp8266.readthedocs.io/en/latest/esp8266wifi/bearssl-client-secure-class.html#settrustanchors-bearssl-x509list-ta"><code>WiFiClientSecure::setTrustAnchors()</code></a>.</p>
<p>Anyway, I'm glad I could help.<br>
I would be very grateful if, in return, you could <a href="https://github.com/bblanchon/ArduinoJson/stargazers">star ArduinoJson on GitHub</a>. ❤</p>
<p>If you want to support the project (and learn a lot of stuff as well), you can purchase <a href="/book/">Mastering ArduinoJson</a>.<br>
Alternatively, you can <a href="https://github.com/sponsors/bblanchon">sponsor the project on GitHub</a>.</p>
<p>Should you have any idea on how I could improve myself, please <a href="https://github.com/bblanchon/ArduinoJson/issues/new">open an issue on GitHub</a>.</p>
<p>Bye!</p>
<p>--<br>
<em>ArduinoJson Troubleshooter</em></p>
`,needs_assistance:!1,choices:void 0},"deserialization/emptyinput/http-certificate":{content:`<p>With HTTPS, <a href="/v6/api/misc/deserializationerror/#emptyinput"><code>EmptyInput</code></a> can be due to an error during the validation of the certificate.</p>
<p>Please try to disable certificate validation by calling <a href="https://arduino-esp8266.readthedocs.io/en/latest/esp8266wifi/bearssl-client-secure-class.html#setinsecure"><code>WiFiClientSecure::setInsecure()</code></a> before starting the HTTP request.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"certificate-problem",label:"Yes",summary:"Calling `WiFiClientSecure::setInsecure()` solves the issue.",next:"deserialization/emptyinput/http-certificate-insecure"},{id:"certificate-ok",label:"No",summary:"Calling `WiFiClientSecure::setInsecure()` doesn't solve the issue.",next:"deserialization/emptyinput/http-timeout"}]},"deserialization/emptyinput/http-redirect":{content:`<p><a href="/v6/api/misc/deserializationerror/#emptyinput"><code>EmptyInput</code></a> in the context of an HTTP response usually means that the server returns a <a href="https://developer.mozilla.org/en-US/docs/Web/HTTP/Redirections">redirection code</a> (like <a href="https://en.wikipedia.org/wiki/HTTP_301">301 Moved Permanently</a> or <a href="https://en.wikipedia.org/wiki/HTTP_302">302 Found</a>), but the client doesn't follow the redirection.</p>
<p>Please check the HTTP status code in the response. If it's 301 or 302, you must modify your program to handle the redirection.</p>
<p>If you use an HTTP library, make sure that you enabled the &quot;follow redirection&quot; feature.
For example, if you use <code>HTTPClient</code> on ESP8266 or ESP32, you must call the following function before sending the request:</p>
<pre><code class="hljs language-c++">http.<span class="hljs-built_in">setFollowRedirects</span>(HTTPC_FORCE_FOLLOW_REDIRECTS);
</code></pre>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Following redirection fixes the issue",next:"done"},{id:"no-redirection",label:"No",summary:"It's not a redirection",next:"deserialization/emptyinput/http-certificate"}]},"deserialization/emptyinput/http-timeout":{content:`<p><a href="/v6/api/misc/deserializationerror/#emptyinput"><code>EmptyInput</code></a> could be caused by a timeout while reading the response.</p>
<p>Please increase the value of the timeout by calling <a href="https://www.arduino.cc/reference/en/language/functions/communication/stream/streamsettimeout/"><code>Stream::setTimeout()</code></a> before calling <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>. For example:</p>
<pre><code class="hljs language-c++">client.<span class="hljs-built_in">setTimeout</span>(<span class="hljs-number">10000</span>);  <span class="hljs-comment">// 10 seconds</span>
</code></pre>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Increasing the timeout fixes the issue",next:"done"},{id:"timeout-increased",label:"No",summary:"Increasing the timeout doesn't fix the issue",next:"deadend"}]},"deserialization/emptyinput/other-nul":{content:`<p>This confirms that the problem comes from the input: it is indeed empty, or at least starts with a NUL character.</p>
<p>You must now find a way to fix your input; I cannot help you with that. Sorry.</p>
`,needs_assistance:!1,choices:void 0},"deserialization/emptyinput/other-stream":{content:`<p>Please print the ASCII code for the first character, like so:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">println</span>(stream.<span class="hljs-built_in">peek</span>());
</code></pre>
<p>What do you see?</p>
`,needs_assistance:!1,choices:[{id:"zero",label:"<code>0</code>",summary:"The first character is NUL",next:"deserialization/emptyinput/other-nul"},{id:"non-zero",label:"Something else",summary:"The first character is not NUL",next:"deadend"}]},"deserialization/emptyinput/other-string":{content:`<p>Please print the ASCII code for the first character, like so:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">println</span>((<span class="hljs-type">int</span>)input[<span class="hljs-number">0</span>]);
</code></pre>
<p>What do you see?</p>
`,needs_assistance:!1,choices:[{id:"zero",label:"<code>0</code>",summary:"The first character is NUL",next:"deserialization/emptyinput/other-nul"},{id:"non-zero",label:"Something else",summary:"The first character is not NUL",next:"deadend"}]},"deserialization/emptyinput/other":{content:`<p>The input is probably starting with a NUL character.</p>
<p>What type of input are you passing to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>?</p>
`,needs_assistance:!1,choices:[{id:"string",label:"A string (<code>const char*</code>, <code>String</code>, <code>std::string</code>...)",summary:"Input type is a string",next:"deserialization/emptyinput/other-string"},{id:"stream",label:"A stream (<code>Serial</code>, <code>WiFiClient</code>, <code>File</code>...)",summary:"Input type is a stream",next:"deserialization/emptyinput/other-stream"}]},"deserialization/emptyinput/serial-crlf":{content:`<p><a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> also returns <a href="/v6/api/misc/deserializationerror/#emptyinput"><code>EmptyInput</code></a> when the input contains only spaces.</p>
<p>For example, this can happen when you call <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> repeatedly and there are spaces (or line breaks) between the documents. Suppose you use the following program to parse JSON input from the serial port:</p>
<pre><code class="hljs language-c++"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">loop</span><span class="hljs-params">()</span> </span>{
  <span class="hljs-comment">// wait from an incomming message</span>
  <span class="hljs-keyword">while</span> (Serial.<span class="hljs-built_in">available</span>() == <span class="hljs-number">0</span>)
    <span class="hljs-built_in">delay</span>(<span class="hljs-number">100</span>);
    
  StaticJsonDocument&lt;<span class="hljs-number">1024</span>&gt; doc;
  DeserializationError err = <span class="hljs-built_in">deserializeJson</span>(doc, Serial);
  
  ...
}
</code></pre>
<p>If you use the default settings of the Arduino Serial Monitor, <code>err</code> will contains <a href="/v6/api/misc/deserializationerror/#ok"><code>Ok</code></a> and then <a href="/v6/api/misc/deserializationerror/#emptyinput"><code>EmptyInput</code></a> each time you press &quot;Send&quot;.</p>
<p>Indeed, by default the Arduino Serial Monitor appends <a href="https://fr.wikipedia.org/wiki/Carriage_Return_Line_Feed">CRLF</a> at the end of the message, so when you enter <code>{&quot;hello&quot;:&quot;world&quot;}</code> in the input box, what is really sent is <code>{&quot;hello&quot;:&quot;world&quot;}\\r\\n</code>.
Since <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> stops reading immediately at the end of the object, the <code>\\r\\n</code> remains in the serial buffer.
Therefore, <a href="https://www.arduino.cc/reference/en/language/functions/communication/serial/available/"><code>Serial::available()</code></a> returns <code>2</code> and the program calls <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> again.
<a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> reads <code>\\r\\n</code>, which are just white spaces for him, so it continues reading until a timeout occurs (1s by default), and it finally returns <a href="/v6/api/misc/deserializationerror/#emptyinput"><code>EmptyInput</code></a>.</p>
<p>You can fix this problem by changing the configuration of the Serial Monitor to &quot;No line ending&quot;.
If you cannot remove the CR+LF at the end of the message, you must add a flush loop after <code>deserializeJson(doc, Serial)</code>, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-keyword">while</span> (<span class="hljs-built_in">isspace</span>(Serial.<span class="hljs-built_in">peek</span>())
  Serial.<span class="hljs-built_in">read</span>();
</code></pre>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Removing CRLF fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Removing CRLF doesn't fix the issue",next:"deadend"}]},"deserialization/emptyinput/serial-timeout":{content:`<p><a href="/v6/api/misc/deserializationerror/#emptyinput"><code>EmptyInput</code></a> can be caused by a timeout while waiting for the input.</p>
<p>In that case, the solution is to wait until some data is available before calling <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>. A simple loop can do the trick:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// wait from an incomming message</span>
<span class="hljs-keyword">while</span> (Serial.<span class="hljs-built_in">available</span>() == <span class="hljs-number">0</span>)
  <span class="hljs-built_in">delay</span>(<span class="hljs-number">100</span>);

<span class="hljs-built_in">deserializeJson</span>(doc, Serial);
</code></pre>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Adding a wait loop fixes the issue",next:"done"},{id:"available",label:"No",summary:"Adding a wait loop doesn't fix the issue",next:"deserialization/emptyinput/serial-crlf"}]},"deserialization/emptyinput":{content:`<p><a href="/v6/api/misc/deserializationerror/#emptyinput"><code>EmptyInput</code></a> means that the input was empty or contained only spaces or comments.</p>
<p>Where does your JSON input come from?</p>
`,needs_assistance:!1,choices:[{id:"http",label:"An HTTP response",summary:"Input comes from an HTTP response",next:"deserialization/emptyinput/http-redirect"},{id:"file",label:"A file",summary:"Input comes from a file",next:"deserialization/emptyinput/file"},{id:"serial",label:"A serial port",summary:"Input comes from a serial port",next:"deserialization/emptyinput/serial-timeout"},{id:"other",label:"Something else",summary:"Input comes neither from an HTTP response, nor a file, nor a serial port",next:"deserialization/emptyinput/other"}]},"deserialization/incompleteinput/stream-buffer":{content:`<p><a href="/v6/api/misc/deserializationerror/#incompleteinput"><code>IncompleteInput</code></a> can be caused by an interruped connection. For example, this problem happens when the client reads to slowly.</p>
<p>Indeed, because it reads bytes one by one, <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> can be slow with some implementations of <a href="https://www.arduino.cc/reference/en/language/functions/communication/stream/"><code>Stream</code></a>. To speed up the reading, we must add a buffer between the <a href="https://www.arduino.cc/reference/en/language/functions/communication/stream/"><code>Stream</code></a> and <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>. The easiest way to do this is to use <code>ReadBufferingStream</code> from the <a href="https://github.com/bblanchon/ArduinoStreamUtils">StreamUtils library</a>:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// replace the following line:</span>
<span class="hljs-built_in">deserializeJson</span>(doc, stream);

<span class="hljs-comment">// with these two lines:</span>
<span class="hljs-function">ReadBufferingStream <span class="hljs-title">bufferedStream</span><span class="hljs-params">(stream, <span class="hljs-number">64</span>)</span></span>;
<span class="hljs-built_in">deserializeJson</span>(doc, bufferedStream);
</code></pre>
<p>Thanks to <code>ReadBufferingStream</code>, the program will read the input in chunks of 64 bytes, which will be much faster.
Hopefully, this will keep the connection opened till the end of the message.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Adding a buffer solves the issue",next:"done"},{id:"same-with-buffer",label:"No",summary:"Adding a buffer doesn't solve the issue",next:"deserialization/incompleteinput/stream-log"}]},"deserialization/incompleteinput/stream-log":{content:`<p>Maybe seeing where the input stops will give you an idea of what's going wrong.</p>
<p>We can log the characters read by <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> by inserting a <code>ReadLoggingStream</code>, another class from the <a href="https://github.com/bblanchon/ArduinoStreamUtils">StreamUtils library</a>:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// replace the following line</span>
<span class="hljs-built_in">deserializeJson</span>(doc, wifiClient);

<span class="hljs-comment">// with these two lines:</span>
<span class="hljs-function">ReadLoggingStream <span class="hljs-title">loggingStream</span><span class="hljs-params">(stream, Serial)</span></span>;
<span class="hljs-built_in">deserializeJson</span>(doc, loggingStream);
</code></pre>
<p>Thanks to <code>ReadLoggingStream</code>, this program prints every character read by <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>. Therefore, you'll be able to see where the input stops; hopefully, this will give you a better understanding of the problem.</p>
<p>When using <code>ReadLoggingStream</code>, it's crucial that you configure the serial port with a very high baud rate; otherwise the log will slow down the reading process, which is a problem as I explained previously.</p>
<p>Did this information help you solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Logging helped fixing the issue",next:"done"},{id:"failure",label:"No",summary:"Logging didn't help fixing the issue",next:"deadend"}]},"deserialization/incompleteinput/stream-timeout":{content:`<p><a href="/v6/api/misc/deserializationerror/#incompleteinput"><code>IncompleteInput</code></a> can be caused by a timeout.</p>
<p>Please increase the value of the timeout by calling <a href="https://www.arduino.cc/reference/en/language/functions/communication/stream/streamsettimeout/"><code>Stream::setTimeout()</code></a> before calling <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>. For example:</p>
<pre><code class="hljs language-c++">stream.<span class="hljs-built_in">setTimeout</span>(<span class="hljs-number">10000</span>);  <span class="hljs-comment">// 10 seconds</span>
</code></pre>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Inreasing the timeout solves the issue",next:"done"},{id:"timeout-increased",label:"No",summary:"Inreasing the timeout doesn't solve the issue",next:"deserialization/incompleteinput/stream-buffer"}]},"deserialization/incompleteinput/string-object":{content:`<p>Please print the content of the string to the serial port, like so:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">println</span>(input);
</code></pre>
<p>You should see that the JSON document is truncated.
This can be caused by a lack of RAM: the string object failed to allocate a buffer large enough for the whole document.</p>
<p>This issue may come from <a href="https://cpp4arduino.com/2018/11/06/what-is-heap-fragmentation.html">heap fragmentated</a>.
Fixing this problem is very hard because it involve fixing the whole program, not just the JSON deserialization.</p>
<p>Yet, you can try to preallocate the buffer by calling <a href="https://www.arduino.cc/reference/en/language/variables/data-types/string/functions/reserve/"><code>String::reserve()</code></a> before loading the content of the input, like so:</p>
<pre><code class="hljs language-c++">input.<span class="hljs-built_in">reserve</span>(<span class="hljs-number">1024</span>);  <span class="hljs-comment">// adapt the value to the size of your input</span>
</code></pre>
<p>Hopefully, you'll be able to reserve a buffer large enough to store the whole string.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Calling `String::reserve()` solves the issue",next:"done"},{id:"same-with-reserve",label:"No",summary:"Calling `String::reserve()` doesn't solve the issue",next:"deserialization/incompleteinput/string-stream"}]},"deserialization/incompleteinput/string-pointer":{content:`<p>I'll assume that your input is stored in a buffer similar to this one:</p>
<pre><code class="hljs language-c++"><span class="hljs-type">char</span> input[<span class="hljs-number">1024</span>];
<span class="hljs-type">size_t</span> input_size;
</code></pre>
<p>Please print the content of the string to the serial port, like so:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">write</span>(input, input_size);
</code></pre>
<p>You should see that the JSON document is truncated.
This is probably caused by the size of the buffer: it's too small to store the complete document.</p>
<p>Please increase the capacity of the buffer, but remember that the size of the stack is limited so you may need to move your buffer to the heap.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Increasing the buffer size solves the issue",next:"done"},{id:"larger-buffer",label:"No",summary:"Increasing the buffer size doesn't solve the issue",next:"deserialization/incompleteinput/string-stream"}]},"deserialization/incompleteinput/string-stream":{content:`<p>To reduce the memory usage, you should try to pass the source (file, connection, stream, etc) directly to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>, either via the <a href="https://www.arduino.cc/reference/en/language/functions/communication/stream/"><code>Stream</code></a> interface or via a <a href="/news/2019/11/01/version-6-13-0/">custom reader</a>. This will avoid the need for an intermediate buffer.</p>
<p>As usual, make sure the capacity of the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> is adapted to your needs by using the <a href="/v6/assistant/">ArduinoJson Assistant</a></p>
<p>Additionaly, you can add a <a href="/news/2020/03/22/version-6-15-0/">filter</a> to remove all unneeded information from the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>.</p>
<p>You can see examples here:</p>
<ul>
<li><a href="/v6/example/config/">JsonConfigFile.ino</a></li>
<li><a href="/v6/example/http-client/">JsonHttpClient.ino</a></li>
<li><a href="/v6/doc/deserialization/">Deserialization tutorial</a></li>
<li><a href="/book/">Mastering ArduinoJson</a></li>
</ul>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Reading from stream fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Reading from stream doesn't fix the issue",next:"deadend"}]},"deserialization/incompleteinput":{content:`<p><a href="/v6/api/misc/deserializationerror/#incompleteinput"><code>IncompleteInput</code></a> means that <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> managed to parse the beginning of the JSON document, but the end was missing.</p>
<p>What type of input do you pass to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>?</p>
`,needs_assistance:!1,choices:[{id:"stream",label:"A stream",summary:"Input comes from a stream",next:"deserialization/incompleteinput/stream-timeout"},{id:"string",label:"A string object (like <code>String</code>)",summary:"Input comes from a string object",next:"deserialization/incompleteinput/string-object"},{id:"pointer",label:"A pointer (like <code>char*</code> or <code>const char*</code>)",summary:"Input comes from a pointer",next:"deserialization/incompleteinput/string-pointer"}]},deserialization:{content:`<p>What is the value of <a href="/v6/api/misc/deserializationerror/"><code>DeserializationError</code></a>?</p>
`,needs_assistance:!1,choices:[{id:"ok",label:"<code>Ok</code>",next:"deserialization/noerror",summary:"`deserializeJson()` returns `Ok`"},{id:"emptyinput",label:"<code>EmptyInput</code>",next:"deserialization/emptyinput",summary:"`deserializeJson()` returns `EmptyInput`"},{id:"incompleteinput",label:"<code>IncompleteInput</code>",next:"deserialization/incompleteinput",summary:"`deserializeJson()` returns `IncompleteInput`"},{id:"invalidinput",label:"<code>InvalidInput</code>",next:"deserialization/invalidinput",summary:"`deserializeJson()` returns `InvalidInput`"},{id:"nomemory",label:"<code>NoMemory</code>",next:"deserialization/nomemory",summary:"`deserializeJson()` returns `NoMemory`"},{id:"notsupported",label:"<code>NotSupported</code>",next:"deserialization/notsupported",summary:"`deserializeJson()` returns `NotSupported`"},{id:"toodeep",label:"<code>TooDeep</code>",next:"deserialization/toodeep",summary:"`deserializeJson()` returns `TooDeep`"},{id:"crash",label:"I can't tell because the program crashes",next:"deserialization/crash",summary:"The program crashes"},{id:"unknown",label:"I don't know what you're talking about",next:"deserialization/check-error",summary:"The program doesn't check the error"}]},"deserialization/invalidinput/array-twice-cast":{content:`<p>If you must keep the two calls to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>, you need to disable the zero-copy mode by passing a read-only input buffer. Casting the pointer will do the trick:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// replace this:</span>
<span class="hljs-built_in">deserializeJson</span>(doc, input, inputSize);

<span class="hljs-comment">// ...with this:</span>
<span class="hljs-built_in">deserializeJson</span>(doc, (<span class="hljs-type">const</span> <span class="hljs-type">char</span>*)input, inputSize);
</code></pre>
<p>Now that the zero-copy mode is disabled, you probably need a larger <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, so don't forget to update the capacity. As usual, use the <a href="/v6/assistant/">ArduinoJson Assistant</a> to compute the right capacity for your project.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Casting the pointer fixes the issue",next:"done"},{id:"cast",label:"No",summary:"Casting the pointer doesn't fix the issue",next:"deserialization/invalidinput/string-jsonlint"}]},"deserialization/invalidinput/array-twice":{content:`<p>When you pass a writeable buffer as the input of <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>, ArduinoJson uses the zero-copy mode. Instead of copying the strings from the input into the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, it stores pointers to the input buffer.</p>
<p>While doing this, the parser modifies the input buffer to unescape special characters and add string terminator.
When <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> returns the input buffer doesn't contain a valid JSON document anymore.
For this reason, you cannot call <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> twice with the same input buffer (if the input is writable).</p>
<p>I don't see any reason for calling <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> twice with the same input, so I recommend that your remove the second call.</p>
<p>Can you avoid duplicate calls to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes, and it fixes the issue",summary:"Removing the second call fixes the issue",next:"done"},{id:"second-removed",label:"Yes, but it doesn't fix the issue",summary:"Removing the second call doesn't fix the issue",next:"deserialization/invalidinput/string-jsonlint"},{id:"cannot-remove",label:"No",summary:"The second call cannot be removed",next:"deserialization/invalidinput/array-twice-cast"}]},"deserialization/invalidinput/array":{content:`<p>Did you call <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> twice with the same input buffer?</p>
`,needs_assistance:!1,choices:[{id:"two-calls",label:"Yes",summary:"Program calls `deserializeJson()` twice",next:"deserialization/invalidinput/array-twice"},{id:"one-call",label:"No",summary:"Program doesn't call `deserializeJson()` twice",next:"deserialization/invalidinput/string-jsonlint"}]},"deserialization/invalidinput/bom-none":{content:`<p>So, the JSON document is valid and not preceded by anything; yet, <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> returns <a href="/v6/api/misc/deserializationerror/#invalidinput"><code>InvalidInput</code></a>.</p>
<p>Well... I'm out of idea :-(</p>
`,needs_assistance:!0,choices:void 0},"deserialization/invalidinput/bom-utf16":{content:`<p><code>254</code> or <code>255</code> is the first byte of the UTF-16 <a href="https://en.wikipedia.org/wiki/Byte_order_mark">Byte Order Mark</a> (BOM). ArduinoJson doesn't support UTF-16; it only supports ASCII and UTF-8. As a workaround, we can ask the server to encode the response with UTF-8.</p>
<p>To do this, add the following header to the HTTP request:</p>
<pre><code class="hljs language-http"><span class="hljs-attribute">Accept</span><span class="hljs-punctuation">: </span>application/json;charset=utf-8
</code></pre>
<p>Did this solve your problem?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"`Accept: application/json;charset=utf-8` solves the issue",next:"done"},{id:"failure",label:"No",summary:"`Accept: application/json;charset=utf-8` doesn't solve the issue",next:"deadend"}]},"deserialization/invalidinput/bom-utf8":{content:`<p><code>239</code> is the first by of the UTF-8 <a href="https://en.wikipedia.org/wiki/Byte_order_mark">Byte Order Mark</a> (BOM).</p>
<p>ArduinoJson doesn't skip the BOM, you need to do it in your program by adding the following lines <strong>before</strong> calling <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// skip BOM</span>
stream.<span class="hljs-built_in">read</span>();  <span class="hljs-comment">// 0xEF</span>
stream.<span class="hljs-built_in">read</span>();  <span class="hljs-comment">// 0xBB</span>
stream.<span class="hljs-built_in">read</span>();  <span class="hljs-comment">// 0xBF</span>
</code></pre>
<p>Admitedly, this is quite hacky, so I recommend that you try to fix the server which should not include a BOM anyway.</p>
<p>Did this fix your problem?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Skipping the first 3 bytes solves the issue",next:"done"},{id:"failure",label:"No",summary:"Skipping the first 3 bytes doesn't solve the issue",next:"deadend"}]},"deserialization/invalidinput/file-bom":{content:`<p>We must check that the JSON document is not preceded by a <a href="https://en.wikipedia.org/wiki/Byte_order_mark">Byte Order Mark</a> (BOM).</p>
<p>To do this, we'll peek at the first byte in the file and print the value to the serial port. Insert the following line <em>before</em> the call to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">println</span>(file.<span class="hljs-built_in">peek</span>());
</code></pre>
<p>What value does it print?</p>
`,needs_assistance:!1,choices:[{id:"utf8",label:"<code>239</code>",summary:"Input's first byte suggests a UTF-8 BOM",next:"deserialization/invalidinput/bom-utf8"},{id:"utf16",label:"<code>254</code> or <code>255</code>",summary:"Input's first byte suggests a UTF-16 BOM",next:"deserialization/invalidinput/bom-utf16"},{id:"no-bom",label:"Something else",summary:"Input's first byte doesn't suggest a BOM",next:"deserialization/invalidinput/bom-none"}]},"deserialization/invalidinput/file-jsonlint":{content:`<p>We need to make sure that the JSON document is valid.
To do that, please print the input to the serial port, like so:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">print</span>(file.<span class="hljs-built_in">readString</span>());
</code></pre>
<p>Then, copy the content and paste it in a JSON linter, like <a href="https://jsonlint.com/">jsonlint.com</a>.</p>
<p>Is the JSON document valid?</p>
`,needs_assistance:!1,choices:[{id:"valid",label:"Yes",summary:"`jsonlint` says the document is valid",next:"deserialization/invalidinput/file-bom"},{id:"invalid",label:"No",summary:"`jsonlint` says the document is invalid",next:"deserialization/invalidinput/jsonlint-bad"}]},"deserialization/invalidinput/http-library":{content:`<p>Unfortunately, I only know how to use the following HTTP libraries:</p>
<ul>
<li><a href="https://github.com/esp8266/Arduino/tree/master/libraries/ESP8266HTTPClient">ESP8266HTTPClient</a></li>
<li><a href="https://github.com/espressif/arduino-esp32/tree/master/libraries/HTTPClient">HTTPClient for ESP32</a></li>
</ul>
`,needs_assistance:!0,choices:void 0},"deserialization/invalidinput/http-manual-headers-warning":{content:`<p>On some Arduino core (most notably <a href="https://github.com/arduino/ArduinoCore-avr/blob/master/cores/arduino/Stream.h">AVR core</a>), <a href="https://www.arduino.cc/reference/en/language/functions/communication/stream/streamfind/"><code>Stream::find()</code></a> takes a <code>char*</code> instead of a <code>const char*</code>. In this case, you'll get a compiler warning, which you can fix by extracting a char array, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-type">char</span> endOfHeaders[] = <span class="hljs-string">&quot;\\r\\n\\r\\n&quot;</span>;
client.<span class="hljs-built_in">find</span>(endOfHeaders);
</code></pre>
<p>Did this fix your problem?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Using a `char[]` fixes the warning",next:"done"},{id:"failure",label:"No",summary:"Using a `char[]` doesn't fix the warning",next:"deadend"}]},"deserialization/invalidinput/http-manual-headers":{content:`<p>Just like the HTTP request, the response contains some headers, followed by an empty line, and then followed by the body.
The body is the part that contains the JSON document; therefore, before calling <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>, you must skip the headers.</p>
<p>To skip the headers, call <a href="https://www.arduino.cc/reference/en/language/functions/communication/stream/streamfind/"><code>Stream::find()</code></a>, like so:</p>
<pre><code class="hljs language-c++">client.<span class="hljs-built_in">find</span>(<span class="hljs-string">&quot;\\r\\n\\r\\n&quot;</span>);
</code></pre>
<p>This function consumes the input stream until it finds the empty line (<code>\\r\\n\\r\\n</code>).</p>
<p>See the example <a href="/v6/example/http-client/">JsonHttpClient.ino</a> for a complete implementation.</p>
<p>Did this fix your problem?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Skipping HTTP headers solves the issue",next:"done"},{id:"warning",label:"Yes, but there is a compiler warning about <code>client.find()</code>",summary:"Skipping HTTP headers solves the issue, but there is a warning",next:"deserialization/invalidinput/http-manual-headers-warning"},{id:"header-skipped",label:"No",summary:"Skipping HTTP headers doesn't solve the issue",next:"deserialization/invalidinput/http-manual-http10"}]},"deserialization/invalidinput/http-manual-http10":{content:`<p>When you use HTTP 1.1, the server can send the response with <a href="https://en.wikipedia.org/wiki/Chunked_transfer_encoding">Chunked Transfer Encoding</a>.
As a result, the response's body may contain some hexadecimal number at the begining of each chunk.
The most straightforward workaround is to downgrade the connection to HTTP 1.0.</p>
<p>To do this, replace <code>HTTP/1.1</code> with <code>HTTP/1.0</code> in the first line of the request.</p>
<p>Did this solve your problem?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Using `HTTP/1.0` solves the issue",next:"done"},{id:"http10",label:"No",summary:"Using `HTTP/1.0` doesn't solve the issue",next:"deserialization/invalidinput/inputtype"}]},"deserialization/invalidinput/http-manual":{content:`<p>Before calling <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>, did you skip the HTTP headers?</p>
`,needs_assistance:!1,choices:[{id:"headers-skipped",label:"Yes",summary:"HTTP headers are skipped",next:"deserialization/invalidinput/http-manual-http10"},{id:"headers-not-skipped",label:"No",summary:"HTTP headers are not skipped",next:"deserialization/invalidinput/http-manual-headers"}]},"deserialization/invalidinput/http":{content:`<p>Do you use an HTTP library?</p>
`,needs_assistance:!1,choices:[{id:"httpclient",label:"Yes, I'm using <code>HTTPClient</code>",summary:"Program uses `HTTPClient`",next:"deserialization/invalidinput/httpclient"},{id:"http-library",label:"Yes, but it's not <code>HTTPClient</code>",summary:"Program uses an unknown HTTP library",next:"deserialization/invalidinput/http-library"},{id:"no-library",label:"No",summary:"Program doesn't use an HTTP library",next:"deserialization/invalidinput/http-manual"}]},"deserialization/invalidinput/httpclient-readstring":{content:`<p>You're calling the wrong function:</p>
<ul>
<li><code>HTTPClient::getString()</code> extracts the response's body</li>
<li><code>HTTPClient::readString()</code> is inherited from <a href="https://www.arduino.cc/reference/en/language/functions/communication/stream/"><code>Stream</code></a> and reads the complete response</li>
</ul>
<p>Unless you have very good reasons, you should call <code>getString()</code> not <code>readString()</code>.</p>
<p>Do calling <code>HTTPClient::getString()</code> solves your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Calling `HTTPClient::getString()` solves the issue",next:"done"},{id:"getstring",label:"No",summary:"Calling `HTTPClient::getString()` doesn't solves the issue",next:"deserialization/invalidinput/string-jsonlint"}]},"deserialization/invalidinput/httpclient-stream-http10":{content:`<p>When you call <code>HTTPClient::getStream()</code>, you bypass the part that handles <a href="https://en.wikipedia.org/wiki/Chunked_transfer_encoding">Chunked Transfer Encoding</a>.
As a result, the response's body may contain some hexadecimal number at the begining of each chunk.
The most straightforward workaround is to downgrade the connection to HTTP 1.0.</p>
<p>To do this, add the following line <strong>before</strong> sending the request:</p>
<pre><code class="hljs language-c++">http.<span class="hljs-built_in">useHTTP10</span>(<span class="hljs-literal">true</span>);  <span class="hljs-comment">// avoid Chunked Transfer Encoding</span>
</code></pre>
<p>Did this solve your problem?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Calling `HTTPClient::useHTTP10(true)` solves the issue",next:"done"},{id:"changed",label:"No",summary:"Calling `HTTPClient::useHTTP10(true)` doesn't solve the issue",next:"deserialization/invalidinput/httpclient-stream-jsonlint"}]},"deserialization/invalidinput/httpclient-stream-jsonlint":{content:`<p>We need to make sure that the JSON document is valid.
To do that, please print the content of the response to the serial port like so:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">print</span>(http.<span class="hljs-built_in">getStream</span>().<span class="hljs-built_in">readString</span>());
</code></pre>
<p>Then, copy the response and paste it in a JSON linter, like <a href="https://jsonlint.com/">jsonlint.com</a>.</p>
<p>Is the JSON document valid?</p>
`,needs_assistance:!1,choices:[{id:"valid",label:"Yes",summary:"jsonlint says the document is valid",next:"deserialization/invalidinput/stream-bom"},{id:"invalid",label:"No",summary:"jsonlint says the document is invalid",next:"deserialization/invalidinput/jsonlint-bad"}]},"deserialization/invalidinput/httpclient-stream":{content:`<p>Did you call <code>HTTPClient::useHTTP10(true)</code>?</p>
`,needs_assistance:!1,choices:[{id:"http10-true",label:"Yes",summary:"Program calls `HTTPClient::useHTTP10(true)`",next:"deserialization/invalidinput/httpclient-stream-jsonlint"},{id:"http10-false",label:"No",summary:"Program doesn't call `HTTPClient::useHTTP10(true)`",next:"deserialization/invalidinput/httpclient-stream-http10"}]},"deserialization/invalidinput/httpclient":{content:`<p>Which function of <code>HTTPClient</code> do you call to get the response?</p>
`,needs_assistance:!1,choices:[{id:"getstream",label:"<code>getStream()</code>",summary:"Response comes from `HTTPClient::getStream()`",next:"deserialization/invalidinput/httpclient-stream"},{id:"getstring",label:"<code>getString()</code>",summary:"Response comes from `HTTPClient::getString()`",next:"deserialization/invalidinput/string-jsonlint"},{id:"readstring",label:"<code>readString()</code>",summary:"Response comes from `HTTPClient::readString()`",next:"deserialization/invalidinput/httpclient-readstring"}]},"deserialization/invalidinput/inputtype":{content:`<p>What's the type of the input that you pass to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a></p>
`,needs_assistance:!1,choices:[{id:"stream",label:"A stream",summary:"The input is a stream",next:"deserialization/invalidinput/stream-bom"},{id:"string",label:"A string",summary:"The input is a string",next:"deserialization/invalidinput/string-bom"}]},"deserialization/invalidinput/jsonlint-bad":{content:`<p>What is wrong with this JSON document?</p>
`,needs_assistance:!1,choices:[{id:"comments",label:"It contains comments",summary:"The document contains comments",next:"deserialization/invalidinput/jsonlint-comments"},{id:"other-error",label:"Something else",summary:"The document doesn't contain comments",next:"deserialization/invalidinput/jsonlint-other"}]},"deserialization/invalidinput/jsonlint-comments":{content:`<p><a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> can read JSON documents that contain comments, but the support is disabled by default to reduce the footprint of the library.</p>
<p>To enable the support for comments, define <a href="/v6/api/config/enable_comments/"><code>ARDUINOJSON_ENABLE_COMMENTS</code></a> to <code>1</code> before including <code>ArduinoJson.h</code>, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-meta">#<span class="hljs-keyword">define</span> ARDUINOJSON_ENABLE_COMMENTS 1</span>
<span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;ArduinoJson.h&gt;</span></span>
</code></pre>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Setting ARDUINOJSON_ENABLE_COMMENTS to 1 solves the issue",next:"done"},{id:"failure",label:"No",summary:"Setting ARDUINOJSON_ENABLE_COMMENTS to 1 doesn't solve the issue",next:"deadend"}]},"deserialization/invalidinput/jsonlint-other":{content:`<p>Well, if the JSON document is invalid, you have to fix it, otherwise ArduinoJson won't be able to parse it.</p>
<p>The ball is in your court; there is nothing more I can do.</p>
`,needs_assistance:!1,choices:void 0},"deserialization/invalidinput/serial-buffer":{content:`<p>Sometimes, the input is invalid because some bytes were dropped when receiving the document.</p>
<p>This problem occurs when the sender writes bytes faster than the receiver reads them, filling up the receiver's serial buffer until it overflows and drops incoming bytes.
For example, this problem happens when the receiver is busy doing some other task while the sender is transmitting.
It also occurs when the receiver logs incoming data at a slower rate; that's why it's crucial to get the &quot;debug&quot; serial port running much faster than the &quot;communication&quot; serial.</p>
<p>You could solve this issue by increasing the serial buffer size; the details depend on each platform.
For Arduino UNO, the default is 64 and can be changed by defining the <code>SERIAL_RX_BUFFER_SIZE</code> macro.
For <a href="https://en.wikipedia.org/wiki/ESP8266">ESP8266</a> and <a href="https://en.wikipedia.org/wiki/ESP32">ESP32</a>, the default is 256 and can be changed by calling <code>setRxBufferSize()</code>.</p>
<p>If your JSON document is significantly larger than the buffer, please try to increase the buffer size.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Increasing serial buffer size solves the issue",next:"done"},{id:"larger-buffer",label:"No",summary:"Increasing serial buffer size doesn't solves the issue",next:"deserialization/invalidinput/stream-bom"}]},"deserialization/invalidinput/serial-cable":{content:`<p>Any electric wire acts as an antenna; the longer the wire, the stronger the effect. This antenna picks up every electromagnetic field in the environment, which induces a current in the wire. On long wires, this current is strong enough to introduce errors in the communication.</p>
<p>Reducing the length of the cable certainly improve the error ratio, but is rarely applicable.
We can, however, improve the quality of the cable.
For example, you can replace the wires with a coaxial cable: the shielding will prevent the inner wire from acting as an antenna.</p>
<p>Please upgrade or shorten your cable.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Upgrading the cables solves the issue",next:"done"},{id:"improved",label:"Somehow",summary:"Upgrading the cables improves the situation",next:"deserialization/invalidinput/serial-hamming"},{id:"no-improvment",label:"No",summary:"Upgrading the cables doesn't solve the issue",next:"deserialization/invalidinput/serial-jsonlint"}]},"deserialization/invalidinput/serial-flush":{content:`<p><a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> may return <a href="/v6/api/misc/deserializationerror/#invalidinput"><code>InvalidInput</code></a> because it starts reading the input mid-stream.</p>
<p>For example, it can happen if your program calls <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> in a loop like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">loop</span><span class="hljs-params">()</span> </span>{
  <span class="hljs-keyword">if</span> (Serial1.<span class="hljs-built_in">available</span>()) {
    StaticJsonDocument&lt;<span class="hljs-number">64</span>&gt; doc;
    DeserializationError err = <span class="hljs-built_in">deserializeJson</span>(doc, Serial1);

    <span class="hljs-keyword">if</span> (err) {
      Serial.<span class="hljs-built_in">println</span>(err.<span class="hljs-built_in">c_str</span>());
      <span class="hljs-keyword">return</span>;
    }
}
</code></pre>
<p>The problem with this program is that, if <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> returns an error (such as <a href="/v6/api/misc/deserializationerror/#nomemory"><code>NoMemory</code></a>), any subsequent call to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> will return <a href="/v6/api/misc/deserializationerror/#invalidinput"><code>InvalidInput</code></a>. Indeed, <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> stops reading as soon as it encounters an error, so the remainder of the document is still in the serial buffer.</p>
<p>The solution is to flush the serial buffer any time an error is detected:</p>
<pre><code class="hljs language-c++"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">loop</span><span class="hljs-params">()</span> </span>{
  <span class="hljs-keyword">if</span> (Serial1.<span class="hljs-built_in">available</span>()) {
    StaticJsonDocument&lt;<span class="hljs-number">64</span>&gt; doc;
    DeserializationError err = <span class="hljs-built_in">deserializeJson</span>(doc, Serial1);

    <span class="hljs-keyword">if</span> (err) {
      Serial.<span class="hljs-built_in">println</span>(err.<span class="hljs-built_in">c_str</span>());

      <span class="hljs-keyword">while</span> (Serial1.<span class="hljs-built_in">available</span>() &gt; <span class="hljs-number">0</span>)
        Serial1.<span class="hljs-built_in">read</span>();

      <span class="hljs-keyword">return</span>;
    }
}
</code></pre>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Flushing the serial buffer fixes the issue",next:"done"},{id:"flush",label:"No",summary:"Flushing the serial buffer doesn't fix the issue",next:"deserialization/invalidinput/serial-voltage"}]},"deserialization/invalidinput/serial-hamming":{content:`<p><a href="https://en.wikipedia.org/wiki/Error_correction_code">Error-correction codes (ECC)</a> are a way of transmitting the data with redundant information that allows the receiver to fix most of the errors.
The most basic error-correction code is <a href="https://en.wikipedia.org/wiki/Hamming(7,4)">Hamming(7,4)</a>, which transmits 7 bits for every 4 bits of actual data.
In other words, it adds 3 bits of redundancy for every 4 bits that you send.
The magic with this code is that it can correct any 1-bit error in the 7 bits.</p>
<p>The simplest way to implement <a href="https://en.wikipedia.org/wiki/Hamming(7,4)">Hamming(7,4)</a> on Arduino is to use the <code>HammingStream</code> class from the <a href="https://github.com/bblanchon/ArduinoStreamUtils">StreamUtils</a> library:</p>
<pre><code class="hljs language-c++"><span class="hljs-function">HammingStream&lt;7, 4&gt; <span class="hljs-title">eccLinkSerial</span><span class="hljs-params">(linkSerial)</span></span>;
</code></pre>
<p>Now, you can use <code>eccSerial1</code> in place of the original <code>Serial1</code>; it will automatically encode and decode the information.</p>
<p>As <a href="https://en.wikipedia.org/wiki/Hamming(7,4)">Hamming(7,4)</a> only transmits 7 bits of data, you can safely downgrade the serial link from 8 to 7 bits.
You can do this by passing <code>SERIAL_7N1</code> as the second argument of <a href="https://www.arduino.cc/reference/en/language/functions/communication/serial/begin/"><code>Serial::begin()</code></a>.
This feature is not supported by <a href="https://www.arduino.cc/en/Reference/SoftwareSerial"><code>SoftwareSerial</code></a>, which is yet another reason to avoid it.</p>
<p>Error-correction codes are very powerful, but they'll never eliminate errors completely. For example, <a href="https://en.wikipedia.org/wiki/Hamming(7,4)">Hamming(7,4)</a> can only fix a 1-bit error, so if two or more bits are swapped, it will not fix them.
To get more confidence in the integrity of the received data, the ultimate solution is to add an error detection scheme, like a checksum.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Error-correction codes fix the issue",next:"done"},{id:"ecc",label:"No",summary:"Error-correction codes doesn't fix the issue",next:"deserialization/invalidinput/serial-jsonlint"}]},"deserialization/invalidinput/serial-jsonlint":{content:`<p>We need to make sure that the JSON document is valid.
To do that, please print the input to the serial port, like so:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">print</span>(Serial1.<span class="hljs-built_in">readString</span>());
</code></pre>
<p>Note that <a href="https://www.arduino.cc/reference/en/language/functions/communication/serial/readstring/"><code>Serial1.readString()</code></a> will wait for a timeout before returning, so the statement may appear frozen. Be patient.</p>
<p>Then, copy the response and paste it in a JSON linter, like <a href="https://jsonlint.com/">jsonlint.com</a>.</p>
<p>Is the JSON document valid?</p>
`,needs_assistance:!1,choices:[{id:"valid",label:"Yes",summary:"`jsonlint` says the document is valid",next:"deserialization/invalidinput/serial-buffer"},{id:"invalid",label:"No",summary:"`jsonlint` says the document is invalid",next:"deserialization/invalidinput/jsonlint-bad"}]},"deserialization/invalidinput/serial-rate":{content:`<p>Any kind of communication is subject to errors: sometimes you send a <code>1</code> and receive a <code>0</code>.
The problem with the serial port is that there is no built-in way to fix these errors.
However, we can take some measures to reduce the error ratio to something acceptable.</p>
<p>The faster the communication, the higher the error ratio, so the first thing to try is to reduce the communication speed.
Please try to reduce the baud rate.</p>
<p class="short-warning">If your program receives from one port and logs to another one, <strong>make sure the latter runs at a much higher speed</strong>. Logging must be at least ten times faster, or it will slow down the receiving port, which may drop incoming bytes.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Reducing baud rate fixes the issue",next:"done"},{id:"better",label:"Somehow",summary:"Reducing baud rate improves the situation",next:"deserialization/invalidinput/serial-cable"},{id:"not-better",label:"No",summary:"Reducing baud rate doesn't fix the issue",next:"deserialization/invalidinput/serial-jsonlint"}]},"deserialization/invalidinput/serial-software":{content:`<p>The <a href="https://en.wikipedia.org/wiki/AVR_microcontrollers">AVR</a> implementation of <a href="https://www.arduino.cc/en/Reference/SoftwareSerial"><code>SoftwareSerial</code></a> is <strong>notoriously unreliable</strong> 😱.
The main problem is that it disables interrupts when sending data, which causes many issues like dropping incoming bytes on the regular <a href="https://www.arduino.cc/reference/en/language/functions/communication/serial/"><code>Serial</code></a>.</p>
<p>You may consider the following alternative libraries, but none of them is perfect:</p>
<ul>
<li><a href="https://github.com/PaulStoffregen/AltSoftSerial">AltSoftSerial</a></li>
<li><a href="https://github.com/MustBeArt/SlowSoftSerial">SlowSoftSerial</a></li>
<li><a href="https://github.com/SlashDevin/NeoSWSerial">NeoSoftSerial</a></li>
</ul>
<p>However, I highly recommend that you use a hardware implementation (such as <code>Serial</code> or <code>Serial1</code>). Don't hesitate to upgrade your board to get one with more than one <a href="https://en.wikipedia.org/wiki/Universal_asynchronous_receiver-transmitter">UART</a>.</p>
<p>For example, if you're currently using an Arduino UNO, which has only one UART, you can upgrade to the Arduino Leonardo, which has three.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Removing `SoftwareSerial` solves the issue",next:"done"},{id:"removed",label:"No",summary:"Removing `SoftwareSerial` doesn't solve the issue",next:"deserialization/invalidinput/serial-voltage"}]},"deserialization/invalidinput/serial-voltage":{content:`<p>Not all microcontrollers use the same voltage for the serial port.
Some use 5V logic; others use 3.3V.</p>
<p>For example, the Arduino UNO uses 5V whereas the ESP8266 uses 3.3V.</p>
<p>If you need to wire two devices with different voltages, you need a <a href="https://www.amazon.com/dp/B00NAY2BBY?tag=bblanchon0b-20">logic-level converter</a>.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"The issue is a voltage mismatch",next:"done"},{id:"voltage-ok",label:"No",summary:"The issue is not a voltage mismatch",next:"deserialization/invalidinput/serial-rate"}]},"deserialization/invalidinput/serial":{content:`<p>Are you using <a href="https://www.arduino.cc/en/Reference/SoftwareSerial"><code>SoftwareSerial</code></a>?</p>
`,needs_assistance:!1,choices:[{id:"software",label:"Yes",summary:"Program uses `SoftwareSerial`",next:"deserialization/invalidinput/serial-software"},{id:"hardware",label:"No",summary:"Program doesn't use `SoftwareSerial`",next:"deserialization/invalidinput/serial-flush"}]},"deserialization/invalidinput/stream-bom":{content:`<p>We must check that the JSON document is not preceded by a  <a href="https://en.wikipedia.org/wiki/Byte_order_mark">Byte Order Mark</a> (BOM).</p>
<p>To do this, we'll peek at the first byte in the stream and print the value to the serial. Insert the following line before the call to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">println</span>(stream.<span class="hljs-built_in">peek</span>());
</code></pre>
<p>What value does it print?</p>
`,needs_assistance:!1,choices:[{id:"utf8",label:"<code>239</code>",summary:"Input's first byte suggests a UTF-8 BOM",next:"deserialization/invalidinput/bom-utf8"},{id:"utf16",label:"<code>254</code> or <code>255</code>",summary:"Input's first byte suggests a UTF-16 BOM",next:"deserialization/invalidinput/bom-utf16"},{id:"no-bom",label:"Something else",summary:"Input's first byte doesn't suggest a BOM",next:"deserialization/invalidinput/bom-none"}]},"deserialization/invalidinput/stream-buffer":{content:`<p>Sometimes, the input is invalid because some bytes were dropped when receiving the document.
Usually, this happens when the receiver reads the stream too slowly, which overflows a buffer somewhere in the path.</p>
<p>Indeed, because it reads bytes one by one, <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> can be slow with some implementations of <a href="https://www.arduino.cc/reference/en/language/functions/communication/stream/"><code>Stream</code></a>. To speed up the reading, we must add a buffer between the <a href="https://www.arduino.cc/reference/en/language/functions/communication/stream/"><code>Stream</code></a> and <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>. The easiest way to do this is to use <code>ReadBufferingStream</code> from the <a href="https://github.com/bblanchon/ArduinoStreamUtils">StreamUtils library</a>:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// replace the following line:</span>
<span class="hljs-built_in">deserializeJson</span>(doc, stream);

<span class="hljs-comment">// with these two lines:</span>
<span class="hljs-function">ReadBufferingStream <span class="hljs-title">bufferedStream</span><span class="hljs-params">(stream, <span class="hljs-number">64</span>)</span></span>;
<span class="hljs-built_in">deserializeJson</span>(doc, bufferedStream);
</code></pre>
<p>Thanks to <code>ReadBufferingStream</code>, the program will read the input in chunks of 64 bytes, which will be much faster.
Hopefully, it will be fast enough to read the whole message without droping any byte.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Adding a buffer solves the issue",next:"done"},{id:"buffer",label:"No",summary:"Adding a buffer doesn't solve the issue",next:"deserialization/invalidinput/stream-bom"}]},"deserialization/invalidinput/stream-jsonlint":{content:`<p>We need to make sure that the JSON document is valid.
To do that, please print the input to the serial port, like so:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">print</span>(stream.<span class="hljs-built_in">readString</span>());
</code></pre>
<p>Then, copy the response and paste it in a JSON linter, like <a href="https://jsonlint.com/">jsonlint.com</a>.</p>
<p>Is the JSON document valid?</p>
`,needs_assistance:!1,choices:[{id:"valid",label:"Yes",summary:"`jsonlint` says the document is valid",next:"deserialization/invalidinput/stream-buffer"},{id:"invalid",label:"No",summary:"`jsonlint` says the document is invalid",next:"deserialization/invalidinput/jsonlint-bad"}]},"deserialization/invalidinput/string-bom":{content:`<p>We must check that the JSON document is not preceded by a  <a href="https://en.wikipedia.org/wiki/Byte_order_mark">Byte Order Mark</a> (BOM).</p>
<p>To do this, we'll print the first byte to the serial port. Insert the following line before the call to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">println</span>(input[<span class="hljs-number">0</span>]);
</code></pre>
<p>What value does it print?</p>
`,needs_assistance:!1,choices:[{id:"utf8",label:"<code>239</code>",summary:"Input's first byte suggests a UTF-8 BOM",next:"deserialization/invalidinput/bom-utf8"},{id:"utf16",label:"<code>254</code> or <code>255</code>",summary:"Input's first byte suggests a UTF-16 BOM",next:"deserialization/invalidinput/bom-utf16"},{id:"no-bom",label:"Something else",summary:"Input's first byte doesn't suggest a BOM",next:"deserialization/invalidinput/bom-none"}]},"deserialization/invalidinput/string-jsonlint":{content:`<p>We need to make sure that the JSON document is valid.
To do that, please print the content of the string to the serial port, like so:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">print</span>(input);
</code></pre>
<p>Then, copy the response and paste it in a JSON linter, like <a href="https://jsonlint.com/">jsonlint.com</a>.</p>
<p>Is the JSON document valid?</p>
`,needs_assistance:!1,choices:[{id:"valid",label:"Yes",summary:"`jsonlint` says the document is valid",next:"deserialization/invalidinput/string-bom"},{id:"invalid",label:"No",summary:"`jsonlint` says the document is invalid",next:"deserialization/invalidinput/jsonlint-bad"}]},"deserialization/invalidinput":{content:`<p>Where does the input come from?</p>
`,needs_assistance:!1,choices:[{id:"http",label:"An HTTP response",summary:"Input comes from HTTP",next:"deserialization/invalidinput/http"},{id:"serial",label:"A serial port",summary:"Input comes from a serial port",next:"deserialization/invalidinput/serial"},{id:"file",label:"A file",summary:"Input comes from a file",next:"deserialization/invalidinput/file-jsonlint"},{id:"stream",label:"A stream",summary:"Input comes from a stream",next:"deserialization/invalidinput/stream-jsonlint"},{id:"string",label:"A string (like <code>String</code> or <code>const char*</code>)",summary:"Input comes from a string",next:"deserialization/invalidinput/string-jsonlint"},{id:"char-ptr",label:"A char array or pointer (like <code>char[]</code> or <code>char*</code>)",summary:"Input comes from a char array",next:"deserialization/invalidinput/array"}]},"deserialization/noerror/firstchar-brace":{content:`<p><a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> stops reading as soon as the JSON document ends.
In your case, it will read the input until it reaches the matching <code>}</code> and ignore any remaining characters.</p>
<p>Suppose the input looks like this:</p>
<pre><code class="hljs language-json"><span class="hljs-punctuation">{</span><span class="hljs-attr">&quot;key&quot;</span><span class="hljs-punctuation">:</span><span class="hljs-string">&quot;value&quot;</span><span class="hljs-punctuation">}</span>GARBAGE
</code></pre>
<p>Here, <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> reads the JSON object <code>{&quot;key&quot;:&quot;value&quot;}</code> and returns <code>Ok</code> ignoring the <code>GARBAGE</code> part.</p>
<p>This feature enables <a href="/v6/how-to/deserialize-a-very-large-document/#deserialization/in-chunks">deserializing in chunks</a> and allows non-zero-terminated input strings.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Garbage characters follow the input",next:"done"},{id:"lint",label:"No",summary:"No garbage characters follow the input",next:"deserialization/noerror/jsonlint"}]},"deserialization/noerror/firstchar-bracket":{content:`<p><a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> stops reading as soon as the JSON document ends.
In your case, it will read the input until it reaches the matching <code>]</code> and ignore any remaining characters.</p>
<p>Suppose the input looks like this:</p>
<pre><code class="hljs language-json"><span class="hljs-punctuation">[</span><span class="hljs-number">1</span><span class="hljs-punctuation">,</span><span class="hljs-number">2</span><span class="hljs-punctuation">,</span><span class="hljs-number">3</span><span class="hljs-punctuation">]</span>GARBAGE
</code></pre>
<p>Here, <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> reads the JSON array <code>[1,2,3]</code> and returns <code>Ok</code> ignoring the <code>GARBAGE</code> part.</p>
<p>This feature enables <a href="/v6/how-to/deserialize-a-very-large-document/#deserialization/in-chunks">deserializing in chunks</a> and allows non-zero-terminated input strings.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Garbage characters follow the input",next:"done"},{id:"lint",label:"No",summary:"No garbage characters follow the input",next:"deserialization/noerror/jsonlint"}]},"deserialization/noerror/firstchar-quote":{content:`<p><a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> stops reading as soon as the JSON document ends.
In your case, it will read the input until it reaches the matching <code>&quot;</code> and ignore any remaining characters.</p>
<p>Suppose the input looks like this:</p>
<pre><code class="hljs language-json"><span class="hljs-string">&quot;hello&quot;</span>GARBAGE
</code></pre>
<p>Here, <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> reads the JSON string <code>&quot;hello&quot;</code> and returns <code>Ok</code> ignoring the <code>GARBAGE</code> part.</p>
<p>This feature enables <a href="/v6/how-to/deserialize-a-very-large-document/#deserialization/in-chunks">deserializing in chunks</a> and allows non-zero-terminated input strings.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Garbage characters follow the input",next:"done"},{id:"lint",label:"No",summary:"No garbage characters follow the input",next:"deserialization/noerror/jsonlint"}]},"deserialization/noerror/firstchar":{content:`<p>What's the first character of your input?</p>
`,needs_assistance:!1,choices:[{id:"brace",label:"An opening brace: <code>{</code>",next:"deserialization/noerror/firstchar-brace",summary:"The first character is `{`"},{id:"bracket",label:"An opening bracket: <code>[</code>",next:"deserialization/noerror/firstchar-bracket",summary:"The first character is `[`"},{id:"quote",label:"A quote: <code>&quot;</code>",next:"deserialization/noerror/firstchar-quote",summary:'The first character is `"`'},{id:"other",label:"None of the above",next:"deserialization/noerror/unacceptable",summary:'The first character is neither `{`, nor `[`, nor `"`'}]},"deserialization/noerror/garbage-zerocopy-cast":{content:`<p>The easiest solution is to disable the zero-copy mode by passing a read-only input buffer. Casting the pointer will do the trick:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// replace this:</span>
<span class="hljs-built_in">deserializeJson</span>(doc, input, inputSize);

<span class="hljs-comment">// ...with this:</span>
<span class="hljs-built_in">deserializeJson</span>(doc, (<span class="hljs-type">const</span> <span class="hljs-type">char</span>*)input, inputSize);
</code></pre>
<p>Now that the zero-copy mode is disabled, you probably need a larger <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, so don't forget to update the capacity. As usual, use the <a href="/v6/assistant/">ArduinoJson Assistant</a> to compute the right capacity for your project.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Casting the pointer fixes the issue",next:"done"},{id:"cast",label:"No",summary:"Casting the pointer doesn't fix the issue",next:"deadend"}]},"deserialization/noerror/garbage-zerocopy-stream":{content:`<p>The easiest solution is to remove the buffer and pass the input stream directly to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>.</p>
<p>For example, assuming the JSON document comes from a file:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// replace this:</span>
<span class="hljs-function">std::unique_ptr&lt;<span class="hljs-type">char</span>[]&gt; <span class="hljs-title">buf</span><span class="hljs-params">(<span class="hljs-keyword">new</span> <span class="hljs-type">char</span>[size])</span></span>;
file.<span class="hljs-built_in">readBytes</span>(buf.<span class="hljs-built_in">get</span>(), size);
<span class="hljs-built_in">deserializeJson</span>(doc, buf.<span class="hljs-built_in">get</span>());

<span class="hljs-comment">// ...with this:</span>
<span class="hljs-built_in">deserializeJson</span>(doc, file);
</code></pre>
<p>Now that the zero-copy mode is disabled, you probably need a larger <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, so don't forget to update the capacity. As usual, use the <a href="/v6/assistant/">ArduinoJson Assistant</a> to compute the right capacity for your project.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Passing the stream to `deserializeJson()` fixes the issue",next:"done"},{id:"cast",label:"No",summary:"Passing the stream to `deserializeJson()` doesn't fix the issue",next:"deadend"}]},"deserialization/noerror/garbage-zerocopy":{content:`<p>When you pass a writeable buffer as the input of <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>, ArduinoJson uses the zero-copy mode. Instead of copying the strings from the input into the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, it stores pointers to the input buffer.
Therefore, the input buffer must stay alive during the whole lifetime of the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>; otherwise, all the strings in the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> will be dangling pointers.</p>
<p>Does your program fills this buffer from a <a href="https://www.arduino.cc/reference/en/language/functions/communication/stream/"><code>Stream</code></a> (like <a href="https://www.arduino.cc/en/Reference/SD"><code>File</code></a>, <a href="https://www.arduino.cc/reference/en/language/functions/communication/serial/"><code>Serial</code></a>, <a href="https://www.arduino.cc/en/Reference/EthernetClient"><code>EthernetClient</code></a>, <a href="https://www.arduino.cc/en/Reference/WiFiClient"><code>WifiClient</code></a>...)?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes, the input comes from a stream",summary:"Input comes from a stream",next:"deserialization/noerror/garbage-zerocopy-stream"},{id:"cast",label:"No, it doesn't",summary:"Input doesn't come from a stream",next:"deserialization/noerror/garbage-zerocopy-cast"}]},"deserialization/noerror/garbage":{content:`<p>What is the type of the second argument passed to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>?</p>
`,needs_assistance:!1,choices:[{id:"char-ptr",label:"<code>char*</code> (or <code>char[]</code>)",summary:"Input type is `char*`",next:"deserialization/noerror/garbage-zerocopy"},{id:"const-char-ptr",label:"<code>const char*</code>",summary:"Input type is `const char*`",next:"deadend"},{id:"string",label:"<code>String</code> (or <code>std::string</code>)",summary:"Input type is `String`",next:"deadend"},{id:"stream",label:"<code>Stream</code> (or <code>std::istream)</code>",summary:"Input type is `Stream`",next:"deadend"}]},"deserialization/noerror/input-modified":{content:`<p>When you pass a writeable buffer as the input of <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>, ArduinoJson uses the zero-copy mode. Instead of copying the strings from the input into the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, it stores pointers to the input buffer.</p>
<p>While doing this, the parser modifies the input buffer to unescape special characters and add string terminator.
When <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> returns the input buffer doesn't contain a valid JSON document anymore.</p>
<p>For more information, please see <a href="/v6/issues/altered-input/">Why is the input modified?</a></p>
<p>If you must preserve the input as it is, you must disable the zero-copy mode by passing a read-only input buffer. Casting the pointer will do the trick:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// replace this:</span>
<span class="hljs-built_in">deserializeJson</span>(doc, input, inputSize);

<span class="hljs-comment">// ...with this:</span>
<span class="hljs-built_in">deserializeJson</span>(doc, (<span class="hljs-type">const</span> <span class="hljs-type">char</span>*)input, inputSize);
</code></pre>
<p>Now that the zero-copy mode is disabled, you probably need a larger <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, so don't forget to update the capacity. As usual, use the <a href="/v6/assistant/">ArduinoJson Assistant</a> to compute the right capacity for your project.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Casting the pointer fixes the issue",next:"done"},{id:"cast",label:"No",summary:"Casting the pointer doesn't fix the issue",next:"deadend"}]},"deserialization/noerror/jsonlint-invalid":{content:`<blockquote>
<p>ArduinoJson isn't very picky about the input: its implementation favors code size and speed over strict conformance.
After all, size and speed are what matter the most for embedded software, right?</p>
<p>This means that ArduinoJson's parser may accept documents that would be rejected by other parsers.</p>
<p>For example, <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> tolerates the following substitutions:</p>
<ul>
<li><code>'hello'</code> instead of <code>&quot;hello&quot;</code> (single quotes)</li>
<li><code>nULL</code> or <code>n0n3</code> instead if <code>null</code> (only checks first character and length)</li>
<li><code>tRUE</code> or <code>t0t0</code> instead of <code>true</code> (ditto)</li>
<li><code>fALSE</code> or <code>fAkk3</code> instead of <code>false</code> (ditto)</li>
</ul>
</blockquote>
<p>It also ignores the follwing UTF-8 errors:</p>
<ul>
<li><code>&quot;\\ud83d&quot;</code> (a leading surrogate without a trailing surrogate)</li>
<li><code>&quot;\\udda4&quot;</code> (a trailing surrogate without a leading surrogate)</li>
<li><code>&quot;\\ud83d\\ud83d&quot;</code> (two leading surrogates)</li>
</ul>
<p>Lastly, it supports C++-style comments when <a href="/v6/api/config/enable_comments/">ARDUINOSJSON_ENABLE_COMMENTS</a> is set to <code>1</code>.</p>
<p>As a result, you cannot use <a href="/v6/api/json/deserializejson/"><code>deserializeJson</code></a> as a JSON validator because you'd get many false-negatives.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"acceptable",label:"Yes",next:"done",summary:"`deserializeJson()` tolerates the error, and that's OK"},{id:"unacceptable",label:"No",next:"deserialization/noerror/unacceptable",summary:"`deserializeJson()` should not let this error pass"}]},"deserialization/noerror/jsonlint-valid":{content:`<p>So, <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> returns <code>Ok</code>, and jsonlint confirms that the input is valid.</p>
<p>I don't see any issue here; maybe you should <a href="/v6/troubleshooter/">start over</a>?</p>
`,needs_assistance:!1,choices:void 0},"deserialization/noerror/jsonlint":{content:`<p>Let's make sure that your input is actually invalid.</p>
<p>Please go to <a href="https://jsonlint.com/">jsonlint.com</a>, paste your input, and press &quot;Validate JSON.&quot;</p>
<p>What does it say?</p>
`,needs_assistance:!1,choices:[{id:"valid",label:"Document is valid",next:"deserialization/noerror/jsonlint-valid",summary:"jsonlint says the document is valid"},{id:"invalid",label:"Document is invalid",next:"deserialization/noerror/jsonlint-invalid",summary:"jsonlint says the document is invalid"}]},"deserialization/noerror/novalue/confusion":{content:`<p>When a program fails to extract the values from a <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, it's often because there is a confusion between arrays and objects.</p>
<p>For example, a common mistake is to write:</p>
<pre><code class="hljs language-c++"><span class="hljs-type">const</span> <span class="hljs-type">char</span>* temperature = doc[<span class="hljs-string">&quot;list&quot;</span>][<span class="hljs-string">&quot;main&quot;</span>][<span class="hljs-string">&quot;temp&quot;</span>];
</code></pre>
<p>instead of</p>
<pre><code class="hljs language-c++"><span class="hljs-type">const</span> <span class="hljs-type">char</span>* temperature = doc[<span class="hljs-string">&quot;list&quot;</span>][<span class="hljs-number">0</span>][<span class="hljs-string">&quot;main&quot;</span>][<span class="hljs-string">&quot;temp&quot;</span>];
</code></pre>
<p>You can find other examples in <a href="/v6/issues/cannot-get-values/">deserializeJson() succeeds but I cannot read any value</a>, but the simplest solution is to use the <a href="/v6/assistant/">ArduinoJson Assistant</a> because the last step shows how to extract the values.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Issue is a confusion between array and object",next:"done"},{id:"array-object",label:"No",summary:"Issue is not a confusion between array and object",next:"deadend"}]},"deserialization/noerror/novalue/filter":{content:`<p>Maybe the filter excludes the part that it should keep. Let's verify that it's doing its job correctly:</p>
<p>Please print the filter to the serial port like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-built_in">serializeJsonPretty</span>(filter, Serial);
</code></pre>
<p>Then, open the <a href="/v6/assistant/">ArduinoJson Assistant</a>:</p>
<ul>
<li>in step 1: choose &quot;Deserialize and filter&quot;</li>
<li>in step 2:
<ul>
<li>in the &quot;input&quot; column, paste a sample input</li>
<li>in the &quot;filter&quot; column, paste the filter that your program just printed</li>
</ul>
</li>
</ul>
<p>The column &quot;filtered input&quot; shows the result of applying the filter to the input.<br>
Make sure the result is what you expect, and adjust the filter as needed.<br>
Once happy with the result, you can move to step 4 and copy the block that fills the filter.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Fixing the filter solves the issue",next:"done"},{id:"filter-ok",label:"No",summary:"Fixing the filter doesn't solve the issue",next:"deserialization/noerror/novalue/print"}]},"deserialization/noerror/novalue/json-or-msgpack":{content:`<p>Which function do you call to deserialize the input?</p>
`,needs_assistance:!1,choices:[{id:"json",label:"<code>deserializeJson()</code>",summary:"Program calls `deserializeJson()`",next:"deadend"},{id:"msgpack",label:"<code>deserializeMsgPack()</code>",summary:"Program calls `deserializeMsgPack()`",next:"deserialization/noerror/novalue/msgpack"}]},"deserialization/noerror/novalue/msgpack":{content:`<p>It seems that your program fed <a href="/v6/api/msgpack/deserializemsgpack/"><code>deserializeMsgPack()</code></a> with garbage input.</p>
<p>Indeed, any byte in the following ranges is a valid one-byte MessagePack document:</p>
<ul>
<li><code>0x00</code>-<code>0x7f</code>: positive fixint</li>
<li><code>0xc0</code>: nil</li>
<li><code>0xc2</code>: false</li>
<li><code>0xc3</code>: true</li>
<li><code>0xe0</code>-<code>0xff</code>: negative fixint</li>
</ul>
<p>As you can see, these values cover about 63% of all possible values for a random byte; that's why they are very likely to occur.</p>
<p>When the first byte in the input is one of these, <a href="/v6/api/msgpack/deserializemsgpack"><code>deserializeMsgPack()</code></a> saves the corresponding value in the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> and immediately returns <a href="/v6/api/misc/deserializationerror/#ok"><code>Ok</code></a>, which is the correct behavior.</p>
<p>To fix this issue, you must repair the input.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"The input was incorrect",next:"done"},{id:"not-garbage",label:"No",summary:"The input is correct",next:"deadend"}]},"deserialization/noerror/novalue/print":{content:`<p>Please print the content of the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> to the serial port like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-built_in">serializeJson</span>(doc, Serial);
Serial.<span class="hljs-built_in">println</span>(); <span class="hljs-comment">// adds a line break (optional)</span>
</code></pre>
<p>What's the first character?</p>
`,needs_assistance:!1,choices:[{id:"quote",label:"A quote (<code>&quot;</code>)",summary:"Serialized document starts with a quote",next:"deserialization/noerror/novalue/quote"},{id:"bracket",label:"A square bracket (<code>[</code>)",summary:"Serialized document starts with a square bracket",next:"deserialization/noerror/novalue/confusion"},{id:"brace",label:"A curly brace (<code>{</code>)",summary:"Serialized document starts with a curly brace",next:"deserialization/noerror/novalue/confusion"},{id:"not-in-list",label:"None of the above",summary:"Serialized document starts with neither a quote, a bracket, nor a brace.",next:"deserialization/noerror/novalue/json-or-msgpack"}]},"deserialization/noerror/novalue/quote-keep":{content:`<p>If you cannot fix the server, you can at least reverse the double serialization (stringification) to get back the original object.</p>
<p>To do this, you must call <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> twice, like so:</p>
<pre><code class="hljs language-c++">StaticJsonDocument&lt;<span class="hljs-number">512</span>&gt; doc1, doc2;
<span class="hljs-built_in">deserializeJson</span>(doc1, input);
<span class="hljs-built_in">deserializeJson</span>(doc2, doc1.<span class="hljs-built_in">as</span>&lt;<span class="hljs-type">const</span> <span class="hljs-type">char</span>*&gt;());
String value = doc2[<span class="hljs-string">&quot;hello&quot;</span>];
</code></pre>
<p>In this snippet, <code>doc1</code> is only used during the second deserialization and can be safely discarded after that.
The cleanest way to do this is to wrap the double deserialization in a function, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-function">DeserilizationError <span class="hljs-title">deserializeJsonTwice</span><span class="hljs-params">(JsonDocument&amp; doc, Stream&amp; input)</span> </span>{
  StaticJsonDocument&lt;<span class="hljs-number">512</span>&gt; tmp;
  DeserializationError err = <span class="hljs-built_in">deserializeJson</span>(tmp, input);
  <span class="hljs-keyword">if</span> (err) <span class="hljs-keyword">return</span> err;
  <span class="hljs-keyword">return</span> <span class="hljs-built_in">deserializeJson</span>(doc, tmp.<span class="hljs-built_in">as</span>&lt;<span class="hljs-type">const</span> <span class="hljs-type">char</span>*&gt;());
}
</code></pre>
<p>Of course, if you need to use the <a href="/news/2020/03/22/version-6-15-0/">filtering feature</a>, you must pass <code>DeserializationOption::Filter</code> to the second call to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Calling `deserializeJson()` twice fixes the issue",next:"done"},{id:"call-twice",label:"No",summary:"Calling `deserializeJson()` twice doesn't fix the issue",next:"deadend"}]},"deserialization/noerror/novalue/quote-server":{content:`<p>In the server implementation, please look at the view that returns the faulty JSON.
You will probably find that this function explicitly stringifies the response to JSON, which is incorrect because the underlying framework already does it for you.</p>
<p>Of course the details will depend on the framework you used to implement the server, but in any case, you'll have to remove a call to a function that stringifies to JSON. Here are some example:</p>
<ul>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/JSON/stringify"><code>JSON.stringify()</code></a> in JavaScript</li>
<li><a href="https://www.php.net/manual/en/function.json-encode.php"><code>json_encode()</code></a> in PHP</li>
<li><a href="https://docs.python.org/3/library/json.html"><code>json.dumps()</code></a> in Python</li>
<li><a href="https://ruby-doc.org/stdlib-2.6.3/libdoc/json/rdoc/JSON.html"><code>JSON.generate()</code></a> in Ruby</li>
<li><a href="https://docs.microsoft.com/en-us/dotnet/api/system.text.json.jsonserializer.serialize"><code>JsonSerializer.Serialize()</code></a> on .NET</li>
<li><a href="https://www.javadoc.io/doc/com.google.code.gson/gson/latest/com.google.gson/com/google/gson/Gson.html"><code>gson.toJson()</code></a> in Java</li>
<li><a href="https://golang.org/pkg/encoding/json/"><code>json.Marshal()</code></a> in Go</li>
</ul>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Fixing the server fixes the issue",next:"done"},{id:"server-ok",label:"No",summary:"Fixing the server doesn't fix the issue",next:"deserialization/noerror/novalue/quote-keep"}]},"deserialization/noerror/novalue/quote":{content:`<p>If you see a double-quote at the beginning of the JSON input, it means that the whole document is neither an object or an array; it's a string. This happens when the input was incorrectly generated, by serializing (stringifying) to JSON twice.</p>
<p>For example, the following document:</p>
<pre><code class="hljs language-json"><span class="hljs-string">&quot;{\\&quot;hello\\&quot;:\\&quot;world\\&quot;}&quot;</span>
</code></pre>
<p>...is not a JSON <em>object</em> but a <em>string</em>.
It's the result of the JSON serialization (stringification) of the following JSON document:</p>
<pre><code class="hljs language-json"><span class="hljs-punctuation">{</span><span class="hljs-attr">&quot;hello&quot;</span><span class="hljs-punctuation">:</span><span class="hljs-string">&quot;world&quot;</span><span class="hljs-punctuation">}</span>
</code></pre>
<p>...which was alread serialized (stringified) to JSON.</p>
<p>So, the bug isn't in the Arduino code, but on the other size, most likely a server.</p>
<p>Can you modify the code of the server?</p>
`,needs_assistance:!1,choices:[{id:"server-fixable",label:"Yes",summary:"User can modify the server",next:"deserialization/noerror/novalue/quote-server"},{id:"server-unfixable",label:"No",summary:"User cannot modify the server",next:"deserialization/noerror/novalue/quote-keep"}]},"deserialization/noerror/novalue":{content:`<p>Do you use a filter (with <code>DeserializationOption::Filter</code>)?</p>
`,needs_assistance:!1,choices:[{id:"filter",label:"Yes",summary:"Program uses `DeserializationOption::Filter`",next:"deserialization/noerror/novalue/filter"},{id:"no-filter",label:"No",summary:"Program doesn't use `DeserializationOption::Filter`",next:"deserialization/noerror/novalue/print"}]},"deserialization/noerror/slow":{content:`<p>Indeed, <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> can be pretty slow with unbuffered implementations of <a href="https://www.arduino.cc/reference/en/language/functions/communication/stream/"><code>Stream</code></a> (such as <code>File</code> and <code>WiFiClient</code>) because it reads characters one by one.</p>
<p>Why does it reads bytes one by one?<br>
Because it has to stop exactly at the end of the document to support <a href="https://en.wikipedia.org/wiki/JSON_streaming">JSON streaming</a> and similar techniques.</p>
<p>To speed up the process, we need to insert a buffer between the <a href="https://www.arduino.cc/reference/en/language/functions/communication/stream/"><code>Stream</code></a> and <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>.  The easiest way to do this is to use <code>ReadBufferingStream</code> from the <a href="https://github.com/bblanchon/ArduinoStreamUtils">StreamUtils library</a>:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// replace the following line:</span>
<span class="hljs-built_in">deserializeJson</span>(doc, stream);

<span class="hljs-comment">// with these two lines:</span>
<span class="hljs-function">ReadBufferingStream <span class="hljs-title">bufferedStream</span><span class="hljs-params">(stream, <span class="hljs-number">64</span>)</span></span>;
<span class="hljs-built_in">deserializeJson</span>(doc, bufferedStream);
</code></pre>
<p>Thanks to <code>ReadBufferingStream</code>, the program will read the input in chunks of 64 bytes, which will be much faster.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"`ReadBufferingStream` solves the issue",next:"done"},{id:"buffered",label:"No",summary:"`ReadBufferingStream` doesn't solve the issue",next:"deadend"}]},"deserialization/noerror/truncatedstrings/http-httpclient-stream":{content:`<p>When you call <code>HTTPClient::getStream()</code>, you bypass the part that handles <a href="https://en.wikipedia.org/wiki/Chunked_transfer_encoding">Chunked Transfer Encoding</a>.
As a result, the response's body may be split in multiple chunks, making some strings look like they are truncated (in reality, they contain a line-break).</p>
<p>The most straightforward workaround is to downgrade the connection to HTTP 1.0.
To do this, add the following line <strong>before</strong> sending the request:</p>
<pre><code class="hljs language-c++">http.<span class="hljs-built_in">useHTTP10</span>(<span class="hljs-literal">true</span>);  <span class="hljs-comment">// avoid Chunked Transfer Encoding</span>
</code></pre>
<p>Did this solve your problem?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Calling `HTTPClient::useHTTP10(true)` solves the issue",next:"done"},{id:"http10",label:"No",summary:"Calling `HTTPClient::useHTTP10(true)` doesn't solve the issue",next:"deadend"}]},"deserialization/noerror/truncatedstrings/http-httpclient":{content:`<p>Which function of <code>HTTPClient</code> do you call to get the response?</p>
`,needs_assistance:!1,choices:[{id:"getstream",label:"<code>getStream()</code>",summary:"Response comes from `HTTPClient::getStream()`",next:"deserialization/noerror/truncatedstrings/http-httpclient-stream"},{id:"getstring",label:"<code>getString()</code>",summary:"Response comes from `HTTPClient::getString()`",next:"deadend"}]},"deserialization/noerror/truncatedstrings/http-manual":{content:`<p>When you use HTTP 1.1, the server can send the response with <a href="https://en.wikipedia.org/wiki/Chunked_transfer_encoding">Chunked Transfer Encoding</a>.
As a result, the response's body may be split in multiple chunks, making some strings look like they are truncated (in reality, they contain a line-break).</p>
<p>The most straightforward workaround is to downgrade the connection to HTTP 1.0.
To do this, replace <code>HTTP/1.1</code> with <code>HTTP/1.0</code> in the first line of the request.</p>
<p>Did this solve your problem?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Using `HTTP/1.0` solves the issue",next:"done"},{id:"http10",label:"No",summary:"Using `HTTP/1.0` doesn't solve the issue",next:"deadend"}]},"deserialization/noerror/truncatedstrings/http-otherlib":{content:`<p>Unfortunately, I only know how to use the following HTTP libraries:</p>
<ul>
<li><a href="https://github.com/esp8266/Arduino/tree/master/libraries/ESP8266HTTPClient">ESP8266HTTPClient</a></li>
<li><a href="https://github.com/espressif/arduino-esp32/tree/master/libraries/HTTPClient">HTTPClient for ESP32</a></li>
</ul>
`,needs_assistance:!0,choices:void 0},"deserialization/noerror/truncatedstrings/http":{content:`<p>Do you use an HTTP library?</p>
`,needs_assistance:!1,choices:[{id:"httpclient",label:"Yes, I'm using <code>HTTPClient</code>",summary:"Program uses `HTTPClient`",next:"deserialization/noerror/truncatedstrings/http-httpclient"},{id:"http-library",label:"Yes, but it's not <code>HTTPClient</code>",summary:"Program uses an unknown HTTP library",next:"deserialization/noerror/truncatedstrings/http-otherlib"},{id:"no-library",label:"No",summary:"Program doesn't use an HTTP library",next:"deserialization/noerror/truncatedstrings/http-manual"}]},"deserialization/noerror/truncatedstrings/input":{content:`<p>Does the JSON input come from an HTTP response?</p>
`,needs_assistance:!1,choices:[{id:"http",label:"Yes",summary:"The input comes from an HTTP response",next:"deserialization/noerror/truncatedstrings/http"},{id:"no-nul",label:"No",summary:"The input doesn't come from an HTTP response",next:"deadend"}]},"deserialization/noerror/truncatedstrings":{content:`<p>Does the JSON input contain the Unicode character <code>\\u0000</code>?</p>
`,needs_assistance:!1,choices:[{id:"nul",label:"Yes",summary:"The input contains `\\u0000`",next:"nul-not-supported"},{id:"no-nul",label:"No",summary:"The input doesn't contain `\\u0000`",next:"deserialization/noerror/truncatedstrings/input"}]},"deserialization/noerror/unacceptable":{content:`<p><a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> accepted a JSON document that was clearly invalid.<br>
You need to talk to a human about that.</p>
`,needs_assistance:!0,choices:void 0},"deserialization/noerror":{content:`<p>So, what's the problem then?</p>
`,needs_assistance:!1,choices:[{id:"slow",label:"It's slow",summary:"Deserialisation is slow",next:"deserialization/noerror/slow"},{id:"invalid",label:"I expected <code>InvalidInput</code> instead of <code>Ok</code>",summary:"`Ok` is incorrect; it should be `InvalidInput`",next:"deserialization/noerror/firstchar"},{id:"crash",label:"My program crashes after calling <code>deserializeJson()</code>",summary:"Program crashes after calling `deserializeJson()`",next:"deserialization/crash/after"},{id:"empty",label:"<code>JsonDocument</code> returns empty/null values",summary:"Program fails to extract values from the `JsonDocument`",next:"deserialization/noerror/novalue"},{id:"garbage",label:"<code>JsonDocument</code> returns garbage",summary:"`JsonDocument` returns garbage",next:"deserialization/noerror/garbage"},{id:"changing-strings",label:"The strings in the <code>JsonDocument</code> change for no reason",summary:"The strings in the `JsonDocument` change for no reason",next:"deserialization/noerror/garbage"},{id:"altered",label:"<code>deserializeJson()</code> alters the content of the input",summary:"Input buffer is modified",next:"deserialization/noerror/input-modified"},{id:"truncated-strings",label:"Some strings are truncated",summary:"Some strings are truncated",next:"deserialization/noerror/truncatedstrings"}]},"deserialization/nomemory/assistant-settings":{content:`<p>I'm pretty confident the Assistant computes the right capacity; however, some settings can affect the result:</p>
<ol>
<li><em>Input type</em>, in step 1, should match the type of the argument passed to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a></li>
<li><em>Deduplicate values when measuring the capacity</em>, in step 3, should be unchecked, most likely.</li>
<li><em>Deduplicate keys when measuring the capacity</em>, in step 3, it should be unchecked if the keys are dynamic or if you used placeholders.</li>
</ol>
<p>Please verify that these settings are correct.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Changing the Assistant settings solves the issue",next:"done"},{id:"failure",label:"No",summary:"Changing the Assistant settings doesn't solve the issue",next:"deadend"}]},"deserialization/nomemory/document-basic-capacity-0-global":{content:`<p><code>doc.capacity()</code> returns <code>0</code>, which means <code>allocate()</code> returned <code>NULL</code>.</p>
<p>I think that, because <code>doc</code> is declared global, <code>allocate()</code> is called too early and the heap isn't ready.
I know this happens on ESP32 with the <code>SpiRamJsonDocument</code> shown in <a href="/v6/how-to/use-external-ram-on-esp32/">How to use external RAM on ESP32?</a>.</p>
<p>Please try to make <code>doc</code> a local variable.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Making `doc` local fixes the issue.",next:"done"},{id:"failure",label:"No",summary:"Making `doc` local doesn't fix the issue.",next:"deadend"}]},"deserialization/nomemory/document-basic-capacity-0":{content:`<p>Is <code>doc</code> a global variable?</p>
`,needs_assistance:!1,choices:[{id:"global",label:"Yes",summary:"`doc` is a global variable",next:"deserialization/nomemory/document-basic-capacity-0-global"},{id:"local",label:"No",summary:"`doc` is not a global variable",next:"deserialization/nomemory/assistant-settings"}]},"deserialization/nomemory/document-basic":{content:`<p>Let's verify that the memory allocation succeeded.</p>
<p>Please print the capacity of the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> to the serial port, like so:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">println</span>(doc.<span class="hljs-built_in">capacity</span>());
</code></pre>
<p>What value does it show?</p>
`,needs_assistance:!1,choices:[{id:"capacity-0",label:"<code>0</code>",summary:"`doc.capacity()` returns `0`",next:"deserialization/nomemory/document-basic-capacity-0"},{id:"capacity-ok",label:"The value passed to the constructor of <code>DynamicJsonDocument</code>",summary:"`doc.capacity()` returns the right value",next:"deserialization/nomemory/assistant-settings"}]},"deserialization/nomemory/document-dynamic-toobig":{content:`<p>Well, that's very bad news. It seems that your microcontroller doesn't have enough RAM to hold the JSON document.</p>
<p>Please read <a href="/v6/how-to/reduce-memory-usage/">How to reduce memory usage</a>.
It shows several techniques you can use to use less RAM.
Hopefully, you'll make enough room for the memory pool.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Reducing memory usage fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Reducing memory usage doesn't fix the issue",next:"deadend"}]},"deserialization/nomemory/document-dynamic":{content:`<p>It's possible that the <a href="/v6/api/dynamicjsondocument/"><code>DynamicJsonDocument</code></a> failed to allocate the memory pool.</p>
<p>Please print the capacity of the <a href="/v6/api/dynamicjsondocument/"><code>DynamicJsonDocument</code></a> to the serial port, like so:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">println</span>(doc.<span class="hljs-built_in">capacity</span>());
</code></pre>
<p>What value does it show?</p>
`,needs_assistance:!1,choices:[{id:"capacity-0",label:"<code>0</code>",summary:"`doc.capacity()` returns `0`",next:"deserialization/nomemory/document-dynamic-toobig"},{id:"capacity-ok",label:"The value passed to the constructor of <code>DynamicJsonDocument</code>",summary:"`doc.capacity()` returns the right value",next:"deserialization/nomemory/assistant-settings"}]},"deserialization/nomemory/document":{content:`<p>What kind of <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> are you using?</p>
`,needs_assistance:!1,choices:[{id:"basic",label:"<code>BasicJsonDocument&lt;T&gt;</code>",summary:"The document is a `BasicJsonDocument<T>`",next:"deserialization/nomemory/document-basic"},{id:"dynamicjsondocument",label:"<code>DynamicJsonDocument</code>",summary:"The `JsonDocument` is a `DynamicJsonDocument`",next:"deserialization/nomemory/document-dynamic"},{id:"staticjsondocument",label:"<code>StaticJsonDocument</code>",summary:"The `JsonDocument` is a `StaticJsonDocument`",next:"deserialization/nomemory/assistant-settings"}]},"deserialization/nomemory":{content:`<p><a href="/v6/api/misc/deserializationerror/#nomemory"><code>NoMemory</code></a> means that the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> is too small to hold the entire document.</p>
<p>Please use the <a href="/v6/assistant/">ArduinoJson Assistant</a> to compute the right capacity for your document.
Use the recommended capacity when creating <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> and retry.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Increasing the capacity of the `JsonDocument` fixes the issue",next:"done"},{id:"increased-capacity",label:"No",summary:"Increasing the capacity of the `JsonDocument` doesn't fix the issue",next:"deserialization/nomemory/document"}]},"deserialization/notsupported/msgpack":{content:`<p>So, I guess that you're not using <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> but <a href="/v6/api/msgpack/deserializemsgpack/"><code>deserializeMsgPack()</code></a>, right?</p>
<p>Indeed, before <a href="/news/2021/05/04/version-6-18-0/">ArduinoJson 6.18</a>, <a href="/v6/api/msgpack/deserializemsgpack/"><code>deserializeMsgPack()</code></a> returned <a href="/v6/api/misc/deserializationerror/#notsupported"><code>NotSupported</code></a> as soon as the input contains an unsupported MsgPack value type:</p>
<ul>
<li>a binary value</li>
<li>an extension value</li>
<li>an object key that is not a string</li>
</ul>
<p>If that's your case, then you must upgrade the library. Instead of returning an error, newer versions simply ignore the unsupported values and replace them with nulls.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Upgrading solve the issue",next:"done"},{id:"upgraded",label:"No",summary:"Upgrading doesn't solve the issue",next:"deadend"}]},"deserialization/notsupported/unicode":{content:`<p><a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> returns <a href="/v6/api/misc/deserializationerror/#notsupported"><code>NotSupported</code></a> when is read an UTF-16 escape sequence but support is disabled.</p>
<p>To fix this issue, you must enable support UTF-16 escape sequences by defining <a href="/v6/api/config/decode_unicode/"><code>ARDUINOJSON_DECODE_UNICODE</code></a> to <code>1</code> before including <code>ArduinoJson.h</code>, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-meta">#<span class="hljs-keyword">define</span> ARDUINOJSON_DECODE_UNICODE 1</span>
<span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;ArduinoJson.h&gt;</span></span>
</code></pre>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Defining `ARDUINOJSON_DECODE_UNICODE` solves the issue",next:"done"},{id:"decode-unicode-set",label:"No",summary:"Defining `ARDUINOJSON_DECODE_UNICODE` doesn't solve the issue",next:"deserialization/notsupported/msgpack"}]},"deserialization/notsupported/upgrade-duplicates":{content:`<p>If you're still seeing <code>NotSupported</code> after upgrading the library, it means that your code is not using the upgraded library.</p>
<p>Several versions of ArduinoJson are installed on your machine; you must either:</p>
<ul>
<li>removed the duplicates</li>
<li>upgrade the duplicates</li>
<li>make sure your project uses the right one</li>
</ul>
<p>To find the duplicates, run a disk search for a file named <code>ArduinoJson.h</code>.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Removing duplicates fixes the issue",next:"done"},{id:"duplicates-removed",label:"No",summary:"Removing duplicates doesn't fix the issue",next:"deserialization/notsupported/unicode"}]},"deserialization/notsupported/upgrade":{content:`<p>Please upgrade ArduinoJson to the latest version.<br>
You may need to consult the <a href="https://github.com/bblanchon/ArduinoJson/blob/6.x/CHANGELOG.md">change log</a> to make sure there aren't any breaking changes that could break your existing code.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Upgrading solves the issue",next:"done"},{id:"updraded",label:"No",summary:"Upgrading doesn't solves the issue",next:"deserialization/notsupported/upgrade-duplicates"}]},"deserialization/notsupported":{content:`<p><a href="/v6/api/misc/deserializationerror/#notsupported"><code>NotSupported</code></a> means that the document was valid but contained features not supported by the library.</p>
<p>This error code was removed in <a href="/news/2021/05/04/version-6-18-0/">ArduinoJson 6.18</a>, so you should not see it anymore.</p>
<p>Can you upgrade the library?</p>
`,needs_assistance:!1,choices:[{id:"can-uprade",label:"Yes",summary:"Library can be upgraded",next:"deserialization/notsupported/upgrade"},{id:"cannot-upgrade",label:"No",summary:"Library cannot be upgraded",next:"deserialization/notsupported/unicode"}]},"deserialization/toodeep":{content:`<p><a href="/v6/api/misc/deserializationerror/#toodeep"><code>TooDeep</code></a> means that the input document has too many levels of nesting.</p>
<p><a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> is implemented with a recursive function: it's fast, small, and efficient, but it can cause a stack-overflow if the input document has too many levels. To protect your program against malicious input that could trigger a stack-overflow on purpose, ArduinoJson sets a limit on how many levels it accepts.</p>
<p>The default nesting limit is configured by <a href="/v6/api/config/default_nesting_limit/"><code>ARDUINOJSON_DEFAULT_NESTING_LIMIT</code></a>, which is set to <code>10</code> by default. To increase the limit, you can change this setting, or you can pass an extra argument to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-built_in">deserializeJson</span>(doc, input, DeserializationOption::<span class="hljs-built_in">NestingLimit</span>(<span class="hljs-number">20</span>));
</code></pre>
<p>The <a href="/v6/assistant/">ArduinoJson Assistant</a> shows an alert when your input overpasses the default limit and includes the appropriate <code>DeserializationOption::NestingLimit</code> in the sample program.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Increasing the limit fixes the issue",next:"done"},{id:"limit-increased",label:"No",summary:"Increasing the limit doesn't fix the issue",next:"deadend"}]},done:{content:`<p class="display-4">🙋🏻‍♂️</p>
<p>Awesome! I'm glad I could help.</p>
<p>I would be very grateful if, in return, you could <a href="https://github.com/bblanchon/ArduinoJson/stargazers">star ArduinoJson on GitHub</a>. ❤</p>
<p>If you want to support the project (and learn a lot of stuff as well), you can purchase <a href="/book/">Mastering ArduinoJson</a>.<br>
Alternatively, you can <a href="https://github.com/sponsors/bblanchon">sponsor the project on GitHub</a>.</p>
<p>Should you have any idea on how I could improve myself, please <a href="https://github.com/bblanchon/ArduinoJson/issues/new">open an issue on GitHub</a>.</p>
<p>Bye!</p>
<p>--<br>
<em>ArduinoJson Troubleshooter</em></p>
`,needs_assistance:!1,choices:void 0},index:{content:`<p>When does your issue happen?</p>
`,needs_assistance:!1,choices:[{id:"compiletime",label:"At compile time",next:"compiletime",summary:"The issue happens at compile time"},{id:"runtime",label:"At run time",next:"runtime",summary:"The issue happens at run time"}]},"nul-not-supported":{content:`<p>First, ArduinoJson only supports NUL characters since <a href="/news/2022/01/08/arduinojson-6-19-0/">version 6.19</a>, so make sure you are up-to-date.</p>
<p>Next, the Arduino <a href="https://www.arduino.cc/reference/en/language/variables/data-types/stringobject/"><code>String</code></a> class doesn't support NUL characters, so you have to use another string type, like <a href="/v6/api/jsonstring/"><code>JsonString</code></a>, like this:</p>
<pre><code class="hljs language-c++">JsonString myString = doc[<span class="hljs-string">&quot;myString&quot;</span>];
<span class="hljs-type">const</span> <span class="hljs-type">char</span>* myStringData = myString.<span class="hljs-built_in">c_str</span>();
<span class="hljs-type">size_t</span> myStringSize = myString.<span class="hljs-built_in">size</span>();
</code></pre>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Using `JsonString` solves the issue",next:"done"},{id:"failure",label:"No",summary:"Using `JsonString` doesn't solves the issue",next:"deadend"}]},runtime:{content:`<p>Does your issue concern serialization or deserialization?</p>
`,needs_assistance:!1,choices:[{id:"serialization",label:"Serialization",next:"serialization",summary:"The issue concerns serialization"},{id:"deserialization",label:"Deserialization",next:"deserialization",summary:"The issue concerns deserialization"}]},"serialization/crash/charptr-comment":{content:`<p>Please replace all those string pointers with literals.</p>
<p>For example, replace:</p>
<pre><code class="hljs language-c++">doc[<span class="hljs-string">&quot;name&quot;</span>] = name;
</code></pre>
<p>with:</p>
<pre><code class="hljs language-c++">doc[<span class="hljs-string">&quot;name&quot;</span>] = <span class="hljs-string">&quot;name&quot;</span>;
</code></pre>
<p>Does your program still crash?</p>
`,needs_assistance:!1,choices:[{id:"crash-with-literals",label:"Yes",summary:"Replacing string pointers with literals doesn't fix the crash",next:"serialization/crash/output-type"},{id:"no-crash-with-literals",label:"No",summary:"Replacing string pointers with literals fixes the crash",next:"serialization/crash/charptr-print"}]},"serialization/crash/charptr-print":{content:`<p>One of these string pointers is probably dangling or points to a non-zero-terminated string.</p>
<p>Please print them all to the serial port to find the faulty pointer.</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">print</span>(name);
doc[<span class="hljs-string">&quot;name&quot;</span>] = <span class="hljs-string">&quot;name&quot;</span>;
</code></pre>
<p>Can you locate the faulty string in the output?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Printing the string reveals the faulty pointer",next:"done"},{id:"no-faulty-pointer",label:"No",summary:"Printing the string doesn't show any faulty pointer",next:"deadend"}]},"serialization/crash/charptr":{content:`<p>Does your program insert string pointers in the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>?<br>
(for example, <code>doc[&quot;name&quot;] = name</code> where <code>name</code> is a <code>const char*</code>)</p>
`,needs_assistance:!1,choices:[{id:"charptr",label:"Yes",summary:"Program inserts string pointers into the `JsonDocument`",next:"serialization/crash/charptr-comment"},{id:"no-charptr",label:"No",summary:"Program doesn't insert string pointers into the `JsonDocument`",next:"serialization/crash/output-type"}]},"serialization/crash/output-char-array":{content:`<p>I think this output buffer could cause a <a href="https://en.wikipedia.org/wiki/Stack_buffer_overflow">stack overflow</a>.</p>
<p>Please try with a <a href="https://www.arduino.cc/reference/en/language/variables/data-types/stringobject/"><code>String</code></a> instead of a <code>char[]</code>.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Switching to `String` solves the issue",next:"done"},{id:"failure",label:"No",summary:"Switching to `String` doesn't solve the issue",next:"deadend"}]},"serialization/crash/output-type":{content:`<p>What is the type of the second argument passed to <a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a>?</p>
`,needs_assistance:!1,choices:[{id:"char-array",label:"<code>char[]</code>",summary:"Output type is `char[]`",next:"serialization/crash/output-char-array"},{id:"char-ptr",label:"<code>char*</code>",summary:"Output type is `char*`",next:"deadend"},{id:"arduino-string",label:"<code>String</code>",summary:"Output type is `String`",next:"deadend"},{id:"arduino-stream",label:"<code>Stream</code>",summary:"Output type is `Stream`",next:"deadend"}]},"serialization/crash/progmem-cast":{content:`<p>When you use <code>PROGMEM</code> with ArduinoJson (and any other Arduino code, really), you must make sure that the addresses in Flash memory aren't interpreted as addresses in RAM.</p>
<p>Indeed, <a href="https://en.wikipedia.org/wiki/Harvard_architecture">Harvard architectures</a> (like <a href="https://en.wikipedia.org/wiki/AVR_microcontrollers">AVR</a> and <a href="https://en.wikipedia.org/wiki/ESP8266">ESP8266</a>) use different addresses spaces for RAM and Flash. This means that the same address can refer to either Flash or RAM, so the CPU has no way to tell that some pointers target RAM and some others target Flash. That's why the program needs to treat the two areas separately.</p>
<p>Getting back to your program, you must make sure that ArduinoJson can clearly identify Flash strings as such. The <code>PROGMEM</code> attribute itself isn't sufficient because it doesn't alter the C++ type of the string. That's why ArduinoJson needs you to pass a pointer of type <code>const __FlashStringHelper*</code> when you refer to a Flash string.</p>
<p>For example, the following program is incorrect and very likely to crash:</p>
<pre><code class="hljs language-c++"><span class="hljs-type">const</span> <span class="hljs-type">char</span> PROGMEM key[] = <span class="hljs-string">&quot;the answer&quot;</span>;

doc[key] = <span class="hljs-number">42</span>; <span class="hljs-comment">// &lt;- BOOOOOM!!!!</span>
</code></pre>
<p>Because, it saw a string of type <code>const char*</code>, ArduinoJson assumed the pointer was refering to the RAM address space.
To fix this issue, you must cast the pointer to \`\`const __FlashStringHelper*\`, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-type">const</span> <span class="hljs-type">char</span> PROGMEM key_data[] = <span class="hljs-string">&quot;the answer&quot;</span>;
<span class="hljs-type">const</span> __FlashStringHelper* key = (<span class="hljs-type">const</span> __FlashStringHelper*)key_data;

doc[key] = <span class="hljs-number">42</span>;
</code></pre>
<p>As you can see, this is quite ugly. Fortunately, Arduino provides the F(), which declares the Flash string and return the appropriate type:</p>
<pre><code class="hljs language-c++">doc[<span class="hljs-built_in">F</span>(<span class="hljs-string">&quot;the answer&quot;</span>)] = <span class="hljs-number">42</span>;
</code></pre>
<p>There is one drawback with the <code>F()</code>, though: it doesn't perform <a href="https://cpp4arduino.com/2018/10/23/what-is-string-interning.html">string interning</a>.
This means that every call to the macro creates a new string in the program memory, even if the same string was already present.
So, be careful with this macro; otherwise, you'll end up with multiple copies of the same string, and the program will be bigger than it should.
If you need to use the Flash string in several places, you better define a variable, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-keyword">auto</span> key = <span class="hljs-built_in">F</span>(<span class="hljs-string">&quot;the answer&quot;</span>);

doc[key] = <span class="hljs-number">42</span>;
</code></pre>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Casting the pointer solves the issue",next:"done"},{id:"failure",label:"No",summary:"Casting the pointer doesn't solve the issue",next:"deadend"}]},"serialization/crash/progmem":{content:`<p>Do you use <code>PROGMEM</code> (Flash memory) in your program?</p>
`,needs_assistance:!1,choices:[{id:"progmem",label:"Yes",summary:"Program uses `PROGMEM`",next:"serialization/crash/progmem-cast"},{id:"no-progmem",label:"No",summary:"Program doesn't use `PROGMEM`",next:"serialization/crash/charptr"}]},"serialization/crash/static":{content:`<p>Because a <a href="/v6/api/staticjsondocument/"><code>StaticJsonDocument</code></a> resides on the stack, it may cause a <a href="https://en.wikipedia.org/wiki/Stack_buffer_overflow">stack-overflow</a>.</p>
<p>Some platforms can detect stack overflows, in which case they raise an exception; others let the program do nothing and let the program crash on its own.
The tricky part with stack overflows is that the program doesn't always crash; it can also expose all kind of weird behavior.</p>
<p>If you have no idea of what I'm talking about, please have a look a &quot;The missing C++ course&quot; in <a href="/book/">Mastering ArduioJson</a>, it explains the roles of the different areas of memory, as well as many other important stuffs.</p>
<p>Please switch to a <a href="/v6/api/dynamicjsondocument/"><code>DynamicJsonDocument</code></a></p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Switching to `DynamicJsonDocument` fixes the issue",next:"done"},{id:"dynamicjsondocument-tried",label:"No",summary:"Switching to `DynamicJsonDocument` doesn't fix the issue",next:"serialization/crash/progmem"}]},"serialization/crash":{content:`<p>Do you use <a href="/v6/api/dynamicjsondocument/"><code>DynamicJsonDocument</code></a> or <a href="/v6/api/staticjsondocument/"><code>StaticJsonDocument</code></a>?</p>
`,needs_assistance:!1,choices:[{id:"dynamicjsondocument",label:"<code>DynamicJsonDocument</code>",summary:"Program uses `DynamicJsonDocument`",next:"serialization/crash/progmem"},{id:"staticjsondocument",label:"<code>StaticJsonDocument</code>",summary:"Program uses `StaticJsonDocument`",next:"serialization/crash/static"}]},"serialization/empty/capacity-0-basic-global":{content:`<p><code>doc.capacity()</code> returns <code>0</code>, which means <code>allocate()</code> returned <code>NULL</code>.</p>
<p>I think that, because <code>doc</code> is declared global, <code>allocate()</code> is called too early and the heap isn't ready.
I know this happens on ESP32 with the <code>SpiRamJsonDocument</code> shown in <a href="/v6/how-to/use-external-ram-on-esp32/">How to use external RAM on ESP32?</a>.</p>
<p>Please try to make <code>doc</code> a local variable.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Making `doc` local fixes the issue.",next:"done"},{id:"failure",label:"No",summary:"Making `doc` local doesn't fix the issue.",next:"deadend"}]},"serialization/empty/capacity-0-basic":{content:`<p>Is <code>doc</code> a global variable?</p>
`,needs_assistance:!1,choices:[{id:"global",label:"Yes",summary:"`doc` is a global variable",next:"serialization/empty/capacity-0-basic-global"},{id:"local",label:"No",summary:"`doc` is not a global variable",next:"serialization/empty/capacity-0-oom"}]},"serialization/empty/capacity-0-oom":{content:`<p>Well, that's very bad news. It seems that your microcontroller doesn't have enough RAM to hold the JSON document.</p>
<p>Please read <a href="/v6/how-to/reduce-memory-usage/">How to reduce memory usage</a>.
It shows several techniques you can use to use less RAM.
Hopefully, you'll make enough room for the memory pool.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Reducing memory usage fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Reducing memory usage doesn't fix the issue",next:"deadend"}]},"serialization/empty/capacity-0":{content:`<p>What kind of <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> do you use?</p>
`,needs_assistance:!1,choices:[{id:"basic",label:"<code>BasicJsonDocument&lt;T&gt;</code>",summary:"The document is a `BasicJsonDocument<T>`",next:"serialization/empty/capacity-0-basic"},{id:"dynamic",label:"<code>DynamicJsonDocument</code>",summary:"The document is a `DynamicJsonDocument`",next:"serialization/empty/capacity-0-oom"},{id:"static",label:"<code>StaticJsonDocument</code>",summary:"The document is a `StaticJsonDocument`",next:"deadend"}]},"serialization/empty":{content:`<p>Please print the capacity of the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> to the serial port, like so:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">println</span>(doc.<span class="hljs-built_in">capacity</span>());
</code></pre>
<p>What value does it show?</p>
`,needs_assistance:!1,choices:[{id:"capacity-0",label:"<code>0</code>",summary:"`doc.capacity()` returns `0`",next:"serialization/empty/capacity-0"},{id:"capacity-ok",label:"a positive integer",summary:"`doc.capacity()` looks good",next:"deadend"}]},"serialization/emptystrings/deserialize-zerocopy":{content:`<p>When you pass a writeable buffer as the input of <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>, ArduinoJson uses the <strong>zero-copy mode</strong>. Instead of copying the strings from the input into the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, it stores pointers to the input buffer.
Therefore, the input buffer must stay alive during the whole lifetime of the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>; otherwise, all the strings in the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> will be dangling pointers.
Usually, this produces grabage in the output, but it can sometimes produce empty strings.</p>
<p>The easiest solution is to disable the zero-copy mode by passing a read-only input buffer. Casting the pointer will do the trick:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// replace this:</span>
<span class="hljs-built_in">deserializeJson</span>(doc, input, inputSize);

<span class="hljs-comment">// ...with this:</span>
<span class="hljs-built_in">deserializeJson</span>(doc, (<span class="hljs-type">const</span> <span class="hljs-type">char</span>*)input, inputSize);
</code></pre>
<p>Now that the zero-copy mode is disabled, you probably need a larger <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, so don't forget to update the capacity. As usual, use the <a href="/v6/assistant/">ArduinoJson Assistant</a> to compute the right capacity for your project.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Casting the pointer fixes the issue",next:"done"},{id:"casting-fails",label:"No",summary:"Casting the pointer doesn't fix the issue",next:"deadend"}]},"serialization/emptystrings/deserialize":{content:`<p>What's the type of the second argument passed to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>?</p>
`,needs_assistance:!1,choices:[{id:"char-ptr",label:"<code>char*</code> (or <code>char[]</code>)",summary:"Input type is `char*`",next:"serialization/emptystrings/deserialize-zerocopy"},{id:"const-char-ptr",label:"<code>const char*</code>",summary:"Input type is `const char*`",next:"deadend"},{id:"string",label:"<code>String</code> (or <code>std::string</code>)",summary:"Input type is `String`",next:"deadend"},{id:"stream",label:"<code>Stream</code> (or <code>std::istream)</code>",summary:"Input type is `Stream`",next:"deadend"}]},"serialization/emptystrings":{content:`<p>Do you call <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> before calling <a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a>? (with the same <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>)</p>
`,needs_assistance:!1,choices:[{id:"deserialize-yes",label:"Yes",summary:"Program calls `deserializeJson()`",next:"serialization/emptystrings/deserialize"},{id:"deserialize-no",label:"No",summary:"Program doesn't call `deserializeJson()`",next:"deadend"}]},"serialization/floats/double-rounded":{content:`<p>I'm surprised that the previous solution didn't work.</p>
<p>We can try to change ArduinoJson's storage type to see if it solves your issue.</p>
<p>Please define <a href="/v6/api/config/use_double/">ARDUINOJSON_USE_DOUBLE</a> to <code>0</code> before including <code>ArduinoJson.h</code>, like so:</p>
<pre><code class="hljs language-c++"><span class="hljs-meta">#<span class="hljs-keyword">define</span> ARDUINOJSON_USE_DOUBLE 0</span>
<span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;ArduinoJson.h&gt;</span></span>
</code></pre>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Defining `ARDUINOJSON_USE_DOUBLE` to `0` fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Defining `ARDUINOJSON_USE_DOUBLE` to `0` doesn't fix the issue",next:"deadend"}]},"serialization/floats/double":{content:`<p>Please add the following function somewhere in your program:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// rounds a number to 2 decimal places</span>
<span class="hljs-comment">// example: round(3.14159) -&gt; 3.14</span>
<span class="hljs-function"><span class="hljs-type">double</span> <span class="hljs-title">round2</span><span class="hljs-params">(<span class="hljs-type">double</span> value)</span> </span>{
   <span class="hljs-keyword">return</span> (<span class="hljs-type">int</span>)(value * <span class="hljs-number">100</span> + <span class="hljs-number">0.5</span>) / <span class="hljs-number">100.0</span>;
}
</code></pre>
<p>Now, call this function when inserting the value in the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>; for example:</p>
<pre><code class="hljs language-c++">doc[<span class="hljs-string">&quot;value&quot;</span>] = <span class="hljs-built_in">round2</span>(value);
</code></pre>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Rounding the value fixes the issue",next:"done"},{id:"double",label:"No",summary:"Rounding the value doesn't fix the issue",next:"serialization/floats/double-rounded"}]},"serialization/floats":{content:`<p>Supposing that the JSON output looks like this:</p>
<pre><code class="hljs language-json"><span class="hljs-punctuation">{</span><span class="hljs-attr">&quot;value&quot;</span><span class="hljs-punctuation">:</span><span class="hljs-number">23.79999924</span><span class="hljs-punctuation">}</span>
</code></pre>
<p>I'm assuming you expect it to look like that:</p>
<pre><code class="hljs language-json"><span class="hljs-punctuation">{</span><span class="hljs-attr">&quot;value&quot;</span><span class="hljs-punctuation">:</span><span class="hljs-number">24.8</span><span class="hljs-punctuation">}</span>
</code></pre>
<p>Please change the type of the variable from <code>float</code> to <code>double</code> and try again.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Replacing `float` with `double` fixes the issue",next:"done"},{id:"double",label:"No",summary:"Replacing `float` with `double` doesn't fix the issue",next:"serialization/floats/double"}]},"serialization/garbage/deserialize-zerocopy":{content:`<p>When you pass a writeable buffer as the input of <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>, ArduinoJson uses the <strong>zero-copy mode</strong>. Instead of copying the strings from the input into the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, it stores pointers to the input buffer.
Therefore, the input buffer must stay alive during the whole lifetime of the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>; otherwise, all the strings in the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> will be dangling pointers.</p>
<p>The easiest solution is to disable the zero-copy mode by passing a read-only input buffer. Casting the pointer will do the trick:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// replace this:</span>
<span class="hljs-built_in">deserializeJson</span>(doc, input, inputSize);

<span class="hljs-comment">// ...with this:</span>
<span class="hljs-built_in">deserializeJson</span>(doc, (<span class="hljs-type">const</span> <span class="hljs-type">char</span>*)input, inputSize);
</code></pre>
<p>Now that the zero-copy mode is disabled, you probably need a larger <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, so don't forget to update the capacity. As usual, use the <a href="/v6/assistant/">ArduinoJson Assistant</a> to compute the right capacity for your project.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Casting the pointer fixes the issue",next:"done"},{id:"casting-fails",label:"No",summary:"Casting the pointer doesn't fix the issue",next:"serialization/garbage/serialize"}]},"serialization/garbage/deserialize":{content:`<p>What's the type of the second argument passed to <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a>?</p>
`,needs_assistance:!1,choices:[{id:"char-ptr",label:"<code>char*</code> (or <code>char[]</code>)",summary:"Input type is `char*`",next:"serialization/garbage/deserialize-zerocopy"},{id:"const-char-ptr",label:"<code>const char*</code>",summary:"Input type is `const char*`",next:"serialization/garbage/serialize"},{id:"string",label:"<code>String</code> (or <code>std::string</code>)",summary:"Input type is `String`",next:"serialization/garbage/serialize"},{id:"stream",label:"<code>Stream</code> (or <code>std::istream)</code>",summary:"Input type is `Stream`",next:"serialization/garbage/serialize"}]},"serialization/garbage/document-dynamic-string":{content:`<p>You can insert <a href="https://www.arduino.cc/reference/en/language/variables/data-types/stringobject/"><code>String</code></a> objects in a <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, but make sure you don't store the result of <a href="https://www.arduino.cc/reference/en/language/variables/data-types/string/functions/c_str/"><code>String::c_str()</code></a>.</p>
<p>The pointer returned by <a href="https://www.arduino.cc/reference/en/language/variables/data-types/string/functions/c_str/"><code>String::c_str()</code></a> is valid only if you don't modify or destroy the string.
Indeed, this function returns the address of the internal buffer of the <a href="https://www.arduino.cc/reference/en/language/variables/data-types/stringobject/"><code>String</code></a> instance.
This buffer is freed by <a href="https://www.arduino.cc/reference/en/language/variables/data-types/stringobject/"><code>String</code></a>'s destructor and may be reallocated each time the  <a href="https://www.arduino.cc/reference/en/language/variables/data-types/stringobject/"><code>String</code></a> changes. It's crucial that you don't save the pointer returned of <a href="https://www.arduino.cc/reference/en/language/variables/data-types/string/functions/c_str/"><code>String::c_str()</code></a>; otherwise, the pointer may be dangling.</p>
<p>For example, the following lines create a dangling pointer:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// DON&#x27;T DO THAT!!!  💀</span>
doc[<span class="hljs-string">&quot;address&quot;</span>] = address.<span class="hljs-built_in">toString</span>().<span class="hljs-built_in">c_str</span>();
<span class="hljs-comment">// ...</span>
<span class="hljs-built_in">serializeJson</span>(doc, Serial);  <span class="hljs-comment">// &lt;- likely to produce garbage</span>
</code></pre>
<p>Here, <code>address.toString()</code> returns a temporary <a href="https://www.arduino.cc/reference/en/language/variables/data-types/stringobject/"><code>String</code></a> which is destructed after executing the first statement. When <a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a> runs, the pointer stored in the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> will be pointing to an invalid location. It may work by accident, but if the memory location is reused by another variable, it will print garbage to the serial port.</p>
<p>The solution is to duplicate string instead of saving a pointer.
Since ArduinoJson automatically duplicates <a href="https://www.arduino.cc/reference/en/language/variables/data-types/stringobject/"><code>String</code></a> instances, all you need to do is  removing the call to <a href="https://www.arduino.cc/reference/en/language/variables/data-types/string/functions/c_str/"><code>String::c_str()</code></a>, like so:</p>
<pre><code class="hljs language-c++">doc[<span class="hljs-string">&quot;address&quot;</span>] = address.<span class="hljs-built_in">toString</span>(); <span class="hljs-comment">// &lt;- duplicates</span>
<span class="hljs-comment">// ...</span>
<span class="hljs-built_in">serializeJson</span>(doc, Serial);
</code></pre>
<p>Now, ArduinoJson sees a <a href="https://www.arduino.cc/reference/en/language/variables/data-types/stringobject/"><code>String</code></a> and knows that it needs to make a copy of the string in the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Removing `.c_str()` fixes the issue",next:"done"},{id:"failure",label:"No",summary:"Removing `.c_str()` doesn't fix the issue",next:"deadend"}]},"serialization/garbage/document-dynamic":{content:`<p>Do you use <a href="https://www.arduino.cc/reference/en/language/variables/data-types/stringobject/"><code>String</code></a> in your program?</p>
`,needs_assistance:!1,choices:[{id:"string",label:"Yes",summary:"Program uses `String`",next:"serialization/garbage/document-dynamic-string"},{id:"no-string",label:"No",summary:"Program doesn't use `String`",next:"deadend"}]},"serialization/garbage/document-static":{content:`<p><a href="/v6/api/staticjsondocument/"><code>StaticJsonDocument</code></a> stores its memory pool in the stack.
A large memory pool can overflow the stack a produce all kinds of strange behavior.
In particural, a <a href="https://en.wikipedia.org/wiki/Stack_buffer_overflow">stack overflow</a> can cause garbage in <a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a>'s output.</p>
<p>Please switch to a <a href="/v6/api/dynamicjsondocument/"><code>DynamicJsonDocument</code></a>.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Switching to `DynamicJsonDocument` fixes the issue",next:"done"},{id:"dynamic",label:"No",summary:"Switching to `DynamicJsonDocument` doesn't fix the issue",next:"serialization/garbage/document-dynamic"}]},"serialization/garbage/document":{content:`<p>Which kind of <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> do you use?</p>
`,needs_assistance:!1,choices:[{id:"dynamic",label:"<code>DynamicJsonDocument</code>",summary:"Program uses `DynamicJsonDocument`",next:"serialization/garbage/document-dynamic"},{id:"static",label:"<code>StaticJsonDocument</code>",summary:"Program uses `StaticJsonDocument`",next:"serialization/garbage/document-static"}]},"serialization/garbage/json":{content:`<p>Do you call <a href="/v6/api/json/deserializejson/"><code>deserializeJson()</code></a> before calling <a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a>? (with the same <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>)</p>
`,needs_assistance:!1,choices:[{id:"deserialize-yes",label:"Yes",summary:"Program calls `deserializeJson()`",next:"serialization/garbage/deserialize"},{id:"deserialize-no",label:"No",summary:"Program doesn't call `deserializeJson()`",next:"serialization/garbage/serialize"}]},"serialization/garbage/jsonarray":{content:`<p><a href="/v6/api/jsonarray/"><code>JsonArray</code></a> doesn't contain any data: it is a reference to an object stored in the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>. It becomes invalid as soon as the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> is destroyed; this could explain the garbage you see in the output.</p>
<p>For example, here is a function that creates a dangling <a href="/v6/api/jsonarray/"><code>JsonArray</code></a>:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// DON&#x27;T DO THAT!!!  💀</span>
<span class="hljs-function">JsonArray <span class="hljs-title">createArray</span><span class="hljs-params">()</span> </span>{
  StaticJsonDocument&lt;<span class="hljs-number">200</span>&gt; doc;
  JsonArray arr = doc.<span class="hljs-built_in">to</span>&lt;JsonArray&gt;();
  arr.<span class="hljs-built_in">add</span>(<span class="hljs-string">&quot;hello&quot;</span>);
  arr.<span class="hljs-built_in">add</span>(<span class="hljs-string">&quot;world&quot;</span>);
  <span class="hljs-keyword">return</span> arr;
}
</code></pre>
<p>The <a href="/v6/api/jsonarray/"><code>JsonArray</code></a> returned by this function points to a destructed <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, and therefore is likely to produce garbage or crash the program.</p>
<p>The best way to fix this function is to pass the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> as an argument:</p>
<pre><code class="hljs language-c++"><span class="hljs-function">JsonArray <span class="hljs-title">createArray</span><span class="hljs-params">(JsonDocument&amp; doc)</span> </span>{
  JsonArray arr = doc.<span class="hljs-built_in">to</span>&lt;JsonArray&gt;();
  arr.<span class="hljs-built_in">add</span>(<span class="hljs-string">&quot;hello&quot;</span>);
  arr.<span class="hljs-built_in">add</span>(<span class="hljs-string">&quot;world&quot;</span>);
  <span class="hljs-keyword">return</span> arr;
}
</code></pre>
<p>This way, you can keep the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> alive when you call <a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a></p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Extending the lifetime of the `JsonDocument` solves the issue",next:"done"},{id:"not-destroyed",label:"No",summary:"Extending the lifetime of the `JsonDocument` doesn't solve the issue",next:"serialization/garbage/reference"}]},"serialization/garbage/jsonobject":{content:`<p><a href="/v6/api/jsonobject/"><code>JsonObject</code></a> doesn't contain any data: it is a reference to an object stored in the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>. It becomes invalid as soon as the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> is destroyed; this could explain the garbage you see in the output.</p>
<p>For example, here is a function that creates a dangling <a href="/v6/api/jsonobject/"><code>JsonObject</code></a>:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// DON&#x27;T DO THAT!!!  💀</span>
<span class="hljs-function">JsonObject <span class="hljs-title">createObject</span><span class="hljs-params">()</span> </span>{
  StaticJsonDocument&lt;<span class="hljs-number">200</span>&gt; doc;
  JsonObject obj = doc.<span class="hljs-built_in">to</span>&lt;JsonObject&gt;();
  obj[<span class="hljs-string">&quot;hello&quot;</span>] = <span class="hljs-string">&quot;world&quot;</span>;
  <span class="hljs-keyword">return</span> obj;
}
</code></pre>
<p>The <a href="/v6/api/jsonobject/"><code>JsonObject</code></a> returned by this function points to a destructed <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, and therefore is likely to produce garbage or crash the program.</p>
<p>The best way to fix this function is to pass the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> as an argument:</p>
<pre><code class="hljs language-c++"><span class="hljs-function">JsonObject <span class="hljs-title">createObject</span><span class="hljs-params">(JsonDocument&amp; doc)</span> </span>{
  JsonObject obj = doc.<span class="hljs-built_in">to</span>&lt;JsonObject&gt;();
  obj[<span class="hljs-string">&quot;hello&quot;</span>] = <span class="hljs-string">&quot;world&quot;</span>;
  <span class="hljs-keyword">return</span> obj;
}
</code></pre>
<p>This way, you can keep the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> alive when you call <a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a></p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Extending the lifetime of the `JsonDocument` solves the issue",next:"done"},{id:"not-destroyed",label:"No",summary:"Extending the lifetime of the `JsonDocument` doesn't solve the issue",next:"serialization/garbage/reference"}]},"serialization/garbage/jsonvariant":{content:`<p><a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a> doesn't contain any data: it is a reference to an object stored in the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>. It becomes invalid as soon as the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> is destroyed; this could explain the garbage you see in the output.</p>
<p>For example, here is a function that creates a dangling <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a>:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// DON&#x27;T DO THAT!!!  💀</span>
<span class="hljs-function">JsonVariant <span class="hljs-title">createVariant</span><span class="hljs-params">()</span> </span>{
  StaticJsonDocument&lt;<span class="hljs-number">200</span>&gt; doc;
  JsonVariant var = doc.<span class="hljs-built_in">to</span>&lt;JsonVariant&gt;();
  var[<span class="hljs-string">&quot;hello&quot;</span>] = <span class="hljs-string">&quot;world&quot;</span>;
  <span class="hljs-keyword">return</span> var;
}
</code></pre>
<p>The <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a> returned by this function points to a destructed <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>, and therefore is likely to produce garbage or crash the program.</p>
<p>The best way to fix this function is to pass the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> as an argument:</p>
<pre><code class="hljs language-c++"><span class="hljs-function">JsonVariant <span class="hljs-title">createVariant</span><span class="hljs-params">(JsonDocument&amp; doc)</span> </span>{
  JsonVariant var = doc.<span class="hljs-built_in">to</span>&lt;JsonVariant&gt;();
  var[<span class="hljs-string">&quot;hello&quot;</span>] = <span class="hljs-string">&quot;world&quot;</span>;
  <span class="hljs-keyword">return</span> var;
}
</code></pre>
<p>This way, you can keep the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> alive when you call <a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a></p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Extending the lifetime of the `JsonDocument` solves the issue",next:"done"},{id:"not-destroyed",label:"No",summary:"Extending the lifetime of the `JsonDocument` doesn't solve the issue",next:"serialization/garbage/reference"}]},"serialization/garbage/msgpack-only":{content:`<p><a href="/v6/api/msgpack/serializemsgpack/"><code>serializeMsgPack()</code></a> share a lot of code with <a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a>. There is no reason you should produce garbage with one and not the other. You may have found a bug.</p>
`,needs_assistance:!0,choices:void 0},"serialization/garbage/msgpack":{content:`<p><a href="https://msgpack.org/">MessagePack</a> is a binary format, so if you print a document to the serial port, the result would look weird.</p>
<p>For example, the following program:</p>
<pre><code class="hljs language-c++">StaticJsonDocument&lt;<span class="hljs-number">256</span>&gt; doc;
doc[<span class="hljs-string">&quot;val&quot;</span>] = <span class="hljs-number">42</span>;
<span class="hljs-built_in">serializeMsgPack</span>(doc, Serial);
</code></pre>
<p>produces:</p>
<pre><code class="hljs language-text">⸮⸮val*
</code></pre>
<p>Yes. This is the expected result.</p>
<p>Now that this question is cleared, let's move on with the diagnostic.</p>
<p>I need you to edit your program to replace <a href="/v6/api/msgpack/serializemsgpack/"><code>serializeMsgPack()</code></a> with <a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a>.</p>
<p>Do you see the garbage in the JSON output?</p>
`,needs_assistance:!1,choices:[{id:"json",label:"Yes, <code>serializeJson()</code> produces garbage too",summary:"`serializeJson()` produces garbage too",next:"serialization/garbage/json"},{id:"deserialize-no",label:"No, <code>serializeJson()</code> doesn't produces garbage",summary:"`serializeJson()` doesn't produces garbage",next:"serialization/garbage/msgpack-only"}]},"serialization/garbage/reference-garbagecollect":{content:`<p><a href="/v6/api/jsondocument/garbagecollect/"><code>JsonDocument::garbageCollect()</code></a> invalidates all previously acquired <a href="/v6/api/jsonarray/"><code>JsonArray</code></a>, <a href="/v6/api/jsonobject/"><code>JsonObject</code></a>, and <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a>.</p>
<p>After you call it, make sure to reassign  every references, like so:</p>
<pre><code class="hljs language-c++">JsonObject cfg = doc.<span class="hljs-built_in">createNestedObject</span>(<span class="hljs-string">&quot;config&quot;</span>);
<span class="hljs-comment">// ...</span>
doc.<span class="hljs-built_in">garbageCollect</span>();
cfg = doc[<span class="hljs-string">&quot;config&quot;</span>];
</code></pre>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Reassigning references fixes the issue",next:"done"},{id:"reassigned",label:"No",summary:"Reassigning references doesn't fix the issue",next:"serialization/garbage/document"}]},"serialization/garbage/reference-shrinktofit":{content:`<p><a href="/v6/api/basicjsondocument/shrinktofit/"><code>JsonDocument::shrinkToFit()</code></a> invalidates all previously acquired <a href="/v6/api/jsonarray/"><code>JsonArray</code></a>, <a href="/v6/api/jsonobject/"><code>JsonObject</code></a>, and <a href="/v6/api/jsonvariant/"><code>JsonVariant</code></a>.</p>
<p>After you call it, make sure to reassign  every references, like so:</p>
<pre><code class="hljs language-c++">JsonObject cfg = doc.<span class="hljs-built_in">createNestedObject</span>(<span class="hljs-string">&quot;config&quot;</span>);
<span class="hljs-comment">// ...</span>
doc.<span class="hljs-built_in">shrinkToFit</span>();
cfg = doc[<span class="hljs-string">&quot;config&quot;</span>];
</code></pre>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Reassigning references fixes the issue",next:"done"},{id:"reassigned",label:"No",summary:"Reassigning references doesn't fix the issue",next:"serialization/garbage/document"}]},"serialization/garbage/reference":{content:`<p>Do you use one of the following function?</p>
`,needs_assistance:!1,choices:[{id:"garbagecollect",label:"<code>JsonDocument::garbageCollect()</code>",summary:"Program calls `JsonDocument::garbageCollect()`",next:"serialization/garbage/reference-garbagecollect"},{id:"shrinktofit",label:"<code>JsonDocument::shrinkToFit()</code>",summary:"Program calls `JsonDocument::shrinkToFit()`",next:"serialization/garbage/reference-shrinktofit"},{id:"none",label:"No",summary:"Program doesn't call an invalidating function",next:"serialization/garbage/document"}]},"serialization/garbage/serialize":{content:`<p>What is the type of the first argument passed to <a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a>?</p>
`,needs_assistance:!1,choices:[{id:"document",label:"<code>JsonDocument</code>",summary:"Program calls `serializeJson(const JsonDocument&, ...)`",next:"serialization/garbage/document"},{id:"array",label:"<code>JsonArray</code> (or <code>JsonArrayConst</code>)",summary:"Program calls `serializeJson(JsonArrayConst, ...)`",next:"serialization/garbage/jsonarray"},{id:"object",label:"<code>JsonObject</code> (or <code>JsonObjectConst</code>)",summary:"Program calls `serializeJson(JsonObjectConst, ...)`",next:"serialization/garbage/jsonobject"},{id:"variant",label:"<code>JsonVariant</code> (or <code>JsonVariantConst</code>)",summary:"Program calls `serializeJson(JsonVariantConst, ...)`",next:"serialization/garbage/jsonvariant"}]},"serialization/garbage":{content:`<p>What function produces the garbage?</p>
`,needs_assistance:!1,choices:[{id:"json",label:"<code>serializeJson()</code> (or <code>serializeJsonPretty()</code>)",summary:"`serializeJson()` produces garbage",next:"serialization/garbage/json"},{id:"msg",label:"<code>serializeMsgPack()</code>",summary:"`serializeMsgPack()` produces garbage",next:"serialization/garbage/msgpack"}]},"serialization/incomplete/loop-outside-clear":{content:`<p>Please replace the call to <a href="/v6/api/jsondocument/clear/"><code>JsonDocument::clear()</code></a> with <a href="/v6/api/jsondocument/garbagecollect/"><code>JsonDocument::garbageCollect()</code></a>.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Calling `JsonDocument::garbageCollect()` solves the issue",next:"done"},{id:"gc",label:"No",summary:"Calling `JsonDocument::garbageCollect()` doesn't solve the issue",next:"serialization/incomplete/noloop"}]},"serialization/incomplete/loop-outside":{content:`<p>We can try to clear the content of the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>.</p>
<p>Please call <a href="/v6/api/jsondocument/clear/"><code>JsonDocument::clear()</code></a> at the beginning of the loop.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Calling `JsonDocument::clear()` solves the issue",next:"done"},{id:"clear",label:"No",summary:"Calling `JsonDocument::clear()` doesn't solve the issue",next:"serialization/incomplete/noloop"},{id:"missing",label:"Yes, but now some values are missing",summary:"Calling `JsonDocument::clear()` solves the issue but removes other values",next:"serialization/incomplete/loop-outside-clear"}]},"serialization/incomplete/loop":{content:`<p>Calling <a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a> is safe, but you'll run into issues if you modify the same <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> in a loop.</p>
<p>The best workaround is to move the declaration of the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> inside the loop.</p>
<p>For example:</p>
<pre><code class="hljs language-c++"><span class="hljs-comment">// Before</span>
StaticJsonDocument&lt;<span class="hljs-number">128</span>&gt; doc;
<span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i=<span class="hljs-number">0</span>; i&lt;<span class="hljs-number">10</span>; i++) {
  doc[<span class="hljs-string">&quot;userid&quot;</span>] = <span class="hljs-built_in">String</span>(<span class="hljs-string">&quot;user&quot;</span>) + i;
  <span class="hljs-built_in">serializeJson</span>(doc, Serial);
}

<span class="hljs-comment">// After</span>
<span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i=<span class="hljs-number">0</span>; i&lt;<span class="hljs-number">10</span>; i++) {
  StaticJsonDocument&lt;<span class="hljs-number">128</span>&gt; doc;
  doc[<span class="hljs-string">&quot;userid&quot;</span>] = <span class="hljs-built_in">String</span>(<span class="hljs-string">&quot;user&quot;</span>) + i;
  <span class="hljs-built_in">serializeJson</span>(doc, Serial);
}
</code></pre>
<p>Please read <a href="/v6/issues/memory-leak/">I found a memory leak in the library!</a> for an explanation.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Moving the `JsonDocument` inside the loop solves the issue",next:"done"},{id:"inside",label:"No",summary:"Moving the `JsonDocument` inside the loop doesn't solve the issue",next:"serialization/incomplete/noloop"},{id:"outside",label:"I cannot move the declaration inside the loop",summary:"The `JsonDocument` cannot be moved inside the loop",next:"serialization/incomplete/loop-outside"}]},"serialization/incomplete/noloop":{content:`<p>Please print the value of <a href="/v6/api/jsondocument/overflowed/"><code>JsonDocument::overflowed()</code></a>, like so:</p>
<pre><code class="hljs language-c++">Serial.<span class="hljs-built_in">println</span>(doc.<span class="hljs-built_in">overflowed</span>());
</code></pre>
<p>What value does it print?</p>
`,needs_assistance:!1,choices:[{id:"overflowed-1",label:"<code>1</code> (or <code>true</code>)",summary:"`JsonDocument::overflowed()` returns `true`",next:"serialization/incomplete/overflowed"},{id:"overflowed-0",label:"<code>0</code> (or <code>false</code>)",summary:"`JsonDocument::overflowed()` returns `false`",next:"serialization/incomplete/nul-char"}]},"serialization/incomplete/nul-char":{content:`<p>Does one of the strings in the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> contains a NUL (i.e.,  ASCII code 0, or <code>\\u0000</code>)?</p>
`,needs_assistance:!1,choices:[{id:"nul",label:"Yes",summary:"One or more strings contain a NUL",next:"nul-not-supported"},{id:"no-nul",label:"No",summary:"No string contains a NUL",next:"deadend"}]},"serialization/incomplete/overflowed":{content:`<p><a href="/v6/api/jsondocument/overflowed/"><code>JsonDocument::overflowed()</code></a> returns <code>true</code> when you tried to insert a value in the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a> but there isn't enough room to store it.</p>
<p>The solution is to increase the capacity of the <a href="/v6/api/jsondocument/"><code>JsonDocument</code></a>. Don't forget to account for the size of the duplicated strings. As usual, use the <a href="/v6/assistant/">ArduinoJson Assistant</a> to compute the right capacity for your project.</p>
<p>For more explanations on this issue, please see <a href="/v6/issues/incomplete-output/">Why is the output incomplete?</a>.</p>
<p>Did this solve your issue?</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Increasing the capacity of the `JsonDocument` solves the issue.",next:"done"},{id:"capacity-increased",label:"No",summary:"Increasing the capacity of the `JsonDocument` doesn't solve the issue.",next:"deadend"}]},"serialization/incomplete":{content:`<p>Does your program call <a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a> in a loop?</p>
`,needs_assistance:!1,choices:[{id:"loop",label:"Yes",summary:"Program calls `serializeJson()` in a loop",next:"serialization/incomplete/loop"},{id:"no-loop",label:"No",summary:"Program doesn't call `serializeJson()` in a loop",next:"serialization/incomplete/noloop"}]},serialization:{content:`<p>What's the problem?</p>
`,needs_assistance:!1,choices:[{id:"crash",label:"The program crashes",summary:"Program crashes",next:"serialization/crash"},{id:"empty",label:"The output is empty (e.g. <code>{}</code>, <code>[]</code>, or <code>null</code>)",summary:"Output is empty",next:"serialization/empty"},{id:"incomplete",label:"The output is incomplete",summary:"Output is incomplete",next:"serialization/incomplete"},{id:"garbage",label:"The output contains garbage",summary:"Output contains garbage",next:"serialization/garbage"},{id:"empty-strings",label:"The output contains empty strings",summary:"Output contains empty strings",next:"serialization/emptystrings"},{id:"null",label:"The output contains <code>null</code>",summary:"Output contains null",next:"serialization/incomplete"},{id:"float",label:"Floating-point values contain too many decimal digits",summary:"Floating-point values contain too many decimal digits",next:"serialization/floats"},{id:"slow",label:"It's slow",summary:"Serialization is slow",next:"serialization/slow"}]},"serialization/slow":{content:`<p><a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a> writes the JSON document mostly one character at a time, which can be pretty slow with unbuffered streams (such as <a href="https://www.arduino.cc/en/Reference/EthernetClient"><code>EthernetClient</code></a>, <a href="https://www.arduino.cc/en/Reference/WiFiClient"><code>WifiClient</code></a>, <a href="https://www.arduino.cc/en/Reference/SD"><code>File</code></a>, and <a href="https://github.com/knolleary/pubsubclient/">PubSubClient</a>).</p>
<p>To speed up the serialization process, you must insert a buffer between the stream and <a href="/v6/api/json/serializejson/"><code>serializeJson()</code></a>.
The easiest way to do so it by using the <code>WriteBufferingStream</code> from the <a href="https://github.com/bblanchon/ArduinoStreamUtils">StreamUtils</a> library.</p>
<p>Replace the following:</p>
<pre><code class="hljs language-c++"><span class="hljs-built_in">serializeJson</span>(doc, stream);
</code></pre>
<p>with</p>
<pre><code class="hljs language-c++"><span class="hljs-function">WriteBufferingStream <span class="hljs-title">bufferedStream</span><span class="hljs-params">(stream, <span class="hljs-number">64</span>)</span></span>;
<span class="hljs-built_in">serializeJson</span>(doc, bufferedStream);
bufferedStream.<span class="hljs-built_in">flush</span>();
</code></pre>
<p>The first line creates a new stream <code>bufferedStream</code> that implements buffering on top of the original (this is the <a href="https://en.wikipedia.org/wiki/Decorator_pattern">decorator pattern</a>).<br>
The second line writes the JSON document to the <a href="https://www.arduino.cc/en/Reference/WiFiClient"><code>WiFiClient</code></a> through the buffer.<br>
The last line flushes the buffer to make sure we send the end of the document.</p>
`,needs_assistance:!1,choices:[{id:"success",label:"Yes",summary:"Adding `WriteBufferingStream` fixes the issue",next:"serialization/incomplete/overflowed"},{id:"failure",label:"No",summary:"Adding `WriteBufferingStream` doesn't fix the issue",next:"deadend"}]}};function Ha(e,s,n){var a;const t=Ma[e];return{...t,number:n||1,slug:e,hash:s||"#",id:(s||"#start").substring(1),choices:(a=t.choices)==null?void 0:a.map(o=>({...o,inputId:(s?s.substring(1)+"/":"")+o.id,hash:(s?s+"/":"#")+o.id,missing:!Ma[o.next],selected:!1}))}}function sl(e){var n;const s=[Ha("index")];if(e){let t=s[0];for(let a of e.substring(1).split("/")){const o=(n=t.choices)==null?void 0:n.find(i=>i.id===a);if(!o){console.error(`Choice "${a}" not found`);break}if(o.selected=!0,t.selectedChoice=o,t=Ha(o.next,o.hash,t.number+1),!t)break;s.push(t)}}return s}function nl(e){return e.map(s=>s.selectedChoice).filter(s=>!!s).map((s,n)=>`${n+1}. ${s.summary}`).join(`
`)}const bl="",Ya=e=>new Promise(s=>setTimeout(s,e)),tl={components:{AssistanceModal:Or,TroubleshooterStep:el},data(){return{reportCopied:!1,hash:""}},mounted(){this.hash=location.hash,window.addEventListener("hashchange",()=>this.hash=location.hash)},computed:{needsAssistance(){return this.steps[this.steps.length-1].needs_assistance},steps(){return sl(this.hash)},report(){return nl(this.steps)}},watch:{async hash(e){const s=e.substring(1);await Ya(300);const n=document.getElementById(s);n?n.scrollIntoView({behavior:"smooth"}):console.error(`Element with id "${s}" not found`)}},methods:{choose(e){document.location.assign(e.hash),ga("set","page",document.location.pathname+document.location.hash),ga("send","pageview")},async copyReport(){await navigator.clipboard.writeText(this.report),this.reportCopied=!0,await Ya(2e3),this.reportCopied=!1}}},al=ae("div",null,[ae("p",null,"Hi!"),ae("p",null,[an(" I'm the "),ae("i",null,"ArduinoJson Troubleshooter"),an(", and I'm here to help you fix your problem. I'll ask you a series of questions and give you some instructions along the way. ")]),ae("p",null,"Ready? Here we go!")],-1),ol={key:0},il=ae("button",{type:"button",class:"btn btn-primary","data-toggle":"modal","data-target":"#assistance-modal"}," Contact support ",-1),rl=["disabled"];function ll(e,s,n,t,a,o){const i=Zs("TroubleshooterStep"),r=Zs("AssistanceModal");return de(),_e("div",null,[al,ue(yr,{name:"fade",mode:"out-in"},{default:Ft(()=>[(de(!0),_e(he,null,Qt(o.steps,c=>(de(),$n(i,{key:c.hash,id:c.id,step:c,onChoose:o.choose},null,8,["id","step","onChoose"]))),128))]),_:1}),o.needsAssistance?(de(),_e("div",ol,[il,ae("button",{class:Ds(["btn",{"btn-outline-primary":!a.reportCopied,"btn-success":a.reportCopied}]),disabled:a.reportCopied,onClick:s[0]||(s[0]=(...c)=>o.copyReport&&o.copyReport(...c))},As(a.reportCopied?"✓ Report copied":"Copy troubleshooter's report"),11,rl),ue(r,{id:"assistance-modal",report:o.report},null,8,["report"])])):Vn("",!0)])}Tr(Ss(tl,[["render",ll]])).provide("debug",!1).mount("#troubleshooter-app")})();
