A line that starts with `note:` is not a compilation error but additional information about an error.

Scroll up a few lines to locate the message starting with `error:`, then check out the error list again.
